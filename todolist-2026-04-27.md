# TODO - 2026-04-27 - Frontend Batch 4A Editor and Rendered Content Theme Readability

## User Instructions

- Improve frontend UI/theme/readability for Tiptap editor and rendered content code blocks.
- Apply consistently to editor content, admin preview/content rendering, public blog/work/page detail rendering, and shared renderer paths.
- Light mode code blocks: soft gray background, readable dark text, padding, rounded corners, optional subtle border; inline code remains distinct.
- Dark mode code blocks: softer dark gray background, soft readable text, subtle separation, preserved Korean/English readability; inline code remains distinct.
- Do not broaden into WorkVideo tests, AI tests, or public API error-boundary tests.
- Do not change backend behavior, stored content shape, copy/paste behavior, editor behavior unrelated to code/pre rendering, or public content SSR rendering.
- Add focused component/unit tests and one or two browser computed-style tests. Avoid large visual regression infrastructure and pixel-perfect screenshot assertions.
- Update `frontend/reports/frontend-test-coverage-audit-2026-04-26/frontend-test-coverage-audit-2026-04-26.md` with a `Batch 4A - Editor and Rendered Content Theme Readability` section.
- Run focused tests first, then full validation: `npm test -- --run`, `npm run lint`, `npm run typecheck`, `npm run build`, and `git diff --check`.
- Do not touch unrelated dirty/deleted files and do not run `git add .`.

## Specialist Skill Search

- Ran `npx skills find nextjs testing ui accessibility`.
- Results were lower-install external options; no new skill was installed.
- Use already available skills: `find-skills`, `tdd`, `nextjs-app-router-patterns`, and `ui-ux-pro-max`.

## Plan Alignment Check Before Execution

The planned work matches the task: changes are limited to shared frontend code/pre styling, deterministic renderer/editor tests, focused browser computed-style tests, and audit artifacts. Backend behavior, WorkVideo, AI, public API error-boundary tests, and broad visual infrastructure are out of scope.

## TODOs

- [x] Prepare backups for targeted files before modifying production/test/audit files.
- [x] RED: Add focused renderer/editor tests for block code, inline code distinction, Korean text, multiline preservation, and editor code block configuration.
- [x] RED: Add focused Playwright computed-style coverage for light/dark code block readability using deterministic public content fixtures.
- [x] GREEN: Implement scoped shared styling tokens/classes for `.prose pre`, `.prose pre code`, inline `.prose code`, and legacy `BlockRenderer` code blocks.
- [x] REFACTOR: Keep styling centralized and remove any overly specific assumptions from tests.
- [x] Validate focused unit/component tests.
- [x] Validate focused browser tests.
- [x] Run full validation commands.
- [x] Update the coverage audit Markdown with Batch 4A details.
- [x] Generate persistent Batch 4A audit report artifacts.

## Progress Log

- Prepared backups in `.codex-backups/frontend-batch-4a-2026-04-27/` for the existing production, test, and audit files expected to be edited.
- Added focused renderer/editor/browser tests. Confirmed the first RED slice: `npm test -- --run src/test/*tiptap*.test.tsx src/test/*renderer*.test.tsx src/test/*content*.test.tsx` failed on the new Tiptap shared styling hook assertion.
- Implemented shared code styling tokens and `.content-code-block` for prose, Tiptap code blocks, and legacy `BlockRenderer` code blocks.
- GREEN unit verification passed for the focused Tiptap/renderer/content slice.
- Started the documented alternate-port dev stack with `BACKEND_PUBLISH_PORT=18080 ./scripts/dev-up.sh`.
- Focused browser verification passed for the new DM-18 computed-style test.
- Focused dark-mode browser suite passed.
- Full Vitest validation passed.
- Lint validation passed with existing warnings only.
- TypeScript validation passed.
- Production build passed.
- Whitespace diff check passed.
- Compose cleanup completed after browser validation.
- Lint validation passed with warnings only.
- TypeScript validation passed.
- Production build passed.
- Whitespace diff check passed.
- Updated `frontend/reports/frontend-test-coverage-audit-2026-04-26/frontend-test-coverage-audit-2026-04-26.md` with the Batch 4A audit section.
- Generated persistent audit report artifacts under `frontend/reports/frontend-batch-4a-editor-rendered-content-theme-readability-2026-04-27/`.
- After TODO completion review: actual work still matches the planned TODO list. Scope remained limited to frontend code/readability styling, targeted tests, and audit artifacts.
- Final artifact sanity checks passed.

## Verification Log

- Backup verification: targeted `cp` commands completed successfully.
- RED unit verification: failed as expected because existing `CodeBlockLowlight` still used `rounded-md bg-gray-900 text-gray-100 p-4 font-mono my-4` instead of `content-code-block`.
- Focused unit verification: `npm test -- --run src/test/*tiptap*.test.tsx src/test/*renderer*.test.tsx src/test/*content*.test.tsx` passed with 6 files and 32 tests.
- Focused browser verification: `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 npm run test:e2e -- tests/dark-mode.spec.ts --grep "DM-18"` passed with 1 test, 0 latency budget failures, 0 warnings.
- Dark-mode browser verification: `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 npm run test:e2e -- tests/dark-mode.spec.ts` passed with 25 tests, 0 latency budget failures, 0 warnings.
- Full unit/component validation: `npm test -- --run` passed with 65 files and 369 tests. Known baseline warnings appeared from Pact V3 upgrade messages and jsdom navigation.
- Lint validation: `npm run lint` passed with 0 errors and 6 warnings in existing backup/test/script files.
- TypeScript validation: `npm run typecheck` passed.
- Build validation: `npm run build` passed.
- Whitespace validation: `git diff --check` passed.
- Cleanup validation: `docker compose -f docker-compose.dev.yml down --remove-orphans` passed.
- Build validation: `npm run build` passed.
- Whitespace validation: `git diff --check` passed.
- Cleanup validation: `docker compose -f docker-compose.dev.yml down --remove-orphans` passed.
- Final audit JSON parse validation passed.
- Final post-audit `git diff --check` passed.
- Cleanup validation: `docker compose -f docker-compose.dev.yml down --remove-orphans` passed.
- Final compose status check showed no project services running.
- Final compose status check showed no project services running.

---

# TODO - 2026-04-27 - Frontend Batch 5 AI Failure and Partial Failure UI Reinforcement

## User Instructions

- Add frontend AI failure and partial-failure UI coverage from `frontend/reports/frontend-test-coverage-audit-2026-04-26/frontend-test-coverage-audit-2026-04-26.md`.
- Scope is frontend AI failure/partial-failure UI tests, with minimal production fixes only if tests reveal real behavior bugs.
- Target runtime-config 500/malformed/empty-provider fallback behavior, blog AI POST 504/500 failure, work enrich mocked happy/failure path, batch mixed succeeded/failed rendering, apply partial failure, cancel/remove failure, and safe retry/error UI without false success.
- Do not use live AI, real OpenAI, Azure, Codex, external services, or `PLAYWRIGHT_LIVE_AI`.
- Use route mocks and component-level mocked API responses.
- Do not broaden into WorkVideo tests, public API error-boundary tests, or backend behavior.
- Do not skip tests, weaken assertions, touch unrelated dirty/deleted files, or run `git add .`.
- Update the audit Markdown with `Batch 5 - AI Failure and Partial Failure UI Reinforcement`.
- Generate persistent audit report artifacts before finishing.

## Skill Search

- Used `find-skills` as required and ran `npx skills find playwright testing ai ui`.
- Results were low-install external Playwright/testing skills; no new skill was installed.
- Using existing local skills: `tdd` for red-green-refactor slices and `e2e-testing-patterns` for the focused route-mocked browser flow.

## Plan Alignment Check Before Execution

The planned work matches Batch 5: component tests come first for deterministic AI dialog and batch panel failure states, with one focused route-mocked Playwright spec for critical blog/work AI dialog browser behavior. Production edits are allowed only for real false-success or unsafe retry bugs exposed by the tests.

## TODOs

- [x] Inspect current AI dialog, batch panel, route-mocked specs, and audit context.
- [x] Prepare backups for targeted files before modifying tests, production code, TODO, or audit artifacts.
- [x] RED/GREEN: add component coverage for `AIFixDialog` runtime config failures, malformed config fallback, empty provider fallback, AI POST 504/500 failures, preserved content, retry, and work enrich request shape.
- [x] RED/GREEN: add component coverage for `AdminBlogBatchAiPanel` runtime fallback, mixed succeeded/failed item rendering, apply partial failure, cancel failure, remove failure, and safe retry/error UI.
- [x] RED/GREEN: add one route-mocked Playwright flow covering blog AI POST failure and mocked work enrich failure-to-success retry without live AI.
- [x] Implement minimal production fixes if tests reveal false success or unsafe failure behavior.
- [x] Run focused validation commands.
- [x] Run full validation commands.
- [x] Update the frontend coverage audit Markdown with Batch 5 results.
- [x] Generate persistent Batch 5 audit report artifacts.

## Progress Log

- Initial scope review confirmed Batch 4A/4B entries are present and AI remains the next recommended batch.
- Current worktree contains unrelated dirty Batch 4 theme/readability files; Batch 5 will not touch them.
- Verified plugin cache directories before skill search and later long validation commands.
- Prepared targeted backups under `.codex-backups/frontend-batch-5-ai-failure-partial-ui-2026-04-27/`.
- AIFixDialog component coverage passed for runtime-config failure, malformed config fallback, empty provider list fallback, blog AI 504/500 errors, safe retry, and work enrich failure/retry.
- AdminBlogBatchAiPanel RED slice failed as expected because partial apply responses with failed items still showed the full-success toast.
- Fixed the partial apply false-success behavior in `AdminBlogBatchAiPanel` by reporting partial apply results as an error toast while preserving failed rows.
- AdminBlogBatchAiPanel component coverage passed after the partial apply fix.
- Added route-mocked Playwright coverage in `tests/admin-blog-ai-dialog.spec.ts` for blog AI failure/retry and work enrich failure-to-success. No live AI routes are used.
- Found and fixed a Playwright harness issue: explicitly named optional specs were still ignored under the forced core profile. `playwright.config.ts` now permits explicitly requested optional spec paths without adding them to normal core full-suite runs.
- Full Playwright validation passed against the existing alternate-port compose stack.
- Full Vitest validation passed.
- Lint, typecheck, production build, and whitespace checks passed.
- Updated the frontend coverage audit Markdown with Batch 5 details.
- Generated persistent Batch 5 audit artifacts under `frontend/reports/frontend-batch-5-ai-failure-partial-ui-reinforcement-2026-04-27/`.
- After TODO completion review: actual work still matches the planned TODO list. Scope stayed limited to frontend AI failure/partial-failure tests, one minimal production false-success fix, one E2E harness fix, and audit artifacts.

## Verification Log

- Backup verification: targeted `cp` commands completed successfully.
- Focused AIFixDialog validation: `npm test -- --run src/test/admin-ai-fix-dialog.test.tsx` passed with 1 file and 11 tests.
- RED batch validation: `npm test -- --run src/test/admin-blog-batch-ai-panel.test.tsx` failed as expected on missing partial-apply error toast.
- Focused batch validation after fix: `npm test -- --run src/test/admin-blog-batch-ai-panel.test.tsx` passed with 1 file and 17 tests.
- Initial focused AI E2E command ran only `tests/admin-blog-ai-dialog.spec.ts`; the batch specs were ignored by the core-profile optional-spec configuration.
- Focused AI E2E after harness fix: `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 npm run test:e2e -- tests/admin-blog-ai-dialog.spec.ts tests/admin-ai-batch-jobs.spec.ts tests/admin-ai-batch-cancel.spec.ts` passed with 6 tests, 0 latency budget failures, and 0 warnings.
- Full browser validation: `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 npm run test:e2e` passed with 418 passed, 5 skipped, 0 latency budget failures, and 46 warnings.
- Full unit/component validation: `npm test -- --run` passed with 65 files and 382 tests. Known Pact V3 upgrade warnings and jsdom navigation warning appeared.
- Lint validation: `npm run lint` passed with 0 errors and 6 existing warnings.
- TypeScript validation: `npm run typecheck` passed.
- Build validation: `npm run build` passed.
- Whitespace validation: `git diff --check` passed.
- Final audit JSON parse validation passed.
- Final post-audit `git diff --check` passed.

---

# TODO - 2026-04-27 - Frontend Batch 4B Global Dark Mode Readability Pass

## User Instructions

- Improve frontend global dark mode readability and theme-token behavior without rewriting the design system.
- Keep Batch 4A code block readability intact.
- Prefer soft charcoal surfaces, off-white text, muted borders, softer inputs/dialogs/cards, and less aggressive accents.
- Do not broaden into AI failure tests, WorkVideo behavior tests, backend behavior, broad visual regression infrastructure, or pixel-perfect screenshots.
- Add focused computed-style Playwright coverage in `tests/dark-mode.spec.ts`.
- Update `frontend/reports/frontend-test-coverage-audit-2026-04-26/frontend-test-coverage-audit-2026-04-26.md` with `Batch 4B - Global Dark Mode Readability Pass`.
- Run focused dark-mode E2E first, then full validation.
- Do not touch unrelated dirty/deleted files and do not run `git add .`.

## Skill Search

- Ran `npx skills find dark mode accessibility ui testing`.
- Results were low-install external skills; no new skill was installed.
- Using existing skills: `find-skills`, `tdd`, `nextjs-app-router-patterns`, and `ui-ux-pro-max`.

## Plan Alignment Check Before Execution

The planned work matches the requested Batch 4B scope: add a focused computed-style dark-mode readability test, soften shared dark theme tokens and dialog/sheet surfaces, keep light tokens and Batch 4A code block tokens stable, validate, then update audit artifacts.

## TODOs

- [x] Inspect existing dark mode tokens, public/admin surfaces, and hard-coded harsh dark classes.
- [x] Prepare backups before code/config/test/audit edits.
- [x] RED: add focused global dark-mode computed-style Playwright coverage.
- [x] GREEN: soften shared dark mode tokens and dialog/sheet surfaces without changing content structure.
- [x] Verify Batch 4A code block expectations still pass.
- [x] Run focused `tests/dark-mode.spec.ts`.
- [x] Run full validation commands.
- [x] Update coverage audit Markdown with Batch 4B details.
- [x] Generate persistent Batch 4B audit report artifacts.

## Progress Log

- Existing compose stack is running at `http://127.0.0.1:3000` with backend published at `127.0.0.1:18080`.
- Backups created under `.codex-backups/frontend-batch-4b-2026-04-27/`.
- Added RED Playwright coverage for global dark palette computed styles. Existing app failed because dark body background was too close to black.
- Softened shared dark mode tokens and aligned dialog/sheet surfaces to `popover`.
- Replaced selected hard-coded harsh admin/login dark classes with theme tokens.
- Rebuilt the compose frontend with `BACKEND_PUBLISH_PORT=18080 ./scripts/dev-up.sh`.
- GREEN browser verification passed for the new DM-25 global readability test.
- Updated existing DM-13 login dark-mode assertion from old harsh-dark expectations to Batch 4B soft-surface expectations.
- Focused dark-mode browser suite passed.
- Full Vitest validation passed.
- Lint validation passed with existing warnings only.
- TypeScript validation passed.
- Production build passed.
- Whitespace diff check passed.
- Compose cleanup completed after browser validation.
- Updated `frontend/reports/frontend-test-coverage-audit-2026-04-26/frontend-test-coverage-audit-2026-04-26.md` with Batch 4B details.
- Generated persistent Batch 4B audit artifacts under `frontend/reports/frontend-batch-4b-global-dark-mode-readability-2026-04-27/`.
- After TODO completion review: actual work still matches the planned TODO list. Scope stayed limited to global frontend dark-mode readability, targeted computed-style tests, and audit artifacts.
- Final artifact sanity checks passed.

## Verification Log

- Backup verification: targeted `cp` commands completed successfully.
- RED browser verification: `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 npm run test:e2e -- tests/dark-mode.spec.ts --grep "DM-25"` failed as expected with body background minimum RGB channel `7`, below the soft-surface threshold `24`.
- Focused browser verification: `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 npm run test:e2e -- tests/dark-mode.spec.ts --grep "DM-25"` passed with 1 test, 0 latency budget failures, 0 warnings.
- Focused dark-mode suite: `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 npm run test:e2e -- tests/dark-mode.spec.ts` passed with 26 tests, 0 latency budget failures, 0 warnings.
- Full unit/component validation: `npm test -- --run` passed with 65 files and 369 tests. Known Pact V3 upgrade and jsdom navigation warnings appeared.
- Lint validation: `npm run lint` passed with 0 errors and 6 existing warnings.
- TypeScript validation: `npm run typecheck` passed.
- Build validation: `npm run build` passed.
- Whitespace validation: `git diff --check` passed.
- Cleanup validation: `docker compose -f docker-compose.dev.yml down --remove-orphans` passed.
- Final audit JSON parse validation passed.
- Final post-audit `git diff --check` passed.
- Final compose status check showed no project services running.

---

# TODO - 2026-04-27 - Frontend Batch 6 Public API Error States and Error Boundary Reinforcement

## User Instructions

- Add frontend public API error-state, notFound, empty-state, and error-boundary coverage from `frontend/reports/frontend-test-coverage-audit-2026-04-26/frontend-test-coverage-audit-2026-04-26.md`.
- Scope is public blog/work/page/resume API failure and error-boundary tests, with minimal production fixes only for real behavior bugs.
- Cover blog list/detail API 500, blog detail missing slug, work list/detail API 500, work detail missing slug, incomplete work video data, introduction/contact API failure or missing content, resume fetch failure and no-PDF state, public loading/error boundary behavior where deterministic, and no stack trace/admin-only UI leaks.
- Add one or two deterministic route-mocked Playwright browser cases only if feasible.
- Do not broaden into AI tests, WorkVideo admin tests, backend behavior, external services, broad visual regression, skipped tests, or weakened assertions.
- Update the frontend coverage audit Markdown with `Batch 6 - Public API Error States and Error Boundary Reinforcement`.
- Run focused tests first, then full validation commands.
- Do not touch unrelated dirty/deleted files and do not run `git add .`.

## Skill Search

- Used `find-skills` as required and ran `npx skills find nextjs testing error-boundary`.
- Results were low-install external skills; no new skill was installed.
- Using existing local skills: `tdd`, `nextjs-app-router-patterns`, and `vercel-react-best-practices`.

## Plan Alignment Check Before Execution

The planned work matches Batch 6: deterministic Vitest coverage comes first for API client failures, server page notFound/fallback behavior, error/loading components, and resume empty/failure states. A small route-mocked browser slice will be added only if it can run against deterministic mocked public API responses. Production edits are limited to preventing public error detail leaks or broken public empty states exposed by tests.

## TODOs

- [x] Inspect current public API clients, public route components, error/loading files, and existing tests.
- [x] Prepare backups for targeted files before modifying tests, production code, TODO, or audit artifacts.
- [x] RED/GREEN: add public API client/contract coverage for blog/work/page/resume 500/null/empty semantics.
- [x] RED/GREEN: add server/component coverage for blog/work detail notFound and public error/loading components with no stack/admin leaks.
- [x] RED/GREEN: add resume page failure/no-PDF coverage and PDF viewer missing URL safety coverage.
- [x] RED/GREEN: add page introduction/contact missing-content and API failure coverage where deterministic.
- [x] Add one focused route-mocked Playwright public failure flow if deterministic.
- [x] Implement minimal production fixes for any exposed public error detail leak or broken empty state.
- [x] Run focused validation commands.
- [x] Run full validation commands.
- [x] Update the frontend coverage audit Markdown with Batch 6 results.
- [x] Generate persistent Batch 6 audit report artifacts.

## Progress Log

- Existing worktree contains unrelated dirty Batch 4A theme/readability files and existing report artifacts; Batch 6 will not touch them unless directly required by the requested tests.
- Verified plugin cache directories before skill search and long validation commands.
- Initial inspection found public segment and blog detail error boundaries render `error.message`, which can leak technical API detail on public failure pages.
- Prepared targeted backups under `.codex-backups/frontend-batch-6-public-api-error-states-2026-04-27/`.
- RED validation failed as expected on public error detail leaks and resume API failure throwing through the page.
- Fixed public segment and blog detail error boundaries to render generic public-safe copy.
- Fixed the public resume page to log fetch failures server-side and render the existing no-resume fallback without a broken download link or PDF viewer.
- Deterministic route-mocked Playwright coverage was not added because public route initial data fetches are server-side and cannot be intercepted by `page.route`; using live data or local-QA flags would violate this batch's determinism rule.
- Added `unstable_rethrow` to preserve Next internal control-flow errors after a build run showed the first resume fallback catch was also logging a dynamic-render bailout.
- Full final validation passed.
- Updated `frontend/reports/frontend-test-coverage-audit-2026-04-26/frontend-test-coverage-audit-2026-04-26.md` with Batch 6 details.
- Generated persistent Batch 6 audit artifacts under `frontend/reports/frontend-batch-6-public-api-error-states-error-boundary-reinforcement-2026-04-27/`.
- After TODO completion review: actual work still matches the planned TODO list. Scope stayed limited to public API error states, public route boundaries, resume fallback behavior, and audit artifacts.

## Verification Log

- Skill search completed; no new skill installation performed.
- RED/GREEN focused slice: `npm test -- --run src/test/public-api-clients.test.ts src/test/public-api-contracts.test.ts src/test/public-detail-boundary.test.tsx src/test/public-page-error-states.test.tsx src/test/resume-server-render.test.tsx src/test/work-video-player.test.tsx` passed after the minimal production fixes with 6 files and 48 tests.
- Required focused API validation: `npm test -- --run src/test/public-api-clients.test.ts src/test/public-api-contracts.test.ts` passed with 2 files and 22 tests.
- Required focused page/renderer validation: `npm test -- --run src/test/page-content.test.ts src/test/interactive-renderer.test.tsx` passed with 2 files and 12 tests.
- Required focused resume/video validation: `npm test -- --run src/test/resume-server-render.test.tsx src/test/work-video-player.test.tsx` passed with 2 files and 16 tests.
- Focused resume rerun after `unstable_rethrow`: `npm test -- --run src/test/resume-server-render.test.tsx` passed with 1 file and 3 tests.
- Full unit/component validation: `npm test -- --run` passed with 66 files and 395 tests. Known Pact V3 upgrade warnings and jsdom navigation warning appeared.
- Lint validation: `npm run lint` passed with 0 errors and 6 existing warnings.
- TypeScript validation: `npm run typecheck` passed.
- Production build: `npm run build` passed.
- Whitespace validation: `git diff --check` passed.

---

# TODO - 2026-04-27 - Frontend Batch 7 Form and Media Validation Edge Case Reinforcement

## User Instructions

- Add frontend form/media validation edge-case coverage from `frontend/reports/frontend-test-coverage-audit-2026-04-26/frontend-test-coverage-audit-2026-04-26.md`.
- Scope is frontend form/media validation tests with minimal production fixes only for real behavior bugs.
- Cover image upload type/size validation across editors, video upload validation where frontend-owned, resume PDF validation edge cases, Tiptap inline image upload failure and state preservation, home/work thumbnail/icon validation, upload failure no false success, and retry/reselect safety.
- Do not broaden into AI tests, public API error-boundary tests, backend behavior, real external storage, real large files, skipped tests, weakened assertions, broad editor rewrites, arbitrary sleeps, or broad visual regression.
- Prefer deterministic component tests. Use Playwright only for one or two deterministic representative upload flows if needed.
- Update the frontend coverage audit Markdown with `Batch 7 - Form and Media Validation Edge Case Reinforcement`.
- Run focused tests first, then full validation.
- Do not touch unrelated dirty/deleted files and do not run `git add .`.

## Skill Search

- Used `find-skills` as required and ran `npx skills find react file upload validation testing`.
- Results were low-install external skills; no new skill was installed.
- Using existing skills: `tdd`; `e2e-testing-patterns` only if deterministic browser upload tests are changed.

## Plan Alignment Check Before Execution

The planned work matches Batch 7: component tests first for resume, Tiptap inline image, work thumbnail/icon/video, and home image upload validation. Production edits are limited to client-side validation and state-preservation fixes revealed by tests. Browser E2E will be skipped unless deterministic component coverage is insufficient.

## TODOs

- [x] Inspect existing media editor components, tests, and audit context.
- [x] Prepare backups for targeted files before modifying tests, production code, TODO, or audit artifacts.
- [x] RED/GREEN: add resume PDF edge validation and retry/no-false-success tests.
- [x] RED/GREEN: add Tiptap inline image rejection/failure/retry tests.
- [x] RED/GREEN: add work thumbnail/icon invalid file, failure preservation, remove, and video file validation tests.
- [x] RED/GREEN: add home image invalid file, upload failure state preservation, no false success, and retry tests.
- [x] Implement minimal frontend validation fixes where tests expose accepted invalid files or unsafe state.
- [x] Run focused validation commands.
- [x] Run full validation commands.
- [x] Update coverage audit Markdown with Batch 7 details.
- [x] Generate persistent Batch 7 audit report artifacts.

## Progress Log

- Existing worktree contains unrelated dirty Batch 4A files, `todolist-2026-04-27.md`, and untracked artifacts. Batch 7 will not touch unrelated source/test files.
- Verified plugin cache directories before skill search and long validation commands.
- Initial inspection found frontend-owned validation gaps: `HomePageEditor` accepts non-image files when tests bypass the `accept` attribute; `WorkEditor` thumbnail/icon inputs accept non-image files; `WorkEditor` HLS input accepts non-MP4 files. Resume already blocks non-PDF, and Tiptap drag/paste already ignores non-image file types.
- Prepared targeted backups under `.codex-backups/frontend-batch-7-form-media-validation-2026-04-27/`.
- RED validation failed as expected because the shared validation helper did not exist yet and invalid files reached Home/Work/Resume upload paths.
- Added `src/lib/file-validation.ts` with deterministic image/PDF/MP4 type, extension, empty-file, unknown-MIME, and uppercase-extension handling.
- Wired shared validation into `ResumeEditor`, `HomePageEditor`, and `WorkEditor`.
- Focused RED/GREEN component slice passed after minimal frontend validation fixes.
- Moved the Batch 7 backup directory to `/tmp/codex-backups/frontend-batch-7-form-media-validation-2026-04-27/` after lint picked up a backup test fixture warning from the in-repo copy. The backup still exists, but it no longer changes lint output.
- Full validation passed. No browser E2E specs were changed or run because component coverage deterministically exercises the upload validation behavior without local data or storage calls.
- Updated `frontend/reports/frontend-test-coverage-audit-2026-04-26/frontend-test-coverage-audit-2026-04-26.md` with Batch 7 details.
- Generated persistent Batch 7 audit artifacts under `frontend/reports/frontend-batch-7-form-media-validation-edge-case-reinforcement-2026-04-27/`.
- After TODO completion review: actual work still matches the planned TODO list. Scope stayed limited to frontend form/media validation, deterministic component tests, minimal validation fixes, and audit artifacts.

## Verification Log

- Skill search completed; no new skill installation performed.
- RED/GREEN focused validation: `npm test -- --run src/test/file-validation.test.ts src/test/resume-editor.test.tsx src/test/tiptap-editor.test.tsx src/test/work-editor.test.tsx src/test/admin-editor-exceptions.test.tsx` failed before fixes and passed after fixes with 5 files and 87 tests.
- Required focused resume validation: `npm test -- --run src/test/resume-editor.test.tsx` passed with 1 file and 13 tests.
- Required focused Tiptap validation: `npm test -- --run src/test/tiptap-editor.test.tsx` passed with 1 file and 11 tests.
- Required focused work editor validation: `npm test -- --run src/test/work-editor.test.tsx` passed with 1 file and 32 tests.
- Additional focused validation for changed helper/home tests: `npm test -- --run src/test/file-validation.test.ts src/test/admin-editor-exceptions.test.tsx` passed with 2 files and 31 tests.
- Interim whitespace validation: `git diff --check` passed.
- Full unit/component validation: `npm test -- --run` passed with 67 files and 426 tests. Known Pact V3 upgrade warnings and jsdom navigation warning appeared.
- Lint validation: first run passed with 0 errors and 7 warnings because the in-repo Batch 7 backup copy was linted; after moving that backup to `/tmp/codex-backups/`, `npm run lint` passed with 0 errors and the baseline 6 warnings.
- TypeScript validation: `npm run typecheck` passed.
- Production build: `npm run build` passed.
- Final whitespace validation: `git diff --check` passed.

---

# TODO - 2026-04-27 - Frontend Batch 8 Loading and Empty State Reinforcement

## User Instructions

- Scope is frontend only.
- Add or reinforce deterministic loading and empty-state tests using the existing frontend coverage audit and Batch 7 report as baseline.
- Cover public route `loading.tsx` render tests, admin route `loading.tsx` render tests, dashboard empty collections/stats fallback, admin members empty state, blog/work/page list empty states where frontend-owned, and public blog/work empty list states when deterministic without local-QA flags.
- Ensure empty/loading UI does not expose admin affordances to anonymous public users.
- Ensure loading/error/empty UI does not leak stack traces or raw API details.
- Prefer Vitest component/server-render tests first; add Playwright only where browser behavior is necessary and deterministic.
- Do not use live external services, real storage, AI, WorkVideo upload, media validation, brittle visual-only tests, or broad production behavior changes.
- Preserve existing production behavior unless a test exposes a real bug.
- Run focused tests first, then full validation: `npm test -- --run`, `npm run lint`, `npm run typecheck`, `npm run build`, and `git diff --check`.
- Update the frontend coverage audit Markdown and add a Batch 8 report under `frontend/reports`.

## Skill Search

- Used `find-skills` as required and ran `npx skills find "nextjs testing loading empty states"`.
- Results were low-install external skills; no new skill was installed.
- Using existing local skills: `tdd` and `nextjs-app-router-patterns`.

## Plan Alignment Check Before Execution

The planned work matches Batch 8: deterministic component/server-render tests first for route loading skeletons, dashboard/member/admin-list empty states, public home/list empties, and public empty-state privacy. No Playwright is planned unless these surfaces require browser-only behavior. Production edits are allowed only if the new tests expose a real loading or empty-state behavior bug.

## TODOs

- [x] Inspect route-level loading components, public list/home components, dashboard collections, members page, and existing tests.
- [x] Prepare backups for targeted files before modifying tests, audit, TODO, or report artifacts.
- [x] RED/GREEN: add admin route loading render coverage and reinforce public loading no-leak coverage.
- [x] RED/GREEN: add dashboard zero-stat and empty collections fallback coverage.
- [x] RED/GREEN: add admin members, blog, work, and page empty-state coverage where frontend-owned.
- [x] RED/GREEN: add public blog/work/home empty-state tests with no anonymous admin affordance, stack trace, or raw API detail leaks.
- [x] Implement minimal frontend production fixes only if tests expose a real behavior bug.
- [x] Run focused validation commands.
- [x] Run full validation commands.
- [x] Update coverage audit Markdown with Batch 8 details.
- [x] Generate persistent Batch 8 audit report artifacts.

## Progress Log

- Existing worktree contains uncommitted Batch 7 changes and older unrelated dirty artifacts; Batch 8 will not touch media validation, AI, WorkVideo upload, or unrelated files.
- Verified plugin cache directories before skill search and long validation commands.
- Initial inspection found existing public loading coverage and dashboard collection empty coverage, but missing admin loading, admin members/blog empty states, dashboard zero-stat fallback, and public empty-list privacy assertions.
- Prepared targeted backups under `/tmp/codex-backups/frontend-batch-8-loading-empty-state-reinforcement-2026-04-27/`.
- Added `src/test/route-loading-states.test.tsx` for public and admin loading skeletons with no raw error detail leakage.
- Reinforced public blog/work feed empty states and public home empty featured/recent lists with no anonymous admin affordances or stack/API detail leaks.
- Added admin dashboard zero-stat with empty collections coverage plus empty admin blog/member/work table assertions.
- No production code changes were needed; the only focused failure was a test isolation issue from an earlier dashboard component mock.
- First full Vitest run found an existing test harness timing issue in the public Study mobile restore-state test. The test could read page-1 session storage before page-2 state persisted during full-suite execution.
- Tightened the restore-state test to wait for page-2 persistence before asserting the final scroll snapshot.
- Full validation passed after the harness timing fix.
- Updated `frontend/reports/frontend-test-coverage-audit-2026-04-26/frontend-test-coverage-audit-2026-04-26.md` with Batch 8 details.
- Generated persistent Batch 8 audit artifacts under `frontend/reports/frontend-batch-8-loading-empty-state-reinforcement-2026-04-27/`.
- After TODO completion review: actual work still matches the planned TODO list. Scope stayed limited to frontend loading/empty-state tests, one test harness timing fix, and audit artifacts.

## Verification Log

- Skill search completed; no new skill installation performed.
- Focused validation: `npm test -- --run src/test/route-loading-states.test.tsx src/test/admin-page-success-states.test.tsx src/test/public-responsive-feed.test.tsx src/test/public-page-error-states.test.tsx` passed with 4 files and 34 tests after fixing the test mock isolation.
- Focused public feed rerun: `npm test -- --run src/test/public-responsive-feed.test.tsx` passed with 1 file and 12 tests after the restore-state timing fix.
- Full unit/component validation: first `npm test -- --run` failed with 1 public feed restore-state timing assertion; final `npm test -- --run` passed with 68 files and 434 tests. Known Pact V3 warnings and jsdom navigation warning appeared.
- Lint validation: `npm run lint` passed with 0 errors and 6 existing warnings.
- TypeScript validation: `npm run typecheck` passed.
- Production build: `npm run build` passed.
- Interim whitespace validation: `git diff --check` passed.
- Final audit JSON parse validation passed.
- Final whitespace validation: `git diff --check` passed.

---

# TODO - 2026-04-27 - Frontend Batch 9 Accessibility and Keyboard State Reinforcement

## User Instructions

- Scope frontend only.
- Use the existing frontend coverage audit and Batch 8 report as baseline.
- Add focused accessibility and keyboard-state reinforcement for loading, empty, and error states plus critical public/admin navigation.
- Cover public skip link and main landmark behavior.
- Cover public navigation keyboard focus and mobile menu/sheet keyboard behavior.
- Cover admin sidebar/mobile admin navigation keyboard focus behavior.
- Cover delete dialog keyboard behavior: focus, cancel, confirm, and Escape.
- Cover AI dialog/panel keyboard/focus behavior only if deterministic and not involving live AI.
- Cover admin tables empty-state accessibility: table semantics, empty row/cell text, and accessible action names where applicable.
- Cover loading/empty/error state accessibility: safe headings, status/alert roles only where appropriate, and no raw stack/API detail leakage.
- Avoid broad visual-only tests.
- Prefer Playwright for real keyboard/focus flows.
- Use Vitest component tests only where browser behavior is unnecessary.
- Do not use live external services.
- Do not broaden into AI failure behavior, WorkVideo upload behavior, media validation, or public API error-state logic unless directly needed for accessibility.
- Do not introduce a broad visual regression framework.
- Do not make production changes unless a test exposes a real accessibility or keyboard bug.
- Keep tests deterministic and avoid timing-sensitive selectors.
- Preserve existing production behavior where possible.
- Run focused tests first, then `npm test -- --run`, `npm run lint`, `npm run typecheck`, `npm run build`, and `git diff --check`.
- If Playwright tests are added, run the focused Playwright specs. Only run full E2E if the changed surfaces justify it or if an existing E2E harness file is modified.
- Update the frontend coverage audit report.
- Add a new Batch report under `frontend/reports`.

## Skill Search

- Used `find-skills` as required and ran `npx skills find playwright accessibility`.
- Results were low-install external skills; no new skill was installed.
- Using existing local skills: `tdd` and `e2e-testing-patterns`.

## Plan Alignment Check Before Execution

The planned work matches the requested Batch 9 scope: browser tests will cover public/admin keyboard and focus flows, while component tests will cover table/loading/error semantics. Production edits are allowed only for real accessibility or keyboard bugs exposed by the new tests. AI coverage will stay limited to deterministic dialog open/close focus behavior without invoking live AI.

## TODOs

- [x] Inspect the coverage audit, Batch 8 report, existing keyboard/a11y tests, nav components, delete dialogs, loading/error components, and admin table components.
- [x] Prepare backups for targeted production, test, TODO, and audit files.
- [x] RED/GREEN: add public skip-link/main landmark and mobile sheet keyboard/focus coverage.
- [x] RED/GREEN: add admin sidebar/mobile keyboard focus coverage.
- [x] RED/GREEN: add delete dialog focus, cancel, confirm, and Escape keyboard coverage.
- [x] RED/GREEN: add deterministic AI dialog keyboard/focus coverage without invoking live AI.
- [x] RED/GREEN: add admin table empty-state semantics and accessible action-name component assertions.
- [x] RED/GREEN: add loading/error accessibility assertions for safe headings, appropriate roles, and no raw detail leakage.
- [x] Implement minimal production fixes only for real a11y/keyboard bugs exposed by tests.
- [x] Run focused Vitest and Playwright validation.
- [x] Run full required validation commands.
- [x] Update the frontend coverage audit Markdown with Batch 9 results.
- [x] Generate persistent Batch 9 audit report artifacts.

## Progress Log

- Existing worktree had unrelated modified frontend files and untracked earlier report artifacts before this batch; those will be preserved.
- Verified plugin cache directories at `/home/kimwoonggon/.codex/plugins/cache` and `/home/kimwoonggon/.codex/.tmp/plugins` before skill search and long validation work.
- Prepared backups under `.codex-backups/frontend-batch-9-accessibility-keyboard-2026-04-27/`.
- Added public Playwright keyboard coverage for public main landmarks, mobile sheet focus trap, Escape close, focus restoration, and keyboard navigation.
- Added admin Playwright keyboard coverage for the labeled admin nav, mobile sequential focus, deterministic AI dialog open/close focus, and delete dialog Escape/cancel/confirm flows.
- Added component assertions for admin table semantics, empty-state cells/colspans, accessible row action names, and loading skeleton role boundaries.
- RED browser validation exposed three real accessibility/keyboard issues: the public sheet lacked an accessible dialog name, admin sidebar navigation lacked an accessible name, and controlled delete dialogs did not restore focus to the delete trigger on Escape.
- Fixed the public sheet with an sr-only title/description and explicit trigger focus restoration on close.
- Fixed the admin sidebar by adding `aria-label="Admin navigation"`.
- Fixed blog/work delete dialogs by remembering the triggering button and restoring focus on Escape/cancel while skipping restoration after successful deletion.
- Focused browser validation passed after rebuilding the Docker frontend image.
- Full Vitest initially failed because the existing `navbar-mobile-nav.test.tsx` sheet mock lacked the new `SheetTitle` and `SheetDescription` exports; the mock was updated and focused Navbar validation passed.
- Full required validation passed after the mock fix.
- Updated `frontend/reports/frontend-test-coverage-audit-2026-04-26/frontend-test-coverage-audit-2026-04-26.md` with Batch 9 details.
- Generated persistent Batch 9 audit artifacts under `frontend/reports/frontend-batch-9-accessibility-keyboard-state-reinforcement-2026-04-27/`.
- After TODO completion review: actual work still matches the planned TODO list. Scope stayed limited to frontend accessibility/keyboard tests, minimal accessibility/focus fixes, and audit artifacts.

## Verification Log

- Backup verification: targeted `cp` commands completed successfully.
- Focused component validation: `npm test -- --run src/test/admin-page-success-states.test.tsx src/test/route-loading-states.test.tsx src/test/public-detail-boundary.test.tsx` passed with 3 files and 25 tests before and after production fixes.
- RED focused browser validation: `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 npm run test:e2e -- tests/public-keyboard-accessibility.spec.ts tests/ui-admin-keyboard-accessibility.spec.ts tests/ui-admin-delete-dialog.spec.ts` failed as expected on missing public sheet name, missing admin nav name, and missing delete-dialog Escape focus restoration.
- Interim focused browser validation after first fixes passed 6 tests and failed 1 public menu focus-restoration assertion.
- Focused browser validation after public menu focus restoration passed with 7 tests, 0 latency budget failures, and 0 warnings.
- Focused Navbar mock validation: `npm test -- --run src/test/navbar-mobile-nav.test.tsx` passed with 1 file and 5 tests.
- Full unit/component validation: first `npm test -- --run` failed with 5 Navbar mock failures; final `npm test -- --run` passed with 68 files and 434 tests. Known Pact V3 warnings and jsdom navigation warning appeared.
- Lint validation: `npm run lint` passed with 0 errors and 6 existing warnings.
- TypeScript validation: `npm run typecheck` passed.
- Production build validation: `npm run build` passed.
- Whitespace validation: `git diff --check` passed.
- Final audit JSON parse validation passed.
- Final post-audit whitespace validation: `git diff --check` passed.
---

# TODO - 2026-04-27 - Frontend Batch 10 Pagination and Search Failure State Reinforcement

## User Instructions

- Scope frontend only.
- Reinforce pagination/search failure states and deterministic search/empty-result behavior.
- Public blog/work targets: failed load-more/append failure, empty search result state, query reset/preserve behavior, stale sessionStorage restore state, failed incremental load preserving existing items, and no raw API/admin/undefined/null leakage.
- Admin blog/work targets: empty list state, empty search result state, list fetch failure where frontend-owned/testable, no bulk selection leakage across search/page changes, and table semantics after empty/failure states.
- Shared helpers only where directly relevant to normalized search or responsive page-size/restore-state behavior.
- Prefer Vitest component tests; add Playwright only if browser routing/history behavior requires it.
- Do not use live external services, real storage, seeded backend data, AI, WorkVideo upload, media validation, dark mode, public API error-boundary work, brittle visual-only tests, or local-QA-gated paths.
- Preserve production behavior unless a test exposes a real user-facing bug; keep fixes minimal and explain them.
- Run focused tests first, then `npm test -- --run`, `npm run lint`, `npm run typecheck`, `npm run build`, and `git diff --check`.
- Add report directory `frontend/reports/frontend-batch-10-pagination-search-failure-state-reinforcement-2026-04-27/`.
- Update `frontend/reports/frontend-test-coverage-audit-2026-04-26/frontend-test-coverage-audit-2026-04-26.md`.

## Skill Search

- Used required local skills: `find-skills`, `tdd`, `nextjs-app-router-patterns`, and `vercel-react-best-practices`.
- Ran `npx skills find "nextjs pagination search failure testing"`.
- Results were low-install external skills; no new skill was installed.

## Plan Alignment Check Before Execution

The planned work matches Batch 10: deterministic Vitest/component tests first for public feed failures, stale restore-state behavior, admin empty/search/failure states, and admin bulk selection scope. Browser Playwright will be skipped unless a routing/history behavior cannot be covered in Vitest.

## TODOs

- [x] Inspect Batch 9 report, coverage audit baseline, public feed/search/pagination components, admin table clients, and existing specs.
- [x] Prepare backups for targeted production, test, TODO, and audit files before implementation edits.
- [x] Confirm before TODO execution that the actual work still matches this Batch 10 plan.
- [x] RED: add focused public feed tests for failed incremental load, safe error copy, preserved rendered items, empty search states, query changes, and stale restore state.
- [x] GREEN: apply minimal public feed/restore-state fixes only for exposed user-facing bugs.
- [x] Verify focused public tests and record results.
- [x] RED: add focused admin tests for empty search results, list fetch failure panels, table semantics, and bulk selection reset across page/search changes.
- [x] GREEN: apply minimal admin selection-scope fixes only for exposed user-facing bugs.
- [x] Verify focused admin tests and record results.
- [x] Run full validation commands.
- [x] Update the frontend coverage audit Markdown with Batch 10 details.
- [x] Generate persistent Batch 10 audit report artifacts.
- [x] Confirm after TODO progress that completed work still matches the planned TODO list.

## Progress Log

- Existing worktree was clean before Batch 10 edits.
- Plugin cache directories were verified as absolute paths before skill search and future validation commands.
- Inspected baseline reports and current public/admin pagination/search implementations.
- Found likely user-facing bugs to validate with RED tests: public incremental fetch errors can leak arbitrary thrown messages, stale Study restore state for a different query can suppress current mobile query normalization, and admin bulk selection can remain active after page changes.
- Backups prepared under `/tmp/codex-backups/frontend-batch-10-2026-04-27/`.
- Before execution alignment check passed: planned work remains frontend-only, test-first, deterministic, and scoped to pagination/search failure state reinforcement.
- Added public feed and responsive page-size RED tests for load-more failure safety, preserved existing cards, empty search states, query-change reset behavior, and stale Study restore state.
- Public RED verification failed as expected on unsafe public error copy and stale query restore suppression.
- Implemented minimal public fixes: public load-more/restore failures now use fixed safe messages, and Study restore skip logic requires a matching active query.
- Focused public verification passed after fixes.
- Added admin tests for distinct empty-search rows, safe admin list fetch failure panels, preserved table semantics, and bulk selection clearing across search/page changes.
- Admin RED verification failed as expected on generic empty-search messages and selection leakage across search/page changes.
- Implemented minimal admin fixes: empty-search rows now use matching-result copy, and bulk selections clear when the search/page scope changes.
- Focused Batch 10 verification passed after public and admin fixes.
- Full Vitest initially failed only because the existing public home empty-state server-component test timed out under full-suite load; the same file passed focused. Backed it up and added an explicit 15s timeout without changing assertions.
- First lint run failed on `react-hooks/set-state-in-effect` for the selection-scope cleanup effect. Refactored selection clearing into existing event handlers and deferred URL/page sync callbacks, then revalidated the focused admin tests.
- Full validation commands completed successfully.
- Updated the coverage audit Markdown with Batch 10 details.
- Generated persistent Batch 10 Markdown, JSON, and HTML report artifacts under `frontend/reports/frontend-batch-10-pagination-search-failure-state-reinforcement-2026-04-27/`.
- After TODO completion review: actual work still matches the Batch 10 plan. Scope remained frontend-only, deterministic, component-test first, and did not broaden into AI, WorkVideo upload, media validation, dark mode, backend behavior, or public API error-boundary work.

## Verification Log

- Backup verification: targeted `cp` commands completed successfully.
- Public RED verification: `npm test -- --run src/test/public-responsive-feed.test.tsx src/test/responsive-page-size-sync.test.tsx` failed before fixes on unsafe error copy and stale restore query handling.
- Focused public verification: `npm test -- --run src/test/public-responsive-feed.test.tsx src/test/responsive-page-size-sync.test.tsx` passed with 2 files and 24 tests.
- Admin RED/focused verification: `npm test -- --run src/test/public-responsive-feed.test.tsx src/test/responsive-page-size-sync.test.tsx src/test/admin-bulk-table.test.tsx src/test/admin-page-success-states.test.tsx` failed before admin fixes on empty-search copy and selection leakage, then passed after fixes with 4 files and 61 tests.
- Full Vitest attempt 1: `npm test -- --run` failed with 1 timeout in `src/test/public-page-error-states.test.tsx`; no assertion failures.
- Focused timeout check: `npm test -- --run src/test/public-page-error-states.test.tsx` passed with 1 file and 4 tests.
- Full unit/component validation: `npm test -- --run` passed on rerun with 68 files and 447 tests. Known Pact V3 warnings and jsdom navigation warning appeared.
- Focused admin post-lint refactor verification: `npm test -- --run src/test/admin-bulk-table.test.tsx src/test/admin-page-success-states.test.tsx` passed with 2 files and 37 tests.
- Lint attempt 1: `npm run lint` failed on a new `react-hooks/set-state-in-effect` error in `AdminBlogTableClient`.
- Lint validation: `npm run lint` passed with 0 errors and 6 existing warnings.
- TypeScript validation: `npm run typecheck` passed.
- Build validation: `npm run build` passed.
- Whitespace validation: `git diff --check` passed.
- Final audit JSON parse validation passed.
- Final post-report whitespace validation: `git diff --check` passed.
