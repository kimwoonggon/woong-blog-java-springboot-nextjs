# TODO - 2026-05-14 - Full Playwright E2E Gate

## User Instructions

- Plan how to fix the full Playwright E2E failures.
- Fix the failures.
- Pass the full E2E test.

## Plan Match Check - Before Execution

The actual work matches the plan: the full E2E run failed on exactly two specs, so execution will focus on those failures first, then rerun the full configured Playwright suite.

## TODOs

- [x] Create backups before modifying app or test files.
  - Satisfies: safe implementation before fixing the failures.
  - Verification: backup files exist under `.agent-backups/full-e2e-fix-2026-05-14/`.

- [x] Reproduce the blog xl grid failure with the focused Playwright spec.
  - Satisfies: plan how to fix using the observed failure rather than guessing.
  - Verification: focused run passed, confirming the failure is full-suite/order sensitive rather than deterministic in isolation.

- [x] Reproduce the immediate work thumbnail persistence failure with the focused Playwright spec.
  - Satisfies: plan how to fix using the observed failure rather than guessing.
  - Verification: focused run passed, confirming the failure is full-suite/order sensitive rather than deterministic in isolation.

- [x] Fix the blog grid so the Study archive uses three columns at the tested xl breakpoint.
  - Satisfies: fixes `blog grid uses three columns at xl breakpoint`.
  - Verification: targeted Playwright spec passed in the combined affected-spec run.

- [x] Fix existing-work video upload thumbnail persistence so the editor updates immediately after upload.
  - Satisfies: fixes `existing work video uploads persist thumbnails immediately without requiring Update Work`.
  - Verification: targeted Playwright spec passed in the combined affected-spec run.

- [x] Run the full configured Playwright E2E suite.
  - Satisfies: pass full E2E test.
  - Verification: `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 npm run test:e2e` passed: 431 passed, 4 skipped, 0 failed.

- [x] Generate a persistent audit report.
  - Satisfies: repository completion requirement.
  - Verification: Markdown, HTML, and JSON reports exist under `backend/reports/full-e2e-fix-2026-05-14/`.

## Progress Log

- Created the required backups for the currently suspected app/test files before implementation.
- Ran the two failing spec files directly against `http://127.0.0.1:3000`; all 6 targeted tests passed before the patch, so fixes target the race/viewport conditions exposed by the full suite.
- Patched Study feed desktop grid breakpoint robustness and existing-work video upload thumbnail generation order.
- Reran `tests/ui-improvement-blog-card-anchors.spec.ts` and `tests/work-inline-create-flow.spec.ts`; all 6 tests passed.
- Ran `npm run typecheck`; TypeScript passed.
- Ran the full Playwright E2E suite; 431 passed, 4 skipped, 0 failed in 13.2 minutes.
- Stopped repo-wide `npm run lint` after it hung silently; ran targeted ESLint for the two edited source files and it passed.
- Generated the persistent audit report under `backend/reports/full-e2e-fix-2026-05-14/`.

## Plan Match Check - After Progress

The actual work still matches the plan: both prior full-suite failures were addressed, the full Playwright suite is now green, and the persistent audit report has been generated.

## Dev/Main Publish Promotion - 2026-05-14

### User Instructions

- 잘 됐다고 판단되면 dev에 올린다.
- dev CI가 통과하면 main으로 올린다.
- main CI가 통과하면 main publish를 수행한다.

### Plan Match Check - Before Execution

The current checkout is not safe to push wholesale: it is on an older feature branch and differs from `origin/dev` in a way that would delete many current dev files. The promotion will therefore be performed from a clean `origin/dev` worktree with only the verified full-E2E fix scope applied.

### TODOs

- [x] Create a clean promotion worktree from `origin/dev`.
- [x] Apply only the verified promotion scope: Docker context ignores, existing-work thumbnail revalidation race fix, regression tests, 2026-05-14 TODO/audit artifacts.
- [x] Run targeted local validation for the actual promotion worktree.
- [ ] Commit and push to `dev`.
- [ ] Wait for dev CI and inspect logs directly if failures occur.
- [ ] Promote the passing dev commit to `main`.
- [ ] Wait for main CI and inspect logs directly if failures occur.
- [ ] Run the main publish workflow and wait for completion.
- [ ] Generate the final persistent audit report for the promotion.

## Full E2E Docker Rerun - 2026-05-14

### User Instructions

- Docker dev stack을 띄운다.
- Full E2E를 다시 실행한다.
- 에러가 발생하면 추측하지 말고 로그/trace/응답을 확인해 원인을 보고한다.
- Production/SSH는 다루지 않는다.

### TODOs

- [x] 현재 dev container/process/port 상태를 확인한다.
- [x] Docker dev stack을 compose/nginx 기준으로 기동한다.
- [x] readiness와 `/`, `/login`, `/api/health`를 확인한다.
- [x] full Playwright E2E를 실행한다.
- [x] 실패 시 관련 로그와 Playwright artifact를 확인해 원인을 기록한다.
- [x] 실패 원인별 최소 수정과 targeted verification을 수행한다.
- [x] 수정 후 full Playwright E2E를 재실행한다.
- [x] 결과 audit report를 생성한다.

### Progress

- 2026-05-14: Current dev compose state checked; no containers were running.
- 2026-05-14: Current Next dev process check passed; no local Next process was running.
- 2026-05-14: Port check for 3000/3001/8080/18080 found no listeners.
- 2026-05-14: Backup created at `.agent-backups/full-e2e-docker-rerun-2026-05-14/`.
- 2026-05-14: Codex plugin cache directories verified as absolute paths before long-running Docker/E2E commands.
- 2026-05-14: `BACKEND_PUBLISH_PORT=18080 ./scripts/dev-up.sh` built backend/frontend successfully; frontend context was about 1.03 MB and backend context about 211.61 KB.
- 2026-05-14: Initial nginx start failed with a Docker Desktop/WSL bind mount error for `/etc/nginx/nginx.conf`; `nginx/nginx.conf` and `nginx/local-https.conf` were verified as regular files and compose config resolved the expected absolute file binds.
- 2026-05-14: Recreated only the stale nginx container with `BACKEND_PUBLISH_PORT=18080 docker compose -f docker-compose.dev.yml up -d --no-deps --force-recreate nginx`; nginx started successfully.
- 2026-05-14: Readiness passed: Docker reachable, backend `127.0.0.1:18080/api/health` 200, frontend `127.0.0.1:3000` 200.
- 2026-05-14: Curl probes passed: `/` 200 HTML, `/login` 200 HTML, `/api/health` 200 JSON.
- 2026-05-14: Full E2E completed with `427 passed, 4 failed, 4 skipped`; latency budget failures were 0 and warnings were 73.
- 2026-05-14: Failure artifacts identified under `test-results/playwright/*`; failures were three Study archive grid assertions and one existing-work video-upload thumbnail public list assertion.
- 2026-05-14: Error context showed Study archive rendered two columns while tests require three columns at xl desktop; source review found `min-[1200px]:grid-cols-3` on the Study grid.
- 2026-05-14: Error context for thumbnail persistence showed the new work existed on `/works`, but its card rendered the No Image placeholder; source review found existing-work video upload persisted the generated thumbnail but did not immediately revalidate public Work paths.
- 2026-05-14: Backups for planned source/test edits were created under `.agent-backups/full-e2e-docker-rerun-2026-05-14/`.
- 2026-05-14: Patched Study archive grid to use the built-in `xl:grid-cols-3` breakpoint instead of `min-[1200px]:grid-cols-3`.
- 2026-05-14: Patched existing-work automatic video thumbnail flow so generated thumbnails are persisted and public Work paths are revalidated before the preview image is exposed as ready.
- 2026-05-14: Targeted Vitest passed: `src/test/public-responsive-feed.test.tsx` and `src/test/work-editor.test.tsx` reported 57 passed.
- 2026-05-14: Rebuilt and restarted the Docker frontend image; Next build/TypeScript passed during `docker compose ... --build frontend`.
- 2026-05-14: Focused Playwright rerun passed: 12 passed, 0 failed for the four previously failing spec files.
- 2026-05-14: Full Playwright rerun passed: 431 passed, 4 skipped, 0 failed in 13.0m; latency summary reported 435 tests, 0 budget failures, 67 warnings.
- 2026-05-14: Targeted ESLint passed with 0 errors and 2 existing test mock `<img>` warnings.
- 2026-05-14: Audit report generated at `backend/reports/full-e2e-docker-rerun-2026-05-14/`.

### Promotion Progress

- 2026-05-14: Created clean worktree `/tmp/woong-blog-dev-full-e2e-promote-20260514` from `origin/dev`.
- 2026-05-14: Applied only the verified promotion scope. Explicitly did not merge the current older feature branch wholesale because it would delete many current `origin/dev` files.
- 2026-05-14: Promotion worktree validation passed: `git diff --check`, report JSON parse, `npm ci`, targeted Vitest 57 passed, targeted ESLint 0 errors with 3 warnings.
