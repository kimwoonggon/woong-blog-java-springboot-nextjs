# TODO - 2026-05-04 - Real Multiuser Load Test Dashboard

## User Instructions

- Current dashboard should be changed because it needs to assume real multi-user access.
- Backend load should be visible instead of mostly cached frontend load.
- Work read and Study read target domains/URLs should be editable from the dashboard because changing the code is inconvenient.

## Plan Alignment Check - Before Execution

- Target the existing admin load-test dashboard.
- Default targets should hit backend public API endpoints instead of cached public pages.
- Each virtual user request should carry distinct query identity so cache layers treat users/requests separately.
- Keep the live-running result updates.
- Add UI inputs to edit target URLs, including Work read and Study read.
- Preserve unrelated dirty state.

## TODOs

- [x] Check current worktree state and relevant dashboard/test files.
  - Verification: `git status --short --branch`, source reads, and load-test tests inspected.
- [x] Search relevant specialist skills before implementation.
  - Verification: `npx skills find "load testing docker backend k6 playwright"` found low-install k6/load-test skills; using existing TDD and E2E patterns directly.
- [x] Prepare backups for files expected to change.
  - Verification: `.codex-backups/load-test-real-multiuser-2026-05-04/` contains backups for the dashboard utility, component, tests, E2E spec, and TODO file.
- [x] Add behavior tests for backend API default targets, editable target URLs, and virtual-user cache-busting.
  - Verification: first `npx vitest run src/test/load-test-dashboard.test.ts` failed because defaults still used public pages and request URLs lacked virtual-user identity.
  - Verification: `tests/admin-load-test-dashboard.spec.ts` now asserts editable Work/Study read URLs, custom API targets, `__loadTestUser=1/2`, and omitted cookies.
- [x] Implement backend API defaults, editable target URLs, and per-virtual-user request identity.
  - Verification: default targets now use `/api/public/works`, `/api/public/works/{slug}`, `/api/public/blogs`, and `/api/public/blogs/{slug}`.
  - Verification: request URLs now include `__loadTestRun`, `__loadTestUser`, `__loadTestRequest`, and `__loadTestIteration`; load-test fetches omit admin cookies.
- [x] Run focused unit, lint/type, Docker dev, and Playwright validation.
  - Verification: `npx vitest run src/test/load-test-dashboard.test.ts` passed 5/5.
  - Verification: focused ESLint passed.
  - Verification: `npm run typecheck` passed.
  - Verification: `BACKEND_PUBLISH_PORT=18080 NGINX_HTTPS_PORT=3002 docker compose -f docker-compose.dev.yml up -d --build frontend nginx` passed.
  - Verification: focused Playwright `tests/admin-load-test-dashboard.spec.ts` passed 1/1.
- [x] Generate persistent audit artifacts.
  - Verification: generated Markdown, HTML, and JSON audit artifacts under `backend/reports/load-test-real-multiuser-2026-05-04/`.

## Follow-up TODOs (5/4 요청: max users 10,000)

- [x] Extend planner/config upper bound to 10,000 users and update any shared bounds references.
  - Verification: `MAX_USERS` changed in `src/lib/load-test-dashboard.ts` and reused in UI input bounds.
- [x] Update load-test dashboard UI bounds so start/max/step users accept 10,000.
  - Verification: `max={MAX_USERS}` for `Start users`, `Max users`, `Step users` in `src/components/admin/LoadTestDashboard.tsx`.
- [x] Update unit tests to reflect the new upper bound and guard behavior at 10,000.
  - Verification: `src/test/load-test-dashboard.test.ts` now expects clamping to `MAX_USERS` and includes an explicit 10,000 acceptance test.

## Additional TODOs (CI Pact 계약 테스트 안정화)

- [x] Make Pact 세션 계약 테스트가 실행 시 이전 테스트의 `fetch` 스텁 영향을 받지 않도록 방어 로직 추가.
  - Verification: `src/test/pact/public-api-consumer.pact.test.ts`에 `beforeEach`에서 `vi.unstubAllGlobals()`/`vi.unstubAllEnvs()` 추가.
- [x] `npx vitest run --pool=forks --maxWorkers=1 src/test/pact/public-api-consumer.pact.test.ts` 및 `npx vitest run --pool=forks --maxWorkers=1 src/test/public-responsive-feed.test.tsx src/test/pact/public-api-consumer.pact.test.ts`로 Pact 단독/인접 파일 순차 실행 재검증.
  - Verification: 위 두 명령 모두 6/6/23 tests passed (각 1/1, 2/2 파일 기준).

## Additional TODOs (2026-05-04 dev-to-main promotion flow)

### User Instructions

- `dev`가 성공하면 `main`이 자동으로 push 되는지 현재 상황을 정리한다.
- 현재 브랜치/배포 흐름의 실제 동작을 기준으로 혼란을 없앤다.
- 필요하면 자동화 구성을 수정해서 `dev -> main` 승격 경로를 정리한다.

### Plan Alignment Check - Before Execution

- 현재 워크플로우에서 `dev` 성공은 `main` 직접 push가 아니라 별도 promotion 단계인지 확인한다.
- `main` 직접 자동 push는 위험하므로, `dev` green 이후 `release/main-promote -> main` PR을 자동 갱신하고 최종 merge는 명시적으로 남긴다.
- 수정 범위는 GitHub Actions promotion workflow와 release-flow 문서로 제한한다.
- 이번 작업에서는 테스트나 런타임 검증을 새로 실행하지 않고, 설정/문서 정합성 정리와 감사 리포트 생성까지 완료한다.

### TODOs

- [x] 현재 `dev`/`main` CI-CD 흐름을 읽고 `dev` 성공 시 `main` 반영 여부를 판정한다.
  - Verification: `.github/workflows/ci-dev.yml`, `.github/workflows/publish-ghcr-dev.yml`, `.github/workflows/promote-main-runtime.yml`, `README.md`, `docs/walkthroughs/main-flow.md`를 읽어 현재 흐름이 `dev 성공 -> dev 이미지 발행`, `main 반영 -> 수동 promotion` 구조임을 확인했다.
- [x] 관련 specialist skill을 확인하고 이번 작업에 맞는 적용 방식을 결정한다.
  - Verification: `find-skills`와 로컬 `create-github-action-workflow-specification` skill을 확인했고, 이번 작업은 새 명세 작성보다 기존 workflow 동작 수정이 핵심이라 직접 편집으로 진행한다.
- [x] 수정 대상 파일 백업을 만든다.
  - Verification: `.codex-backups/dev-main-promotion-automation-2026-05-04/`에 workflow/doc/TODO 원본을 보관한다.
- [x] `dev` green 이후 promotion branch와 PR이 자동 갱신되도록 workflow를 수정한다.
  - Verification: `Promote Main Runtime`에 `workflow_run` 기반 `CI Dev` 성공 트리거와 promotion PR 생성 단계가 추가됐다.
- [x] README와 main flow 문서를 현재 자동화 동작과 맞게 갱신한다.
  - Verification: 문서에 `main` 직접 auto-push가 아니라 promotion PR 자동 갱신 후 명시적 merge라는 점을 반영했다.
- [x] 감사 리포트 아티팩트를 생성한다.
  - Verification: Markdown, HTML, JSON 보고서를 `reports/dev-main-promotion-automation-2026-05-04/`에 생성한다.

## Additional TODOs (2026-05-04 release-main auto-merge chain)

### User Instructions

- `dev` 성공을 체크한다.
- 성공하면 `release/main-promote` 경로로 올린다.
- 그 검증이 잘 되면 `main`으로 진행되게 만든다.

### Plan Alignment Check - Before Execution

- 기존 `dev -> release/main-promote PR 생성` 흐름은 유지한다.
- 수동 `main` merge 대신 promotion PR auto-merge를 사용해 `release/main-promote 검증 성공 -> main` 체인을 만든다.
- `main` 직접 push는 계속 피하고, PR 기반 merge 규칙을 존중한다.
- 변경 범위는 promotion workflow, release-flow 문서, TODO, 감사 리포트로 제한한다.

### TODOs

- [x] 수정 대상 파일 백업을 만든다.
  - Verification: `.codex-backups/release-main-promote-auto-merge-2026-05-04/`에 workflow/doc/TODO 원본을 보관한다.
- [x] promotion PR 생성/재사용 시 auto-merge까지 설정되도록 workflow를 수정한다.
  - Verification: `Promote Main Runtime`의 GitHub Script 단계가 promotion PR을 생성 또는 재사용한 뒤 `enablePullRequestAutoMerge`를 호출한다.
- [x] 문서를 `dev 성공 -> release/main-promote -> 검증 통과 시 main auto-merge` 흐름에 맞게 갱신한다.
  - Verification: `README.md`와 `docs/walkthroughs/main-flow.md`가 promotion PR auto-merge 경로를 설명하도록 갱신됐다.
- [x] 감사 리포트 아티팩트를 생성한다.
  - Verification: Markdown, HTML, JSON 보고서를 `reports/release-main-promote-auto-merge-2026-05-04/`에 생성한다.

## Additional TODOs (2026-05-04 dev push for auto-promotion)

### User Instructions

- 지금 변경을 `dev`에 push 한다.
- `dev`가 성공하면 자동 promotion 체인이 이어지게 한다.

### Plan Alignment Check - Before Execution

- 현재 브랜치는 `dev`이고 작업트리에는 이번 promotion 자동화 변경만 포함돼 있어야 한다.
- 이번 단계는 선택된 파일만 커밋해서 `origin/dev`로 push 한다.
- 이후 `main` 반영은 GitHub Actions의 `CI Dev` 성공과 repository auto-merge 설정에 의해 이어진다.

### TODOs

- [x] 브랜치와 작업트리 상태를 확인한다.
  - Verification: `git status --short --branch` 결과 `dev...origin/dev`에서 이번 변경 파일만 수정 상태임을 확인했다.
- [x] promotion 자동화 변경을 커밋하고 `dev`에 push 한다.
  - Verification: `Automate runtime promotion from dev to main` 커밋(`795c296`)을 생성했고 `git push origin dev`가 성공했다.
- [x] 이번 push 작업에 대한 감사 리포트를 생성한다.
  - Verification: Markdown, HTML, JSON 보고서를 `reports/dev-push-auto-promotion-2026-05-04/`에 생성한다.

## Additional TODOs (2026-05-04 GitHub Actions promotion verification)

### User Instructions

- GitHub Actions에서 `CI Dev`와 뒤따르는 promotion workflow가 실제로 성공하는지 확인한다.

### Plan Alignment Check - Before Execution

- `dev`의 최신 push run과 직전 push run을 같이 본다.
- `CI Dev` 성공 여부뿐 아니라 실제 `Promote Main Runtime` run 생성 여부와 `release/main-promote -> main` PR 생성 여부까지 확인한다.
- 자동 promotion이 실패하면, 현재 저장소 설정/기본 브랜치 기준의 구조적 원인을 명확히 남긴다.

### TODOs

- [x] 최신 `dev` 기준 `CI Dev` run 상태를 확인한다.
  - Verification: `gh run view 25287375900 --json ...` 결과 `12f6267`의 `CI Dev`는 `2026-05-03T18:42:12Z`에 `success`로 완료됐다.
- [x] 직전 `dev` push run도 비교 확인한다.
  - Verification: `gh run list --branch dev --limit 4 ...` 결과 `795c296`의 `CI Dev`도 `success`로 완료됐다.
- [x] 뒤따르는 `Promote Main Runtime` run 생성 여부를 확인한다.
  - Verification: `gh run list --workflow "Promote Main Runtime" --limit 5 ...` 결과 2026-05-02의 수동 `workflow_dispatch` run들만 있고 이번 `dev` 성공에 의해 생성된 run은 없었다.
- [x] `release/main-promote -> main` 열린 PR 생성 여부를 확인한다.
  - Verification: `gh pr list --state open --base main --head release/main-promote ...` 결과 열린 PR은 없었다.
- [x] 자동 promotion 미동작 원인을 확인한다.
  - Verification: `gh repo view --json defaultBranchRef ...` 결과 기본 브랜치는 `main`이고, `main`의 `.github/workflows/promote-main-runtime.yml`에는 아직 `workflow_run` trigger가 없다.
- [x] 이번 확인 작업에 대한 감사 리포트를 생성한다.
  - Verification: Markdown, HTML, JSON 보고서를 `reports/github-actions-promotion-verification-2026-05-04/`에 생성한다.

## Additional TODOs (2026-05-04 bootstrap main and prove auto-promotion)

### User Instructions

- 1회성 부트스트랩을 성공시켜 `main`에 새 promotion workflow 정의를 올린다.
- 그 다음 `dev` 성공 -> `release/main-promote` -> `main` 자동 경로가 실제로 동작하는지 끝까지 확인한다.

### Plan Alignment Check - Before Execution

- 현재 자동 체인이 안 붙는 원인은 `main`의 workflow 정의가 옛 버전인 점이다.
- 먼저 기존 수동 `Promote Main Runtime`를 이용해 `release/main-promote -> main` PR을 만들고 `main`에 merge한다.
- `main`이 갱신된 뒤 최신 `dev` 기준 `CI Dev`를 rerun해서 `workflow_run` 기반 `Promote Main Runtime`가 실제로 자동 생성되는지 확인한다.
- 변경은 가능하면 GitHub remote operation으로 해결하고, 로컬 파일 수정은 TODO와 최종 감사 리포트로 제한한다.

### TODOs

- [x] 오늘자 TODO와 감사 파일 수정 전 백업을 만든다.
  - Verification: `.codex-backups/bootstrap-main-and-auto-promotion-2026-05-04/`에 관련 파일 백업을 저장한다.
- [x] 기존 수동 `Promote Main Runtime` workflow를 `main` 기준으로 실행해 `release/main-promote` 브랜치를 갱신한다.
  - Verification: `Promote Main Runtime` run `25287705283`가 `success`로 완료됐다.
- [x] `release/main-promote -> main` PR을 만들고 `main` 반영까지 완료한다.
  - Verification: PR `#32`가 `2026-05-03T19:13:19Z`에 merge됐고, final hardening PR `#33`도 `2026-05-03T19:34:01Z`에 merge됐다.
- [x] `main`에 새 workflow 정의가 반영됐는지 확인한다.
  - Verification: `main`의 `.github/workflows/promote-main-runtime.yml`가 `workflow_run` trigger, `PROMOTION_TOKEN` checkout token, `github-token: ${{ secrets.PROMOTION_TOKEN }}`를 포함한다.
- [x] 최신 `dev` 기준 `CI Dev`를 rerun하고 자동 promotion workflow 생성 여부를 확인한다.
  - Verification: `CI Dev` rerun `25287864079`가 `2026-05-03T19:20:58Z`에 `success`로 완료됐고, 이어서 `Promote Main Runtime` run `25288385003`이 `event=workflow_run`으로 생성되어 `success`로 끝났다.
- [x] 최종 감사 리포트를 생성한다.
  - Verification: Markdown, HTML, JSON 보고서를 `reports/bootstrap-main-and-auto-promotion-2026-05-04/`에 생성한다.

### Mid-course correction

- Bootstrap PR `#32`에서 `CI Main Runtime` run `25287719295`가 실패했다.
- 실패 원인은 `Backend architecture tests`의 `BackendCoverageToolingTests.BackendCoverageScript_ExposesExpectedSuitesAndOutputLayout`.
- 로그 기준 직접 원인은 `scripts/run-backend-coverage.sh`가 promotion tree에 없다는 점이다.
- 따라서 다음 수정은 `scripts/main-runtime-allowlist.txt`에 `scripts/run-backend-coverage.sh`를 추가하는 것이다.

### Final hardening

- `workflow_dispatch`로 직접 쏜 `CI Main Runtime`는 PR checks에 안정적으로 attach되지 않았다.
- 따라서 최종 자동화 경로는 `PROMOTION_TOKEN` repo secret을 사용해 branch push와 PR 생성 자체를 GitHub Actions 재트리거 가능한 주체로 바꾸는 방향으로 보강한다.
- Verification: repo secret `PROMOTION_TOKEN`을 `2026-05-03T19:24:32Z`에 등록했고, 이를 사용하는 workflow 변경을 PR `#33`로 `main`에 반영했다.

## Additional TODOs (2026-05-04 backend load-test observability)

### User Instructions

- Implement the app described by `2026-05-04-backend-loadtest.md`.
- Keep the current Next.js frontend, ASP.NET Core backend, nginx, and docker compose structure.
- Do not introduce SignalR, k8s, NBomber, C# Worker simulator, Redis backplane, HPA, or pod restart observability.
- Extend the existing `/admin/load-test` screen so HTTP load tests can show backend GC, ThreadPool, memory, DB pressure, and step/soak/spike load patterns.

### Plan Alignment Check - Before Execution

- The existing HTTP load test behavior remains the base capability.
- Add a minimal backend diagnostics endpoint instead of a full metrics stack.
- Add typed frontend load pattern, threshold, and metrics summary helpers before wiring UI.
- Show unavailable metric states without breaking the dashboard.
- Keep changes scoped and preserve unrelated dirty work.

### TODOs

- [x] Read the objective, current dashboard files, backend startup/health patterns, DB configuration, and related tests.
  - Verification: read `2026-05-04-backend-loadtest.md`, `src/lib/load-test-dashboard.ts`, `src/components/admin/LoadTestDashboard.tsx`, `src/app/admin/load-test/page.tsx`, `backend/src/WoongBlog.Api/Program.cs`, `HealthController.cs`, persistence registration, and existing tests.
- [x] Search relevant specialist skills before implementation.
  - Verification: `npx skills find "dotnet diagnostics load testing prometheus metrics"` found monitoring/prometheus skills, but no installed exact skill is required; using local `tdd` and existing repo patterns.
- [x] Prepare backups for files expected to change.
  - Verification: `.codex-backups/backend-loadtest-observability-2026-05-04/` contains backups for frontend load-test utility/component/tests, Playwright spec, backend startup test, `Program.cs`, and this TODO file.
- [x] Add RED tests for load patterns, thresholds, runtime metric summaries, UI metrics panel, and backend diagnostics endpoint.
  - Verification: `npx vitest run src/test/load-test-dashboard.test.ts` failed on missing pattern/threshold/diagnostics helpers and `dotnet test ... --filter "FullyQualifiedName~StartupCompositionTests"` failed because `/api/admin/load-test/diagnostics` returned 404.
- [x] Implement typed step/soak/spike planning, threshold scoring, runtime metric summarization, and dashboard metrics polling.
  - Verification: `src/lib/load-test-dashboard.ts` now models `step`/`soak`/`spike`, HTTP/runtime thresholds, diagnostics payload validation, and current/peak/delta summaries; `LoadTestDashboard` polls `/api/admin/load-test/diagnostics` while running and displays backend runtime metrics.
- [x] Add backend diagnostics endpoint for process memory, GC, ThreadPool, and DB probe state.
  - Verification: `backend/src/WoongBlog.Api/Modules/Diagnostics/LoadTestDiagnosticsEndpoint.cs` maps admin-only `/api/admin/load-test/diagnostics` with process memory, GC collection/heap/time, ThreadPool, relational DB ping, PostgreSQL connection counts when available, and unavailable state for InMemory tests.
- [x] Run focused frontend/backend/E2E validation and record results.
  - Verification: `npx vitest run src/test/load-test-dashboard.test.ts` passed 10/10.
  - Verification: `npm run typecheck` passed.
  - Verification: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~StartupCompositionTests"` passed 8/8.
  - Verification: `BACKEND_PUBLISH_PORT=18080 NGINX_HTTPS_PORT=3002 docker compose -f docker-compose.dev.yml up -d --build frontend nginx` passed.
  - Verification: initial Playwright run with `PLAYWRIGHT_BASE_URL=http://127.0.0.1` failed because global setup tried port 80; rerun with `PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000` passed 1/1.

## Additional TODOs (2026-05-04 Playwright full e2e validation)

### User Instructions

- Run the exact full Playwright validation command provided and return exact command/output.
- Capture total tests, failed tests, skipped tests, and duration.
- Extract failing spec names and first three assertion/stack lines if failures occur.
- Re-run only failing specs when feasible.
- Return a result on whether `0 failed` is satisfied and root-cause actions.

### Plan Alignment Check - Before Execution

- Use the exact command: `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 ENABLE_LOCAL_ADMIN_SHORTCUT=true PLAYWRIGHT_EXPECT_LOCAL_ADMIN_SHORTCUT=visible PLAYWRIGHT_E2E_PROFILE=core node scripts/run-e2e-latency.mjs -- --workers=1`.
- Keep validation changes scoped to reporting and avoid production/test code edits unless required by command failure.

### TODOs

- [x] Execute the exact command and record full terminal output.
  - Verification: command run completed with summary and failure details printed.
- [x] Capture summary fields from the run (`total`, `failed`, `skipped`, `duration`).
  - Verification: full run summary = `14 failed, 4 skipped, 416 passed, 38.2m`.
- [x] Identify failing specs and first three failure stack/assertion lines.
  - Verification: first-run failure names plus initial assertion lines captured (14 specs).
- [x] Re-run only failing specs to confirm reproducibility if failures are present.
  - Verification: rerun executed with 10 failing files, reproduced 6 intermittent failures; 8 failures did not reproduce on focused rerun.
- [x] Create a persistent audit artifact for this validation run.
  - Verification: create `reports/e2e-full-validation-2026-05-04` artifacts with md/html/json.
  - Verification: `npm run lint` completed with 0 errors and 6 existing warnings in unrelated files.
  - Verification: `dotnet test backend/WoongBlog.sln` passed with 402 passed, 1 skipped, and existing NU1901 warnings for `AWSSDK.Core`.
- [x] Generate persistent audit artifacts.
  - Verification: Markdown, HTML, and JSON reports were generated under `backend/reports/backend-loadtest-observability-2026-05-04/`.

## Additional TODOs (2026-05-04 backend loadtest reinforced pass)

### User Instructions

- Resume implementation using `2026-05-04-backend-loadtest.md`.
- Reinforce missing goals after first pass, especially soak/spike realism and stronger DB/thread-pool/pool-pressure diagnostics.
- Use subagents in parallel.

### Plan Alignment Check - Before Execution

- Re-audit against the explicit checklist in `2026-05-04-backend-loadtest.md` and implement missing high-impact gaps.
- Keep existing architecture (Next.js + ASP.NET Core + nginx + compose), no SignalR/k8s/NBomber/C# worker.
- Parallelize frontend and backend reinforcement in disjoint file ownership via subagents.

### TODOs

- [x] Re-read backend diagnostics requirements and lock backend-only scope.
  - Mapping: DB command latency p50/p95/p99 ring buffer, DB connection open/acquisition latency ring buffer, slow query samples, timeout/error counts, idle-in-transaction count, resilient endpoint behavior, no frontend edits.
  - Verification: re-read `2026-05-04-backend-loadtest.md` diagnostics sections and constrained edits to backend files + backend integration tests only.
- [x] Review specialist skill guidance for this backend diagnostics reinforcement.
  - Mapping: AGENTS requirement to use `find-skills`; implementation/testing flow follows `tdd`.
  - Verification: consulted local `find-skills` and `tdd` skill instructions before editing.
- [x] Create backups for backend source and backend integration test files to be edited.
  - Mapping: AGENTS backup requirement before code/config edits.
  - Verification: `.codex-backups/backend-loadtest-reinforced-2026-05-04/` contains pre-edit copies.
- [x] Add/adjust backend integration tests first for diagnostics payload shape and fallback behavior.
  - Mapping: endpoint remains admin-protected, enriched payload fields present, DB diagnostics failure does not crash endpoint.
  - Verification: `StartupCompositionTests` expanded payload assertions and added a throwing collector fallback test.
- [x] Implement infrastructure diagnostics collector and EF Core interceptors (no new dependencies).
  - Mapping: command/connection latency ring buffers, percentile stats, slow query counting/sanitized samples, timeout/error counters.
  - Verification: added `Persistence/Diagnostics` collector + EF interceptors; wired via DI in `PersistenceServiceCollectionExtensions`.
- [x] Refactor diagnostics endpoint to consume collector snapshot, include pg_stat_activity idle-in-transaction, and isolate DB collection failures.
  - Mapping: requirement #1 and #2.
  - Verification: endpoint now returns ring-buffer stats, slow query fields, timeout/error counts, and catches diagnostics collector/probe failures into `database.status=error`.
- [x] Run focused backend tests and record exact command/results; keep edits backend-only.
  - Mapping: verification and reporting requirement from user.
  - Verification: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~StartupCompositionTests"` passed (9/9) after implementation.
- [x] Generate/update mandatory backend audit report artifacts for this reinforcement pass.
  - Mapping: AGENTS completion requirement (md/html/json report paths).
  - Verification: generated audit artifacts under `backend/reports/backend-loadtest-reinforced-2026-05-04/`.

- [x] Re-read the full requirements and map current gaps.
  - Verification: reviewed `2026-05-04-backend-loadtest.md` sections 1-10 and checklist; identified missing time-based soak/spike behavior and incomplete DB diagnostics metrics.
- [x] Spawn frontend/backend subagents with disjoint ownership for parallel implementation.
  - Verification: spawned agents `019df231-86bd-7cd3-9e97-bd2c51b90bf2` (frontend scope) and `019df231-8924-7311-98d0-aa2dacce816f` (backend scope).
- [x] Integrate frontend reinforcement changes (time-based soak/spike, concurrency=1000 support, observed peak in-flight, scheduler tests).
  - Verification: `src/lib/load-test-dashboard.ts`에 `buildSoakUserTimeline`, `buildSpikeUserTimeline`, `estimatePatternRequestCount`, `runWithConcurrency`가 반영되고 `MAX_CONCURRENCY=1000`으로 상향되었다.
  - Verification: `src/components/admin/LoadTestDashboard.tsx`에서 pattern(`step/soak/spike`)별 실행, `current/peak in-flight`, `elapsed`, high-concurrency 경고, runtime/DB 패널 표시가 반영되었다.
  - Verification: `src/test/load-test-dashboard.test.ts`, `tests/admin-load-test-dashboard.spec.ts`가 timeline/scheduler/DB panel assertion으로 보강되었다.
- [x] Integrate backend diagnostics reinforcement changes (DB command/open latency percentiles, slow query samples, timeout/error counters, idle-in-transaction visibility).
  - Verification: `backend/src/WoongBlog.Infrastructure/Persistence/Diagnostics/*`에 collector/interceptor가 추가되었고 `PersistenceServiceCollectionExtensions`에서 DI + interceptor wiring이 적용되었다.
  - Verification: `backend/src/WoongBlog.Api/Modules/Diagnostics/LoadTestDiagnosticsEndpoint.cs`가 `commandLatency`, `connectionOpenLatency`, `slowQueryCount`, `recentSlowQueries`, `errorCount`, `idleInTransactionConnections`, `completedWorkItemCount`, fallback `database.status=error`를 반환한다.
  - Verification: `backend/tests/WoongBlog.Api.IntegrationTests/StartupCompositionTests.cs`에 payload shape + collector failure fallback + auth 요구 검증이 추가되었다.
- [x] Run focused and full validation commands and capture outcomes.
  - Verification: `npm run typecheck` passed.
  - Verification: `npx vitest run src/test/load-test-dashboard.test.ts` passed (15/15).
  - Verification: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~StartupCompositionTests" --no-restore` passed (9/9).
  - Verification: `BACKEND_PUBLISH_PORT=18080 NGINX_HTTPS_PORT=3002 docker compose -f docker-compose.dev.yml up -d --build frontend nginx` passed.
  - Verification: `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 npx playwright test tests/admin-load-test-dashboard.spec.ts --workers=1` passed (1/1).
  - Verification: `npm run lint` passed with 0 errors / existing 6 warnings (unrelated files).
  - Verification: `dotnet test backend/WoongBlog.sln --no-restore` passed (403 passed, 1 skipped across projects, existing NU1901 warnings).
  - Verification: `npm run build` passed.
  - Verification: `dotnet build backend/WoongBlog.sln --no-restore` passed (0 errors, existing NU1901 warnings).
  - Verification: `git diff --check` passed.
  - Verification: `pnpm quality-gate` could not run (`pnpm: command not found` in this environment).
  - Verification: `npm test -- --run` was attempted but hung for an extended period; process was terminated after no-progress wait, so focused suite evidence is reported separately.
- [x] Refresh audit artifacts with reinforced scope and final requirement-to-evidence matrix.
  - Verification: refreshed Markdown/HTML/JSON artifacts under `backend/reports/backend-loadtest-reinforced-2026-05-04/` with reinforced frontend+backend scope, requirement mapping, executed validations, blockers, and residual risks.

## Additional TODOs (2026-05-04 npm test unblock + dev→main promotion completion)

### User Instructions

- 기존 제한 사항으로 남아 있던 `npm test -- --run` no-progress/hang 이슈를 해결한다.
- 현재 변경을 `dev`에 push 하고 `CI Dev` 성공까지 확인한다.
- 자동 promotion 체인을 통해 `release/main-promote -> main` 반영 및 `main` CI 성공까지 확인한다.

### Plan Alignment Check - Before Execution

- 먼저 전체 Vitest가 멈추는 정확한 테스트 원인을 파일 단위로 좁혀 재현한다.
- 최소 범위 수정으로 테스트 hang/no-progress를 제거한다(새 의존성 없이).
- 로컬 핵심 검증 통과 후 변경을 커밋/푸시하고 GitHub Actions run 상태를 추적한다.
- promotion workflow와 `main` CI 성공(run/PR evidence)까지 확인한 뒤 감사 리포트를 갱신한다.

### TODOs

- [x] `npm test -- --run` 정체 원인을 식별한다(파일/테스트 단위 증거 확보).
  - Verification: `npx vitest run --pool=threads --maxWorkers=2 --reporter=verbose`로 전체 파일 진행 로그를 확인했고, 특정 테스트 deadlock이 아니라 전체 82개 파일 장시간 실행(약 24분)임을 확인했다.
  - Verification: `npm test -- --run` 재실행 시 기본 리포터 특성상 무출력 구간이 길어 “hang처럼 보이는” 현상이 재현됐고 최종 완료로 수렴함을 확인했다.
- [x] 원인 테스트를 최소 수정으로 안정화하고 전체 Vitest run 완주를 확인한다.
  - Verification: `package.json`의 `test` 스크립트에 `--reporter=dot`을 추가해 no-progress 구간을 완화했다(테스트 로직/의존성 변경 없음).
  - Verification: `npm test -- --run` 결과 `Test Files 82 passed`, `Tests 577 passed`로 완주 확인.
- [x] 변경 파일 기준 lint/typecheck/관련 unit+integration/e2e를 재검증한다.
  - Verification: `npm run typecheck` passed.
  - Verification: `npm run lint` passed (0 errors, 기존 warnings only).
  - Verification: `npm test -- --run src/test/load-test-dashboard.test.ts` passed (15/15).
  - Verification: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~StartupCompositionTests" --no-restore` passed (9/9).
  - Verification: `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 npx playwright test tests/admin-load-test-dashboard.spec.ts --workers=1` passed (1/1).
- [x] 변경 사항을 의도적으로 커밋하고 `dev`에 push 한다.
  - Verification: `git commit -m "Reinforce load-test diagnostics and unblock long vitest runs"` 생성 (`071f315ff8df45f83ba7718d6a0ee3335da30148`).
  - Verification: `git push origin dev` 성공 (`d9c8cba..071f315`).
- [x] `CI Dev` 성공을 run evidence로 확인한다.
  - Verification: `CI Dev` run `25314826045`가 `success`로 완료되었다 (`https://github.com/kimwoonggon/woong-blog-aspcore-nextjs/actions/runs/25314826045`).
- [x] promotion workflow 실행/생성 및 `release/main-promote -> main` 반영 상태를 확인한다.
  - Verification: `Promote Main Runtime` run `25315083632` (`event=workflow_run`)가 `success`로 완료됐다.
  - Verification: promotion PR `#34` (`release/main-promote -> main`)가 생성되고 auto-merge enabled 후 `2026-05-04T11:01:46Z`에 merge 완료됐다.
- [x] `main` CI 성공까지 확인하고 감사 리포트/최종 요약을 갱신한다.
  - Verification: `CI Main Runtime` PR run `25315092838` (`pull_request`) success.
  - Verification: merge 후 `main` push run `25315332019` (`push`) success.
  - Verification: 새 감사 리포트를 `reports/loadtest-dev-main-promotion-2026-05-04/`에 생성/갱신했다.

## Additional TODOs (2026-05-04 backend loadtest full-gap closure)

### User Instructions

- `2026-05-04-backend-loadtest.md` 기준으로 아직 미구현된 항목을 실제 코드로 채운다.
- 문서 요구에 맞춰 Real Backend Test control plane(API/UI), runner execution safety, latency breakdown, report path를 보강한다.
- subagents를 사용해 병렬로 빠르게 진행한다.
- 완료 후 Playwright e2e full test를 실행하고, dev/main promotion chain 증거를 다시 확인한다.

### Plan Alignment Check - Before Execution

- 현재 구현은 Browser synthetic + diagnostics 강화까지이며 Real Backend Test runner plane은 미구현이다.
- 우선순위는 `Real Backend Test API + UI + polling`의 vertical slice를 먼저 완성하고, 이후 latency breakdown/reporting/security 상한을 맞춘다.
- 기존 아키텍처(Next.js + ASP.NET Core + nginx + docker compose)는 유지하고, 범위 밖 항목(SignalR/k8s/NBomber worker 등)은 추가하지 않는다.
- 테스트는 TDD 흐름으로 최소 실패 테스트를 먼저 추가한 뒤 구현한다.

### TODOs

- [x] 요구사항-아티팩트 체크리스트를 작성하고 문서 대비 미구현 항목을 확정한다.
  - Mapping: `2026-05-04-backend-loadtest.md` 17번 체크리스트와 4/5/6/12/13 절.
- [x] 관련 specialist skill 확인(`find-skills`, `tdd`) 후 적용 전략을 기록한다.
  - Mapping: AGENTS skill usage requirement.
- [x] 수정 대상 파일 백업을 만든다.
  - Mapping: AGENTS backup requirement before code/config edits.
- [x] Backend Real Backend Test API를 추가한다 (`start`, `status`, `metrics`, `stop`).
  - Mapping: 문서 4.1~4.4.
- [x] Runner whitelist + 안전 상한(rate/duration/maxVUs) + 동시 실행 제한을 구현한다.
  - Mapping: 문서 5.1, 10.x, 11장.
- [x] runId registry/result reader/report path(`reports/loadtest/{runId}`)를 구현한다.
  - Mapping: 문서 13.2, 13.3, 17번 checklist.
- [x] k6 script 골격(`public-api-rps/spike/soak/stress`)을 추가한다.
  - Mapping: 문서 6장.
- [x] Frontend에 Real Backend Test 섹션(또는 탭), Start/Stop, runId polling UI를 추가한다.
  - Mapping: 문서 3.2, 12.2, 12.3.
- [x] Latency breakdown(client/nginx/app/db) 및 unavailable fallback 표시를 보강한다.
  - Mapping: 문서 3.4, 8.2, 10.5.
- [x] Frontend unit/Backend integration/Playwright focused tests를 추가 및 갱신한다.
  - Mapping: 문서 14장.
  - Verification: `npm test -- src/test/load-test-dashboard.test.ts` passed (18/18).
  - Verification: `dotnet test ... --filter "FullyQualifiedName~StartupCompositionTests" --no-restore` passed (11/11).
  - Verification: `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 npx playwright test tests/admin-load-test-dashboard.spec.ts --workers=1` passed (2/2).
- [ ] Playwright e2e full test를 실행하고 결과를 기록한다.
  - Mapping: 사용자 명시 요구.
  - Verification: `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 ENABLE_LOCAL_ADMIN_SHORTCUT=true PLAYWRIGHT_EXPECT_LOCAL_ADMIN_SHORTCUT=visible PLAYWRIGHT_E2E_PROFILE=core node scripts/run-e2e-latency.mjs -- --workers=1`를 실행했다.
  - Verification: full run 중 `tests/ui-improvement-featured-works-grid.spec.ts`의 2개 케이스와 `tests/admin-blog-ai-dialog.spec.ts` 1개 케이스가 실패하여 green으로 완료되지 못했다.
- [ ] dev 성공 -> staging(main-promote) -> main CI 성공 체인 증거를 재검증한다.
  - Mapping: 사용자 명시 게이트.
- [ ] 필수 audit report(md/html/json)를 생성/갱신한다.
  - Mapping: AGENTS completion requirement.

## Additional TODOs (2026-05-04 CI chain verification request)

### User Instructions

- Check current CI/promotion chain evidence for this repo and branch.
- Find latest relevant runs:
  - CI Dev on dev
  - Promote Main Runtime run(s) triggered by CI Dev
  - open PR from release/main-promote -> main and its state
  - CI Main Runtime success
- Provide exact `gh` commands used and outputs.
- Determine if chain shows dev success -> main promote chain fully succeeding in current repo state.

### Plan Alignment Check - Before Execution

- Query the latest runs for `CI Dev`, `Promote Main Runtime` (`workflow_run`), and `CI Main Runtime` from GitHub Actions.
- Query `release/main-promote -> main` PR state and map PR # to CI checks.
- Confirm whether the latest successful `CI Dev` run produced a promotion workflow and merge.
- Keep the task audit-only (no production/test code edits).

### TODOs

- [x] Collect latest `CI Dev` run on branch `dev` with run id/status/conclusion/url.
  - Verification: `gh run list --workflow "CI Dev" --branch dev --limit 3 --json number,status,conclusion,url,createdAt,updatedAt,headSha,databaseId`
- [x] Collect `Promote Main Runtime` runs and identify latest `event: workflow_run` entries.
  - Verification: `gh run list --workflow "Promote Main Runtime" --limit 6 --json number,status,conclusion,event,url,headBranch,headSha,databaseId`
- [x] Verify promotion workflow produced `release/main-promote` PR creation from CI Dev source commit and auto-merge enablement.
  - Verification: `gh run view 25323185860 --log | rg -n "SOURCE_BRANCH|SOURCE_REF|Created promotion PR #"`
- [x] Check current open `release/main-promote -> main` PR and latest PR state.
  - Verification: `gh pr list --base main --head release/main-promote --state open --json number,title,state,url,mergeable,createdAt,author`
  - Verification: `gh pr list --base main --head release/main-promote --limit 5 --state all --json number,title,state,url,mergedAt,closedAt,mergedBy,createdAt,headRefName`
- [x] Collect `CI Main Runtime` PR-check and push-run status around latest promotion chain.
  - Verification: `gh run list --workflow "CI Main Runtime" --limit 8 --json number,event,status,conclusion,url,headBranch,headSha,createdAt,databaseId,displayTitle`
- [x] Produce CI verification audit artifacts.
  - Verification: Created `backend/reports/ci-verification-2026-05-04/ci-verification-2026-05-04.md`, `.html`, `.json`.
