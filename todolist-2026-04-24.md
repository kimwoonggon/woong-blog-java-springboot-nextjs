# TODO List (2026-04-24)

## User Instructions Mapping
- [x] U1 Admin `View Site` 우측 빨간 `Logout` 버튼 및 로그아웃 액션 분리/접근성/중복클릭방지
- [x] U2 Works 동영상 해상도 기반 동적 크기 조절 + HLS/Upload 타임라인 hover preview
- [x] U3 모바일 검색 UX 변경(`/blog`, `/works`, navbar 돋보기 오픈/포커스)
- [x] U4 모바일 홈 개편(하단 3칸 제거, Study Notes 6개 2x3)
- [x] U5 모바일 More Studies 5개 + Current 중앙
- [x] U6 TOC에서 `#` 헤딩 포함(`h1/h2/h3`) 복원
- [x] U7 백엔드 구조/네임스페이스 1차 정리 + 규칙 강화
- [x] U8 필수 검증 수행(`npm test`, `lint`, `typecheck`, `dotnet test`, `test:e2e`, `run-e2e-latency`)
- [x] U9 감사보고서 3종 생성(`.md`, `.html`, `.json`)

## Skill Usage (pre-implementation)
- [x] `find-skills` 스킬 확인
- [x] `tdd` 스킬 확인
- [x] `dotnet-best-practices` 참고
- [x] `find-skills` CLI 검색 실행 및 결과 반영

## Execution Plan
- [x] P1 현 상태 갭 분석(프론트/백엔드/테스트)
- [x] P2 테스트 보강(Vitest/xUnit/Playwright)
- [x] P3 기능 구현/보정
- [x] P4 전체 검증 실행
- [x] P5 감사보고서 작성

## Verification Log
- [x] `npm test` → pass (`62 files, 308 tests`)
- [x] `npm run lint` → pass (`0 errors`, warnings only)
- [x] `npm run typecheck` → pass
- [x] `dotnet test backend/WoongBlog.sln` → pass (`98 integration 포함 전체 통과`)
- [x] `npm run test:e2e` (= `node scripts/run-e2e-latency.mjs -- --workers=1` 포함) → fail (`571 passed, 2 failed, 9 skipped`)
- [x] `node scripts/run-e2e-latency.mjs -- --workers=1` → `npm run test:e2e`에서 동일 명령 경로로 실행됨

## E2E Failures To Track
- `tests/e2e-visitor-content-exploration.spec.ts:37` strict locator 충돌(`main h1`이 2개)
- `tests/ui-admin-notion-library-sheet.spec.ts:15` `notion-library-sheet` 노출 실패

## Follow-up UX Pass (2026-04-24 PM)

### User Instructions Mapping
- [x] F1 카드 열기/편집 동선 excerpt/body 분리 원인 확인 완료
- [x] F2 모바일 `/blog`, `/works` 자동 무한 로드 제거, 수동 `Load more` + 종료 상태 유지
- [x] F3 모바일 검색은 상단 navbar 돋보기 하나만 사용하고, 페이지 본문 아래 기본 검색바는 숨김
- [x] F4 모바일 돋보기 클릭 시 현재 스크롤 위치 유지, 상단 바로 아래 검색창 오픈, Enter/버튼 제출 지원
- [x] F5 Notion 모드 autosave 체감 속도 개선

### Execution Plan
- [x] FP1 관련 구현/테스트/실사용 동선 재확인
- [x] FP2 최소 테스트 추가 또는 갱신
- [x] FP3 모바일 검색/피드 UX 수정
- [x] FP4 Notion autosave 지연 완화
- [x] FP5 표적 검증 실행

### Follow-up Verification Log
- [x] `npx vitest run src/test/navbar-mobile-nav.test.tsx src/test/public-responsive-feed.test.tsx src/test/blog-notion-workspace.test.tsx src/test/auth-csrf.test.ts` → pass (`23 tests`)
- [x] `npm run typecheck` → pass
- [x] `npm run lint` → pass (`0 errors`, warnings only)
- [x] `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 npx playwright test tests/mobile-public-navigation.spec.ts tests/public-blog-pagination.spec.ts tests/public-works-pagination.spec.ts tests/ui-admin-notion-autosave-info.spec.ts --workers=1` → pass (`13 tests`)

## Audit-Only Inspection (2026-04-24 13:31 KST)

### User Instructions Mapping
- [x] A1 Inspect the current working tree as-is.
- [x] A2 Focus only on the `/admin/blog/notion` autosave backend/request path.
- [x] A3 Inspect HTTP/API handlers and any revalidation or cache invalidation work triggered by autosave.
- [x] A4 Do not edit production or test code.
- [x] A5 Return concise findings with concrete file references and likely latency-dominant steps.

### Execution Plan
- [x] AP1 Trace client autosave trigger to HTTP calls.
- [x] AP2 Trace backend handlers for session/auth, blog update, and revalidation.
- [x] AP3 Identify likely latency-dominant steps from the current working tree.
- [x] AP4 Produce audit artifacts only.

### Verification Log
- [x] Source trace with `rg`/`nl` across `src/` and `backend/src/` completed.
- [x] Current working tree scope explicitly confirmed by user after detecting local modifications.
- [x] No production or test code changed.
- [x] No runtime tests executed; this was a static path inspection.

## Follow-up Structure / Detail / Notion Pass (2026-04-24 PM-2)

### User Instructions Mapping
- [x] S1 Notion view autosave를 즉시저장 대신 10초 간격 기준으로 조정
- [x] S2 Notion view 타이핑 체감 지연 완화(매 입력마다 전체 상위 상태 재렌더링 축소)
- [x] S3 `/blog/[slug]`, `/works/[slug]` internal error 원인 파악 및 상세 페이지 복구
- [x] S4 `WoongBlog.Application`, `WoongBlog.Api`, `WoongBlog.Infrastructure` 폴더/네임스페이스 중복 정리
- [x] S5 테스트 코드 네임스페이스/using/파일 경로 정합성 보정
- [x] S6 `WoongBlog.Infrastructure/Infrastructure/*` 루트 중복 폴더 평탄화

### Execution Plan
- [x] SP1 Notion typing/autosave 병목 재확인 및 동작 재설계
- [x] SP2 detail route 500 재현 및 production 경로 원인 추적
- [x] SP3 backend 폴더 평탄화 + namespace 정리
- [x] SP4 test/code reference 보정
- [x] SP5 표적 및 솔루션 검증 실행
- [x] SP6 infrastructure root 평탄화 + import 정리

### Verification Log
- [x] `npm run typecheck` → pass
- [x] `npm run lint` → pass (`0 errors`, warnings only)
- [x] `npx vitest run src/test/blog-notion-workspace.test.tsx src/test/auth-csrf.test.ts` → pass (`14 tests`)
- [x] `dotnet test backend/WoongBlog.sln` → pass (`176 tests across backend test projects`)
- [x] `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 npx playwright test tests/mobile-public-navigation.spec.ts tests/public-blog-pagination.spec.ts tests/public-works-pagination.spec.ts tests/public-seo-metadata.spec.ts tests/public-blog-toc-layout.spec.ts tests/ui-admin-notion-autosave-info.spec.ts --workers=1` → pass (`18 passed, 1 skipped`)
- [x] `curl http://127.0.0.1:3000/blog/<slug>` and `curl http://127.0.0.1:3000/works/<slug>` → `200`

## Follow-up Notion / Video Pass (2026-04-24 PM-3)

### User Instructions Mapping
- [x] N1 Notion autosave를 약 1초 수준으로 재조정
- [x] N2 Notion view에서 `Ctrl+S`로 즉시 저장 지원
- [x] N3 Work video seek 구간 hover preview 보강(가능 범위 내)
- [x] N4 Work video desktop resize/size mode 추가
- [x] N5 관련 단위/브라우저 테스트 갱신
- [x] N6 autoplay 제거 및 기본 수동 재생 복귀
- [x] N7 backend time-indexed sprite/VTT preview 파이프라인 보강
- [x] N8 admin works 업로드 상태 문구를 `업로드 중 -> 처리 중 -> 완료`로 표시

### Execution Plan
- [x] NP1 notion save path 단일화 및 keyboard shortcut 설계
- [x] NP2 work video custom seek/preview/resize UX 구현
- [x] NP3 테스트 추가 및 조정
- [x] NP4 표적 검증 실행
- [x] NP5 backend preview asset generation 고도화
- [x] NP6 works 업로드 상태 단계형 문구 추가

### Verification Log
- [x] `npx vitest run src/test/blog-notion-workspace.test.tsx src/test/work-video-player.test.tsx src/test/auth-csrf.test.ts` → pass (`24 tests`)
- [x] `npx vitest run src/test/work-editor.test.tsx src/test/work-video-player.test.tsx src/test/blog-notion-workspace.test.tsx src/test/auth-csrf.test.ts` → pass (`41 tests`)
- [x] `npm run typecheck` → pass
- [x] `npm run lint` → pass (`0 errors`, warnings only)
- [x] `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter WorkVideoEndpointsTests` → pass (`7 tests`)
- [x] `dotnet test backend/WoongBlog.sln` → pass (`176 tests across backend test projects`)
- [x] `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 npx playwright test tests/ui-admin-notion-autosave-info.spec.ts tests/public-work-videos.spec.ts --workers=1` → pass (`8 passed, 1 skipped`)

## Recording Pass (2026-04-24 PM-4)

### User Instructions Mapping
- [x] R1 긴 영상 fixture 기반 preview recording spec 추가
- [x] R2 Playwright 실행
- [x] R3 `.webm` 산출물 저장
- [x] R4 결과 공유

### Verification Log
- [x] `tests/video-preview-recording-0424.spec.ts` 추가
- [x] `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 npx playwright test tests/video-preview-recording-0424.spec.ts --workers=1` 실행
- [x] 결과: spec failed (`hover preview bubble did not become visible in live browser run`)
- [x] 녹화 산출물 저장:
  - `tests/playwright/0424test/video-preview/work-video-hover-preview-long.webm`
  - raw failure run artifact under `test-results/playwright/video-preview-recording-04-64aed-er-preview-after-processing-chromium-public/video.webm`

## Final Preview Stabilization Pass (2026-04-24 PM-5)

### User Instructions Mapping
- [x] FPV1 프론트/백엔드가 둘 다 실제 hover preview까지 완전히 동작하도록 정리
- [x] FPV2 live browser에서 `work-video-timeline-preview` bubble 비가시 원인 제거
- [x] FPV3 recording spec을 green으로 올리고 `.webm` 산출물 갱신
- [x] FPV4 관련 unit/Playwright/도커 검증 재실행
- [x] FPV5 감사보고서 갱신

### Execution Plan
- [x] FPVP1 `WorkVideoPlayer` hover capture/preview 좌표 계산을 progress overlay 기준으로 정리
- [x] FPVP2 unit test와 runtime-ready signal을 현재 플레이어 구조에 맞게 보정
- [x] FPVP3 도커 HTTP 스택 재빌드 후 preview asset + live browser hover 재검증
- [x] FPVP4 recording spec 실행 및 stable `.webm` 재생성

### Verification Log
- [x] `npx vitest run src/test/work-video-player.test.tsx` → pass (`8 tests`)
- [x] `npx vitest run src/test/work-editor.test.tsx src/test/work-video-player.test.tsx src/test/blog-notion-workspace.test.tsx src/test/auth-csrf.test.ts` → pass (`41 tests`)
- [x] `dotnet test backend/tests/WoongBlog.Api.UnitTests/WoongBlog.Api.UnitTests.csproj --filter WorkVideoHlsJobPlanTests` → pass (`2 tests`)
- [x] `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter WorkVideoEndpointsTests` → pass (`7 tests`)
- [x] `npm run typecheck` → pass
- [x] `npm run lint` → pass (`0 errors`, warnings only)
- [x] `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 npx playwright test tests/public-work-videos.spec.ts --grep "PF-044" --workers=1` → pass
- [x] `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 npx playwright test tests/video-preview-recording-0424.spec.ts --workers=1` → pass
- [x] `curl` 검증: 새 short upload work(`public-video-preview-1777023334596`)의 `timeline.vtt` / `timeline-sprite.jpg` 모두 `200`

## Video Flow Documentation Pass (2026-04-24 PM-6)

### User Instructions Mapping
- [x] DOC1 프론트에서 업로드 시 영상이 어떻게 등록되는지 MD로 설명
- [x] DOC2 시간 progress hover preview가 어떻게 YouTube식으로 보이는지 MD로 설명
- [x] DOC3 실제 코드 기준 파일/흐름으로 문서화
- [x] DOC4 감사보고서 생성

### Execution Plan
- [x] DOCP1 프론트 업로드 경로와 백엔드 HLS/preview 생성 경로 재추적
- [x] DOCP2 `docs/` 아래 구현 설명 MD 작성
- [x] DOCP3 문서 작업 감사보고서 작성

### Verification Log
- [x] `rg`/`sed`로 `WorkEditor`, `WorkVideoPlayer`, `StartWorkVideoHlsJobCommandHandler`, `FfmpegVideoTranscoder`, `WorkVideoHlsOutputPublisher`, `WorkQueryStore`, `WorkVideoQueryStore` 구현 경로 재확인
- [x] 문서 생성: `docs/work-video-upload-preview-flow-2026-04-24.md`
- [x] 감사보고서 생성: `backend/reports/work-video-upload-preview-doc-2026-04-24/*`

## Video Player Overlay Tweak Pass (2026-04-24 PM-7)

### User Instructions Mapping
- [x] VP1 영상이 멈춰있을 때 가운데 play overlay 버튼 표시
- [x] VP2 재생 후에는 가운데 play overlay 숨김
- [x] VP3 hover preview 이미지 표시 크기 축소
- [x] VP4 관련 단위 검증 및 감사보고서 생성

### Execution Plan
- [x] VPP1 `WorkVideoPlayer`와 기존 unit test 구조 재확인
- [x] VPP2 center play overlay / preview scale 관련 unit test 보강
- [x] VPP3 `WorkVideoPlayer` 구현 보정
- [x] VPP4 표적 검증 및 감사보고서 작성

### Verification Log
- [x] `npx vitest run src/test/work-video-player.test.tsx` → pass (`9 tests`)
- [x] `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 npx playwright test tests/public-work-videos.spec.ts --grep "PF-044" --workers=1` → pass
- [x] `npm run typecheck` → pass
- [x] `npm run lint` → pass (`0 errors`, warnings only in unrelated files)

## Dev CI Hotfix Pass (2026-04-24 PM-8)

### User Instructions Mapping
- [x] CI1 `dev` CI 실패 원인 확인
- [x] CI2 실제 실패 케이스 수정
- [ ] CI3 관련 검증 후 branch/dev 반영
- [x] CI4 감사보고서 생성

### Execution Plan
- [x] CIP1 GitHub Actions 실패 로그 확인
- [x] CIP2 `work-video-player` 단위 테스트 기대값을 구현과 정합화
- [ ] CIP3 표적 검증 후 커밋/푸시
- [ ] CIP4 `dev` 반영 및 CI 재실행 대기

### Verification Log
- [x] `gh run view 24891406974 --log-failed` → `src/test/work-video-player.test.tsx` failure confirmed
- [x] `npx vitest run src/test/work-video-player.test.tsx` → pass (`9 tests`)
- [x] `npm run typecheck` → pass
- [x] `npm run lint` → pass (`0 errors`, warnings only in unrelated files)

## Main Runtime Allowlist Hotfix Pass (2026-04-24 PM-9)

### User Instructions Mapping
- [x] MRA1 `main` CI 실패 원인 재확인
- [x] MRA2 runtime promotion allowlist 누락 파일 보강
- [ ] MRA3 license 포함 상태로 `main` 재승격
- [x] MRA4 감사보고서 생성

### Execution Plan
- [x] MRAP1 `CI Main Runtime` 실패 로그에서 누락 파일 확인
- [x] MRAP2 `scripts/main-runtime-allowlist.txt` 보강
- [ ] MRAP3 runtime promotion 재실행 후 `main` 재푸시
- [ ] MRAP4 `main` CI 재실행 확인

### Verification Log
- [x] `gh run view 24893006800 --log-failed` → allowlist 누락 + pact helper mismatch 확인
- [x] `npx vitest run src/test/pact/public-api-consumer.pact.test.ts src/test/e2e-latency-budget.test.ts` → pass (`15 tests`)
- [x] audit report generated: `backend/reports/main-runtime-allowlist-pact-hotfix-2026-04-24/*`
