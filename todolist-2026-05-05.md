# TODO - 2026-05-05 - Fix 7 Repro Failures

## User Instructions
- Full 0 failure 목표로 남은 7개 재현 실패를 우선 수정한다.
- AI dialog/E2E 중복 버튼 이슈 정리
- 모바일 피쳐드 워크스 카드의 오버플로우 테스트가 통과되도록 길이 제한 문제 보정

## TODOs
- [x] 수정 대상 3개 테스트의 재현 실패 원인(중복 버튼, 네비게이션 라벨 오버플로우) 정합 확인
- [x] `src/components/admin/AIFixDialog.tsx`: Start AI Fix 버튼 중복 식별 충돌 제거
- [x] `src/components/layout/Navbar.tsx`: 긴 브랜드명에서 모바일 overflow 방지 클래스 보강
- [ ] `npx playwright` 핵심 3개 테스트 재실행: tests/ui-improvement-featured-works-grid.spec.ts 141,200 / tests/admin-blog-ai-dialog.spec.ts / tests/ui-admin-keyboard-accessibility.spec.ts
- [ ] 7개 재현 실패 대상 테스트 7건 전체 재실행 후 fail count 확인
- [ ] 변경 미반영 항목 및 잔여 위험 정리

---

# TODO - 2026-05-05 - Real Backend Load Test Gap

## User Instructions
- `2026-05-04-backend-loadtest-additional.md` 흐름으로 수행하려는데 Real Backend Test 구현이 제대로 되지 않았다.
- 현재 `public-api-spike` / `public-blogs-only` / `k6` / `10` / `30` / `10` 시작 시 `Real backend start failed with 400`, `Run ID not started`, 요청 0, 지표 unavailable만 보이는 상태를 고쳐야 한다.
- Real Backend Test도 기본 Browser Test처럼 URL을 지정해서 `Work list`, `Work read`, `Study list`, `Study read` 대상 부하를 수행해야 한다.
- Real Backend Test는 방법만 외부 runner로 바뀐 것이므로 요청 수, latency, throughput, HTTP counts, latency breakdown 지표가 기본 load test와 유사하게 반영되어야 한다.
- latency breakdown에서는 client/nginx/app/db 구분이 가능한 값은 보여주고, 불가능한 항목만 unavailable로 남겨야 한다.

## Instruction Mapping
- 400 start failure: backend validation/runner registration/start payload tests and implementation.
- URL target workload: frontend posts editable target URL list, backend request model validates and stores targets, runner uses target paths.
- Work/Study list/read coverage: target rows retain `Work list`, `Work read`, `Study list`, `Study read` and real backend request includes the same runnable targets.
- Similar metrics: real backend snapshot/metrics include requests, throughput, latency, HTTP counts, per-target summaries, and latency breakdown.
- nginx/app/db split: k6/fake metric payload can carry app elapsed and nginx p95 values when present; UI only marks missing sources unavailable.

## Plan Alignment Check - Before Execution
- Current changed backend references `K6RealLoadTestRunner`, but `backend/src/WoongBlog.Infrastructure/LoadTesting/K6RealLoadTestRunner.cs` is absent from the worktree.
- Current real backend start payload only sends scenario/target/runner/rate/duration/maxVUs; it does not send the editable Work/Study URL targets.
- Current UI still states Real Backend Test only displays external runner results and does not show a per-target real-run summary.
- Backup created under `.agent-backups/real-backend-loadtest-gap-2026-05-05/` before implementation.
- Relevant skills checked: `find-skills` search found low-install load/k6 skills only; proceeding with local `tdd`, `.NET`, and React guidance instead of installing a new skill.

## TODOs
- [x] 1) Add/adjust behavior tests first for real backend target URL payload, status/metrics parsing, and backend start acceptance.
  - RED evidence:
    - `npx vitest run src/test/load-test-dashboard.test.ts` failed because `buildRealBackendStartPayload` did not exist and target metrics were not parsed.
    - `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~StartupCompositionTests.RealLoadTestControlPlane" --no-restore` failed because `K6RealLoadTestRunner` was missing.
  - GREEN evidence:
    - `npx vitest run src/test/load-test-dashboard.test.ts` passed 22/22.
    - `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~StartupCompositionTests.RealLoadTestControlPlane" --no-restore` passed 3/3.
- [x] 2) Implement backend real-run request target model, validation, run entry storage, metrics contracts, fake runner target metrics, and `K6RealLoadTestRunner`.
  - Implemented target specs, target metrics, latency breakdown contracts, fake runner target cycling, k6 process runner, and disabled-real-runner fallback to fake.
- [x] 3) Implement frontend target propagation and real backend per-target summary so Work/Study list/read URLs are visible in submitted real-run payload and results.
  - Implemented `buildRealBackendStartPayload`, real target URL panel, and real backend target summary table.
- [x] 4) Verify with focused Vitest, backend integration tests, typecheck/build as feasible.
  - `npx vitest run src/test/load-test-dashboard.test.ts` passed 22/22.
  - `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~StartupCompositionTests.RealLoadTestControlPlane" --no-restore` passed 3/3.
  - `dotnet build backend/WoongBlog.sln --no-restore` succeeded with the existing AWSSDK.Core NU1901 warnings.
  - `npm run typecheck` passed.
  - `npm run lint` passed with existing lint warnings outside this task scope.
  - `BACKEND_PUBLISH_PORT=18080 NGINX_HTTPS_PORT=3002 docker compose -f docker-compose.dev.yml up -d --build backend frontend nginx` built/started the dev stack after resolving the default backend port conflict with the documented port override.
  - `docker compose -f docker-compose.dev.yml exec -T backend k6 version` returned `k6 v1.0.0`.
  - `curl -fsS http://127.0.0.1:3000/api/health` returned healthy API JSON.
  - `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 ENABLE_LOCAL_ADMIN_SHORTCUT=true PLAYWRIGHT_EXPECT_LOCAL_ADMIN_SHORTCUT=visible PLAYWRIGHT_E2E_PROFILE=core npx playwright test tests/admin-load-test-dashboard.spec.ts --workers=1` passed 2/2.
  - Live exact-shape smoke for `public-api-spike` / `public-blogs-only` / `k6` / `10` / `30` / `10` returned start `200`, runner `k6`, status `completed`, `totalRequests=402`, `failedRequests=0`, `p95Ms=5.5`, `p99Ms=5.5`, `appElapsedP95Ms=5.2`, `nginxRequestTimeP95Ms=6`, and two Study target metrics with 201 requests each.
  - Follow-up UI reproduction found real backend start/stop mutations still used raw `fetch` without CSRF, causing the user's browser to keep seeing start 400 even though direct API smoke with a manual CSRF header passed.
  - Fixed start/stop to use `fetchWithCsrf`, rebuilt frontend compose, and verified actual UI click for `public-api-spike` / `public-blogs-only` / `k6` / `10` / `30` / `10` starts with HTTP 200 and fills Study list/read target metrics.
  - Re-ran `npm run typecheck` successfully.
  - Re-ran `npm run lint` successfully with the existing six unrelated warnings.
- [x] 5) Generate mandatory audit artifacts for this task and record validation evidence.
  - Markdown: `backend/reports/real-backend-loadtest-gap-2026-05-05/real-backend-loadtest-gap-2026-05-05.md`
  - JSON: `backend/reports/real-backend-loadtest-gap-2026-05-05/real-backend-loadtest-gap-2026-05-05.json`
  - HTML: `backend/reports/real-backend-loadtest-gap-2026-05-05/real-backend-loadtest-gap-2026-05-05.html`

## Plan Alignment Check - After Execution
- The completed work still matches the planned TODO list: real backend target payload, backend k6 runner, status/metrics contract, dashboard rendering, and live validation were all covered.
- The exact user-reported start shape no longer returns 400 and produces a real completed k6 run with nonzero request, latency, HTTP, per-target, app elapsed, and nginx request timing metrics.
- Unavailable component timings remain source-driven: DB timing and nginx upstream timing are not fabricated.

---

# TODO - 2026-05-05 - Real Backend Scenario Model Clarity

## User Instructions
- Real Backend Test 설정 기준이 헷갈린다.
- Browser Test처럼 `max users`, `concurrency`, `start users`, `step users`를 Real Backend Test에도 그대로 쓰는 것이 맞는지 명확하지 않다.
- k6 Real Backend Test는 scenario별 기준을 분명하게 보여줘야 한다.
- running 중 `Requests 0`, `Latency 0 ms`, component timing `0 ms`가 실제 결과처럼 보이는 문제를 정리해야 한다.

## Instruction Mapping
- Scenario 기준 명확화: `public-api-rps`, `public-api-spike`, `public-api-soak`, `public-api-stress`별로 필요한 입력만 보이게 한다.
- Browser vs k6 모델 분리: Browser Test의 user/concurrency/step 모델을 Real Backend Test에 그대로 복사하지 않는다.
- Spike 기준: base RPS와 peak RPS를 분리해 보여주고 backend/k6가 peak RPS를 실제로 사용한다.
- Stress/Soak 기준: VU 기반 설정으로 표시하고 RPS/concurrency와 혼동하지 않게 한다.
- Running 지표: k6 summary가 아직 들어오기 전에는 0을 결과처럼 보이지 않고 pending/unavailable로 표시한다.

## Plan Alignment Check - Before Execution
- Backup created under `.agent-backups/real-backend-scenario-model-2026-05-05/`.
- Skills used: `tdd` for red-green-refactor and `ui-ux-pro-max` for clearer scenario-specific controls and feedback states.
- Current UI still exposes generic `Rate`, `Duration seconds`, and `Max VUs` for every real backend scenario.
- Current running state can show all-zero metrics while k6 has not emitted its completion summary.

## TODOs
- [x] 1) Add behavior tests for scenario-specific real backend payloads and pending metrics display.
  - RED evidence:
    - `npx vitest run src/test/load-test-dashboard.test.ts` failed on missing `peakRate`/`startVUs`, missing `describeRealBackendExecutionProfile`, and running all-zero metrics not being marked pending.
    - `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~StartupCompositionTests.RealLoadTestControlPlane_StartRejectsSpikePeakRateBelowBaseRate" --no-restore` failed because spike `peakRate < rate` was accepted.
  - GREEN evidence:
    - `npx vitest run src/test/load-test-dashboard.test.ts` passed 24/24 after implementation.
    - `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~StartupCompositionTests.RealLoadTestControlPlane" --no-restore` passed 4/4.
- [x] 2) Implement frontend config model and UI controls for RPS, spike, soak, and stress scenarios.
  - Added `peakRate` and `startVUs` to the real backend config/payload.
  - Added scenario profile text and scenario-specific controls:
    - RPS: `Target RPS`, `Duration seconds`, `Max VUs cap`
    - Spike: `Base RPS`, `Peak RPS`, `Duration seconds`, `Max VUs cap`
    - Soak: `VUs`, `Duration seconds`
    - Stress: `Start VUs`, `Max VUs`, `Duration seconds`
  - Running k6 summary gaps now render as `summary pending` instead of result-looking `0` values.
- [x] 3) Extend backend start contract and k6 runner to accept spike peak RPS and stress start VUs.
  - Backend start request now accepts `PeakRate` and `StartVus`.
  - Control plane validates `PeakRate >= Rate` for `public-api-spike` and validates `StartVus` within `1..MaxVus`.
  - k6 runner passes `PEAK_RATE` and `START_VUS` and uses them in spike/stress scenario options.
- [x] 4) Validate with focused unit/backend/Playwright tests and live UI smoke.
  - `npx vitest run src/test/load-test-dashboard.test.ts` passed 24/24.
  - `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~StartupCompositionTests.RealLoadTestControlPlane" --no-restore` passed 4/4.
  - `npm run typecheck` passed.
  - `npm run lint` passed with existing six unrelated warnings.
  - `BACKEND_PUBLISH_PORT=18080 NGINX_HTTPS_PORT=3002 docker compose -f docker-compose.dev.yml up -d --build backend frontend nginx` built images successfully; the first default-port start failed on backend `127.0.0.1:8080`, then the documented `BACKEND_PUBLISH_PORT=18080` override started the dev stack.
  - `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 npx playwright test tests/admin-load-test-dashboard.spec.ts --project=chromium-authenticated` passed 2/2.
  - Live k6 smoke against dev stack: `public-api-spike`, `public-blogs-only`, `rate=1`, `peakRate=2`, `durationSeconds=10`, `maxVUs=5`, `startVUs=1` completed with `totalRequests=29`, `failedRequests=0`, `p95Ms=5.1`, and two Study target metrics.
  - `dotnet test backend/WoongBlog.sln --no-restore` passed all non-skipped backend suites: component 114, unit 56, architecture 35, integration 202; contract provider verification remained skipped by suite policy.
- [x] 5) Update audit artifacts with the scenario-model correction.
  - Markdown: `backend/reports/real-backend-scenario-model-2026-05-05/real-backend-scenario-model-2026-05-05.md`
  - JSON: `backend/reports/real-backend-scenario-model-2026-05-05/real-backend-scenario-model-2026-05-05.json`
  - HTML: `backend/reports/real-backend-scenario-model-2026-05-05/real-backend-scenario-model-2026-05-05.html`

## Plan Alignment Check - After Execution
- The completed work matches the updated TODO list: the dashboard no longer suggests Browser Test user/concurrency settings apply directly to k6 real backend scenarios.
- Spike and stress scenario-specific inputs now flow through frontend payload, backend validation, and k6 execution.
- Running all-zero k6 snapshots are presented as pending summary state rather than completed metrics.
- Full repository promotion remains blocked until a fresh full e2e run completes green after this correction.

---

# TODO - 2026-05-05 - Full E2E Before Dev/Main Promotion

## User Instructions
- Full e2e 이후 `dev`로 push하고 이상 없으면 `main`으로 push한다.
- Real Backend Test correction이 들어간 뒤 fresh full e2e를 다시 확인한다.

## Plan Alignment Check - Before Execution
- Real Backend scenario-model correction is implemented and focused validations are green.
- Docker dev stack is running at `http://127.0.0.1:3000` with backend published on `127.0.0.1:18080`.
- No branch push has been performed after this correction.

## TODOs
- [x] 1) Run fresh full core Playwright e2e against the Docker dev stack.
  - `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 npm run test:e2e` passed: 430 passed, 4 skipped, budget failures 0, warnings 38.
- [x] 2) If full e2e is green, inspect git diff/status and prepare a scoped commit.
  - `git status --short --branch`, `git diff --cached --stat`, and `git diff --cached --name-status` reviewed.
  - `.agent-backups/*` remains untracked and intentionally excluded from the commit.
- [ ] 3) Push `dev` only after validation is green.
  - First push of `fc07ca5` reached `origin/dev`, but GitHub Actions `CI Dev` run `25338419035` failed in `Frontend lint, types, and unit tests`.
  - Failure was isolated to `src/test/pact/public-api-consumer.pact.test.ts` with the Pact mock server reporting the expected blog list request was not received.
  - Added Pact test global restoration and retry for the flaky blog list/detail consumer contract.
  - Focused validation after fix:
    - `npx vitest run src/test/pact/public-api-consumer.pact.test.ts --pool=threads --maxWorkers=1` passed 6/6.
    - `npm run typecheck` passed.
    - `npm run lint` passed with existing six unrelated warnings.
  - Second push of `4ffa3ff` reached `origin/dev`, but GitHub Actions `CI Dev` run `25339887941` failed again in `Frontend lint, types, and unit tests`.
  - Second failure was isolated to the same Pact consumer file, this time with the expected `GET /api/public/site-settings` request not received.
  - Implemented fix: explicitly clear module mocks for the public API client modules before Pact dynamic imports so unrelated `vi.doMock('@/lib/api/site-settings', ...)` tests cannot leak into this contract file.
  - Backup before implementation: `.agent-backups/pact-ci-flake-2026-05-05/public-api-consumer.pact.test.ts.before-modules-unmock`.
  - Focused validation after module-unmock fix:
    - `npx vitest run src/test/pact/public-api-consumer.pact.test.ts --pool=threads --maxWorkers=1` passed 6/6.
    - `npx vitest run src/test/resume-server-render.test.tsx src/test/admin-page-success-states.test.tsx src/test/pact/public-api-consumer.pact.test.ts --pool=threads --maxWorkers=1` passed 32/32.
    - `npm run typecheck` passed.
    - `npm run lint` passed with existing six unrelated warnings.
    - `npm test -- --run` passed 82/82 test files and 586/586 tests.
  - Audit artifacts:
    - Markdown: `backend/reports/pact-consumer-ci-isolation-2026-05-05/pact-consumer-ci-isolation-2026-05-05.md`
    - JSON: `backend/reports/pact-consumer-ci-isolation-2026-05-05/pact-consumer-ci-isolation-2026-05-05.json`
    - HTML: `backend/reports/pact-consumer-ci-isolation-2026-05-05/pact-consumer-ci-isolation-2026-05-05.html`
- [ ] 4) Promote/push `main` only after `dev` push verification is clean.

---

# TODO - 2026-05-05 - Post-Promotion Main CI Stabilization

## User Instructions
- Full e2e 이후 `dev`로 push하고 이상 없으면 `main`으로 push한다.
- `main` promotion까지 실제로 이상 없는 상태로 끝낸다.
- Real Backend Test와 Pact consumer flake를 더 이상 남기지 않는다.

## Instruction Mapping
- `dev` clean verification: latest `origin/dev` CI must be green before promotion.
- `main` promotion verification: promotion PR merge alone is insufficient; `main` push CI must also finish green.
- Backend failure: real backend metrics storage must not collide/corrupt when separate control planes start the same scenario in the same second.
- Frontend failure: Pact consumer tests must restore real `fetch` instead of pinning a previously leaked stub as native fetch.

## Plan Alignment Check - Before Execution
- `origin/dev` is green after rerunning transient Browser smoke in run `25341771130`.
- Promotion PR #36 merged at `2026-05-04T20:42:06Z`.
- `main` push CI run `25342443040` failed in:
  - `Backend integration tests`: `RealLoadTestReportStore.ReadMetricsAsync` hit a corrupted non-JSON metrics line.
  - `Frontend lint, types, and unit tests`: Pact expected `GET /api/public/site-settings` was not received.
- Backup created under `.agent-backups/main-ci-post-promotion-2026-05-05/`.
- Relevant skill applied: `tdd` red-green-refactor workflow for behavior-focused CI stabilization.

## TODOs
- [ ] 1) Add/adjust behavior tests for unique real backend run IDs and Pact global restoration.
- [ ] 2) Implement the minimum backend and Pact test isolation changes.
- [ ] 3) Run focused backend integration and Pact/Vitest validations.
- [ ] 4) Update audit artifacts for this stabilization.
- [ ] 5) Commit, push `dev`, verify `dev` CI, then verify promotion to `main` and `main` CI.

---

# TODO - 2026-05-05 - Real Backend Measurement Reliability

## User Instructions
- Interpret the HTTPS Real Backend Test result and fix the remaining measurement reliability issues.
- `DB errors 5` must not be reported as green runtime health.
- k6 `p99` must be a true collected p99, not a p95 fallback.
- `nginx upstream p95 unavailable` should be improved.
- Perform internet research and add necessary load-test improvements.
- Push to `dev`, verify CI, promote to `main`, and verify `main` CI.

## Instruction Mapping
- DB error health: add behavior coverage and make any DB diagnostic errors cross a red health threshold.
- True p99: configure k6 summary trend stats so `p(99)` is exported and parsed.
- nginx upstream p95: expose and parse nginx upstream response timing with a stable explicit header alias and robust multi-value parsing.
- Load-test improvements from official docs: add k6 thresholds for HTTP failure rate and latency SLO guardrails.
- Result interpretation: explain that the raw 1000 rps run is healthy, but the current dashboard had false-green/false-zero measurement gaps.

## Plan Alignment Check - Before Execution
- Existing Real Backend runtime diagnostics polling is implemented but DB errors do not affect health.
- Existing k6 summary parser reads `p(99)` only when present, while k6 default summary does not export `p(99)`.
- nginx configs emit `$upstream_response_time`, but k6 only reads one header name and the UI cannot distinguish unavailable DB timing from `0 ms`.
- Backup created under `.agent-backups/loadtest-measurement-reliability-2026-05-05/`.

## TODOs
- [x] 1) Add focused behavior tests for DB errors, true p99 parsing/display, unavailable DB timing display, and upstream timing parsing.
  - Added frontend unit coverage for DB error health red and DB timing availability.
  - Added backend component coverage that k6 script includes `summaryTrendStats`, thresholds, p99, upstream alias, and waiting fallback.
  - Updated Playwright coverage so Real Backend diagnostics polling renders `Runtime red` when DB errors are present.
- [x] 2) Implement the smallest code/config changes for those behaviors.
  - DB errors now cross the runtime/DB red health threshold.
  - DB command/open latency cards show `unavailable` when no diagnostic samples exist instead of `0 ms`.
  - k6 now exports `p(99)` via `summaryTrendStats` and adds conservative `http_req_failed`/`http_req_duration` thresholds.
  - nginx configs now expose `X-Nginx-Upstream-Response-Time` in addition to the old header.
  - k6 records nginx upstream timing from numeric nginx headers when available, otherwise records `response.timings.waiting` as `runner.http_waiting_fallback`.
- [x] 3) Run focused frontend, backend, Playwright, and live Real Backend validations.
  - `npx vitest run src/test/load-test-dashboard.test.ts --pool=threads --maxWorkers=1` passed 26/26.
  - `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter "FullyQualifiedName~RealLoadTestRunnerComponentTests" --no-restore` passed 1/1.
  - `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~StartupCompositionTests.RealLoadTestControlPlane" --no-restore` passed 4/4.
  - `npm run typecheck` passed.
  - `npm run lint` passed with six existing unrelated warnings.
  - `BACKEND_PUBLISH_PORT=18080 NGINX_HTTPS_PORT=3002 docker compose -f docker-compose.dev.yml up -d --build backend frontend nginx` rebuilt and started the dev stack.
  - `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 ENABLE_LOCAL_ADMIN_SHORTCUT=true PLAYWRIGHT_EXPECT_LOCAL_ADMIN_SHORTCUT=visible PLAYWRIGHT_E2E_PROFILE=core npx playwright test tests/admin-load-test-dashboard.spec.ts --project=chromium-authenticated --workers=1` passed 2/2.
  - Live k6 smoke run `20260505-073055-public-api-rps-46e65dd8` completed: requests 26, failed 0, p95 9.6 ms, p99 13.4 ms, nginx upstream p95 9.3 ms from `runner.http_waiting_fallback`.
  - `dotnet test backend/WoongBlog.sln --no-restore` passed: contract 0/1 skipped by suite policy, component 115, unit 56, architecture 35, integration 202.
  - `npm test -- --run` passed: 82 files, 588 tests.
- [x] 4) Generate audit artifacts for this reliability correction.
  - Markdown: `backend/reports/loadtest-measurement-reliability-2026-05-05/loadtest-measurement-reliability-2026-05-05.md`
  - JSON: `backend/reports/loadtest-measurement-reliability-2026-05-05/loadtest-measurement-reliability-2026-05-05.json`
  - HTML: `backend/reports/loadtest-measurement-reliability-2026-05-05/loadtest-measurement-reliability-2026-05-05.html`
- [ ] 5) Commit and push to `dev`, verify `dev` CI, promote to `main`, and verify `main` CI.

---

# TODO - 2026-05-05 - Real Backend Runtime Diagnostics

## User Instructions
- k6 등 Real Backend Test도 Browser Load Test처럼 GC, ThreadPool, 메모리, DB 압박을 추적해야 한다.
- Real Backend Test에는 scenario별 상세 옵션이 더 필요하다.

## Instruction Mapping
- GC/ThreadPool/runtime tracking: Real Backend 실행 중에도 `/api/admin/load-test/diagnostics`를 수집해 `Backend runtime` 패널을 갱신한다.
- DB pressure tracking: Real Backend 실행 중에도 DB command/open/connection metrics를 수집해 `Database pressure` 패널과 component timing의 DB p95를 갱신한다.
- Detailed options: 기존 scenario-specific controls(`Target RPS`, `Peak RPS`, `Start VUs`, `Max VUs`, `Duration seconds`)가 실제 payload와 UI 검증으로 유지되는지 확인한다.

## Plan Alignment Check - Before Execution
- Current diagnostics polling is only gated by Browser Test `status.phase === 'running'`.
- Real Backend polling fetches status/metrics, but does not activate runtime diagnostics collection by itself.
- Backup created under `.agent-backups/real-backend-runtime-diagnostics-2026-05-05/`.

## TODOs
- [x] 1) Add Playwright coverage that Real Backend start triggers diagnostics polling and renders GC/ThreadPool/DB metrics.
- [x] 2) Make diagnostics polling active while Real Backend is starting/running/stopping.
- [x] 3) Re-run focused load-test dashboard Playwright and relevant unit checks.
  - Verification: `npx vitest run src/test/load-test-dashboard.test.ts --pool=threads --maxWorkers=1` passed 24/24.
  - Verification: `npm run typecheck` passed.
  - Verification: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~StartupCompositionTests.RealLoadTestControlPlane" --no-restore` passed 4/4.
  - Verification: `npm run lint` passed with existing six unrelated warnings.
  - Verification: `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 ENABLE_LOCAL_ADMIN_SHORTCUT=true PLAYWRIGHT_EXPECT_LOCAL_ADMIN_SHORTCUT=visible PLAYWRIGHT_E2E_PROFILE=core npx playwright test tests/admin-load-test-dashboard.spec.ts --project=chromium-authenticated --workers=1` passed 2/2.
  - Verification: live Docker UI smoke started real `k6` `public-api-rps` with `rate=1`, `durationSeconds=3`, `maxVUs=2`; run completed, diagnostics endpoint returned 200 five times, and runtime/database/target panels displayed diagnostics and real target metrics.
- [x] 4) Generate audit artifacts for this correction.
  - Markdown: `backend/reports/real-backend-runtime-diagnostics-2026-05-05/real-backend-runtime-diagnostics-2026-05-05.md`
  - JSON: `backend/reports/real-backend-runtime-diagnostics-2026-05-05/real-backend-runtime-diagnostics-2026-05-05.json`
  - HTML: `backend/reports/real-backend-runtime-diagnostics-2026-05-05/real-backend-runtime-diagnostics-2026-05-05.html`

---

# TODO - 2026-05-05 - Prod Real Backend LoadTesting BaseUrl

## User Instructions
- Prod 환경에 `LoadTesting__BaseUrl`이 없으면 `.env.prod`와 `docker-compose.prod.yml`에 무엇을 추가해야 하는지 정리한다.
- Real Backend Test가 prod에서 기본값 `http://127.0.0.1:3000`으로 잘못 실행되지 않도록 한다.

## Instruction Mapping
- `.env.prod` guidance: 운영 env 파일에 실제 Real Backend 대상 base URL을 명시한다.
- `docker-compose.prod.yml`: prod backend 컨테이너 환경에 `LoadTesting__BaseUrl`을 명시적으로 전달한다.
- Bootstrap/defaults: `scripts/main-up.sh`가 만드는 `.env.prod.local`에도 안전한 기본값을 넣어 로컬 prod 부트스트랩 누락을 막는다.

## Plan Alignment Check - Before Execution
- `docker-compose.dev.yml`에는 `LoadTesting__BaseUrl: http://nginx`가 있다.
- `docker-compose.prod.yml`에는 `LoadTesting__BaseUrl` 전달이 없다.
- `.env.prod.example`에도 `LoadTesting__BaseUrl` 예시가 없다.
- 코드 기본값은 `http://127.0.0.1:3000`라서 backend 컨테이너 내부 prod 실행에는 부적절하다.
- Backup created under `.agent-backups/prod-loadtesting-baseurl-2026-05-05/`.

## TODOs
- [x] 1) Add prod compose environment mapping for `LoadTesting__BaseUrl`.
  - Added `LoadTesting__BaseUrl: ${LoadTesting__BaseUrl:-http://127.0.0.1:8080}` to the prod backend service environment.
- [x] 2) Add documented prod env example/default for `LoadTesting__BaseUrl`.
  - Added `.env.prod.example` guidance with `LoadTesting__BaseUrl=https://woonglab.com`.
  - Added `LoadTesting__BaseUrl=http://127.0.0.1:8080` to the `.env.prod.local` bootstrap generated by `scripts/main-up.sh`.
- [x] 3) Validate rendered prod compose config includes the variable.
  - `env APP_ENV_FILE=.env.prod.example docker compose --env-file .env.prod.example -f docker-compose.prod.yml config` rendered `LoadTesting__BaseUrl: https://woonglab.com`.
  - A no-LoadTesting env fixture rendered the compose fallback `LoadTesting__BaseUrl: http://127.0.0.1:8080`.
- [x] 4) Generate audit artifacts for the config correction.
  - Markdown: `backend/reports/prod-loadtesting-baseurl-2026-05-05/prod-loadtesting-baseurl-2026-05-05.md`
  - JSON: `backend/reports/prod-loadtesting-baseurl-2026-05-05/prod-loadtesting-baseurl-2026-05-05.json`
  - HTML: `backend/reports/prod-loadtesting-baseurl-2026-05-05/prod-loadtesting-baseurl-2026-05-05.html`

## TODO - 2026-05-05 - Backend Real Load Performance

### User instructions
- Read `prod-runtime-resource-diagnostics-2026-05-05.md`.
- Improve backend performance autonomously for backend real load tests.
- Use Docker-based tests and launch images with conditions similar to the production server.
- Base work on `dev`, create a feature branch, and merge through CI into `dev` when ready.
- Subagents may be used.

### Planned TODOs
- [x] Confirm current branch/worktree and create feature branch from `dev`.
- [x] Read production runtime diagnostics and identify concrete bottleneck signals.
- [x] Search for relevant specialist skill and install/use `analyzing-dotnet-performance` if feasible.
- [x] Create backup before code/config changes.
- [x] Add/adjust behavior-focused tests for public API query-store performance-sensitive behavior.
  - Updated public query component coverage so public work detail does not verify remote timeline preview object existence, while admin detail still verifies preview assets.
  - Updated load-test dashboard unit/Playwright expectations for deterministic `pageSize=1` list targets.
- [x] Reduce public Work/Blog list/detail hot-path DB round trips and allocation where safe.
  - Blog public list now projects only card columns before materialization instead of loading full `ContentJson`.
  - Work public list now projects only card columns and fetches work videos only for rows that need video thumbnail fallback.
  - Work public detail now trusts persisted preview metadata and avoids storage existence checks on public reads; admin detail keeps verification.
  - Real Backend default target selection now prefers seeded deterministic read targets and `pageSize=1` list targets to avoid accidentally benchmarking large uploaded media or long editorial pages.
  - Output/server response cache was intentionally not used per user instruction.
- [x] Run targeted backend component tests.
  - `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter "FullyQualifiedName~PublicQueryHandlerComponentTests"` passed 34/34 with existing AWSSDK.Core NU1901 warnings.
  - `npm test -- --run src/test/load-test-dashboard.test.ts` passed 26/26.
- [x] Run Docker-based load test under production-like 2 CPU constraints and compare before/after.
  - Created `wb-perf` compose stack with backend/frontend/nginx/db pinned to CPU set `0,1`.
  - Updated the stack to approximate the production total 8 GiB server by splitting memory limits across services: backend 3 GiB, DB 3 GiB, frontend 1 GiB, nginx 512 MiB.
  - Fixed local WSL/Postgres bind-mount permission issue by using a test-only named Postgres volume.
  - Switched backend load-test stack to `ASPNETCORE_ENVIRONMENT=Production`, `Auth__Enabled=false`, and Warning-level EF logs for prod-like measurement.
  - Confirmed k6 must run in a separate external container for server-capacity measurement; running k6 inside backend consumes the same 2 CPU and under-reports server capacity.
  - External k6 300 rps constant, 30s, 2 CPU/total 8 GiB approximation: 9,001 requests, 300.01 rps, failed 0, dropped 0, p95 4.67 ms.
  - External k6 400 rps constant, 30s, 2 CPU/total 8 GiB approximation: 12,000 requests, 400.01 rps, failed 0, dropped 0, p95 5.29 ms.
  - External k6 450 rps constant, 30s, 2 CPU/total 8 GiB approximation: 13,501 requests, 450.01 rps, failed 0, dropped 0, p95 14.51 ms.
  - External k6 500 rps constant, 30s, 2 CPU/total 8 GiB approximation: 15,001 requests, 500.00 rps, failed 0, dropped 0, p95 338.33 ms.
  - External k6 700 rps constant, 30s, pre-8GiB check: 19,667 requests, 655.23 rps, failed rate 0.005%, dropped 1,333, p95 1,626 ms, VU cap 1000 reached.
  - External k6 100->1000->100 spike, pre-8GiB check: 26,826 requests, 447.11 rps, failed 0, dropped 1,173, p95 1,460 ms, VU cap 1000 reached.
  - Current conclusion: deterministic public API hot path is low-latency green through 450 rps on 2 CPU/total 8 GiB approximation; 500 rps sustains throughput but becomes the yellow latency boundary, and 1000 rps spike remains a red capacity test.
- [x] Write persistent audit report with validation and residual risks.
  - Markdown: `backend/reports/backend-real-load-performance-2026-05-05/backend-real-load-performance-2026-05-05.md`
  - JSON: `backend/reports/backend-real-load-performance-2026-05-05/backend-real-load-performance-2026-05-05.json`
  - HTML: `backend/reports/backend-real-load-performance-2026-05-05/backend-real-load-performance-2026-05-05.html`
- [x] Commit, push feature branch, open PR to `dev`, and verify CI before merge.
  - Commit: `953c659 Improve public backend load test hot paths`.
  - PR: `https://github.com/kimwoonggon/woong-blog-aspcore-nextjs/pull/39`.
  - PR CI passed on runs `25382579122` and `25382588680`: backend architecture/component/integration/unit, frontend lint/types/unit, compose dev verification, pact provider verification, and browser smoke.

### Current evidence
- Production host: 2 visible vCPUs, 7.75 GiB Docker-visible memory, no explicit container CPU/memory limits.
- High-concurrency spike produced 5xx concentrated on `/api/public/works/smoke-fluid-simulation`.
- `maxVUs=100` hid part of the server limit; `maxVUs=1000` exposed backend/DB pressure and 5xx.
- Hot path candidates: public work list/read, public blog list/read, DB connection pressure, app allocations/GC.
