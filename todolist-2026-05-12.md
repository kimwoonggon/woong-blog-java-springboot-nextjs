# TODO - 2026-05-12 - Production Redeploy Evidence Verification

## User Objective Mapping

- Server pulls/deploys `main` runtime image and production preflight runs.
- Real Backend Test reruns with `pageSize=12`, no seed, no cache shortcut, real Work/Study URLs.
- The next slice is selected from the result, then full E2E and CI are green.

## Planned Work

- [x] Confirm the remaining objective gap is production deploy/load evidence, not GHCR/CI availability.
- [x] Back up files before edits.
  - Evidence: `.agent-backups/prod-runtime-redeploy-evidence-verify-2026-05-12/`.
- [x] RED: add workflow contract expectations requiring production evidence verification against actual pulled image digests and checked-out main SHA.
  - Evidence: focused Vitest failed before workflow implementation.
- [x] GREEN: update `Production Runtime Redeploy` workflow to compute actual main SHA and pulled runtime image digests.
- [x] GREEN: update workflow to pass those values to `prod-runtime-evidence-bundle.sh` and then run `prod-runtime-evidence-verify.sh`.
- [x] Validate focused workflow contract test.
  - Evidence: `vitest --run src/test/prod-runtime-redeploy-workflow.test.ts` passed, 1 test.
- [x] Validate related production evidence/load scripts tests.
  - Evidence: related Vitest run passed, 6 files and 31 tests.
- [x] Validate shell syntax and diff hygiene.
  - Evidence: `bash -n` passed for production scripts, `eslint` passed for the changed test, `git diff --check` passed, audit JSON parse passed.
- [x] Generate final audit report artifacts.
  - Evidence: `backend/reports/prod-runtime-redeploy-evidence-verify-2026-05-12/`.

## Non-Goals

- No production SSH from this workspace.
- No production secret changes.
- No production Real Backend Test execution without fresh deploy/preflight evidence.

---

# TODO 2026-05-12 - TOC Rail Readability And Collapse

## User Instructions

- `On This Page` 영역에서 collapse 버튼과 펼쳐졌을 때 텍스트가 나오는 부분이 좁은 점까지 고려한다.
- Production/remote server는 이번 작업 범위가 아니다.

## Scope

- Public blog/work detail desktop TOC rail layout.
- `TableOfContents` collapse interaction and text wrapping/readability.
- Focused unit and Playwright coverage for the changed behavior.

## Non-Goals

- Backend load test, production deploy, remote SSH, cache strategy 변경 없음.
- Article body content width 자체 변경 없음.

## TODOs

- [x] Confirm work still matches plan before implementation: target is frontend TOC rail/collapse readability only.
- [x] Prepare backup before edits: `.agent-backups/toc-rail-readable-collapse-2026-05-12/`.
- [x] Add failing behavior checks for wider TOC rail and collapsed-state feedback.
  - Verification: `vitest --run src/test/table-of-contents.test.tsx` failed as expected because `aria-controls`/list id were missing.
- [x] Implement TOC rail width/layout and collapse feedback changes.
- [x] Run focused Vitest and relevant checks.
  - Verification: focused Vitest passed, 9 tests.
  - Verification: focused ESLint passed.
  - Verification: `git diff --check` passed.
- [x] Run or document focused Playwright validation status.
  - Verification: compose build completed frontend Next build and TypeScript stage.
  - Verification: dev stack health returned HTTP 200 on `http://127.0.0.1:13000/api/health`.
  - Verification: focused Playwright passed, 2 tests.
- [x] Generate mandatory audit report artifacts.
  - Markdown: `backend/reports/toc-rail-readable-collapse-2026-05-12/toc-rail-readable-collapse-2026-05-12.md`.
  - HTML: `backend/reports/toc-rail-readable-collapse-2026-05-12/toc-rail-readable-collapse-2026-05-12.html`.
  - JSON: `backend/reports/toc-rail-readable-collapse-2026-05-12/toc-rail-readable-collapse-2026-05-12.json`.
- [x] Confirm final work still matches this TODO list.
  - Confirmation: only public detail TOC rail/collapse readability, related tests, TODO, and audit artifacts were changed.

---

# TODO 2026-05-12 - Public Detail Centering, Home Works Count, Video Upload E2E

## User Instructions

- Work/Blog 본문이 TOC rail 때문에 왼쪽으로 밀리지 않고 가운데에 보여야 한다.
- `On This Page` collapse 영역과 펼쳐진 텍스트 영역이 좁아지는 문제를 같이 고려한다.
- 모바일 홈 Works가 3개만 나와 마지막 한 칸이 비어 보이지 않게, main 기준 짝수 개가 나오게 한다.
- 최대 4개 정도가 적당하다.
- 충분히 동영상 업로드 테스트를 수행한다.
- Production/remote server는 고려하지 않는다. dev 기준으로 검증한다.

## Scope

- Public blog/work detail layout centering and desktop TOC rail breakpoint.
- Home featured Works query count: 3 -> 4.
- Behavior-focused backend component test and focused Playwright checks.
- Dev compose based upload-video E2E verification.

## Non-Goals

- Production deploy, SSH, server runtime pull, production `.env` 변경 없음.
- Cache strategy 변경 없음.
- Seed URL로 Real Backend Test를 우회하지 않음.
- Public list `pageSize=12` 기준 변경 없음.

## TODOs

- [x] Confirm work still matches the plan before implementation.
  - Confirmation: current slice is UI centering, home Works count, and upload-video E2E only.
- [x] Prepare base-version backups for edited files.
  - Evidence: `.agent-backups/public-detail-centered-toc-video-2026-05-12/pre-change/`.
- [x] RED: add backend behavior check proving home featured Works should return up to four items.
  - Verification: focused component test failed with only three featured works before implementation.
- [x] GREEN: update home query to return four featured works.
  - Verification: focused component test passed after implementation.
- [x] Update public detail layout so body remains centered and TOC rail only appears when enough width exists.
  - Verification: focused Playwright layout specs passed, 4 tests.
- [x] Add Playwright assertions for centered public blog/work bodies.
  - Verification: centered body assertions passed for public blog and work detail.
- [x] Make mobile home Works E2E deterministic by creating four published work fixtures before checking the grid.
  - Verification: focused Playwright mobile Works spec passed, 1 test.
- [x] Repair local dev compose readiness for this worktree without touching production.
  - Verification: dev compose healthy at `http://127.0.0.1:3020/api/health`.
- [x] Run focused Playwright layout checks against dev compose.
  - Verification: `tests/public-blog-toc-layout.spec.ts`, `tests/public-work-toc.spec.ts`, and `tests/public-detail-toc-fallback.spec.ts` passed.
- [x] Run sufficient video upload E2E specs against dev compose.
  - Verification: admin/public video upload specs passed, 7 tests.
  - Verification: S3-compatible HLS processing spec passed, 1 test.
- [x] Run full dev E2E after fixes.
  - Verification: `npm run test:e2e` passed, 431 passed and 4 skipped; latency budget failures 0.
- [x] Run final lint/diff hygiene checks.
  - Verification: ESLint focused checks passed, `npm run build` passed, `dotnet test backend/WoongBlog.sln` passed, `git diff --check` passed.
- [x] Generate mandatory audit report artifacts.
  - Markdown: `backend/reports/public-detail-centered-toc-video-2026-05-12/public-detail-centered-toc-video-2026-05-12.md`.
  - HTML: `backend/reports/public-detail-centered-toc-video-2026-05-12/public-detail-centered-toc-video-2026-05-12.html`.
  - JSON: `backend/reports/public-detail-centered-toc-video-2026-05-12/public-detail-centered-toc-video-2026-05-12.json`.
- [x] Confirm final work still matches this TODO list.
  - Confirmation: only dev-scoped public detail centering, TOC readability, home Works count, E2E hardening, TODO, backup, and audit artifacts were changed.

---

# TODO 2026-05-12 - Production Handoff Artifact Persistence

## User Instructions

- Active goal still requires production runtime image pull/deploy, production preflight, and Real Backend Test.
- Production/SSH execution remains out of scope unless explicitly authorized.
- Preserve the current main/GHCR readiness evidence and production handoff so the remaining work can continue without re-discovery.

## Scope

- Persist active-goal state audit artifacts.
- Persist production redeploy and Real Backend Test handoff artifacts.
- Include current public `pageSize=12` real Work/Study target candidates and production secret blocker evidence.

## Non-Goals

- No production workflow dispatch.
- No production SSH.
- No code/runtime behavior changes.
- No cache, seed, or page size changes.

## TODOs

- [x] Confirm work still matches the plan before implementation.
  - Confirmation: docs/audit persistence only; production execution remains blocked.
- [x] Verify current blockers and ready evidence.
  - Evidence: main merge `389a117ee8cda43e84536c85164bf13afd8e38bf`, CI Main Runtime `25717479751` success, Publish GHCR Main `25717736048` success.
  - Evidence: repo secrets expose only `PROMOTION_TOKEN`; required `PROD_SSH_*` secrets are missing.
  - Evidence: anonymous GHCR manifest inspect succeeds for backend/frontend runtime `:main`.
- [x] Generate persistent active-goal and production handoff reports.
  - Markdown/HTML/JSON under `backend/reports/active-goal-main-promotion-state-2026-05-12/`.
  - Markdown/HTML/JSON under `backend/reports/production-redeploy-real-load-handoff-2026-05-12/`.
- [x] Validate JSON report artifacts.
  - Verification: JSON parse checks passed.

---

# TODO 2026-05-12 - Current Main Server Evidence Handoff Refresh

## User Instructions

- Active goal still requires server-side main runtime image pull/deploy, production preflight, and valid Real Backend Test evidence.
- Production SSH remains out of scope.
- Do not treat public-origin probes or green CI as a substitute for server-side deploy/preflight/load evidence.

## Scope

- Add a refreshed completion audit for the active goal.
- Add a server-side evidence handoff script/report updated to the latest `main` runtime image evidence.
- Keep the work documentation-only; no runtime code change.

## Non-Goals

- No production SSH.
- No production workflow dispatch.
- No Real Backend Test run from this workspace.
- No seed/cache/pageSize target change.

## TODOs

- [x] Recheck current GitHub/GHCR evidence.
  - Evidence: `CI Main Runtime` run `25717479751` succeeded for SHA `389a117ee8cda43e84536c85164bf13afd8e38bf`.
  - Evidence: `Publish GHCR Main` run `25717736048` succeeded for SHA `389a117ee8cda43e84536c85164bf13afd8e38bf`.
  - Evidence: backend digest `sha256:ae0679f8166380e0edef07c3d546cc31c9c291380e6f7eccc691c225c44afdf2`.
  - Evidence: frontend digest `sha256:8cd7cdea54ad6388bcb34f3b8b694cf87e9721a35273cafd35d0f5fea0bfb5f3`.
- [x] Recheck blocker evidence.
  - Evidence: repo secrets still expose only `PROMOTION_TOKEN`; no environments returned.
  - Evidence: latest `Production Runtime Redeploy` runs are May 10 failures on old SHA `757ab203e55cadf8f89ee0da42b7ef580deebad3`.
  - Evidence: public Works list at `pageSize=12` still exposes stale `iconUrl` and `period`, so public origin does not prove current runtime deployment.
- [x] Persist refreshed audit and handoff artifacts.
  - Artifacts: `backend/reports/active-goal-completion-audit-2026-05-12/`.
  - Artifacts: `backend/reports/current-main-server-evidence-handoff-2026-05-12/`.
- [x] Validate refreshed artifacts.
  - Verification: JSON parse checks passed.
  - Verification: `bash -n backend/reports/current-main-server-evidence-handoff-2026-05-12/server-current-main-preflight-load-evidence.sh` passed.
- [x] Confirm completion status.
  - Decision: active goal is still not complete because server-side deploy/preflight/load evidence, result-selected slice, full E2E, and post-slice CI evidence are still missing.

---

# TODO 2026-05-12 - Dynamic Current-Main Handoff Fix

## User Instructions

- Active goal still needs a server-side command that works when the operator runs it from current `main`.
- Production SSH remains out of scope.
- New user planning item: Work/Study search should eventually update results live while typing, not only after pressing Enter.

## Scope

- Fix the current-main handoff script so it does not become stale every time the handoff itself is merged to `main`.
- Keep optional exact-SHA pinning available through `EXPECTED_MAIN_SHA`.
- Record Work/Study live-search as a future UI slice, not part of this production handoff fix.

## Non-Goals

- No production SSH.
- No production workflow dispatch.
- No production Real Backend Test execution from this workspace.
- No Work/Study search implementation in this slice.

## TODOs

- [x] Add contract coverage that rejects a hard-coded default `EXPECTED_MAIN_SHA` in the server-side handoff script.
  - Verification: focused Vitest failed before the script/report update because the script pinned a hard-coded default SHA.
- [x] Change the script to resolve latest `origin/main` dynamically after fetch/pull, while preserving explicit `EXPECTED_MAIN_SHA` override behavior.
- [x] Update handoff Markdown/HTML/JSON so operator guidance describes dynamic latest-main behavior instead of pinning a commit that becomes stale after merge.
- [x] Add future-plan item for live Work/Study search results while typing.
  - Decision: recorded as a future frontend UI slice, not implemented in this production handoff fix.
- [x] Run focused verification: Vitest contract test, shell syntax, JSON parse.
  - Verification: `npx vitest run src/test/current-main-server-evidence-handoff.test.ts --pool=threads --maxWorkers=2` passed, 1 test.
  - Verification: `bash -n backend/reports/current-main-server-evidence-handoff-2026-05-12/server-current-main-preflight-load-evidence.sh` passed.
  - Verification: JSON parse checks passed for handoff, active-goal audit, and dynamic-fix audit reports.
  - Verification: grep rejected hard-coded default SHA and literal `sha-<12hex>` handoff tags.
  - Verification: `npx eslint src/test/current-main-server-evidence-handoff.test.ts` passed.
  - Verification: `git diff --check` passed.
- [x] Write mandatory audit report artifacts.
  - Markdown: `backend/reports/current-main-handoff-dynamic-2026-05-12/current-main-handoff-dynamic-2026-05-12.md`.
  - HTML: `backend/reports/current-main-handoff-dynamic-2026-05-12/current-main-handoff-dynamic-2026-05-12.html`.
  - JSON: `backend/reports/current-main-handoff-dynamic-2026-05-12/current-main-handoff-dynamic-2026-05-12.json`.
- [x] Confirm final work still matches this TODO list.
  - Confirmation: changed only the current-main handoff/audit artifacts, focused contract test, TODO, backup, and mandatory audit report; live Work/Study search remains future scope.

---

# TODO 2026-05-12 - Active Goal Current State Audit

## User Instructions

- Continue toward the active goal.
- Server must pull/deploy the main runtime image and run production preflight.
- Real Backend Test must rerun with `pageSize=12`, no seed, no cache, and real Work/Study URLs.
- Pick the next slice from valid results: HLS fatal fix, public detail serialization/body optimization, or DB/index optimization.
- After that, full E2E and CI must be green.

## Scope

- Audit the actual current state after main promotion and GHCR publish.
- Verify whether public origin proves current main deployment.
- Preserve the concrete next server-side command/evidence requirements.

## Non-Goals

- No production SSH from this workspace.
- No production Real Backend Test against unproven public origin.
- No cache/seed/pageSize workaround.
- No implementation slice selection without valid real-load evidence.

## TODOs

- [x] Recheck current main and GHCR evidence.
  - Evidence: `origin/main` is `acdb7ef8e5070d13b3efb711e168c9da50e5db24`.
  - Evidence: `CI Main Runtime` run `25727092625` succeeded.
  - Evidence: `Publish GHCR Main` run `25727410166` succeeded.
  - Evidence: backend manifest digest `sha256:d546c00a7340c1b5c764004c2d20017364ee57d48e1d415d9330115a88ef3174`.
  - Evidence: frontend manifest digest `sha256:554830dcc8c585451d3e94fdcb5add479858b94f4eb3f725cd887a5629f2c04e`.
- [x] Recheck production deploy path and public-origin proof.
  - Evidence: repo secrets still expose only `PROMOTION_TOKEN`; no GitHub environments returned.
  - Evidence: latest `Production Runtime Redeploy` runs are May 10 failures on old SHA `757ab203e55cadf8f89ee0da42b7ef580deebad3`; failed run `25628772593` exits in `Prepare SSH` with missing `PROD_SSH_PRIVATE_KEY`.
  - Evidence: `https://woonglab.com/api/public/works?page=1&pageSize=12` still exposes stale `iconUrl` and `period` keys, so public origin does not prove current main deployment.
  - Evidence: health/list responses are behind Cloudflare and do not expose current runtime image/SHA proof.
- [x] Verify current-main handoff exists in `origin/main` and defaults dynamically.
  - Evidence: `origin/main` handoff script has `EXPECTED_MAIN_SHA="${EXPECTED_MAIN_SHA:-}"`, resolves `target_main_sha="$(git rev-parse origin/main)"`, and derives `sha-${SHA_SHORT}` tags at runtime.
- [x] Generate current-state completion audit artifacts.
  - Markdown: `backend/reports/active-goal-current-state-audit-2026-05-12/active-goal-current-state-audit-2026-05-12.md`.
  - HTML: `backend/reports/active-goal-current-state-audit-2026-05-12/active-goal-current-state-audit-2026-05-12.html`.
  - JSON: `backend/reports/active-goal-current-state-audit-2026-05-12/active-goal-current-state-audit-2026-05-12.json`.

---

# TODO 2026-05-12 - Future UI Slice: Work/Study Live Search

## User Instructions

- Work/Study 검색창은 현재 Enter를 눌러야만 결과가 갱신된다.
- 다음 계획에는 글자를 입력하는 동안에도 연관 Work/Study 결과가 실시간으로 나오게 하는 UX 개선을 포함한다.

## Scope

- Work list/search page and Study list/search page search-input behavior.
- Debounced live query updates while typing, with request cancellation or stale-response guard.
- Existing Enter-submit behavior remains valid for keyboard users.
- Existing URL query/search-param behavior is preserved where currently supported.
- Loading, empty, and error states remain visible and accessible.

## Acceptance Criteria

- Work 검색창에 입력만 해도 짧은 debounce 후 결과가 갱신된다.
- Study 검색창에 입력만 해도 짧은 debounce 후 결과가 갱신된다.
- Enter 입력도 기존처럼 동작하며 접근성/키보드 흐름을 깨지 않는다.
- 빠르게 타이핑할 때 오래된 응답이 최신 결과를 덮어쓰지 않는다.
- 백엔드 요청을 과도하게 발생시키지 않도록 debounce와 in-flight request 정리가 적용된다.
- Playwright에서 Work/Study 각각 `Enter 없이 타이핑 -> 결과 갱신` 흐름을 검증한다.

## Non-Goals

- 이 항목은 현재 production handoff/evidence verification 작업 범위에는 포함하지 않는다.
- 캐시 우회, seed target, `pageSize=12` 기준 변경은 하지 않는다.
- 백엔드 성능 slice 선택을 이 UI slice로 대체하지 않는다.

---

# TODO 2026-05-12 - Current Main Evidence Verifier Support

## User Instructions

- Continue toward the active goal without using production SSH from this workspace.
- Server-side evidence must prove current `main` runtime image pull/deploy, production preflight, and valid Real Backend Test results.
- Real Backend Test evidence must keep `pageSize=12`, no seed/fixture target, no cache workaround, and real Work/Study public URLs.

## Scope

- Extend the production evidence verifier so it accepts the newer `current-main` handoff output shape.
- Preserve legacy `production-runtime-evidence-*` verifier support.
- Make the current-main handoff script persist preflight output inside the evidence bundle so verification is not based on console-only output.
- Add behavior-focused tests before implementation.

## Non-Goals

- No production SSH or production workflow dispatch.
- No production Real Backend Test execution from this workspace.
- No cache/seed/pageSize target change.
- No backend optimization slice selection without valid returned evidence.

## TODOs

- [x] Confirm work still matches the plan before implementation.
  - Confirmation: current slice is verifier/handoff evidence hardening only.
- [x] Prepare backups before edits.
  - Evidence: `.agent-backups/current-main-evidence-verify-2026-05-12/`.
- [x] RED: add verifier contract coverage for current-main evidence directory and nested tarball bundle.
  - Verification: focused Vitest failed before implementation because the verifier required `prod-runtime-preflight.log` at the input root.
- [x] GREEN: update verifier to support current-main manifest/log names and nested bundle extraction.
  - Verification: focused Vitest passed, 9 tests.
- [x] GREEN: update current-main handoff script to tee preflight output into the evidence directory and manifest.
  - Verification: `bash -n` passed for verifier and handoff scripts.
- [x] Run focused tests, shell syntax, JSON parsing, eslint, and diff hygiene checks.
  - Verification: focused Vitest passed, 9 tests.
  - Verification: `bash -n` passed for verifier and handoff scripts.
  - Verification: `npx eslint src/test/prod-runtime-evidence-verify.test.ts` passed.
  - Verification: JSON parse checks passed for current-main handoff, active-goal audit, and this slice audit JSON.
  - Verification: `git diff --check` passed.
- [x] Generate mandatory audit report artifacts.
  - Markdown: `backend/reports/current-main-evidence-verify-2026-05-12/current-main-evidence-verify-2026-05-12.md`.
  - HTML: `backend/reports/current-main-evidence-verify-2026-05-12/current-main-evidence-verify-2026-05-12.html`.
  - JSON: `backend/reports/current-main-evidence-verify-2026-05-12/current-main-evidence-verify-2026-05-12.json`.
- [x] Confirm final work still matches this TODO list.
  - Confirmation: changed only verifier support, current-main handoff evidence persistence/docs, focused tests, TODO, backups, and audit artifacts; no production execution or load-test target weakening was performed.

---

# TODO 2026-05-12 - Active Goal Post Main Promotion Audit

## User Instructions

- Continue toward the active goal.
- Do not use production SSH from this workspace.
- Confirm whether the active goal can be considered complete after main promotion and GHCR publish.

## Scope

- Audit actual current state after PR #193 merged to `main`.
- Check GitHub Actions, GHCR image availability, handoff/verifier coverage, and public-origin proxy signals.
- Produce a prompt-to-artifact checklist and identify remaining gaps.

## Non-Goals

- No production SSH.
- No server-side deploy/preflight/load execution from this workspace.
- No cache, seed, or `pageSize` workaround.
- No backend optimization slice selection without returned server evidence.

## TODOs

- [x] Confirm work still matches the plan before audit.
  - Confirmation: audit-only, no production execution.
- [x] Prepare backup before editing TODO/report artifacts.
  - Evidence: `.agent-backups/active-goal-post-main-promotion-audit-2026-05-12/`.
- [x] Inspect main promotion, CI, GHCR, handoff script, and verifier state.
  - Evidence: `origin/main` is `3885eaeb16266de752fc984258bc30aafd3d7a62`.
  - Evidence: `CI Main Runtime` run `25730480720` succeeded.
  - Evidence: `Publish GHCR Main` run `25730793221` succeeded.
  - Evidence: backend digest `sha256:112355097189a1f160ca5d4335bd00a95301b82c8c7b9e50408c932ce4eb774b`.
  - Evidence: frontend digest `sha256:e43ea1f01e4e9d6bd550ea78c4bf2a13dccdeaad3a499120850040a319518232`.
- [x] Inspect public-origin proxy signals without treating them as completion proof.
  - Evidence: `https://woonglab.com/api/health` returned HTTP 200.
  - Evidence: public Works list still exposes stale `iconUrl` and `period`, so public origin does not prove current runtime deployment.
  - Evidence: public Blogs list does not expose stale body/content keys in the sampled response.
- [x] Generate prompt-to-artifact audit report artifacts.
  - Markdown: `backend/reports/active-goal-post-main-promotion-audit-2026-05-12/active-goal-post-main-promotion-audit-2026-05-12.md`.
  - HTML: `backend/reports/active-goal-post-main-promotion-audit-2026-05-12/active-goal-post-main-promotion-audit-2026-05-12.html`.
  - JSON: `backend/reports/active-goal-post-main-promotion-audit-2026-05-12/active-goal-post-main-promotion-audit-2026-05-12.json`.
- [x] Validate audit artifacts.
  - Verification: audit JSON parse passed.
  - Verification: `git diff --check -- todolist-2026-05-12.md backend/reports/active-goal-post-main-promotion-audit-2026-05-12` passed.
- [x] Confirm final audit conclusion.
  - Decision: active goal is not complete because returned server-side deploy/preflight/load evidence is still missing, and public origin still exposes stale Work list keys.

---

# TODO 2026-05-12 - Current Main Handoff Digest Pin Fail-Fast

## User Instructions

- Continue toward the active goal without production SSH from this workspace.
- Strengthen the server-side evidence path so the server run can prove it is using the intended immutable runtime images.

## Scope

- Add optional expected backend/frontend image digest inputs to the current-main server handoff.
- Fail the server-side handoff immediately if the resolved GHCR digest does not match an explicitly provided expected digest.
- Update handoff docs and focused contract coverage.

## Non-Goals

- No production SSH.
- No production Real Backend Test execution from this workspace.
- No cache/seed/pageSize target change.
- No backend optimization slice selection without returned server evidence.

## TODOs

- [x] Confirm work still matches the plan before implementation.
  - Confirmation: current slice is handoff evidence hardening only.
- [x] Prepare backups before edits.
  - Evidence: `.agent-backups/current-main-handoff-digest-pin-2026-05-12/`.
- [x] RED: add contract coverage requiring optional expected digest inputs and mismatch fail-fast checks.
  - Verification: focused Vitest failed before implementation because the script did not define `EXPECTED_*_IMAGE_DIGEST` inputs.
- [x] GREEN: update current-main handoff script to enforce expected backend/frontend digests when provided.
- [x] Update handoff Markdown/HTML/JSON with digest pin usage.
- [x] Run focused tests, shell syntax, JSON parse, eslint, and diff hygiene checks.
  - Verification: focused Vitest passed, 2 tests.
  - Verification: handoff shell syntax passed.
  - Verification: current-main handoff and post-main audit JSON parse checks passed.
  - Verification: ESLint passed for the focused test file.
- [x] Generate mandatory audit report artifacts.
  - Markdown: `backend/reports/current-main-handoff-digest-pin-2026-05-12/current-main-handoff-digest-pin-2026-05-12.md`.
  - HTML: `backend/reports/current-main-handoff-digest-pin-2026-05-12/current-main-handoff-digest-pin-2026-05-12.html`.
  - JSON: `backend/reports/current-main-handoff-digest-pin-2026-05-12/current-main-handoff-digest-pin-2026-05-12.json`.
- [x] Confirm final work still matches this TODO list.
  - Confirmation: changed only current-main handoff digest pin support, related docs/audit artifacts, focused test, TODO, and backups; no production execution or load-test target weakening was performed.

---

# TODO 2026-05-12 - Future Work/Study Live Search Plan

## User Instructions

- Work/Study search currently updates only after pressing Enter.
- Add to the next plan: typing in the search box should show related Work/Study content in realtime.

## Scope

- Add search-as-you-type behavior for Work and Study search/list pages.
- Keep `pageSize=12` behavior unless a separate product decision changes the visible page contract.
- Use debounce or equivalent request control so the UI does not send one backend request per raw keystroke.
- Reset pagination when the query changes.
- Preserve keyboard accessibility: Enter should still work, but it must not be required.
- Preserve deterministic empty, loading, and error states.
- Verify that public Work/Study list DTOs still include only fields needed for visible list/search cards.

## Non-Goals

- Do not weaken Real Backend Test targets.
- Do not introduce seed/synthetic content as the normal UX target.
- Do not add cache as the primary fix.
- Do not change production deployment flow as part of this UX slice.

## TODOs

- [ ] Confirm the current Work/Study search components, URL query sync, and API query parameters.
- [ ] RED: add focused tests proving typed text updates results without requiring Enter.
- [ ] RED: add coverage for pagination reset and empty/loading states when the query changes.
- [ ] GREEN: implement debounced search-as-you-type for both Work and Study.
- [ ] GREEN: keep Enter submit behavior as an immediate search shortcut.
- [ ] Check backend query/list DTO shape so list endpoints fetch only fields visible in public cards.
- [ ] Run focused Vitest coverage for search behavior.
- [ ] Run Playwright coverage for Work and Study search from the real browser UI.
- [ ] Record validation results and risks in the implementation audit when this slice starts.

---

# TODO 2026-05-12 - Main Runtime Digest-Pin Promotion Status

## User Instructions

- Continue toward the active goal.
- Do not use production SSH from this workspace.
- Main should be usable by pulling the images generated from the current `main` branch and supplying only secrets in `.env`.

## Scope

- Confirm digest-pin handoff changes reached `dev` and `main`.
- Confirm `dev` CI, main promotion PR CI, main push CI, and GHCR publish all succeeded.
- Produce the latest immutable SHA/digest server handoff command.
- Keep the active goal open until server-side deploy/preflight/load evidence is returned and verified.

## Non-Goals

- No production SSH.
- No server-side Real Backend Test execution from this workspace.
- No seed/cache/pageSize workaround.
- No backend optimization slice selection without returned load evidence.

## TODOs

- [x] Confirm work still matches the active goal before auditing.
  - Confirmation: this status slice only verifies GitHub/main/GHCR readiness and prepares the server handoff command.
- [x] Prepare backups before editing TODO/report artifacts.
  - Evidence: `.agent-backups/main-runtime-digest-pin-promotion-2026-05-12/`.
- [x] Verify digest-pin PR reached `dev`.
  - Evidence: PR #194 merged at `2026-05-12T11:46:51Z`, merge commit `62ac17e97567babb1a3f48309b0b171abb590ba3`.
  - Evidence: `CI Dev` run `25732372942` completed successfully for `62ac17e97567babb1a3f48309b0b171abb590ba3`.
- [x] Verify runtime-only promotion reached `main`.
  - Evidence: PR #195 `release/main-promote -> main` merged.
  - Evidence: `origin/main` is `bc88b3fd19ded9f463b1fc92a08b9b7fa9782192`.
  - Evidence: PR `CI Main Runtime` run `25732671941` completed successfully.
  - Evidence: main push `CI Main Runtime` run `25733203959` completed successfully.
- [x] Verify GHCR images published for latest `main`.
  - Evidence: `Publish GHCR Main` run `25733621502` completed successfully.
  - Evidence: backend image `ghcr.io/kimwoonggon/woong-blog-aspcore-nextjs-runtime-backend:sha-bc88b3fd19de` digest `sha256:7c2b6c32785e874589dddce2b949ad9ca335e0ee4d944f7efd01e7d4c0232210`.
  - Evidence: frontend image `ghcr.io/kimwoonggon/woong-blog-aspcore-nextjs-runtime-frontend:sha-bc88b3fd19de` digest `sha256:e7b025c2304a9085cd9dde17737028add6ae56f7e29d3584580d3cb420cacc85`.
- [x] Check public-origin proxy signal without treating it as completion proof.
  - Evidence: `https://woonglab.com/api/health` returned 200.
  - Evidence: public Work list still exposes stale `iconUrl` and `period`, so public origin does not prove latest main deployment.
  - Evidence: public Blog list sample did not expose stale body/content fields.
- [x] Generate audit report artifacts.
  - Markdown: `backend/reports/main-runtime-digest-pin-promotion-2026-05-12/main-runtime-digest-pin-promotion-2026-05-12.md`.
  - HTML: `backend/reports/main-runtime-digest-pin-promotion-2026-05-12/main-runtime-digest-pin-promotion-2026-05-12.html`.
  - JSON: `backend/reports/main-runtime-digest-pin-promotion-2026-05-12/main-runtime-digest-pin-promotion-2026-05-12.json`.
- [x] Validate audit artifacts.
  - Verification: audit JSON parse passed.
  - Verification: `git diff --check -- todolist-2026-05-12.md backend/reports/main-runtime-digest-pin-promotion-2026-05-12` passed.
  - Verification: grep confirmed latest main SHA and backend/frontend digests are present in the audit report.
- [x] Confirm final state against the active objective before reporting.
  - Decision: active goal is not complete because server deploy/preflight evidence, Real Backend Test rerun evidence, result-based slice selection/implementation, and after-slice full E2E are still missing.
- [ ] Persist this TODO/report-only status slice through a dev PR.
- [ ] Verify the status PR CI and merge outcome.

---

# TODO 2026-05-12 - Main Works Responsive Grid Fix

## User Instruction

- Main page Works should support up to 8 items as a `2 row x 4 column` desktop layout.
- Mobile mode should be one card wide and show up to 4 Works.
- The current 4-item state looks wrong because a 3-column desktop grid leaves two empty slots in the second row.
- Light mode page background can stay beige/off-white, but Work/Study detail reading content should sit on a white surface.

## Goal

- Make the homepage Works section render a stable, balanced public layout:
- Desktop: up to 8 cards, 4 columns, at most 2 rows.
- Tablet: 2 columns.
- Mobile: 1 column and only the first 4 cards visible.
- Preserve existing public-card DTO shape and link behavior.
- Keep the light-mode page atmosphere, but make Work/Study detail reading surfaces explicitly white.

## Non-Goals

- Do not change production SSH/deploy steps.
- Do not change public Works list page pagination or `pageSize=12` load-test rules.
- Do not add cache workarounds.
- Do not seed or fake the production target behavior.

## TODOs

- [x] Confirm actual implementation still matches this TODO before code changes.
  - Confirmation: existing backend was `Take(4)` and frontend grid was `md:grid-cols-2 xl:grid-cols-3`; detail reading bodies were transparent.
- [x] RED: update backend component coverage so `/public/home` returns up to 8 featured works.
  - Verification: focused backend test failed against `Take(4)` before implementation.
- [x] RED: update browser coverage so desktop Works grid expects 4 columns.
- [x] RED: update mobile browser coverage so the home Works section has 8 DOM cards but only 4 visible on phone width.
- [x] RED: update browser coverage so Work/Study detail reading surfaces are white in light mode.
  - Verification: focused Playwright test failed before implementation because detail body background was transparent.
- [x] GREEN: update `HomeQueryStore.GetFeaturedWorksAsync` to fetch at most 8 published works.
- [x] GREEN: add a frontend guard that slices featured Works to 8.
- [x] GREEN: update the homepage Works grid to 1 column mobile, 2 columns tablet, 4 columns desktop.
- [x] GREEN: hide featured Works after the fourth card below the tablet breakpoint.
- [x] GREEN: add white surfaced containers to Work/Study detail reading bodies without changing the overall page background.
- [x] Run focused backend component test.
  - Verification: `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter "FullyQualifiedName~GetHomeQueryHandler_ReturnsUpToEightFeaturedWorks_ForBalancedDesktopHomeGrid" --logger "console;verbosity=minimal"` passed.
- [x] Run focused Playwright specs for homepage Works layout.
  - Verification: `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:13000 npx playwright test tests/ui-improvement-featured-works-grid.spec.ts --project=chromium-public --grep "eight DOM cards|two columns on tablet and four on desktop" --workers=1` passed.
  - Verification: `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:13000 npx playwright test tests/ui-quality-blog-detail-visuals.spec.ts --project=chromium-authenticated --grep "white reading surface" --workers=1` passed.
  - Verification: `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:13000 npx playwright test tests/public-detail-toc-fallback.spec.ts tests/public-blog-toc-layout.spec.ts tests/public-work-toc.spec.ts --project=chromium-public --workers=1` passed.
  - Verification: `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:13000 npx playwright test tests/ui-quality-responsive-admin.spec.ts --project=chromium-authenticated --workers=1` passed.
- [x] Run frontend static validation.
  - Verification: `npm run typecheck` passed.
  - Verification: `npm run lint` passed with 5 pre-existing warnings and 0 errors.
- [x] Write mandatory audit report artifacts.
  - Markdown: `backend/reports/home-works-responsive-grid-2026-05-12/home-works-responsive-grid-2026-05-12.md`.
  - HTML: `backend/reports/home-works-responsive-grid-2026-05-12/home-works-responsive-grid-2026-05-12.html`.
  - JSON: `backend/reports/home-works-responsive-grid-2026-05-12/home-works-responsive-grid-2026-05-12.json`.
  - Verification: audit JSON parse passed.
  - Verification: `git diff --check` passed.

---

# TODO 2026-05-12 - Current Main Handoff 2c6b Refresh

## User Instruction

- Continue toward the active goal: server pulls/deploys the current `main` runtime image, production preflight runs, Real Backend Test reruns with `pageSize=12`, no seed/cache workaround, real Work/Study URLs, then a result-based next slice is chosen and validated through full E2E and CI.

## Goal

- Make the server-side evidence handoff match the actual latest `origin/main` and GHCR runtime images.
- Persist a blocker audit explaining why `Production Runtime Redeploy` should not be dispatched until its required secrets exist.

## Non-Goals

- Do not SSH from this workspace.
- Do not run an intentionally failing deploy workflow when required secrets are absent.
- Do not treat public-origin HTTP 200 as proof that current `main` is deployed.
- Do not mark the active goal complete.

## TODOs

- [x] Confirm current `origin/main`.
  - Verification: `git rev-parse origin/main` returned `2c6b8b418990f5388aabbf24c07c3bfe6c3f507c`.
- [x] Confirm current `main` CI and GHCR publish.
  - Verification: `CI Main Runtime` run `25738704187` succeeded.
  - Verification: `Publish GHCR Main` run `25739101934` succeeded.
- [x] Verify the existing dynamic server evidence handoff covers current `main`.
  - Verification: handoff uses dynamic `origin/main` resolution and `sha-${resolvedShaShort}` image tags.
  - Verification: `EXPECTED_MAIN_SHA`, `EXPECTED_BACKEND_IMAGE_DIGEST`, and `EXPECTED_FRONTEND_IMAGE_DIGEST` remain available for exact pinning.
  - Verification: no old pinned `389a117` SHA/digest remains in the handoff or blocker reports.
- [x] Confirm GitHub Actions redeploy blocker.
  - Verification: repo secrets currently expose `PROMOTION_TOKEN` only.
  - Missing for workflow dispatch: `PROD_SSH_HOST`, `PROD_SSH_USER`, `PROD_SSH_PRIVATE_KEY`, `PROD_SSH_KNOWN_HOSTS`.
- [x] Probe public origin without accepting it as completion evidence.
  - Verification: health, Works list, and Blogs list returned HTTP 200.
  - Finding: Works list still exposed stale `iconUrl` and `period`, so public origin does not prove current `main` runtime.
- [x] Write blocker audit artifacts.
  - Markdown: `backend/reports/active-goal-runtime-redeploy-blocker-2026-05-12/active-goal-runtime-redeploy-blocker-2026-05-12.md`.
  - HTML: `backend/reports/active-goal-runtime-redeploy-blocker-2026-05-12/active-goal-runtime-redeploy-blocker-2026-05-12.html`.
  - JSON: `backend/reports/active-goal-runtime-redeploy-blocker-2026-05-12/active-goal-runtime-redeploy-blocker-2026-05-12.json`.
- [x] Run focused artifact validation.
  - Verification: handoff JSON and blocker JSON parsed successfully.
  - Verification: `bash -n backend/reports/current-main-server-evidence-handoff-2026-05-12/server-current-main-preflight-load-evidence.sh` passed.
  - Verification: grep found no old `389a117` SHA/digest evidence in the refreshed handoff/blocker reports.
- [ ] Push this report-only handoff refresh to a dev PR.
- [ ] Verify PR CI and merge/promotion state.
