# TODO: Public Detail Centered TOC And Light Surface - 2026-05-16

## User Instructions Captured

- The wider `On This Page` rail is good, but in light mode the rail/card/background basic color should be white.
- The center Work/Study reading body being white is good; keep that.
- Related cards and the surrounding background should also be white in light mode.
- The wider `On This Page` rail must not push the rendered Work/Study article body left.
- The article body and `On This Page` rail should behave as separate layout pieces, with the article centered independently and the rail visually detached.
- The light-mode basic color is broadly unsatisfactory and should be cleaned up.

## Constraints And Scope

- Work from clean worktree `woong-blog-aspcore-nextjs-detail-layout-20260516` on branch `codex/public-detail-centered-toc-20260516`, based on `origin/dev` at `2820246`.
- Keep edits scoped to public Work/Study detail layout, light-mode surface tokens/classes, focused UI tests, TODO, and audit artifacts.
- Do not use or revert unrelated dirty changes in the primary worktree.
- Use TDD-style focused Playwright coverage for the visual regression before implementation.
- Generate persistent audit artifacts before completion.

## Skill Search And Specialist Guidance

- Used local skills: `tdd`, `nextjs-app-router-patterns`, `e2e-testing-patterns`, `ui-ux-pro-max`.
- External skill search: `npx --yes skills find "Next.js public detail layout white surface Playwright"` found generic Playwright/Next UI skills; no new external skill was installed.

## Work Items

- [x] Confirm current goal and clean branch.
  - Verification: active goal is the user’s centered TOC/light-mode detail feedback; branch is clean at `2820246`.
- [x] Back up target files before edits.
  - Verification: copied target page/component/test/style files under `.agent-backups/public-detail-centered-toc-2026-05-16/`.
- [x] Add focused Playwright tests for centered article body, detached TOC rail, and white light-mode surfaces.
  - Verification: added VA-125 for independent viewport-centered Work/Study detail body with a separated right TOC rail, and VA-126 for white body/page/body/content/TOC/related-card light-mode surfaces.
  - Red verification: `VA-125|VA-126` failed before implementation; body center was 152px off at 1440px and light-mode body background was 7 RGB levels below white.
- [x] Update light-mode base/background/card surfaces to white where requested.
  - Verification: `VA-126` passed locally and through Docker; light-mode body, page shell, detail body, content, TOC, related shell, and related cards are white.
- [x] Update Work/Study detail layout so the body stays viewport-centered while TOC remains a separate right rail.
  - Verification: `VA-125` passed locally and through Docker; the Work/Study body remains viewport-centered while TOC stays to the right with readable width.
- [x] Remove Tailwind source-scan instability caused by generated reports/backups.
  - Verification: replaced unsafe safe-area arbitrary classes with named CSS utilities and restricted Tailwind source scanning to `src`; local and Docker `npm run build` both passed.
- [x] Run focused Playwright, lint/typecheck, and relevant regressions.
  - Verification: focused `VA-125|VA-126` passed; relevant Playwright regression subset passed 25/25; Vitest component subset passed 14/14; ESLint passed; `npm run typecheck` passed; `npm run build` passed; Docker build/readiness passed; Docker focused `VA-125|VA-126` passed.
- [x] Generate Markdown/HTML/JSON audit artifacts.
  - Verification: generated `backend/reports/public-detail-centered-toc-2026-05-16/public-detail-centered-toc-2026-05-16.md`, `.html`, and `.json`.
- [x] Decide and execute remote promotion only if this follow-up change is validated and still matches the goal.
  - Verification: local and Docker validation passed; remote promotion was not executed in this step because the validated Docker URL should be available for inspection before opening/pushing the follow-up branch.

## Plan Match Check After Implementation

- The implementation still matches the user’s latest request: light-mode basic surfaces are white, related card/background surfaces are white, and the wider TOC rail no longer shifts the central Work/Study article body left.
- Scope expanded only to replace existing safe-area arbitrary Tailwind classes and constrain Tailwind source scanning, because Docker/local builds were otherwise blocked by report/backup files being scanned as class sources.

## Promotion Plan Added After User Approval

- User instruction: push this validated branch toward `dev`; if CI is clean, promote to `main`; if main CI is clean, leave the release clean.
- [ ] Commit only the scoped code, tests, TODO, and audit report artifacts.
- [ ] Push `codex/public-detail-centered-toc-20260516` and open a PR to `dev`.
- [ ] Wait for PR CI and inspect failures if any.
- [ ] Merge to `dev` only if CI is clean.
- [ ] Promote the validated `dev` result to `main` through the repository’s release branch/PR flow.
- [ ] Wait for main CI and inspect failures if any.
- [ ] Generate final promotion audit artifacts with PR, commit, CI, and release evidence.

## Plan Match Check Before Implementation

- The plan matches the user’s latest UI feedback: white light-mode basic surfaces and centered article body independent from the wider TOC rail.
- No production/test code has been edited yet in this follow-up worktree.
