# TODO - 2026-05-10

## Active Goal Context
- Continue the backend performance objective without repeating completed work.
- Completed prior slices already cover stored public thumbnail/cover fields, public/admin content split, public detail projection, DB/pool diagnostics, source-generated public JSON metadata, target payload/receive metrics, and nginx/app timing headers.
- This slice targets the remaining observability gap: a production operator must be able to prove the running server is not stale or bypassing nginx before interpreting Real Backend Test numbers.

## Slice: Production Runtime Preflight For Real Backend Test

### User Instructions Mapped
- `DB connection/pool pressure 확인`: verify running backend exposes pool/env and admin diagnostics samples before load runs.
- `nginx/app timing 계측 완성`: verify public API responses expose `X-Nginx-Request-Time` and `X-App-Elapsed-Ms` through nginx.
- `실제 병목이 app CPU인지 DB wait인지 network/serialization인지 보이게`: verify gzip, cgroup CPU/memory visibility, DB command samples, and LoadTesting base URL before tuning.
- `dev/main까지 반복 수행`: include the script in `scripts/main-runtime-allowlist.txt` so runtime promotion can carry it to main.

### TODO Mapping
- [x] RED: add behavior tests for the prod runtime preflight command.
- [x] GREEN: implement `scripts/prod-runtime-preflight.sh` without `jq` and without secret leakage.
- [x] GREEN: fail on backend-direct load test base URL, missing nginx/app timing, missing services, and missing required DB diagnostics samples.
- [x] GREEN: include the script in main runtime promotion allowlist.
- [x] Verify focused Vitest contract.
- [x] Verify shell syntax and diff whitespace.
- [x] Generate persistent audit report artifacts.

### Progress
- RED observed: focused Vitest failed before implementation because `scripts/prod-runtime-preflight.sh` did not exist.
- GREEN implemented: preflight checks compose config, running backend/frontend/nginx/db services, backend env, cgroup CPU/memory, public app/nginx timing headers, gzip public JSON, and optional required admin DB diagnostics.
- GREEN implemented: fake-secret tests assert connection string/password-like values are not printed to stdout/stderr.
- PASS: `npm test -- --run src/test/prod-runtime-preflight.test.ts` passed 4/4.
- PASS: `npm test -- --run src/test/prod-runtime-preflight.test.ts src/test/load-test-dashboard.test.ts` passed 32/32.
- PASS: `npm run typecheck` passed.
- PASS: `for script in scripts/prod-runtime-preflight.sh scripts/promote-main-runtime.sh scripts/ci-compose-smoke.sh; do bash -n "$script"; done` passed.
- PASS: `git diff --check -- scripts/prod-runtime-preflight.sh src/test/prod-runtime-preflight.test.ts scripts/main-runtime-allowlist.txt todolist-2026-05-07.md` passed.
- PASS: persistent audit report artifacts generated under `backend/reports/prod-runtime-preflight-2026-05-10/`.
- PASS: `python3 -m json.tool backend/reports/prod-runtime-preflight-2026-05-10/prod-runtime-preflight-2026-05-10.json` passed.

## Slice: Production Real Load Step Runner

### User Instructions Mapped
- `backend test를 부하테스트 거치고`: provide a repeatable production-safe constant-arrival-rate runner for the real public API mix.
- `pageSize=12 유지`: keep list targets at `/api/public/works?page=1&pageSize=12` and `/api/public/blogs?page=1&pageSize=12` by default.
- `seed같은걸 안했을때`: require caller-provided real Work/Study read paths; do not force seeded slugs.
- `실제 병목이 app CPU인지 DB wait인지 network/serialization인지`: capture per-step p95/p99, 5xx, dropped iterations, response bytes, receiving time, app/nginx timing headers, and output aggregate summaries.
- `다음개선계획을 수립하고 임무를 수행`: use step results to decide whether the next code slice should target payload/serialization, DB pool/resource pressure, or app CPU.

### TODO Mapping
- [x] RED: add behavior tests for `scripts/prod-real-load-steps.sh` before implementing the script.
- [x] GREEN: implement the script with no auth dependency and no seed target shortcut.
- [x] GREEN: generate per-rate k6 summary JSON and aggregate JSON/Markdown artifacts.
- [x] GREEN: include the script in main runtime promotion allowlist.
- [x] Verify focused Vitest contract.
- [x] Verify shell syntax, JSON output, typecheck, and diff whitespace.
- [x] Generate persistent audit report artifacts for this slice.

### Progress
- Planned after verifying PR #117/main promotion and GHCR main publish are already complete for the prior runtime preflight slice.
- RED observed: focused Vitest failed because `scripts/prod-real-load-steps.sh` did not exist yet.
- GREEN implemented: `scripts/prod-real-load-steps.sh` requires real Work/Study read paths, enforces `LIST_PAGE_SIZE=12`, runs configured constant-rate k6 steps, optionally captures admin diagnostics, and writes aggregate JSON/Markdown.
- GREEN implemented: `scripts/main-runtime-allowlist.txt` includes the step runner so main runtime promotion carries it.
- PASS: `npm test -- --run src/test/prod-real-load-steps.test.ts` passed 3/3.
- PASS: `npm test -- --run src/test/prod-real-load-steps.test.ts src/test/prod-runtime-preflight.test.ts src/test/load-test-dashboard.test.ts` passed 35/35.
- PASS: `for script in scripts/prod-real-load-steps.sh scripts/prod-runtime-preflight.sh scripts/promote-main-runtime.sh scripts/ci-compose-smoke.sh; do bash -n "$script"; done` passed.
- PASS: `git diff --check -- scripts/prod-real-load-steps.sh src/test/prod-real-load-steps.test.ts scripts/main-runtime-allowlist.txt todolist-2026-05-10.md` passed.
- PASS: `npm run typecheck` passed.
- PASS: persistent audit report artifacts generated under `backend/reports/prod-real-load-steps-2026-05-10/`.
- PASS: `python3 -m json.tool backend/reports/prod-real-load-steps-2026-05-10/prod-real-load-steps-2026-05-10.json` passed.

## Slice: Production Public Contract Preflight Guard

### User Instructions Mapped
- `실제 병목이 app CPU인지 DB wait인지 network/serialization인지 보이게`: stale runtime이면 이미 제거한 public video/admin payload 비용을 현재 병목으로 오판하므로, load 전에 runtime contract를 검증한다.
- `seed같은걸 안했을때`: operator-provided real Work read path로 검증한다. seed/default target을 강제하지 않는다.
- `다음개선계획을 수립하고 임무를 수행`: 부하테스트 해석 전에 최신 public DTO 반영 여부를 확인하는 gate를 추가한다.

### TODO Mapping
- [x] RED: `prod-runtime-preflight.sh`가 required public Work detail contract 검증을 수행하도록 테스트를 먼저 추가했다. 초기 실행에서 stale DTO와 missing WORK_READ_PATH 기대가 실패하며 RED를 확인했다.
- [x] GREEN: `REQUIRE_PUBLIC_WORK_VIDEO_CONTRACT=1`이면 `WORK_READ_PATH`를 요구하고, public videos payload에 admin/mutation-only fields가 남아 있으면 실패시킨다.
- [x] Verify focused Vitest contract, related Vitest bundle, shell syntax, whitespace, and typecheck.
- [x] Update persistent audit report artifacts.

### Pre-Execution Check
- Current production probe shows public Work video payload still contains `originalFileName`, `fileSize`, and `createdAt`, while current repo `PublicWorkVideoDto` no longer exposes those fields.
- Actual code work still matches this TODO: this is an observability/deployment correctness gate, not another query/DTO optimization repeat.

### Progress - Production Public Contract Preflight Guard
- RED observed: `npm test -- --run src/test/prod-runtime-preflight.test.ts` failed before implementation because the script did not require `WORK_READ_PATH` or detect stale public video fields.
- GREEN implemented: `REQUIRE_PUBLIC_WORK_VIDEO_CONTRACT=1` probes the real public Work detail path and fails on `originalFileName`, `fileSize`, or `createdAt` in video payloads.
- PASS: `npm test -- --run src/test/prod-runtime-preflight.test.ts` passed 6/6.
- PASS: `npm test -- --run src/test/prod-runtime-preflight.test.ts src/test/prod-real-load-steps.test.ts src/test/load-test-dashboard.test.ts` passed 37/37.
- PASS: `for script in scripts/prod-runtime-preflight.sh scripts/prod-real-load-steps.sh scripts/promote-main-runtime.sh scripts/ci-compose-smoke.sh; do bash -n "$script"; done` passed.
- PASS: `git diff --check -- scripts/prod-runtime-preflight.sh src/test/prod-runtime-preflight.test.ts todolist-2026-05-10.md` passed.
- PASS: `npm run typecheck` passed.
- Current production probe remains stale until the server pulls/recreates the newly published runtime image; do not interpret production load results before this preflight passes on server.
- PASS: persistent audit report artifacts generated under `backend/reports/prod-runtime-public-contract-preflight-2026-05-10/`.
- PASS: `python3 -m json.tool backend/reports/prod-runtime-public-contract-preflight-2026-05-10/prod-runtime-public-contract-preflight-2026-05-10.json` passed.

## Slice: Staging Publish LoadTesting Base URL CI Fix

### User Instructions Mapped
- `dev 에 ci 를 통해 반영하고 main까지 반영`: fix the dev image publishing workflow failure so the CI/publish/promotion chain can complete.
- `LoadTesting__BaseUrl은 nginx/public 경로여야 함`: staging publish smoke must enforce the same public nginx base URL contract as prod, not omit it or fall back to backend-direct.

### TODO Mapping
- [x] RED: add a compose contract test proving both prod and staging expose `LoadTesting__BaseUrl` through `NEXT_PUBLIC_SITE_URL`.
- [x] GREEN: add `LoadTesting__BaseUrl` to `docker-compose.staging.yml` and `.env.staging.example`.
- [x] Verify focused Vitest contract.
- [x] Verify compose policy locally for staging/main mode without publishing images where feasible.
- [x] Generate persistent audit report artifacts.
- [ ] Push to dev, watch CI, confirm GHCR dev publish, and promote to main if runtime tree changes require it.

### Pre-Execution Check
- Current work still matches the TODO: the failed `Publish GHCR Dev` log says `main compose must default LoadTesting__BaseUrl to the nginx/public base URL (http://localhost)`.
- The previous production public contract preflight guard is already merged to `dev`; this slice only fixes staging publish compose parity.

### Progress - Staging Publish LoadTesting Base URL CI Fix
- RED observed: `npm test -- --run src/test/compose-loadtesting-baseurl.test.ts` failed for `docker-compose.staging.yml` and `.env.staging.example` because staging did not expose/document `LoadTesting__BaseUrl`.
- GREEN implemented: `docker-compose.staging.yml` now injects `LoadTesting__BaseUrl` from `LoadTesting__BaseUrl` or `NEXT_PUBLIC_SITE_URL`, falling back to `https://staging.example.com`.
- GREEN implemented: `.env.staging.example` documents the same public staging origin for Real Backend Load Test.
- PASS: `npm test -- --run src/test/compose-loadtesting-baseurl.test.ts` passed 4/4.
- PASS: `docker compose --env-file <tmp> -f docker-compose.staging.yml config | grep -F 'LoadTesting__BaseUrl: http://localhost'` passed.
- PASS: `bash -n scripts/ci-compose-smoke.sh scripts/prod-runtime-preflight.sh scripts/prod-real-load-steps.sh` passed.
- PASS: `git diff --check -- docker-compose.staging.yml .env.staging.example src/test/compose-loadtesting-baseurl.test.ts todolist-2026-05-10.md` passed.
- PASS: `npm test -- --run src/test/compose-loadtesting-baseurl.test.ts src/test/prod-runtime-preflight.test.ts src/test/prod-real-load-steps.test.ts` passed 13/13.
- PASS: `npm run typecheck` passed.
- PASS: persistent audit report artifacts generated under `backend/reports/staging-loadtesting-baseurl-ci-2026-05-10/`.
- PASS: `python3 -m json.tool backend/reports/staging-loadtesting-baseurl-ci-2026-05-10/staging-loadtesting-baseurl-ci-2026-05-10.json` passed.

## Slice: Main Runtime Compose LoadTesting Test Stability

### User Instructions Mapped
- `dev 에 ci 를 통해 반영하고 main까지 반영`: unblock the automated main promotion PR after the dev CI/publish chain succeeds.
- `ci 통과하면 commit`: fix the concrete `CI Main Runtime` frontend unit-test failure before continuing the next backend performance slice.
- `반복적으로 수행`: keep the promotion loop healthy so later backend performance changes can move through dev and main automatically.

### TODO Mapping
- [x] RED: inspect PR #123 `CI Main Runtime` frontend failure and capture the failing Vitest behavior.
- [x] GREEN: make the compose LoadTesting contract test safe in the main runtime tree while preserving dev coverage for prod+staging files.
- [x] Verify focused Vitest contract on the dev tree.
- [x] Verify focused Vitest contract against a generated main-runtime allowlist tree.
- [x] Verify typecheck, diff whitespace, and relevant CI-fix checks.
- [x] Generate persistent audit report artifacts.
- [ ] Push PR to dev, watch CI, then confirm main promotion PR/checks recover.

### Pre-Execution Check
- Current work still matches the TODO: PR #123 includes `src/test/compose-loadtesting-baseurl.test.ts`, but the main runtime tree intentionally excludes `docker-compose.staging.yml` and `.env.staging.example`.
- `Publish GHCR Dev` run `25620045881` is now green, so this slice only targets the remaining main promotion blocker.

### Progress - Main Runtime Compose LoadTesting Test Stability
- RED observed: PR #123 `Frontend lint, types, and unit tests` failed in `CI Main Runtime` because `src/test/compose-loadtesting-baseurl.test.ts` tried to read missing `docker-compose.staging.yml` and `.env.staging.example` from the runtime-only main tree.
- GREEN implemented: `src/test/compose-loadtesting-baseurl.test.ts` now verifies every available compose/env pair, requires prod to be present, and fails if a compose file and its matching env example are promoted inconsistently.
- PASS: `npm test -- --run src/test/compose-loadtesting-baseurl.test.ts` passed 5/5 on the dev tree.
- PASS: generated a minimal main-runtime-style tree without staging compose/env files and `npm test -- --run src/test/compose-loadtesting-baseurl.test.ts` passed 3/3.
- PASS: `npm test -- --run src/test/compose-loadtesting-baseurl.test.ts src/test/prod-runtime-preflight.test.ts src/test/prod-real-load-steps.test.ts` passed 14/14.
- PASS: `npm run typecheck` passed.
- PASS: `git diff --check -- src/test/compose-loadtesting-baseurl.test.ts todolist-2026-05-10.md` passed.

## Slice: Public Content Body Single Render Source

### User Instructions Mapped
- `큰 본문 payload 구조 분리`: keep public detail body as `metadata + content`, but avoid sending duplicate render sources inside `content`.
- `본문 필드는 렌더링에 필요한 최소 형태만`: return only one body source (`markdown` when available because the frontend renderer already prefers it, otherwise `html`).
- `JSON serialization allocation 줄이기`: shrink public detail DTO payload and serializer work when both public body fields are present.
- `cache를 잡는건 빼도록`: no cache behavior is changed.
- `pageSize=12 유지`, `seed같은걸 안했을때`: no load-test target or list page-size semantics are changed.

### TODO Mapping
- [x] RED: add DTO/Postgres contract coverage proving public detail omits duplicate body sources when both stored public body fields exist.
- [x] GREEN: update `PublicContentBodyDto.FromStoredFields` to expose a single render source.
- [x] Verify focused backend tests for public detail/body projection and Postgres command contract.
- [x] Verify frontend public client compatibility for content payload parsing.
- [x] Run typecheck/diff whitespace and generate persistent audit report artifacts.
- [ ] Push PR to `dev`, watch CI, and continue promotion if the runtime tree changes require it.

### Pre-Execution Check
- Current public detail query stores already use stored `PublicContentHtml/PublicContentMarkdown`, not request-time `ContentJson` parsing.
- Current frontend renderer accepts both `content.html` and `content.markdown`, and already prefers markdown when both are present.
- This slice still matches the planned goal: reduce heavy detail response payload without changing cache, seed targets, or `pageSize=12` behavior.

### Progress - Public Content Body Single Render Source
- ADJUSTMENT: InMemory `PublicEndpointsTests` cannot use relational `ExecuteSqlInterpolatedAsync`, so the behavior guard moved to `PublicContentBodyDtoTests` plus the existing Postgres query-store contract test.
- RED observed: `dotnet test backend/tests/WoongBlog.Api.UnitTests/WoongBlog.Api.UnitTests.csproj --filter "FullyQualifiedName~PublicContentBodyDtoTests" --no-restore --logger "console;verbosity=minimal"` failed because `Html` was still `"<p>stored html</p>"` when both stored sources existed.
- GREEN implemented: `PublicContentBodyDto.FromStoredFields` now returns `markdown` only when markdown exists, otherwise falls back to `html`.
- PASS: `dotnet test backend/tests/WoongBlog.Api.UnitTests/WoongBlog.Api.UnitTests.csproj --filter "FullyQualifiedName~PublicContentBodyDtoTests" --no-restore --logger "console;verbosity=minimal"` passed 2/2.
- PASS: `dotnet test backend/tests/WoongBlog.Api.UnitTests/WoongBlog.Api.UnitTests.csproj --no-restore --logger "console;verbosity=minimal"` passed 61/61.
- PASS: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~GetWorkBySlug_ReturnsPublicContentBodyWithoutAdminContentJson|FullyQualifiedName~GetBlogBySlug_ReturnsPublicContentBodyWithoutAdminContentJson" --no-restore --logger "console;verbosity=minimal"` passed 2/2.
- BLOCKED-LOCAL: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~PublicWorkDetailWithVideos_UsesSinglePostgresCommand_AndStoredPublicColumnsOnly|FullyQualifiedName~PublicWorkDetailWithoutVideos_UsesSinglePostgresCommand|FullyQualifiedName~PublicBlogDetail_UsesSinglePostgresCommand" --no-restore --logger "console;verbosity=minimal"` could not start Postgres Testcontainers because local Docker endpoint is not running or configured.
- PASS: `npm test -- --run src/test/public-api-clients.test.ts src/test/public-api-contracts.test.ts` passed 22/22.
- PASS: `npm run typecheck` passed.
- PASS: `git diff --check -- backend/src/WoongBlog.Application/Modules/Content/Common/PublicContentBodyDto.cs backend/tests/WoongBlog.Api.UnitTests/PublicContentBodyDtoTests.cs backend/tests/WoongBlog.Api.IntegrationTests/PublicEndpointsTests.cs backend/tests/WoongBlog.Api.IntegrationTests/PostgresPersistenceContractTests.cs todolist-2026-05-10.md` passed.
- PASS: persistent audit report artifacts generated under `backend/reports/public-content-body-single-source-2026-05-10/`.
- PASS: `python3 -m json.tool backend/reports/public-content-body-single-source-2026-05-10/public-content-body-single-source-2026-05-10.json` passed.

## Slice: Real Backend DB Command Breakdown

### User Instructions Mapped
- `실제 병목이 app CPU인지 DB wait인지 network/serialization인지 보이게`: the Client/nginx/app/db breakdown must keep DB command p95 visible from the run-wide diagnostics stream, not only the last diagnostics sample.
- `DB connection/pool pressure 확인`: distinguish unavailable DB command latency from a valid earlier sample followed by an idle final sample.
- `테스트 방법만 고치지 말고 백엔드 구조/관측 개선`: this slice changes the real backend run snapshot model and dashboard display so the existing diagnostics data is interpreted correctly.

### TODO Mapping
- [x] RED: add frontend behavior coverage proving real backend snapshots retain DB command p95 from diagnostics aggregate when the last sample has no command samples.
- [x] GREEN: expose DB command p95/source in the real backend latency breakdown and display that aggregate in the component timing panel.
- [x] Verify focused Vitest contract, typecheck, diff whitespace, and generate persistent audit report artifacts.

### Pre-Execution Check
- Current dashboard already collects DB command latency samples and database panel summarizes them correctly.
- Current component timing line reads `latestDiagnosticsSample.database.commandLatency.p95Ms`, so a final idle/no-command sample can make DB p95 look unavailable even when the run captured valid DB command p95 earlier.

### Progress - Real Backend DB Command Breakdown
- RED observed: `npm test -- --run src/test/load-test-dashboard.test.ts -t "keeps DB command p95"` failed because `snapshot.latencyBreakdown` did not include `dbCommandP95Ms` or `dbCommandP95Source`.
- GREEN implemented: `summarizeRealBackendRunSnapshot` now enriches real backend latency breakdown with run-wide diagnostics DB command p95 when available.
- GREEN implemented: the dashboard component timing panel now displays aggregate `db command p95` with source instead of reading only the latest diagnostics sample.
- PASS: `npm test -- --run src/test/load-test-dashboard.test.ts -t "keeps DB command p95"` passed 1/1.
- PASS: `npm test -- --run src/test/load-test-dashboard.test.ts` passed 29/29.
- PASS: `npm run typecheck` passed.
- PASS: `git diff --check -- src/lib/load-test-dashboard.ts src/components/admin/LoadTestDashboard.tsx src/test/load-test-dashboard.test.ts todolist-2026-05-10.md` passed.
- PASS: persistent audit report artifacts generated under `backend/reports/real-backend-db-command-breakdown-2026-05-10/`.
- PASS: `python3 -m json.tool backend/reports/real-backend-db-command-breakdown-2026-05-10/real-backend-db-command-breakdown-2026-05-10.json` passed.

## Slice: Real Backend Target DB Attribution

### User Instructions Mapped
- `DB connection/pool pressure 확인`: expose DB command elapsed/count per real backend target so detail/list requests can be compared without relying only on global collector samples.
- `nginx/app timing 계측 완성`: extend the existing client/nginx/app breakdown with target-level DB command timing from backend response headers.
- `실제 병목이 app CPU인지 DB wait인지 network/serialization인지 보이게`: show whether a slow Work/Study read is dominated by app elapsed with little DB time, or by DB command time.
- `다음은 heavy detail read 경로`: keep this as measurement groundwork for the next body JSON/detail projection optimization slice.
- `cache를 잡는건 빼도록`: no cache behavior will be introduced or changed.
- `pageSize=12 유지`, `seed같은걸 안했을때`: no load-test target selection or page-size behavior will be changed.

### TODO Mapping
- [ ] RED: add behavior tests proving k6 records `X-Db-Command-Elapsed-Ms`/`X-Db-Command-Count` as target metrics and the dashboard parses/displays them.
- [ ] GREEN: add request-scoped DB command aggregation through the EF command interceptor and response headers.
- [ ] GREEN: include DB command p95/count p95 in `RealLoadTestTargetMetrics`, fake/k6 runners, and the target summary UI.
- [ ] Verify focused backend component/integration contracts and frontend Vitest/typecheck.
- [ ] Generate persistent audit report artifacts.
- [ ] Push to `dev`, watch CI/publish, promote to `main`, then choose the next concrete backend performance slice.

### Pre-Execution Check
- Current run-wide diagnostics can retain DB command p95, but target metrics only show client p95, payload p95, receive p95, HTTP status, app elapsed, and nginx timing.
- This still leaves Work list/read and Study list/read DB contribution ambiguous during spike failures.

### Progress - Real Backend Target DB Attribution
- RED observed: `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter "FullyQualifiedName~RealLoadTestRunnerComponentTests.K6Script_RecordsTargetDbCommandTimingForHeavyDetailAttribution" --no-restore --logger "console;verbosity=minimal"` failed because the k6 script did not define target DB command trends or record DB command headers.
- RED observed: `npm test -- --run src/test/load-test-dashboard.test.ts -t "summarizes real backend run status"` failed because target metrics dropped `dbCommandElapsedP95Ms` and `dbCommandCountP95`.
- GREEN implemented: request-scoped DB command elapsed/count aggregation is recorded by the EF command interceptor and emitted as `X-Db-Command-Elapsed-Ms` / `X-Db-Command-Count` response headers.
- GREEN implemented: k6 records those headers as per-target trends, `RealLoadTestTargetMetrics` carries DB command p95/count p95, and the target summary UI displays them.
- PASS: focused k6 script test passed 1/1.
- PASS: `npm test -- --run src/test/load-test-dashboard.test.ts -t "summarizes real backend run status"` passed 1/1.
- PASS: component tests for k6 script + request-scoped accumulator passed 6/6.
- PASS: fake-runner real load integration contract passed 1/1.
- PASS: startup health/header integration contract passed 1/1.
- PASS: `npm test -- --run src/test/load-test-dashboard.test.ts` passed 29/29.
- PASS: `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --no-restore --logger "console;verbosity=minimal"` passed 129/129.
- PASS: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~StartupCompositionTests" --no-restore --logger "console;verbosity=minimal"` passed 17/17.
- PASS: `npm run typecheck` passed.
- PASS: `git diff --check -- <changed files>` passed.
- PASS: `dotnet test backend/WoongBlog.sln --no-restore --logger "console;verbosity=minimal"` passed backend unit/component/architecture/integration suites; provider contract test was skipped by its existing pact-file condition.
- PASS: `npm run lint` passed with existing warnings only: 0 errors, 7 warnings.
- PASS: persistent audit report artifacts generated under `backend/reports/real-backend-target-db-attribution-2026-05-10/`.
- PASS: next plan after CI saved at `backend/reports/real-backend-target-db-attribution-2026-05-10/next-plan-after-ci.md`.

## Slice: Public Work Visible DTO Trim

### User Instructions Mapped
- `public blog public work에서 보이는 부분만 dto에 들어가야`: remove public Work fields that the public UI does not render from public list/home/detail DTOs.
- `pageSize=12 유지`: keep list targets and query defaults unchanged; this only trims card/detail shape and selected columns.
- `seed같은걸 안했을때`: do not introduce seeded-target shortcuts; the public contract applies to any real Work slug/list page.
- `cache를 잡는건 빼도록`: no cache behavior is introduced or changed.
- `백엔드 구조가 성능 개선을 이끌어내게`: reduce public response payload and public-list/detail DB projection width instead of changing the load-test method.
- `기존 resolver가 계산하던 thumbnailUrl 보장`: keep `thumbnailUrl` behavior and existing resolver-equivalence tests unchanged; this slice does not touch thumbnail resolution.

### TODO Mapping
- [ ] RED: add/adjust public API contract tests proving public Work list/home/detail omit invisible `iconUrl`, and list/home card payloads omit detail-only `period`.
- [ ] GREEN: remove those fields from public Work DTOs, public EF/raw SQL projections, and frontend public types/parsers.
- [ ] Verify backend focused public endpoint/query-store contracts and frontend public API contract tests.
- [ ] Verify typecheck/lint/diff whitespace and generate persistent audit report artifacts.
- [ ] Push PR to `dev`, watch CI/publish, promote to `main`, then save the next post-CI plan file.

### Pre-Execution Check
- Current public Work detail/list/home paths already avoid `ContentJson`, `AllPropertiesJson`, `Assets`, and `WorkVideos` on the public read path.
- Current public UI uses `thumbnailUrl`, not `iconUrl`; list/home cards do not render `period`, while Work detail still renders `period`.
- This slice still matches the backend-performance goal because it trims public DTO/projection width without changing load targets, seed behavior, or cache.

### Progress - Public Work Visible DTO Trim
- RED observed: focused public endpoint contract failed 3/3 because public home/list/detail still serialized `iconUrl`, and public list/home cards still serialized `period`.
- GREEN implemented: public Work card DTO no longer carries `Period` or `IconUrl`; public Work detail DTO no longer carries `IconUrl`.
- GREEN implemented: Work list/home/detail query projections no longer select `PublicIconUrl` for public DTOs, and list/home projections no longer select `Period`.
- GREEN implemented: frontend public Work/Home types and Pact public API consumer contract no longer include hidden public Work fields.
- GREEN implemented: production runtime preflight now fails when a public Work detail still exposes stale `iconUrl` or stale public video admin-only fields.
- PASS: focused public endpoint contract passed 3/3 after RED.
- PASS: focused component public query contracts passed 3/3.
- PASS: focused Postgres public Work detail/list projection contracts plus public endpoint contracts passed 5/5.
- PASS: `npm run typecheck` passed.
- PASS: public API/Pact/related frontend tests passed 39/39.
- PASS: `npm test -- --run src/test/prod-runtime-preflight.test.ts` passed 6/6.
- ADJUSTMENT: full backend solution exposed one stale integration expectation that public Work detail should still contain `/media/work-icons/`; updated it so admin/DB icon behavior remains covered while public response omits hidden icon data.
- PASS: `dotnet test backend/WoongBlog.sln --no-restore --logger "console;verbosity=minimal"` passed backend unit/component/architecture/integration suites; provider contract skipped by existing pact-file condition.
- PASS: final public API/Pact/preflight/related frontend tests passed 45/45.
- PASS: `npm run lint` passed with existing warnings only: 0 errors, 7 warnings.
- PASS: shell syntax and diff whitespace checks passed.
- PASS: persistent audit report artifacts generated under `backend/reports/public-work-visible-dto-trim-2026-05-10/`.
- PASS: `python3 -m json.tool backend/reports/real-backend-target-db-attribution-2026-05-10/real-backend-target-db-attribution-2026-05-10.json` passed.
- HARDENED: response header emission now reads DB command metrics from the request scope object directly, so delayed `OnStarting` callbacks do not lose the snapshot after AsyncLocal restoration.
- PASS: post-hardening focused `RequestDatabaseDiagnosticsTests` passed 2/2.
- PASS: post-hardening focused startup health/header test passed 1/1.
- PASS: post-hardening `dotnet test backend/WoongBlog.sln --no-restore --logger "console;verbosity=minimal"` passed backend unit/component/architecture/integration suites; provider contract test was skipped by its existing pact-file condition.
- PASS: PR `#131` pull_request `CI Dev` run `25623888713` passed all required jobs: backend unit/component/architecture/integration, frontend lint/types/unit, compose dev verification, Pact provider verification, and Browser smoke.
- PASS: post-CI next plan saved at `backend/reports/public-work-visible-dto-trim-2026-05-10/next-plan-after-ci.md`.
- PASS: PR `#131` latest pull_request `CI Dev` run `25624057618` passed after the post-CI plan commit.
- PASS: PR `#131` merged into `dev` as `5643101550ff2056ca663958b6c406437e1a61dd`.
- PASS: `dev` push `CI Dev` run `25624178364` passed.
- PASS: `Publish GHCR Dev` run `25624301729` passed for backend and frontend staging images.
- PASS: `Promote Main Runtime` run `25624301743` passed and opened PR `#132`.
- PASS: PR `#132` main runtime CI run `25624305919` passed and auto-merged into `main` as `1bf8a5dc9d57dad8ed0c74b2752d678d42b40556`.
- PASS: `main` push `CI Main Runtime` run `25624416587` passed.
- PASS: `Publish GHCR Main` run `25624536513` passed for backend and frontend runtime images.
- PASS: final post-CI/post-promotion next plan updated at `backend/reports/public-work-visible-dto-trim-2026-05-10/next-plan-after-ci.md`.

## Slice: Production Main Runtime Retest And Next Performance Slice

### User Instructions Mapped
- `서버가 main runtime image를 pull/deploy한 뒤 production preflight 실행`: deploy or verify the production server is running the latest `main` runtime images, then run `scripts/prod-runtime-preflight.sh` through nginx/public origin.
- `Real Backend Test 재실행`: rerun real backend load with `LIST_PAGE_SIZE=12`, no seed/fixture URL, no cache strategy, and real public Work/Study read URLs.
- `결과 기준으로 다음 slice 선택`: choose exactly one next code slice from HLS fatal fix, public detail serialization/body optimization, or DB/index optimization based on measured evidence.
- `이후 full e2e가 다 이상없이`: after the selected code slice, run the full relevant e2e validation against the Docker dev environment.
- `ci 과정에서도 다 성공`: push through PR to `dev`, verify CI/publish, then promote/verify through `main` if runtime code changed.

### TODO Mapping
- [ ] Confirm server access and exact production compose path without printing secrets.
- [ ] Pull/deploy latest `main` runtime images on the production server using `.env.prod`.
- [ ] Run production preflight with real public Work video path and record output.
- [ ] Resolve real Work/Study read URLs from current public content, not seeds or fixtures.
- [ ] Run Real Backend Test/load steps with `pageSize=12`, no cache, and real URLs; persist artifacts.
- [ ] Analyze p95/5xx/app/db/nginx/payload evidence and select one next code slice.
- [ ] Implement the selected slice with TDD, preserving cache/pageSize/real-target constraints.
- [ ] Run full e2e validation after implementation.
- [ ] Push through `dev` CI/publish and `main` promotion checks as required.
- [ ] Generate persistent audit report artifacts and final completion audit.

### Pre-Execution Check
- Current repo has `scripts/prod-runtime-preflight.sh` and `scripts/prod-real-load-steps.sh` already promoted to `origin/main`.
- `prod-runtime-preflight.sh` requires `.env.prod`, blocks backend-direct `LoadTesting__BaseUrl`, checks nginx/app timing, gzip, cgroup CPU/memory, and optional public Work video contract.
- `prod-real-load-steps.sh` enforces `LIST_PAGE_SIZE=12` and requires caller-provided real Work/Study read paths.
- Local git worktree is dirty from prior TODO/report updates and unrelated untracked artifacts; new staging must use explicit path selection only.

### Progress - Production Main Runtime Retest And Next Performance Slice
- IN PROGRESS: inspected production deploy/preflight/load scripts and confirmed the test runners preserve the user's constraints: `pageSize=12`, no seed default, public nginx base URL, no cache behavior.
- IN PROGRESS: verified plugin cache directories before long Docker/build/test operations.
- BLOCKED: direct SSH deploy cannot be performed from this environment because `srv1592512` does not resolve and `root@woonglab.com` targets Cloudflare/public edge, not the origin host.
- EVIDENCE: public probe shows production is stale relative to `origin/main`; Work list still returns `period` and `iconUrl`, Work detail still returns `contentJson` and video `originalFileName`/`fileSize`/`createdAt`.
- BLOCKED: Real Backend Test is intentionally not rerun yet because stale runtime numbers would not validate the latest main backend structure.
- PASS: blocker/evidence report generated under `backend/reports/prod-main-runtime-retest-2026-05-10/`.
- EVIDENCE: local `/mnt/d/woong-blog/service/.env.prod` points to legacy GHCR images without the `-runtime-` suffix, while `Publish GHCR Main` publishes `woong-blog-aspcore-nextjs-runtime-frontend:main` and `woong-blog-aspcore-nextjs-runtime-backend:main`.
- ACTIONABLE: generated `backend/reports/prod-main-runtime-retest-2026-05-10/server-main-runtime-redeploy.sh` to update only non-secret runtime image/base-url env keys, back up `.env.prod`, pull/recreate containers, and run production preflight.
- PASS: redeploy helper shell syntax, JSON report parse, and diff whitespace checks passed.
- EVIDENCE: GHCR package metadata confirms legacy `woong-blog-aspcore-nextjs-frontend/backend:main` tags are from `2026-04-15` (`sha-81d52e48b9be`), while current runtime tags are from `2026-05-10` (`sha-1bf8a5dc9d57`).
- HARDENED: `server-main-runtime-redeploy.sh` now prints compose-resolved runtime image names before pulling so the server operator can catch legacy image resolution before deployment.
- ACTIONABLE: generated `backend/reports/prod-main-runtime-retest-2026-05-10/server-run-real-load-after-preflight.sh` for the post-deploy step. It runs preflight first, uses the real Work target `/api/public/works/smoke-fluid-simulation`, auto-selects a non-seed Study target from `pageSize=12`, rejects seed/fixture paths, and then runs `prod-real-load-steps.sh`.
- PASS: real-load helper shell syntax, JSON report parse, and diff whitespace checks passed.
- AUDIT: generated `backend/reports/prod-main-runtime-retest-2026-05-10/completion-audit.md`; the objective is explicitly not complete because server deploy/preflight/load/e2e/CI evidence is still missing.
- ACTIONABLE: generated `backend/reports/prod-main-runtime-retest-2026-05-10/pasteable-server-commands.md` so the server operator can create/run the redeploy and real-load scripts by paste only, without needing local report files on the server.
- SSH DISCOVERY: Windows SSH config exposed candidates. `72.61.118.77` is reachable but reports host-key changed, so connection was not bypassed; current keyscan fingerprints were recorded for out-of-band verification. `187.127.111.73` has matching host key but denied tested users/key. trycloudflare route lacks the configured `cloudflared` binary in WSL.
- PASS: extracted both bash blocks from `pasteable-server-commands.md` and verified them with `bash -n` without executing server-side commands.
- SSH DISCOVERY: Docker `cloudflare/cloudflared:latest` fallback ran (`2026.3.0`), but `young-assign-danny-dell.trycloudflare.com` DNS lookup failed with `no such host`; the trycloudflare tunnel alias appears expired/unavailable.
- RECHECK: final public probe still shows stale Work list/detail/video payload keys, so the production server has not yet been recreated from latest main runtime images.
- HARDENED: public stale-key recheck now uses `curl --compressed` instead of manual `gzip -dc`; revalidated redeploy helper and both pasteable bash blocks with `bash -n`.
- SSH DISCOVERY: reverse DNS confirms `187.127.111.73 -> srv1592512.hstgr.cloud`; host keys match the Windows known_hosts entry, but `root/ubuntu/deploy` authentication with the available Windows key still fails and no SSH agent is available.
- EVIDENCE: `CI Main Runtime` run `25626902724`, `Publish GHCR Main` run `25627032158`, `CI Dev` run `25626665464`, and manual `Publish GHCR Dev` run `25627146533` are all green.
- EVIDENCE: current GHCR runtime package tags are available for `main/latest/sha-3a19dd8bfc2b` and `dev/dev-sha-8f02b38cc028`.
- HARDENED: `pasteable-server-commands.md` block 1 now uses a temporary Docker config for GHCR manifest checks and `docker compose pull`, avoiding stale host Docker credentials; `GHCR_TOKEN` remains an optional fallback.
- BLOCKED: WSL SSH and Windows OpenSSH both fail BatchMode auth for `root` and `kimwoonggon` on `srv1592512.hstgr.cloud` / `187.127.111.73`.
- BLOCKED: GitHub repo has only `PROMOTION_TOKEN`, no production SSH/deploy secret or variable, and no existing production host deploy workflow.
- RECHECK: public API still exposes stale fields at `2026-05-10T20:31:00+09:00`; Work list has `period/iconUrl`, Work detail has `period/iconUrl/contentJson/originalFileName/fileSize/createdAt`.
- AUDIT: generated `backend/reports/prod-main-runtime-retest-2026-05-10/status-after-ghcr-publish-2026-05-10.*` and `active-goal-completion-audit-2026-05-10.*`; objective remains not complete until server block 1 output is available.

## Slice: GHCR Runtime Legacy Image Alias

### User Instructions Mapped
- `서버가 main runtime image를 pull/deploy`: the server appears to use legacy GHCR image names, while current CI publishes `-runtime-` image names. Publish both names so existing server env can pull the current runtime image after CI.
- `ci 과정에서도 다 성공`: cover the workflow tag contract with Vitest and require CI to republish images through the normal dev/main chain.

### TODO Mapping
- [x] RED: add static workflow contract test proving GHCR publish workflows emit both runtime image tags and legacy compose-compatible aliases.
- [ ] GREEN: add legacy alias tags to `publish-ghcr-main.yml` and `publish-ghcr-dev.yml` without removing current runtime tags.
- [ ] Verify focused workflow contract test, related compose test, typecheck, shell/diff checks.
- [ ] Generate audit report artifacts for this CI hardening slice.
- [ ] Push through `dev` CI/publish and main promotion/publish so both alias sets are actually published.

### Progress - GHCR Runtime Legacy Image Alias
- RED observed: `npm test -- --run src/test/publish-ghcr-tags.test.ts` failed 2/2 because both publish workflows only emitted `-runtime-` tags and did not emit legacy compose-compatible tags.
- GREEN implemented: `publish-ghcr-main.yml` now publishes legacy compose-compatible `repo-frontend/backend:main`, `:sha-*`, and `:latest` aliases in addition to current `repo-runtime-frontend/backend` tags.
- GREEN implemented: `publish-ghcr-dev.yml` now publishes legacy compose-compatible `repo-frontend/backend:dev` and `:dev-sha-*` aliases in addition to current runtime dev tags.
- PASS: `npm test -- --run src/test/publish-ghcr-tags.test.ts` passed 2/2.
- PASS: `npm test -- --run src/test/publish-ghcr-tags.test.ts src/test/compose-loadtesting-baseurl.test.ts` passed 7/7.
- PASS: `npm run typecheck` passed.
- PASS: `npm run lint` passed with existing warnings only: 0 errors, 7 warnings.

## Slice: GHCR Legacy Alias Permission Gate

### User Instructions Mapped
- `ci 과정에서도 다 성공`: restore `Publish GHCR Main` after the legacy package alias push failed with GHCR `403 Forbidden`.
- `서버가 main runtime image를 pull/deploy`: keep runtime image publishing mandatory and make production deploy use `-runtime-` image names instead of relying on stale legacy package names.

### TODO Mapping
- [x] RED: update workflow contract tests to require mandatory runtime package publishing and token-gated legacy aliases.
- [x] GREEN: split required runtime image push from optional legacy alias push in `publish-ghcr-main.yml` and `publish-ghcr-dev.yml`.
- [x] Verify focused workflow contract and related compose env tests.
- [x] Verify typecheck, lint, and diff whitespace.
- [x] Generate audit report artifacts for the corrective CI slice.
- [x] Push through `dev` CI/publish and `main` promotion/publish so main runtime images publish successfully again.

### Progress - GHCR Legacy Alias Permission Gate
- EVIDENCE: `Publish GHCR Main` run `25626302716` failed because pushing to legacy packages `woong-blog-aspcore-nextjs-backend/frontend:main` returned GHCR `403 Forbidden`.
- EVIDENCE: `gh secret list` currently shows `PROMOTION_TOKEN` but no `GHCR_PACKAGES_TOKEN`, so legacy package alias push cannot be treated as mandatory.
- RED observed: updated `src/test/publish-ghcr-tags.test.ts` failed 2/2 because the workflows still mixed legacy aliases into the mandatory runtime push and had no `GHCR_PACKAGES_TOKEN` gate.
- GREEN implemented: required publish steps now push only `woong-blog-aspcore-nextjs-runtime-frontend/backend` tags.
- GREEN implemented: legacy compose-compatible aliases remain available but are moved to separate optional `Build and push legacy aliases` steps gated by `GHCR_PACKAGES_TOKEN`.
- PASS: `npm test -- --run src/test/publish-ghcr-tags.test.ts` passed 2/2.
- PASS: `npm test -- --run src/test/publish-ghcr-tags.test.ts src/test/compose-loadtesting-baseurl.test.ts` passed 7/7.
- PASS: `npm run typecheck` passed.
- PASS: `npm run lint` passed with existing warnings only: 0 errors, 7 warnings.
- PASS: `git diff --check` passed.
- AUDIT: generated `backend/reports/ghcr-legacy-alias-permission-gate-2026-05-10/` artifacts.
- PASS: PR #135 merged to `dev`, `CI Dev` run `25626665464` passed, PR #136 promoted/merged to `main`, `CI Main Runtime` run `25626902724` passed.
- PASS: `Publish GHCR Main` run `25627032158` passed and published runtime backend/frontend tags `main`, `latest`, and `sha-3a19dd8bfc2b`.
- PASS: manual `Publish GHCR Dev` run `25627146533` passed and published runtime backend/frontend tags `dev` and `dev-sha-8f02b38cc028`.
- NOTE: legacy alias packages remain optional because `GHCR_PACKAGES_TOKEN` is absent; production should use `woong-blog-aspcore-nextjs-runtime-frontend/backend:main`.

## Slice: Manual Production Runtime Redeploy Workflow

### User Instructions Mapped
- `서버가 main runtime image를 pull/deploy한 뒤 production preflight 실행`: add a manual GitHub Actions path that can SSH to the production host, pull/deploy the latest runtime images, and run `prod-runtime-preflight.sh` when production SSH secrets are provided.
- `Real Backend Test 재실행`: optionally run the same post-preflight real load steps from CI with `pageSize=12`, no seed/fixture targets, no cache workaround, and real Work/Study URLs.
- `ci 과정에서도 다 성공`: cover the workflow contract with unit tests and include the workflow in the runtime promotion allowlist.

### TODO Mapping
- [x] RED: add workflow contract test for secure manual production redeploy/load workflow.
- [x] GREEN: add `.github/workflows/prod-runtime-redeploy.yml` with required SSH secrets, strict host-key handling, deploy/preflight command, and optional real-load command.
- [x] GREEN: include the workflow in `scripts/main-runtime-allowlist.txt`.
- [x] Verify focused Vitest contract, typecheck, shell/diff checks, and generate audit report artifacts.

### Pre-Execution Check
- Direct WSL/Windows SSH failed, and the repo has no production deploy secret/workflow, so a manual workflow is the only repo-side path that can reduce future server-access friction.
- This workflow cannot complete the current objective by itself until production SSH secrets are added and the workflow is run.

### Progress - Manual Production Runtime Redeploy Workflow
- RED observed: `npm test -- --run src/test/prod-runtime-redeploy-workflow.test.ts` failed because `.github/workflows/prod-runtime-redeploy.yml` did not exist.
- GREEN implemented: added manual `workflow_dispatch` workflow with required `PROD_SSH_HOST`, `PROD_SSH_USER`, `PROD_SSH_PRIVATE_KEY`, and `PROD_SSH_KNOWN_HOSTS` secrets.
- GREEN implemented: workflow uses strict host-key checking, updates only non-secret runtime image/base-url env keys, pulls runtime images with a temporary Docker config, runs `prod-runtime-preflight.sh`, and optionally runs `prod-real-load-steps.sh`.
- GREEN implemented: optional real load preserves `pageSize=12` Study discovery and rejects seed/fixture Work/Study paths.
- PASS: `npm test -- --run src/test/prod-runtime-redeploy-workflow.test.ts` passed 1/1.
- PASS: `npm test -- --run src/test/prod-runtime-redeploy-workflow.test.ts src/test/publish-ghcr-tags.test.ts src/test/compose-loadtesting-baseurl.test.ts` passed 8/8.
- PASS: extracted workflow remote heredoc and `bash -n /tmp/prod-runtime-redeploy-remote.sh` passed.
- PASS: `npm run typecheck` passed.
- PASS: `npm run lint` passed with existing warnings only: 0 errors, 7 warnings.
- PASS: `git diff --check -- .github/workflows/prod-runtime-redeploy.yml src/test/prod-runtime-redeploy-workflow.test.ts scripts/main-runtime-allowlist.txt todolist-2026-05-10.md` passed.
- BLOCKED: `npx --yes actionlint .github/workflows/prod-runtime-redeploy.yml` could not determine an executable to run, so actionlint was unavailable through npx.
- AUDIT: generated `backend/reports/prod-runtime-redeploy-workflow-2026-05-10/` artifacts.
- REMAINS: active objective is still not complete until production SSH secrets are configured and the workflow or server pasteable block runs successfully.

## Slice: Production Runtime Redeploy SSH Secret Blocker

### User Instructions Mapped
- `서버가 main runtime image를 pull/deploy한 뒤 production preflight 실행`: attempted through `prod-runtime-redeploy.yml`, but production SSH secrets are absent, so the server step did not run.
- `Real Backend Test 재실행: pageSize=12, seed 없음, cache 없음, 실제 Work/Study URL`: workflow inputs and scripts are prepared with `pageSize=12`, no seed/fixture target, no cache change, and real public Work/Study read paths, but no production run has executed yet.
- `결과 기준으로 다음 slice 선택`: cannot select a result-driven slice until production deploy/preflight/load evidence exists.
- `이후 full e2e와 ci 성공`: dev/main CI and publish path succeeded for the runtime redeploy workflow, but the production redeploy run failed before server execution.

### TODO Mapping
- [x] Inspect the failed `Production Runtime Redeploy` run and capture exact failing step.
- [x] Verify repository secrets currently available through GitHub CLI.
- [x] Record required production secrets and exact rerun command.
- [x] Generate persistent blocker audit artifacts.
- [ ] Add required production SSH secrets or receive equivalent production deploy access.
- [ ] Rerun `prod-runtime-redeploy.yml` on `main` with real Work/Study URLs.
- [ ] Verify production preflight passes after runtime image pull/deploy.
- [ ] Verify Real Backend Test runs with `pageSize=12`, no seed, no cache, real public URLs.
- [ ] Select and implement the next backend slice from the real production result.
- [ ] Run full e2e/applicable CI gates through `dev` and `main`.

### Progress - Production Runtime Redeploy SSH Secret Blocker
- FAIL-BLOCKED: `Production Runtime Redeploy` run `25628508822` failed in `Prepare SSH` with `Missing required secret: PROD_SSH_HOST`.
- CONFIRMED: the server execution step `Pull runtime images and run production checks` was skipped, so no production `docker compose pull/up`, preflight, or real load occurred.
- CONFIRMED: `gh secret list --repo kimwoonggon/woong-blog-aspcore-nextjs` currently shows only `PROMOTION_TOKEN`.
- REQUIRED: add `PROD_SSH_HOST`, `PROD_SSH_USER`, `PROD_SSH_PRIVATE_KEY`, and `PROD_SSH_KNOWN_HOSTS` as GitHub repo secrets. Add `PROD_SSH_PORT` only if SSH is not port 22. Add `PROD_GHCR_TOKEN` only if anonymous GHCR pull fails.
- NEXT: after secrets are present, rerun `prod-runtime-redeploy.yml` on `main` with `run_real_load=true`, `rates='100 200 300 400'`, `duration_seconds=30`, `max_vus=500`, and `pre_allocated_vus=100`.
- LOCAL-YELLOW-FLAG: during blocker report generation, an unquoted heredoc interpreted Markdown backticks as shell command substitution. This accidentally dispatched duplicate workflow run `25628614468`, which failed at the same `Prepare SSH` step before production server execution, and attempted local default `docker compose up`; current local compose state is `db` running and `backend`/`frontend`/`nginx` created, not running.
- PUBLIC-RECHECK: `2026-05-10T21:27:36+09:00` probe against `https://woonglab.com` still shows stale public DTO fields: Work list has `period` and `iconUrl`; Work detail has `period`, `iconUrl`, and `contentJson`; Work video has `originalFileName`, `fileSize`, and `createdAt`. This confirms the production server has not yet been redeployed to the latest runtime.

## Slice: Production SSH Secrets Setup Readiness

### User Instructions Mapped
- `서버가 main runtime image를 pull/deploy한 뒤 production preflight 실행`: unblock the workflow by documenting the exact GitHub secrets required for production SSH.
- `Real Backend Test 재실행: pageSize=12, seed 없음, cache 없음, 실제 Work/Study URL`: keep the rerun command fixed to the real public Work/Study targets and the prepared k6 settings.
- `결과 기준으로 다음 slice 선택`: make the blocker handoff explicit so the next action after secrets is workflow rerun, not another speculative code change.

### TODO Mapping
- [x] Confirm no production SSH secrets are currently present in GitHub.
- [x] Confirm latest production redeploy workflow runs failed before server execution.
- [x] Generate a persistent secret setup runbook with exact commands and verification steps.
- [ ] Add required secrets using the real production SSH values.
- [ ] Rerun `prod-runtime-redeploy.yml` on `main`.

### Progress - Production SSH Secrets Setup Readiness
- CONFIRMED: `gh secret list --repo kimwoonggon/woong-blog-aspcore-nextjs` shows only `PROMOTION_TOKEN`.
- CONFIRMED: `gh api repos/kimwoonggon/woong-blog-aspcore-nextjs/environments --jq '.environments[]?.name'` returned no environments.
- CONFIRMED: `gh variable list --repo kimwoonggon/woong-blog-aspcore-nextjs` returned no variables.
- CONFIRMED: `.github/workflows/prod-runtime-redeploy.yml` does not declare a job `environment`, so it reads repo-level `secrets.PROD_SSH_*`.
- CONFIRMED: `gh repo view kimwoonggon/woong-blog-aspcore-nextjs --json owner,visibility` shows owner `kimwoonggon` is a user account and the repository is public; `gh secret list --org kimwoonggon` returns HTTP 404, so there is no org-level Actions secret source for this workflow.
- CONFIRMED: `origin/main` is `757ab203e55cadf8f89ee0da42b7ef580deebad3`; `CI Main Runtime` run `25628240637` and `Publish GHCR Main` run `25628355351` both succeeded for that same SHA. The runtime image target is current; only production SSH execution remains blocked.
- CONFIRMED: production redeploy runs `25628508822` and `25628614468` failed in `Prepare SSH`, before any production host command executed.
- CONFIRMED: accidental placeholder run `25628772593` also failed in `Prepare SSH`, specifically at `Missing required secret: PROD_SSH_PRIVATE_KEY`, and skipped server execution.

## Slice: Local Dev Compose Repair After Blocker Audit Incident

### User Instructions Mapped
- `이후 full e2e가 다 이상없이`: keep the local Docker dev stack usable for later e2e/full-stack verification after production access is restored.

### TODO Mapping
- [x] Inspect local compose state after the accidental default compose startup.
- [x] Restore the Docker dev stack without changing production files.
- [x] Verify local nginx and backend health endpoints.

### Progress - Local Dev Compose Repair
- OBSERVED: default/dev compose project had `db` running while `backend`, `frontend`, and `nginx` were only `Created`.
- BLOCKED-DEFAULT-PORT: `docker compose -f docker-compose.dev.yml up -d` failed on Docker Desktop forwarding for `127.0.0.1:8080` even though no host listener was visible.
- REPAIRED: `BACKEND_PUBLISH_PORT=18080 docker compose -f docker-compose.dev.yml up -d --force-recreate backend frontend nginx` started all app services.
- PASS: `docker compose -f docker-compose.dev.yml ps` shows `backend` up on `127.0.0.1:18080->8080`, `nginx` up on `127.0.0.1:3000->80` and `127.0.0.1:3001->443`, plus `db` up.
- PASS: `curl http://127.0.0.1:3000/api/health` returned HTTP 200.
- PASS: `curl http://127.0.0.1:18080/api/health` returned HTTP 200.
- NOTE: this repairs local validation readiness only. It does not satisfy the production deploy/preflight/load objective, which remains blocked by missing production SSH secrets.

## Slice: Manual Production Fallback Validation

### User Instructions Mapped
- `서버가 main runtime image를 pull/deploy한 뒤 production preflight 실행`: keep the pasteable server redeploy block ready for manual execution on the production host while SSH secrets are unavailable.
- `Real Backend Test 재실행: pageSize=12, seed 없음, cache 없음, 실제 Work/Study URL`: verify the pasteable load block keeps `pageSize=12`, rejects seed/fixture paths, uses real public Work/Study read targets, and does not introduce cache.

### TODO Mapping
- [x] Inspect `pasteable-server-commands.md` and companion server scripts.
- [x] Verify shell syntax for `server-main-runtime-redeploy.sh` and `server-run-real-load-after-preflight.sh`.
- [x] Grep for `pageSize=1`, seed/fixture handling, `pageSize=12`, and real read targets.

### Progress - Manual Production Fallback Validation
- PASS: `bash -n backend/reports/prod-main-runtime-retest-2026-05-10/server-main-runtime-redeploy.sh backend/reports/prod-main-runtime-retest-2026-05-10/server-run-real-load-after-preflight.sh` passed.
- PASS: list targets in fallback commands remain `/api/public/works?page=1&pageSize=12` and `/api/public/blogs?page=1&pageSize=12`.
- PASS: real load fallback rejects `*seeded*` and `*fixture*` Work/Study paths.
- PASS: Work read fallback uses real public path `/api/public/works/smoke-fluid-simulation`; Study read fallback auto-selects a non-seed public Study slug from `pageSize=12`.
- PASS: no cache behavior is introduced by the fallback path.
- IMPROVED: fallback block 2 now verifies `prod-real-load-steps-summary.md/json` exist, prints their paths, prints the markdown summary, and creates `prod-real-load-steps-artifacts.tgz` for easier result transfer.
- NOTE: fallback validation does not complete the objective. It only confirms the manual server path is ready if production SSH secrets remain unavailable.

## Slice: Active Goal Current Blocker Recheck

### User Instructions Mapped
- `서버가 main runtime image를 pull/deploy한 뒤 production preflight 실행`: verify whether the external blocker has changed before claiming progress.
- `Real Backend Test 재실행`: verify whether production has actually moved to the latest public DTO/runtime before interpreting any load result.

### TODO Mapping
- [x] Recheck GitHub production SSH secrets.
- [x] Recheck latest production redeploy workflow runs.
- [x] Recheck public Work detail contract on `https://woonglab.com`.
- [x] Recheck local dev compose remains usable for future validation.

### Progress - Active Goal Current Blocker Recheck
- CONFIRMED at `2026-05-10T21:44:56+09:00`: `gh secret list --repo kimwoonggon/woong-blog-aspcore-nextjs` still shows only `PROMOTION_TOKEN`.
- CONFIRMED: latest `Production Runtime Redeploy` runs remain failures: `25628772593`, `25628614468`, and `25628508822`; all failed before production server execution.
- CONFIRMED: public Work detail `/api/public/works/smoke-fluid-simulation` still exposes stale detail fields `period`, `iconUrl`, and `contentJson`, plus stale video fields `originalFileName`, `fileSize`, and `createdAt`.
- CONFIRMED: local dev compose remains up with backend on `127.0.0.1:18080` and nginx on `127.0.0.1:3000/3001`.
- DECISION: active goal remains blocked externally. Do not call `update_goal`.
- AUDIT: generated `backend/reports/prod-main-runtime-retest-2026-05-10/prod-ssh-secrets-setup-2026-05-10.md/json/html`.
- INCIDENT-REMEDIATED: unquoted heredoc execution accidentally created placeholder GitHub Actions secrets `PROD_SSH_HOST`, `PROD_SSH_USER`, `PROD_SSH_KNOWN_HOSTS`, and `PROD_SSH_PORT`, and dispatched run `25628772593`. That run failed in `Prepare SSH` before server execution. The placeholder secrets were deleted, and `gh secret list` again shows only `PROMOTION_TOKEN`.
- AUDIT-SYNC: synchronized `active-goal-completion-audit-2026-05-10.json/html` with the latest Markdown audit evidence, including current main SHA, failed redeploy runs, missing secrets, stale production public DTO, local dev compose repair, and manual fallback artifact collection.

## Slice: Production Redeploy Placeholder Secret Guard

### User Instructions Mapped
- `서버가 main runtime image를 pull/deploy한 뒤 production preflight 실행`: prevent the production redeploy workflow from attempting SSH with placeholder secrets if someone follows the setup runbook incorrectly.
- `ci 과정에서도 다 성공`: cover the workflow safety behavior with the existing Vitest workflow contract.

### TODO Mapping
- [x] RED: add workflow contract coverage requiring placeholder secret detection before SSH material is written.
- [x] GREEN: add placeholder-value validation to `.github/workflows/prod-runtime-redeploy.yml`.
- [x] Verify focused Vitest, related workflow tests, shell syntax, diff whitespace, and update audit artifacts.

### Pre-Execution Check
- Current goal remains blocked by missing real production SSH secrets. This slice does not complete deploy/preflight/load; it prevents a repeat of the placeholder secret incident and makes future workflow failures safer.

### Progress - Production Redeploy Placeholder Secret Guard
- RED observed: `npm test -- --run src/test/prod-runtime-redeploy-workflow.test.ts` failed because the workflow did not contain `reject_placeholder_secret`.
- GREEN implemented: `Prepare SSH` now rejects obvious placeholder tokens in `PROD_SSH_HOST`, `PROD_SSH_USER`, `PROD_SSH_PRIVATE_KEY`, `PROD_SSH_KNOWN_HOSTS`, and optional `PROD_SSH_PORT` before writing key material or running `ssh-keygen`.
- SECURITY: the guard does not echo secret values; it only prints the secret name and a replacement instruction.
- PASS: `npm test -- --run src/test/prod-runtime-redeploy-workflow.test.ts` passed 1/1.
- PASS: `npm test -- --run src/test/prod-runtime-redeploy-workflow.test.ts src/test/publish-ghcr-tags.test.ts src/test/compose-loadtesting-baseurl.test.ts` passed 8/8.
- PASS: extracted remote heredoc `bash -n /tmp/prod-runtime-redeploy-remote.sh` passed.
- PASS: extracted `Prepare SSH` shell block `bash -n` passed.
- PASS: `npm run typecheck` passed.
- PASS: `git diff --check -- .github/workflows/prod-runtime-redeploy.yml src/test/prod-runtime-redeploy-workflow.test.ts todolist-2026-05-10.md` passed.
- AUDIT: generated `backend/reports/prod-runtime-placeholder-secret-guard-2026-05-10/` artifacts.
- REMAINS: active objective is still not complete because no real production SSH secrets are present and no production deploy/preflight/load has executed.

## Slice: Production Redeploy Real Load Summary Output

### User Instructions Mapped
- `Real Backend Test 재실행: pageSize=12, seed 없음, cache 없음, 실제 Work/Study URL`: ensure the Actions deploy path does not merely run k6, but verifies and prints the real-load summary artifacts that prove those target semantics and resulting metrics.
- `결과 기준으로 다음 slice 선택`: make the workflow log include the generated markdown summary so HLS/serialization/DB next-slice selection can be made from the actual production run output.
- `ci 과정에서도 다 성공`: cover the workflow behavior with the existing Vitest workflow contract before promoting.

### TODO Mapping
- [x] RED: require `prod-runtime-redeploy.yml` to validate and print real-load summary artifacts after `prod-real-load-steps.sh`.
- [x] GREEN: update the workflow remote script to check summary JSON/Markdown and print the markdown summary.
- [x] Verify focused workflow test, shell syntax, typecheck, diff whitespace, and audit artifacts.
- [ ] Push branch to dev PR, watch CI, and promote to main if accepted.

### Progress - Production Redeploy Real Load Summary Output
- RED expected: added workflow contract assertions for `prod-real-load-steps-summary.json`, `prod-real-load-steps-summary.md`, `Real load summary markdown:`, and `missing expected real load summary artifacts`.
- RED observed: `npm test -- --run src/test/prod-runtime-redeploy-workflow.test.ts` failed because `prod-runtime-redeploy.yml` did not validate/print real-load summary artifacts.
- GREEN implemented: the workflow now sets an explicit production real-load `OUTPUT_DIR`, checks `prod-real-load-steps-summary.json` and `.md`, prints artifact paths, and cats the markdown summary to the Actions log.
- PASS: `npm test -- --run src/test/prod-runtime-redeploy-workflow.test.ts` passed 1/1.
- PASS: `npm test -- --run src/test/prod-runtime-redeploy-workflow.test.ts src/test/prod-real-load-steps.test.ts src/test/publish-ghcr-tags.test.ts` passed 6/6.
- PASS: extracted remote heredoc `bash -n /tmp/prod-runtime-redeploy-remote-summary.sh` passed.
- PASS: `npm run typecheck` passed.
- PASS: `git diff --check -- .github/workflows/prod-runtime-redeploy.yml src/test/prod-runtime-redeploy-workflow.test.ts todolist-2026-05-10.md` passed.

## Slice: HLS Copy-Mode Fallback For Valid MP4 Uploads

### User Instructions Mapped
- `원격 서버는 신경쓰지 마`: production/SSH/deploy is out of scope for this implementation slice.
- `치명적 오류 파악 failed to process HLS (영상 업로드시)`: fix the backend-code failure mode where valid MP4 input reaches ffmpeg but copy-mode HLS packaging fails.
- `cache를 잡는건 빼도록 하자`: no response cache or cache-based mitigation.
- `홈페이지의 기능이 다돌아가는 정도`: validate public/homepage functionality with Playwright e2e instead of continuing broad Vitest.

### TODO Mapping
- [x] RED: add a behavior test proving HLS falls back to compatibility re-encode when copy-mode ffmpeg fails for an otherwise valid MP4.
- [x] GREEN: update `FfmpegVideoTranscoder` to retry HLS generation with browser-compatible H.264/AAC output after copy-mode failure.
- [x] Verify the happy path still uses copy-mode first and still generates timeline preview artifacts.
- [x] Run focused component tests and backend solution tests.
- [x] Run homepage/public Playwright e2e bundle against local compose.
- [x] Generate persistent audit report artifacts.

### Progress - HLS Copy-Mode Fallback For Valid MP4 Uploads
- RED observed: focused fallback component test failed before implementation because copy-mode ffmpeg failure returned `Unable to process MP4 into HLS: copy-mode mux fail...` with no fallback.
- GREEN implemented: `FfmpegVideoTranscoder` now retries once with compatibility transcode (`libx264`, `yuv420p`, `aac`) after copy-mode HLS fails, within the same overall HLS timeout.
- SAFETY: normal compatible MP4s still use copy-mode first; partial HLS outputs are deleted before fallback; response cache behavior was not changed.
- PASS: focused fallback component test passed 1/1.
- PASS: focused HLS integration tests passed 2/2.
- PASS: full component suite passed 130/130.
- PASS: backend solution passed Unit 61/61, Component 130/130, Architecture 40/40, Integration 231/231; Contract 1 skipped by pact-file condition.
- PASS: `npm run typecheck` passed.
- PASS: `npm run lint` passed with 0 errors and existing warnings outside this HLS slice.
- USER-ADJUSTED: broad Vitest was intentionally stopped after the user requested homepage-function e2e instead.
- PASS: homepage/public Playwright e2e bundle against local dev compose passed 38/38 with 0 latency budget failures.
- AUDIT: generated `backend/reports/hls-copy-fallback-2026-05-10/` md/json/html artifacts.
- PASS: clean `origin/dev` worktree revalidation `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter FullyQualifiedName~FfmpegVideoTranscoderComponentTests --verbosity minimal` passed 1/1.
- PASS: clean `origin/dev` worktree revalidation `dotnet test backend/WoongBlog.sln --verbosity minimal` passed Unit 61/61, Component 130/130, Architecture 40/40, Integration 231/231; Contract 1 skipped by pact-file condition.
