# Hotfix todolist (2026-04-30)

- [x] 브라우저 시스템 설정 기반 다크/라이트 기본값 반영
  - 변경 파일: src/components/providers/ThemeProvider.tsx, src/components/ui/ThemeToggle.tsx
  - 목표: 사용자 OS 테마 변경에 따라 기본 테마가 동기화되도록 조정

- [x] works/blog AI 보조 기능에서 "Start AI Fix" 재노출 보장
  - 변경 파일: src/components/admin/AIFixDialog.tsx
  - 목표: 다이얼로그 열기 시 이전 결과가 남아 시작 버튼이 사라지는 상태 방지

- [x] gpt-5.5 모델 추가
  - 변경 파일: backend/src/WoongBlog.Api/appsettings.json, backend/src/WoongBlog.Infrastructure/Ai/AiOptionsPostConfigure.cs, backend/src/WoongBlog.Application/Modules/AI/AiOptions.cs, src/components/admin/AIFixDialog.tsx, src/components/admin/AdminBlogBatchAiPanel.tsx
  - 목표: Codex 기본 모델/허용 목록/프런트 폴백에서 `gpt-5.5`를 사용할 수 있게 지원

- [x] 변경 결과 점검(코드 기준)
  - 목표: TODO 항목별 반영 여부 확인 후 TODO 상태 갱신

## 완료 메모

- `src/components/providers/ThemeProvider.tsx`: `defaultTheme`를 `system`으로 변경하고 `enableSystem`을 활성화
- `src/components/ui/ThemeToggle.tsx`: 시스템 동기화 테마(`resolvedTheme`) 기반으로 현재 모드를 표시하고 토글 대상 계산
- `src/components/admin/AIFixDialog.tsx`: 다이얼로그 닫힘 시 `loading`/`fixedContent` 상태를 리셋해 시작 버튼이 다시 보이도록 처리
- `backend/src/WoongBlog.Api/appsettings.json`: `CodexModel`을 `gpt-5.5`로 변경하고 `CodexAllowedModels`에 `gpt-5.5` 추가
- `backend/src/WoongBlog.Infrastructure/Ai/AiOptionsPostConfigure.cs`: 기본 Codex 모델/허용 모델 폴백에 `gpt-5.5` 추가
- `backend/src/WoongBlog.Application/Modules/AI/AiOptions.cs`: 기본 `CodexModel`을 `gpt-5.5`로 갱신
- `src/components/admin/AIFixDialog.tsx`, `src/components/admin/AdminBlogBatchAiPanel.tsx`: 모델 기본값 폴백을 `gpt-5.5`로 갱신
