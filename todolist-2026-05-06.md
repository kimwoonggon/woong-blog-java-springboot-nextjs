
# TODO - 2026-05-06 - Real Backend Target Reality Correction

## User Instructions
- Do not force Real Backend Test read targets to `seeded-work` / `seeded-blog`; that hides real current production content.
- Do not reduce Real Backend Test list targets to `pageSize=1`; `pageSize=12` is the realistic public list workload.
- Keep backend hot-path performance improvements that do not fake the workload.
- Verify public/home query structure so hot paths fetch only required rows/columns instead of globally reading large tables or body JSON.

## TODOs
- [x] Add/restore behavior tests that Real Backend targets use `pageSize=12` list paths and preserve fetched public slug order.
- [x] Revert Real Backend default target generation to real current public content order and `pageSize=12` list targets.
- [x] Update/replace audit artifacts so seed/pageSize=1 load evidence is clearly invalidated and not presented as real backend performance proof.
- [x] Run focused frontend tests, typecheck/lint, relevant backend tests, dev compose build, health check, and focused Playwright.
- [x] Generate persistent audit report artifacts.
- [x] Fix public home composition query shape so featured/recent cards do not read all assets or card-unneeded body JSON.
- [x] Add focused backend coverage for the home-card body-read regression.
- [x] Re-run focused backend tests and update audit artifacts with query-shape and prod-like 2CPU/8GB evidence.
- [x] Commit, push, open PR to `dev`, verify CI, merge, verify `dev` CI, promote to `main`, and verify `main` CI.

## Plan Alignment Check - Before Execution
- Current `dev` contains an incorrect target stabilization change that prioritizes seeded slugs and uses `pageSize=1` in Real Backend default list targets.
- Correction will keep query-store hot-path changes but remove workload-faking target defaults.

Progress 2026-05-06T00:00+09:00:
- Added behavior coverage that Real Backend list targets stay at pageSize=12.
- Added behavior coverage that fetched public slug order is preserved instead of forcing seeded fixtures.
- Added public endpoint assertions that list payloads omit contentJson while detail payloads keep contentJson.

Progress 2026-05-06T01:04:06+0900:
- Marked the 2026-05-05 seed/pageSize=1 performance report as superseded for realistic-load evidence.
- Updated the standalone k6 report helper to use pageSize=12 lists and require explicit current-content read paths instead of defaulting to seeded reads.
- Deleted tracked invalid seed/pageSize=1 k6 summary JSON artifacts from the correction branch.

Validation progress 2026-05-06T01:11:30+0900:
- PASS: npm test -- --run src/test/load-test-dashboard.test.ts (27/27).
- PASS: dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~PublicEndpointsTests" --no-restore (27/27; existing AWSSDK.Core NU1901 warnings).
- PASS: dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter "FullyQualifiedName~PublicQueryHandlerComponentTests" --no-restore (34/34; existing AWSSDK.Core NU1901 warning).
- PASS: npm run typecheck.
- PASS: npm run lint (0 errors, 6 existing unrelated warnings).
- PASS: git diff --check.
- PASS: BACKEND_PUBLISH_PORT=18080 ./scripts/dev-up.sh, including backend publish and Next production build.
- PASS: curl http://127.0.0.1:3000/api/health -> 200.
- PASS: Playwright focused admin-load-test-dashboard spec on dev compose (2/2).

Audit progress 2026-05-06T01:13:00+0900:
- Generated backend/reports/real-backend-target-reality-correction-2026-05-06/real-backend-target-reality-correction-2026-05-06.md.
- Generated backend/reports/real-backend-target-reality-correction-2026-05-06/real-backend-target-reality-correction-2026-05-06.html.
- Generated backend/reports/real-backend-target-reality-correction-2026-05-06/real-backend-target-reality-correction-2026-05-06.json.
- Plan alignment after local execution: work still matches the TODO list; remaining work is GitHub PR/CI promotion through dev and main.

Progress 2026-05-06T02:36:00+0900:
- User clarified that performance work must include backend structure, not only load-test target selection.
- Query-shape scan found public Work list and Blog list now project only card fields, but `HomeQueryStore` still reads all assets and full Work/Blog entities for home cards.
- Plan alignment before the next implementation slice: keep realistic target semantics (`pageSize=12`, no seeded priority) and add a real backend structure fix for public home composition.

Validation progress 2026-05-06T02:46:00+0900:
- RED: `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter "FullyQualifiedName~PublicQueryHandlerComponentTests.GetHomeQueryHandler_DoesNotUseWorkBodyImageAsFeaturedThumbnailFallback" --no-restore` failed before implementation because the old home query used body inline image fallback.
- GREEN: same focused test passed after `HomeQueryStore` was changed to card projection plus targeted asset/video lookup.
- PASS: `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter "FullyQualifiedName~PublicQueryHandlerComponentTests" --no-restore` (35/35; existing AWSSDK.Core NU1901 warning).
- PASS: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~PublicEndpointsTests" --no-restore` (27/27; existing AWSSDK.Core NU1901 warnings).
- PASS: `dotnet test backend/WoongBlog.sln --no-restore` (Component 117/117, Unit 56/56, Architecture 35/35, Integration 202/202, Contract 1 skipped; existing AWSSDK.Core NU1901 warnings).
- PASS: `npm run test -- --run` (82 files, 589 tests).
- PASS: `git diff --check`.
- Updated the persistent audit report with query-shape findings, prod-like 2CPU/8GB measurements, and current CI promotion status.
- Plan alignment after home query slice: implementation still matches user instructions; no seeded target priority, no `pageSize=1`, no cache workaround.

Progress 2026-05-06T03:56:00+0900:
- Completed prior GitHub promotion loop: PR #43 merged to `dev`, dev CI passed, PR #44 promoted to `main`, main runtime CI passed, and GHCR main publish passed.
- Plan alignment after promotion: realistic load target semantics remain `pageSize=12` and real public slug order; no seeded target priority was reintroduced.

---

# TODO - 2026-05-06 - Backend Performance Impact Map

## User Instructions
- Identify where backend performance can still be improved.
- Map each possible improvement to what should improve in Real Backend Test/backend load-test results.
- Keep the workload realistic: no seeded target priority, no `pageSize=1`, and no cache-first answer.
- Treat this as an analysis/audit step before choosing the next implementation slice.

## TODOs
- [x] Confirm the current `dev` code state before analysis.
- [x] Review public Work/Blog list and detail query stores for remaining DB roundtrip, projection, and serialization risks.
- [x] Review public Home and Real Backend Test diagnostics paths for measurable gaps.
- [x] Map improvement candidates to concrete backend-load metrics.
- [x] Generate persistent audit report artifacts.
- [x] Watch the currently running `dev` CI for the latest performance slice.

## Plan Alignment Check - Before Execution
- This is audit-only; production and test source files will not be changed.
- The analysis keeps the user's constraints: no cache workaround, no seed shortcut, and public list `pageSize=12` remains the realistic baseline.

Validation progress 2026-05-06T21:12:00+09:00:
- Used `analyzing-dotnet-performance` and `ef-core` guidance for the scan.
- Confirmed current `dev` HEAD is `c7c38d0 Optimize public hot path queries (#88)`.
- Confirmed worktree has no modified tracked source files; only pre-existing untracked backup/report artifacts.
- Reviewed current public Work/Blog/Home query stores, Real Backend Test contracts/control plane, diagnostics sampler, DB diagnostics collector, and recent load-test artifacts.
- Confirmed latest public hot-path local evidence: 100rps/30s and 300rps/30s realistic four-target runs completed with 0 failures and p95 around 5ms.
- Generated backend performance impact map audit artifacts under `backend/reports/backend-performance-impact-map-2026-05-06/`.
- PASS: `dev` CI run `25433589378` completed successfully for PR #88 merge commit `c7c38d0`.
- Confirmed existing `real-load-run-diagnostics-2026-05-06` and `db-command-diagnostics-async-2026-05-06` work already persists completed-run diagnostics and async DB command timing; next measurement gap is heavy detail response-size/serialization/app-write attribution.
- PASS: `Publish GHCR Dev` run `25433858614` completed successfully.
- PASS: `Promote Main Runtime` run `25433858575` created PR #89.
- PASS: PR #89 CI Main Runtime run `25433875357` completed successfully and auto-merged at `2026-05-06T12:05:15Z` as merge commit `ea6802e0`.
- PASS: main push CI run `25434162908` completed successfully.
- PASS: `Publish GHCR Main` run `25434455253` completed successfully.

# TODO - 2026-05-06 - Heavy Detail Read Optimization

## User Instructions
- Improve backend structure, not only load-test target selection.
- Keep realistic public workload semantics: no seeded read-target priority and no `pageSize=1` shortcut.
- Do not use caching as the primary fix.
- Treat the server as a constrained production-like runtime: backend, nginx, frontend, and DB share 2 CPU and 8 GiB.
- Focus next on heavy public detail reads: avoid body JSON parsing for representative image, split public/admin payload concerns, reduce EF query roundtrips and DB pool pressure, reduce JSON allocation pressure, and complete timing instrumentation.
- Use TDD, run backend tests and load tests, push to `dev`, verify CI, promote forward, then continue the next improvement loop.

## TODOs
- [x] Confirm the current code path still matches the planned heavy-detail read work before implementation.
- [x] Add a behavior test that public Work detail does not use body inline images as thumbnail fallback.
- [x] Refactor public Work detail to project only detail-required fields and resolve explicit thumbnail/icon URLs in the detail row instead of loading the full Work entity plus a separate asset lookup.
- [x] Refactor public Blog detail to project only detail-required fields and resolve cover URL in the detail row instead of loading the full Blog entity plus a separate asset lookup.
- [x] Run focused RED/GREEN component tests after each behavior slice.
- [x] Run relevant backend test suites and static checks.
- [x] Run a constrained local/prod-like backend load test against realistic `pageSize=12` public targets and record the result.
- [x] Generate persistent audit report artifacts for this iteration.
- [ ] Commit, push, open PR to `dev`, verify CI, merge, verify `dev` CI, promote to `main`, and verify `main` CI.
- [ ] Reassess the next iteration: stored representative image write-time model, public/admin content model split, DB/timing instrumentation, or source-generated JSON context.
- [x] Add a second operational slice for the confirmed spike bottlenecks: repo-managed nginx main config with higher worker connection budget and compose-level Npgsql pool cap.
- [x] Re-run compose config validation, nginx syntax validation, backend tests, and constrained load tests after the operational slice.
- [x] Stabilize the frontend Vitest gate blocking PR #45 before merging the completed backend optimization branch.

## Plan Alignment Check - Before Execution
- The next code slice targets public detail read shape only. It must not change Real Backend Test to seed targets or reduce list target page size.
- The intended first fix removes request-time body JSON parsing for public Work thumbnail fallback and narrows detail EF projections.
- This slice does not introduce cache and does not yet add schema columns for stored representative images; schema/data-model work remains a later iteration after this lower-risk hot-path reduction.

Validation progress 2026-05-06T04:46:00+0900:
- RED: `GetWorkBySlugQueryHandler_DoesNotUseBodyImageAsPublicThumbnailFallback` failed before implementation because public Work detail returned `/media/detail-body-inline.png` from body JSON.
- GREEN: same focused component test passed after public Work detail stopped passing `ContentJson` to thumbnail fallback.
- PASS: `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter "FullyQualifiedName~PublicQueryHandlerComponentTests" --no-restore` (36/36; existing AWSSDK.Core NU1901 warning).
- PASS: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~PublicEndpointsTests" --no-restore` (27/27; existing AWSSDK.Core NU1901 warning).
- PASS: `dotnet test backend/WoongBlog.sln --no-restore` (Component 118/118, Unit 56/56, Architecture 35/35, Integration 202/202, Contract 1 skipped; existing AWSSDK.Core NU1901 warning).
- PASS: `git diff --check`.
- PASS: prod-like compose stack started with backend/frontend/db/nginx pinned to cpuset `0,1` and memory limits of 3GiB/1GiB/3GiB/512MiB.
- PASS: constant 100rps 30s k6 with `pageSize=12` and current public list-selected read targets: 3,001 requests, 100.0rps, failed rate 0, p95 5.3ms, dropped 0.
- FAIL as capacity signal: spike 100 -> 1000 -> 100, 60s, maxVUs 1000: exit 99, 35,274 requests, avg 440.9rps, failed rate 1.52%, p95 1,396.1ms, dropped 3,725.
- Diagnosis: nginx logged `1024 worker_connections are not enough`, and Postgres rejected a post-spike diagnostic connection with `too many clients already`.
- Plan alignment before second operational slice: keep `pageSize=12`, current public target order, no seeded priority, and no cache.

Validation progress 2026-05-06T04:55:00+0900:
- Added repo-managed `nginx/nginx.conf` with `worker_connections 4096`, `worker_rlimit_nofile 8192`, and shorter keepalive timeout.
- Added compose-level `POSTGRES_MAX_POOL_SIZE` / `POSTGRES_MIN_POOL_SIZE` defaults, with Npgsql connection strings defaulting to maximum pool size 40.
- PASS: prod-like running nginx `nginx -T` reported syntax OK and showed `worker_connections 4096`.
- PASS: running backend environment showed `ConnectionStrings__Postgres` includes `Maximum Pool Size=40;Minimum Pool Size=0`.
- PASS: dev/staging/prod compose config generation showed nginx main config mount and Npgsql pool cap.
- PASS: `dotnet test backend/WoongBlog.sln --no-restore` after config slice (Component 118/118, Unit 56/56, Architecture 35/35, Integration 202/202, Contract 1 skipped; existing AWSSDK.Core NU1901 warning).
- PASS: post-config constant 100rps 30s k6: 3,001 requests, 100.0rps, failed rate 0, p95 5.3ms, dropped 0.
- PASS with remaining capacity warning: post-config spike 100 -> 1000 -> 100, 60s, maxVUs 1000: exit 0, 36,122 requests, avg 451.5rps, failed rate 0, p95 1,502.3ms, dropped 2,877.
- Diagnosis after operational slice: nginx connection reset/worker connection alerts are gone and DB diagnostics can connect; the remaining 1000rps gap is throughput/latency saturation, not immediate connection failure.
- Plan alignment after second slice: still no cache, no seeded target priority, and list targets remain `pageSize=12`.

Audit progress 2026-05-06T04:58:00+0900:
- Generated `backend/reports/heavy-detail-read-optimization-2026-05-06/heavy-detail-read-optimization-2026-05-06.md`.
- Generated `backend/reports/heavy-detail-read-optimization-2026-05-06/heavy-detail-read-optimization-2026-05-06.html`.
- Generated `backend/reports/heavy-detail-read-optimization-2026-05-06/heavy-detail-read-optimization-2026-05-06.json`.

CI progress 2026-05-06T05:20:00+0900:
- PR #45 CI blocked on `Frontend lint, types, and unit tests`; CI failure was `src/test/pact/public-api-consumer.pact.test.ts` not sending the expected `/api/auth/session` request to the Pact mock server.
- Local focused Pact file passed, but local full `npm run test -- --run` exposed another isolation failure in `src/test/public-responsive-feed.test.tsx` where a pending scroll restore leaked into the next test.
- Plan alignment before the CI-stability slice: production/backend optimization code remains unchanged; this slice only stabilizes the frontend verification gate needed before dev merge.

Validation progress 2026-05-06T04:43:08+0900:
- Added Vitest `unstubGlobals` and `unstubEnvs` so global fetch/env stubs cannot leak into Pact consumer files.
- Updated the Study mobile restore test to wait for both immediate and `requestAnimationFrame` scroll restore calls, preventing a pending callback from leaking into the next test.
- PASS: `npm run test -- --run src/test/pact/public-api-consumer.pact.test.ts` (1 file, 6 tests).
- PASS: `npm run test -- --run src/test/public-responsive-feed.test.tsx` (1 file, 17 tests).
- PASS: `npm run typecheck`.
- PASS: `npm run lint` (0 errors, 6 existing warnings).
- PASS: `npm run test -- --run` (82 files, 589 tests).
- PASS: `git diff --check`.
- Plan alignment after CI-stability slice: production/backend optimization code remains unchanged; the branch is ready for a new PR #45 CI run after commit/push.

Progress 2026-05-06T05:05:36+0900:
- PR #45 was merged into `dev`, and the `dev` CI run passed.
- Runtime-only promotion PR #47 failed `Compose Main Runtime Verification` because `docker-compose.prod.yml` now mounts `nginx/nginx.conf`, but `scripts/main-runtime-allowlist.txt` did not include that file.
- Plan alignment before this blocker fix: this slice does not change backend performance behavior, load target semantics, `pageSize=12`, or cache policy. It only fixes the runtime promotion artifact so the already-merged nginx operational config can reach `main`.
- Backup created under `.agent-backups/main-runtime-nginx-allowlist-2026-05-06/`.

## TODO - 2026-05-06 - Main Runtime Nginx Allowlist Fix

## User Instructions
- Continue the dev-to-main promotion loop after CI passes.
- Do not lose the repo-managed nginx main config added for the backend load-test operational slice.

## TODOs
- [x] Confirm the main runtime CI failure maps to a missing runtime allowlist artifact rather than a backend behavior regression.
- [x] Create a backup before editing config/TODO files.
- [x] Add `nginx/nginx.conf` to `scripts/main-runtime-allowlist.txt`.
- [x] Add focused verification that the runtime allowlist exports `nginx/nginx.conf`.
- [x] Verify prod compose config can resolve the nginx main config mount from the runtime tree.
- [x] Generate persistent audit artifacts for the allowlist fix.
- [ ] Commit, push, open PR to `dev`, verify CI, merge, verify `dev` CI.
- [ ] Regenerate/update PR #47 runtime-only promotion and verify `main` CI.

Validation progress 2026-05-06T05:07:31+0900:
- RED: `grep -Fx 'nginx/nginx.conf' scripts/main-runtime-allowlist.txt` failed before the config edit.
- GREEN: same allowlist check passed after adding `nginx/nginx.conf`.
- PASS: `git archive --format=tar HEAD $(grep -vE '^\\s*(#|$)' scripts/main-runtime-allowlist.txt) | tar -tf - | rg '^nginx/nginx\\.conf$'`.
- PASS: `git diff --check`.
- PASS: `SOURCE_REF=HEAD AUTO_COMMIT=false AUTO_PUSH=false WORKTREE_DIR=../woong-blog-main-runtime-verify PROMOTION_BRANCH=verify/main-runtime-nginx-conf ./scripts/promote-main-runtime.sh`.
- PASS: `test -f ../woong-blog-main-runtime-verify/nginx/nginx.conf`.
- PASS: with `.env.prod.example` copied to `.env.prod` inside the verify worktree, `docker compose --env-file .env.prod -f docker-compose.prod.yml config` resolved `/etc/nginx/nginx.conf` from the runtime tree and preserved `Maximum Pool Size=40;Minimum Pool Size=0`.
- PASS: generated `backend/reports/main-runtime-nginx-allowlist-2026-05-06/main-runtime-nginx-allowlist-2026-05-06.md`, `.html`, and `.json`; JSON parse check passed.
- Plan alignment after local verification: this fix only changes the runtime promotion artifact list and does not alter backend performance behavior, public target selection, `pageSize=12`, or cache policy.

---

# TODO - 2026-05-06 - Main CI Pact Consumer Stability

## User Instructions
- Continue the dev-to-main promotion loop after CI passes.
- Keep backend performance behavior unchanged while unblocking the failing verification gate.
- Do not change Real Backend Test target semantics, seed priority, `pageSize=12`, or cache policy.

## TODOs
- [x] Inspect the failed main CI run and identify the failing job/test.
- [x] Confirm focused Pact consumer tests pass locally before the stability fix.
- [x] Create a backup before editing test/TODO files.
- [x] Add a minimal Pact consumer stability fix for the flaky public work list/detail interaction.
- [x] Run focused Pact tests and related frontend verification.
- [x] Generate persistent audit artifacts for this CI stability slice.
- [ ] Commit, push, open PR to `dev`, verify CI, merge, verify `dev` CI.
- [ ] Regenerate/update runtime-only promotion to `main` and verify `main` CI.

## Plan Alignment Check - Before Execution
- Main runtime compose verification is already fixed and passed on PR #47; the remaining main failure is `Frontend lint, types, and unit tests`.
- CI failure evidence: main push run `25400980868`, job `74500157533`, failed in `src/test/pact/public-api-consumer.pact.test.ts` because the expected `GET /api/public/works?page=1&pageSize=1` Pact request was not received.
- Local focused Pact verification passed before this slice, so the change must target flaky test isolation/retry behavior only, not production API behavior.
- Backup created under `.agent-backups/main-ci-pact-consumer-stability-2026-05-06/`.

Validation progress 2026-05-06T06:03:50+09:00:
- Applied the same `{ retry: 2 }` stability policy to the public work list/detail Pact interaction that already existed on the matching public blog list/detail interaction.
- PASS: `npm run test -- --run src/test/pact/public-api-consumer.pact.test.ts` (1 file, 6 tests).
- PASS: `npm run typecheck`.
- PASS: `npm run lint` (0 errors, 6 existing warnings).
- PASS: `npm run test -- --run src/test/pact/public-api-consumer.pact.test.ts src/test/public-api-clients.test.ts src/test/public-api-contracts.test.ts src/test/public-responsive-feed.test.tsx` (4 files, 45 tests).
- PASS: `git diff --check`.
- INCONCLUSIVE: full local `npm run test -- --run` was stopped after running for more than 14 minutes without a failure summary; do not treat this as a green full-suite signal. The GitHub Actions frontend job remains the required full-suite gate.
- PASS: generated `backend/reports/main-ci-pact-consumer-stability-2026-05-06/main-ci-pact-consumer-stability-2026-05-06.md`, `.html`, and `.json`; JSON parse check passed.
- Plan alignment after local verification: this slice changes only Pact test stability, not backend production behavior, load target selection, `pageSize=12`, or cache policy.

---

# TODO - 2026-05-06 - Main CI Pact Session Stability

## User Instructions
- Continue the dev-to-main promotion loop after CI passes.
- Keep backend performance behavior unchanged while unblocking the failing verification gate.
- Do not change Real Backend Test target semantics, seed priority, `pageSize=12`, or cache policy.

## TODOs
- [x] Inspect the failed main CI run and identify the failing job/test.
- [x] Confirm the failure is still in frontend Pact consumer tests rather than backend/runtime compose.
- [x] Search for relevant skill support before implementation and keep the existing `gh-fix-ci` + `tdd` approach.
- [x] Create a backup before editing test/TODO files.
- [x] Add a minimal Pact consumer stability fix for the unauthenticated session interaction.
- [x] Run focused Pact tests and relevant frontend verification.
- [x] Generate persistent audit artifacts for this CI stability slice.
- [ ] Commit, push, open PR to `dev`, verify CI, merge, verify `dev` CI.
- [ ] Regenerate/update runtime-only promotion to `main` and verify `main` CI.

## Plan Alignment Check - Before Execution
- PR #50 runtime promotion passed all PR checks and merged to `main` as `db6db1d1ee59fe7224ae1c27aa606f9dd3a56d1e`.
- Main push run `25403319917` passed backend architecture/unit/component/integration, Pact provider, and Compose Main Runtime Verification.
- Main push run `25403319917` failed only `Frontend lint, types, and unit tests`, specifically `src/test/pact/public-api-consumer.pact.test.ts > public API consumer Pact contracts > contracts unauthenticated session payload` because the expected `GET /api/auth/session` Pact request was not received.
- This is a test isolation/stability blocker; it does not justify changing production backend behavior, load-test target selection, `pageSize`, or cache policy.
- Backup created under `.agent-backups/main-ci-pact-session-stability-2026-05-06/`.
- Skill search: `npx skills find pact testing` found low-install Pact/contract skills; no new skill was installed. Continue with repo-local tests plus the already available `gh-fix-ci` and `tdd` workflows.

Validation progress 2026-05-06T06:36:57+09:00:
- Local pre-change focused Pact run still passed 6/6, so the RED evidence remains the GitHub Actions main push failure `25403319917`.
- Applied shared `pactTestOptions = { retry: 2 }` to every Pact consumer interaction in `src/test/pact/public-api-consumer.pact.test.ts`, avoiding another whack-a-mole where a different Pact interaction misses its request under full CI load.
- Added `withBrowserApi()` for browser-facing Pact calls so `NEXT_PUBLIC_API_BASE_URL`, module cache, and global/env cleanup match the existing server API helper.
- Added `response.ok` assertion to the session Pact test before parsing JSON, making wrong-target/non-200 failures explicit.
- PASS: `npm run test -- --run src/test/pact/public-api-consumer.pact.test.ts` (1 file, 6 tests).
- PASS: `npm run test -- --run src/test/pact/public-api-consumer.pact.test.ts src/test/public-api-clients.test.ts src/test/public-api-contracts.test.ts src/test/public-admin-client-gate.test.tsx src/test/public-admin-rendering.test.tsx` (5 files, 44 tests).
- PASS: `npm run typecheck`.
- PASS: `npm run lint` (0 errors, 6 existing warnings).
- PASS: `git diff --check`.
- Plan alignment after local verification: this slice changes only frontend Pact test stability; production backend code, load-test target semantics, seed behavior, `pageSize=12`, and cache policy remain unchanged.
- PASS: generated `backend/reports/main-ci-pact-session-stability-2026-05-06/main-ci-pact-session-stability-2026-05-06.md`, `.html`, and `.json`; JSON parse check passed.

---

# TODO - 2026-05-06 - Public Work Thumbnail Read Path

## User Instructions
- Improve real backend structure, not by lowering load-test realism.
- Do not use cache as the primary fix.
- Keep realistic public list behavior such as `pageSize=12`; do not switch to seed-only targets or fake deterministic pages.
- Ensure public blog/public work DTOs only carry the fields needed by public pages.
- Account for a production constraint of 2 CPUs and 8GB total shared by backend, frontend, nginx, and DB.
- Verify whether public reads fetch only necessary DB data rather than loading global/full data.

## TODOs
- [x] Confirm the actual work still matches this plan before code execution.
- [x] Add behavior-focused tests proving public Work card thumbnails do not fall back to video-derived thumbnails.
- [x] Add behavior-focused tests proving public Work detail still returns videos but does not derive `thumbnailUrl` from videos.
- [x] Remove public Work list/home thumbnail dependency on `WorkVideos` while preserving explicit `ThumbnailAssetId` resolution.
- [x] Remove public Work detail thumbnail dependency on video rows while preserving public video DTO output.
- [x] Run targeted backend component tests and broader backend verification where feasible.
- [x] Generate persistent audit artifacts with changed/not-changed/validation/risk summary.

## Plan Alignment Check - Before Execution
- Current `dev` already avoids public list `ContentJson` projection, but public Work list/home still query `WorkVideos` when `ThumbnailAssetId` is null solely to compute thumbnail fallback.
- Public Work detail must still read `WorkVideos` because the detail page returns video DTOs, but thumbnail calculation should depend only on the explicit representative thumbnail asset.
- This slice intentionally does not change cache behavior, target URL selection, seed priority, or `pageSize=12`.
- Backup created under `.agent-backups/public-work-thumbnail-read-path-2026-05-06/`.
- Skill search: `npx skills find "ef core performance public query projection"` found EF Core query optimization skills, but no install was needed because the repository already has usable .NET performance and TDD skills for this scoped slice.

Validation progress 2026-05-06T07:13:21+0900:
- Plan alignment before code execution: current slice still targets public Work read-path DB/data shaping only; no cache behavior, target URL, seed priority, or `pageSize=12` changes.
- RED: `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter "FullyQualifiedName~PublicQueryHandlerComponentTests"` failed 3 newly added tests because public home/list/detail used YouTube video thumbnails as fallback.
- GREEN: removed public Work list/home video-thumbnail fallback and separated public detail thumbnail resolution from video DTO generation.
- PASS: same focused component test filter passed 39/39.
- PASS: `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj` passed 121/121.
- PASS: `dotnet test backend/tests/WoongBlog.Api.UnitTests/WoongBlog.Api.UnitTests.csproj` passed 56/56.
- PASS: `dotnet test backend/tests/WoongBlog.Api.ArchitectureTests/WoongBlog.Api.ArchitectureTests.csproj` passed 35/35.
- PASS: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~PublicEndpointsTests|FullyQualifiedName~WorkVideoEndpointsTests"` passed 45/45.
- PASS: `dotnet test backend/WoongBlog.sln` passed component 121/121, unit 56/56, architecture 35/35, integration 202/202; contract provider test skipped because no pact files were present.
- PASS: `git diff --check`.
- PASS: grep check confirmed `HomeQueryStore` has no `WorkVideos`, `GetWorkVideoLookupAsync`, `featuredWorkVideos`, or `WorkThumbnailUrlResolver` references.
- PASS: generated `backend/reports/public-work-thumbnail-read-path-2026-05-06/public-work-thumbnail-read-path-2026-05-06.md`, `.html`, and `.json`; JSON parse check passed with `node`.
- Plan alignment after verification: implementation still changes only public Work read-path thumbnail fallback behavior and does not change cache, target selection, seed priority, `pageSize=12`, DB schema, or frontend behavior.

---

# TODO - 2026-05-06 - Public Detail Body Split

## User Instructions
- Continue backend performance improvement loops while preserving realistic load-test conditions.
- Do not use cache as the primary fix.
- Keep public list targets realistic, including `pageSize=12`; do not force seed-only targets or fake deterministic small pages.
- Improve the heavy public detail read path next.
- Store public/renderable content shape at write time so public read requests do not return admin editing JSON wholesale.
- Split public detail DTOs into metadata plus body/content and keep the body to the minimum fields needed for rendering.
- Preserve the production-like constraint of 2 CPUs and 8GB total shared by backend, frontend, nginx, and DB.

## TODOs
- [x] Confirm the actual work still matches this plan before code execution.
- [x] Back up scoped files before modifying production/test code.
- [x] Add behavior-focused backend tests proving public blog/work detail returns `content` body and no longer exposes admin `contentJson`.
- [x] Add or update persistence contract coverage for stored public content body columns.
- [x] Implement stored public body fields on Blog/Work and populate them on save/bootstrap.
- [x] Update public detail DTO/query stores to project metadata plus stored public body fields only.
- [x] Update frontend public detail clients/pages/tests/Pact contract to consume `content` while tolerating old `contentJson` during transition if needed.
- [x] Update direct SQL Notion import so it also writes stored public content body fields.
- [x] Run backend component/integration/model tests, frontend focused tests/typecheck/lint, and diff checks.
- [ ] Run a scoped production-like backend load test if local runtime permits.
- [x] Generate persistent audit artifacts for this slice.
- [ ] Commit, push, open PR to `dev`, watch CI, and merge only after CI is green.

## Plan Alignment Check - Before Execution
- Current `dev` already includes `Optimize heavy public detail reads` and `Optimize public work thumbnail read path`.
- Public list/home card paths are already narrowed and must not be reworked by lowering page size, seed priority, or cache behavior.
- Public Work detail still returns full `ContentJson`; Public Blog detail still returns full `ContentJson`.
- This slice targets API payload/data model separation for public detail bodies, not cache or target selection.
- Skill usage: `tdd`, `analyzing-dotnet-performance`, and `dotnet-best-practices` read; `npx skills find "ef core json projection aspnet public dto performance"` found EF Core optimization skills but no extra install was needed.

Validation progress 2026-05-06T08:10:00+0900:
- Plan alignment before code execution: this slice targets public detail body/data-model separation only; no cache behavior, seed priority, target URL, or `pageSize=12` changes.
- Backup created under `.agent-backups/public-detail-body-split-2026-05-06/` for scoped backend, frontend, test, and TODO files.
- RED: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~GetWorkBySlug_ReturnsPublicContentBodyWithoutAdminContentJson|FullyQualifiedName~GetBlogBySlug_ReturnsPublicContentBodyWithoutAdminContentJson" --no-restore` failed 2/2 because public detail JSON still exposes `contentJson` instead of split `content` body.
- GREEN: focused backend integration tests for split public detail body passed 2/2 after adding stored public body fields and `content` DTO.
- PASS: component/model focused tests passed 43/43 for public query handlers and DbContext model contracts.
- PASS: focused backend integration tests passed 57/57 for public endpoints, admin content endpoints, and persistence contracts.
- PASS: focused frontend/Pact/detail tests passed 9 files, 51 tests.
- PASS: `npm run typecheck` completed with TypeScript errors at 0.
- PASS: `dotnet test backend/WoongBlog.sln --no-restore` passed Component 121/121, Unit 56/56, Architecture 35/35, Integration 204/204, Contract 1 skipped due no pact files.
- PASS: `npm run lint` completed with 0 errors and 6 pre-existing warnings.
- PASS: `git diff --check` completed with no whitespace errors.
- Follow-up scan: direct SQL Notion import still wrote only `ContentJson`; this would bypass EF `SaveChanges` synchronization and leave new public detail body fields empty for imported posts.
- PASS: direct SQL Notion import now writes `PublicContentHtml`/`PublicContentMarkdown` together with `ContentJson`; `npm run test -- --run src/test/notion-db-import-lib.test.ts` passed 1 file, 6 tests.
- PASS: reran `npm run typecheck`; completed with TypeScript errors at 0.
- PASS: reran `npm run lint`; completed with 0 errors and the same 6 pre-existing warnings.
- PASS: reran `git diff --check`; completed with no whitespace errors.
- PASS: generated persistent audit artifacts under `backend/reports/public-detail-body-split-2026-05-06/`; JSON parse check passed with `node`.
- DEFERRED: scoped production-like load test still needs a rebuilt current-branch compose stack under the 2 CPU / 8GB shared constraint before claiming measured latency/RPS improvement.

---

# TODO - 2026-05-06 - Async DB Command Diagnostics

## User Instructions
- Continue iterative backend performance improvement toward realistic 1000 rps spike readiness.
- Do not use cache as the primary fix.
- Preserve realistic targets: no seed-only target selection and no `pageSize=12` reduction.
- Fix observability gaps including `db command p95 unavailable` so app vs DB vs serialization bottlenecks can be attributed.
- Reduce DB connection/pool pressure by making EF query count and DB command timing visible before deeper query changes.
- Use TDD and verify through backend tests, then push to `dev` through CI.

## TODOs
- [x] Confirm this slice is not repeating completed public/admin body split work.
- [x] Install/use EF Core skill guidance plus existing TDD and .NET performance guidance.
- [x] Back up scoped files before modifying code/tests.
- [x] Add a behavior-focused Postgres integration test proving async EF commands are captured by the DB diagnostics collector.
- [x] Run the focused test RED and record the failure.
- [x] Add async `DbCommandInterceptor` overrides for reader/scalar/non-query/failure paths.
- [x] Run focused integration tests GREEN.
- [x] Run relevant backend test suites and diff checks.
- [x] Fix standalone k6 load script smoke blocker found while running scoped load smoke.
- [x] Run a scoped load/diagnostics smoke if local runtime permits.
- [x] Generate persistent audit artifacts for this slice.
- [ ] Commit, push, open PR to `dev`, watch CI, and merge only after CI is green.

## Plan Alignment Check - Before Execution
- Completed previous slice already split public detail body payload and merged to `dev`/`main`.
- Remaining issue found in current code: `LoadTestDbCommandDiagnosticsInterceptor` records only synchronous command callbacks while EF hot paths use async queries.
- This explains why load-test UI can show DB connection metrics but `db command p95` remains unavailable.
- This slice changes instrumentation only; it does not change cache behavior, target realism, public DTO body shape, or page size.

Validation progress 2026-05-06T08:40:00+0900:
- Skill usage: `tdd`, `analyzing-dotnet-performance`, installed/read `github/awesome-copilot@ef-core` after `npx skills find "dotnet ef core npgsql diagnostics performance"`.
- Backup created under `.agent-backups/db-command-diagnostics-async-2026-05-06/` for interceptor, Postgres persistence tests, and TODO file.
- RED: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~CommandDiagnosticsInterceptor_RecordsAsyncEfCommands_WithPostgres" --no-restore` failed because async EF commands did not increment `CommandLatency.SampleCount`.
- GREEN: same focused Postgres integration test passed after adding async command interceptor overrides.
- PASS: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~PostgresPersistenceContractTests|FullyQualifiedName~StartupCompositionTests" --no-restore` passed 17/17.
- PASS: `dotnet test backend/WoongBlog.sln --no-restore` passed Component 121/121, Unit 56/56, Architecture 35/35, Integration 205/205, Contract 1 skipped due no pact files.
- PASS: `git diff --check` completed with no whitespace errors.
- RUNTIME: rebuilt backend image in local 2CPU/8GB overlay; first recreate attempt used default backend port 8080 and failed due host port conflict, then recovered with `BACKEND_PUBLISH_PORT=18080 NGINX_HTTPS_PORT=3002`.
- PASS: runtime smoke after rebuild returned `health 200` and public works list `200`.
- BLOCKED: runtime diagnostics endpoint returned `401` and test-login returned `404`, so command latency endpoint smoke cannot be read from local runtime without an admin session.
- RED: standalone k6 smoke failed before traffic with `ReferenceError: maxVUs is not defined` in `scripts/loadtest/public-api-rps.js`; same typo existed in `public-api-spike.js`.
- GREEN: fixed standalone k6 `maxVUs` variable mapping in `public-api-rps.js` and `public-api-spike.js`.
- PASS: 2CPU/8GB overlay k6 smoke `100rps / 15s / pageSize=12` against `/api/public/works` completed 1,501 requests, failure rate 0, p95 4.16 ms; summary saved at `backend/reports/db-command-diagnostics-async-2026-05-06/loadtest/k6-public-works-100rps-15s.json`.
- PASS: `k6 inspect` for `scripts/loadtest/public-api-spike.js` succeeded and resolved `maxVUs`.
- PASS: reran `git diff --check`; completed with no whitespace errors.
- PASS: `npm run lint` completed with 0 errors and the same 6 pre-existing warnings.
- PASS: generated `backend/reports/db-command-diagnostics-async-2026-05-06/db-command-diagnostics-async-2026-05-06.md`, `.html`, and `.json`; JSON parse check passed with `node`.
- Plan alignment after local verification: this slice changes instrumentation and standalone k6 script correctness only; production DTO shape, cache behavior, target realism, seed priority, and `pageSize=12` remain unchanged.

---

# TODO - 2026-05-06 - Public Media Read Model

## User Instructions
- Continue iterative backend performance improvement after the async DB diagnostics slice reached `dev` and `main`.
- Do not use caching as the primary fix.
- Preserve realistic load-test semantics: no seed-only target priority and no `pageSize=12` reduction.
- Store thumbnail/representative image data at write time so request-time public reads do not inspect body JSON or perform avoidable media lookup work.
- Reduce public list/home/detail DB roundtrips under the production-like 2 CPU / 8 GiB shared-runtime constraint.
- Use TDD, run backend tests and load smoke where feasible, then push to `dev`, verify CI, promote to `main`, and continue the loop.

## TODOs
- [x] Confirm the previous async DB diagnostics slice reached `dev` and `main` with green CI.
- [x] Search/install relevant EF Core projection skill support and use it with existing TDD/.NET performance guidance.
- [x] Create a branch from current `dev` and back up scoped files before implementation.
- [x] Add behavior-focused tests proving public Work list/detail/home use stored public media URLs without requiring request-time `Assets` lookup.
- [x] Add behavior-focused tests proving public Blog list/detail/home use stored public cover URL without requiring request-time `Assets` lookup.
- [x] Add required model/schema/backfill coverage for stored public media URL fields.
- [x] Implement stored public media URL fields on Work/Blog and populate them at write/bootstrap/import time.
- [x] Refactor public Work/Blog/Home query stores to project stored URLs directly and remove avoidable public card/detail asset lookup roundtrips.
- [x] Run focused RED/GREEN backend tests, broader backend tests, diff checks, and scoped load smoke if local runtime permits.
- [x] Generate persistent audit artifacts for this slice.
- [ ] Commit, push, open PR to `dev`, watch CI, merge after green, promote to `main`, and verify `main` CI.

## Plan Alignment Check - Before Execution
- Current `dev` includes public detail body split and async DB command diagnostics.
- Public read paths no longer intentionally parse body JSON for public Work thumbnails, but public Work/Blog/Home still perform request-time `Assets` lookups to resolve public media URLs.
- This slice changes the public read model by storing public media URLs on content rows; it does not add cache, seed priority, target changes, or page-size shortcuts.
- Skill usage: `tdd`, `analyzing-dotnet-performance`, installed/read `dotnet/skills@optimizing-ef-core-queries`.
- Backup created under `.agent-backups/public-media-read-model-2026-05-06/`.

Validation progress 2026-05-06T09:45:00+0900:
- RED: `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter "FullyQualifiedName~GetHomeQueryHandler_UsesStoredPublicMediaUrlsWithoutAssetRows" --no-restore` initially failed because `Work.PublicThumbnailUrl`, `Work.PublicIconUrl`, and `Blog.PublicCoverUrl` did not exist yet.
- GREEN: focused component/model tests passed 44/44 for public query handlers and DbContext model contracts.
- GREEN: focused integration tests passed 103/103 for public endpoints, admin content/mutation endpoints, database bootstrapper, Postgres persistence contracts, and startup composition.
- PASS: direct SQL Notion import helper tests passed 1 file, 6 tests with `npm run test -- --run src/test/notion-db-import-lib.test.ts`.
- PASS: `dotnet test backend/WoongBlog.sln --no-restore` passed Unit 56/56, Architecture 35/35, Component 122/122, Integration 205/205, Contract 1 skipped due no pact files.
- PASS: `npm run lint` completed with 0 errors and 6 pre-existing warnings.
- PASS: `git diff --check` completed with no whitespace errors.
- PASS: prod-like Docker stack rebuilt from current branch with backend/frontend/db/nginx pinned to cpuset `0,1` and split memory limits: backend 3GiB, DB 3GiB, frontend 1GiB, nginx 512MiB.
- PASS: k6 300rps/30s smoke under the 2CPU/8GiB split profile completed with 0 failures on all four Real Backend target shapes:
  - Works list `pageSize=12`: 9,001 requests, 300.01 rps, p95 7.48 ms.
  - Blogs list `pageSize=12`: 9,000 requests, 300.03 rps, p95 4.87 ms.
  - Work detail `seeded-work`: 9,001 requests, 300.03 rps, p95 3.99 ms.
  - Blog detail `seeded-blog`: 9,001 requests, 300.02 rps, p95 2.96 ms.
- PASS: k6 summary exports saved under `backend/reports/public-media-read-model-2026-05-06/loadtest/`.
- NOTE: local smoke uses seeded local data because production content is not present in the local test database; this validates query shape and runtime stability, not final production traffic capacity.
- Plan alignment after verification: no cache was introduced, no seed-priority target selection was added, and `pageSize=12` public list semantics were preserved.
- PASS: generated audit artifacts under `backend/reports/public-media-read-model-2026-05-06/`; JSON parse check passed with `node`.

---

# TODO - 2026-05-06 - Main Runtime Admin Blog Batch Prompt CI Fix

## User Instructions
- Continue the iterative dev-to-main promotion loop after backend performance work.
- Keep backend performance behavior unchanged while unblocking the failing verification gate.
- Do not change Real Backend Test target semantics, seed priority, `pageSize=12`, or cache policy.

## TODOs
- [x] Inspect PR #61 failed check and identify the failing job/test.
- [x] Confirm the failure is a frontend Vitest timing issue, not backend performance/runtime compose regression.
- [x] Create a backup before editing test/TODO files.
- [x] Stabilize the `AdminBlogBatchAiPanel` prompt test by waiting for the rendered prompt value, not only the runtime-config mock call.
- [x] Run focused RED/GREEN frontend verification and related static checks.
- [x] Generate persistent audit artifacts for this CI fix.
- [ ] Commit, push, open PR to `dev`, verify CI, merge, verify `dev` CI.
- [ ] Regenerate/update runtime-only promotion PR #61 and verify `main` CI.

## Plan Alignment Check - Before Execution
- Current `dev` head `2ee29bf` passed `CI Dev` in run `25410618080`.
- Runtime-only promotion PR #61 is blocked only by `Frontend lint, types, and unit tests`; backend unit/component/integration/architecture, Pact provider, and Compose Main Runtime Verification passed.
- Failed test evidence from run `25410791523`: `src/test/admin-blog-batch-ai-panel.test.tsx > AdminBlogBatchAiPanel > sends edited custom prompt when creating a batch job` expected `Batch AI system prompt` to contain `Default blog system prompt` but received an empty string.
- Local focused file currently passes, so this is treated as CI timing flake. The fix must be limited to test synchronization and must not alter backend performance behavior, public target selection, `pageSize=12`, or cache policy.
- Backup created under `.agent-backups/main-runtime-admin-blog-batch-prompt-ci-2026-05-06/`.

Validation progress 2026-05-06T10:27:00+0900:
- RED evidence: PR #61 CI Main Runtime run `25410791523` failed `Frontend lint, types, and unit tests` because `src/test/admin-blog-batch-ai-panel.test.tsx` checked the prompt before the rendered state updated.
- GREEN: changed the test to wait for the user-visible `Batch AI system prompt` value instead of only waiting for the runtime-config mock call.
- PASS: `npm run test -- --run src/test/admin-blog-batch-ai-panel.test.tsx` (1 file, 17 tests).
- PASS: `npm run typecheck`.
- PASS: `npm run lint` (0 errors, 6 pre-existing warnings).
- PASS: `git diff --check`.
- PASS: `npm run test -- --run` (82 files, 590 tests; slow local runtime, but completed successfully).
- Plan alignment after local verification: this slice only changes frontend test synchronization and TODO/audit artifacts; backend performance behavior, public target selection, `pageSize=12`, and cache policy remain unchanged.

---

# TODO - 2026-05-06 - Public Work Detail Read Model

## User Instructions
- Continue iterative backend performance improvement without using cache as the primary fix.
- Preserve realistic Real Backend Test semantics: no seed-only target priority and no `pageSize=12` reduction.
- Focus next on heavy detail read path, especially request-time JSON parsing removal, public/admin content model separation, and detail projection.
- Reduce DB/CPU/allocation pressure so public detail reads finish within 1-2 DB roundtrips and avoid reading admin-only JSON on public requests.
- Run backend tests and a scoped load test, push through `dev`, watch CI, and continue the loop.

## TODOs
- [x] Confirm previous `dev` head is clean for tracked files and create a dedicated performance branch.
- [x] Use TDD, EF Core, and .NET performance guidance; no new external skill install was needed because relevant local skills are already available.
- [x] Create a scoped backup before editing code/tests.
- [x] Add RED tests proving Work public social-share data is stored/synchronized at write time and public detail uses that stored field instead of request-time `AllPropertiesJson` parsing.
- [x] Add model/bootstrap/Postgres contract coverage for the new stored public Work field.
- [x] Implement `Work.PublicSocialShareMessage`, write-time synchronization, schema bootstrap/backfill, and public detail projection change.
- [x] Run focused GREEN backend tests, broader backend tests, diff checks, and scoped load smoke under the existing realistic constraints.
- [x] Generate persistent audit artifacts for this slice.
- [ ] Commit, push, open PR to `dev`, watch CI, merge after green, and verify the promotion path before starting the next slice.

## Plan Alignment Check - Before Execution
- Current code already stores public body fields and public media URLs; this slice does not repeat that work.
- Remaining request-time public Work detail cost found in `WorkQueryStore`: public detail projects `AllPropertiesJson` and parses it only to expose `socialShareMessage`.
- This slice moves `socialShareMessage` into a public read-model column populated at write/bootstrap time. It is a data-model/read-model change, not cache.
- This slice intentionally does not change target selection, seeded data priority, `pageSize=12`, or load-test scenario semantics.
- Backup created under `.agent-backups/public-work-detail-read-model-2026-05-06/`.

Validation progress 2026-05-06T11:03:00+0900:
- RED: focused component tests failed to compile because `Work.PublicSocialShareMessage` did not exist.
- GREEN: focused component tests passed 4/4 for public Work detail social-share read model, public detail media/video mapping, and DbContext model synchronization.
- GREEN: focused integration/Postgres/admin tests passed 5/5 for stored public social-share projection, schema bootstrap column/patch, save-time synchronization, and admin create/update persistence.
- PASS: `dotnet test backend/WoongBlog.sln --no-restore` passed Unit 56/56, Architecture 35/35, Component 122/122, Integration 206/206, Contract 1 skipped due no pact files.
- PASS: `git diff --check` completed with no whitespace errors.
- PASS: current-branch backend image rebuilt under the existing 2 CPU / 8 GiB split compose overlay and health/list probes returned HTTP 200.
- PASS: Dockerized k6 100rps/30s Work detail smoke against current list-selected slug `internal-admin-workbench` completed 3,001 requests at 100.03 rps, 0 failures, p95 11.49 ms.
- PASS: Dockerized k6 100rps/30s Work list smoke with `pageSize=12` completed 3,001 requests at 100.03 rps, 0 failures, p95 3.52 ms.
- NOTE: host `k6` was unavailable, so the same k6 script was run through `grafana/k6:1.0.0` on the compose network.
- Plan alignment after local verification: this is a write-time public read-model change, not cache; no Real Backend target selection, seeded priority, `pageSize=12`, or scenario semantics were changed.
- PASS: generated audit artifacts under `backend/reports/public-work-detail-read-model-2026-05-06/`; JSON parse check passed with `node`.
- PASS: committed `4a40240` (`Store public work social share message`), pushed branch `perf/public-work-detail-read-model-2026-05-06`, and opened PR #63 to `dev`.
- PASS: PR #63 CI passed, merged to `dev` as `fec1685`, and `CI Dev` run `25412715971` passed.
- NOTE: main promotion PR #64 was created automatically from `dev` and `CI Main Runtime` was still in progress when this next slice started.

---

# TODO - 2026-05-06 - Public List Window Query

## User Instructions
- Continue the backend performance improvement work; do not stop at analysis.
- Preserve realistic load-test semantics: no seeded-target priority, no `pageSize=12` reduction, and no cache-first fix.
- Improve backend structure so public reads fetch only what they need and reduce DB pressure under the 2 CPU / 8 GiB shared runtime constraint.
- Push through `dev` CI and continue promotion toward `main`.

## TODOs
- [x] Confirm `dev` is clean for tracked files and create a dedicated branch.
- [x] Use TDD, EF Core, .NET performance, and find-skills guidance; no new external skill install was needed because relevant local skills are already available.
- [x] Create a scoped backup before editing code/tests/TODO.
- [x] Add RED Postgres contract tests proving no-search first-page public Work/Blog list reads use one DB command instead of `COUNT(*)` plus page SELECT.
- [x] Implement PostgreSQL `COUNT(*) OVER()` first-page public list read path while keeping InMemory/search/page>1 fallback behavior unchanged.
- [x] Run focused GREEN tests, full backend tests, diff checks, and scoped realistic load smoke.
- [x] Generate persistent audit artifacts.
- [ ] Commit, push, open PR to `dev`, verify CI, merge, and continue main promotion checks.

## Plan Alignment Check - Before Execution
- This slice targets the list endpoints in the user's real backend mix: `/api/public/works?page=1&pageSize=12` and `/api/public/blogs?page=1&pageSize=12`.
- Current EF path executes two DB commands per list request: `CountAsync` for totals and then the page projection. Under the user's failed spike, that amplifies DB pool pressure across backend/frontend/db/nginx sharing 2 CPU and 8 GiB.
- The intended fix is not cache: it computes the same total in the same PostgreSQL read using `COUNT(*) OVER()` and returns the same DTO shape.
- To avoid behavior risk, the optimized path will be limited to no-search `page == 1` on PostgreSQL. Search and page-clamping cases keep the existing portable EF path.
- No Real Backend Test target selection, seed priority, `pageSize=12`, cache policy, or endpoint contract is changed.
- Backup created under `.agent-backups/public-list-window-query-2026-05-06/`.

Validation progress 2026-05-06T11:55:00+0900:
- RED setup typo: first run failed to compile because `ContentSearchMode` using pointed to the wrong namespace; fixed before accepting RED evidence.
- RED: focused Postgres tests failed as intended because current public Work/Blog first-page list reads execute 2 DB commands (`COUNT(*)` plus page SELECT), not 1.
- GREEN: focused Postgres command-count tests passed 2/2 after adding PostgreSQL first-page window queries for public Work/Blog list reads.
- PASS: `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter "FullyQualifiedName~PublicQueryHandlerComponentTests" --no-restore` passed 40/40.
- PASS: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~PostgresPersistenceContractTests" --no-restore` passed 7/7.
- PASS: `dotnet test backend/WoongBlog.sln --no-restore` passed Unit 56/56, Architecture 35/35, Component 122/122, Integration 208/208, Contract 1 skipped due no pact files.
- PASS: `git diff --check` completed with no whitespace errors.
- PASS: main promotion PR #64 had all CI Main Runtime checks green and was already merged.
- PASS: backend image rebuilt under the 2 CPU / 8 GiB split compose overlay; health, Work list, and Blog list probes returned HTTP 200.
- PASS: k6 Work list cold 300rps/30s completed 8,987 requests at 299.57 rps, 0 failures, p95 222.63 ms; warm rerun completed 9,001 requests at 300.05 rps, 0 failures, p95 4.58 ms.
- PASS: k6 Blog list 300rps/30s completed 9,001 requests at 300.04 rps, 0 failures, p95 2.54 ms.
- PASS: generated audit artifacts under `backend/reports/public-list-window-query-2026-05-06/`; JSON parse check passed with `node`.
- PASS: committed `8307530` (`Reduce public list query roundtrips`), pushed branch `perf/public-list-window-query-2026-05-06`, and opened PR #65 to `dev`.

---

# TODO - 2026-05-06 - Public Work Detail Video Query

## User Instructions
- Continue executing backend performance improvements, not only test-method changes.
- Keep realistic load-test semantics: no cache-first fix, no seeded-target priority, and no `pageSize=12` reduction.
- Improve backend structure so public reads fetch only the data they need under the 2 CPU / 8 GiB shared runtime constraint.
- Push changes through `dev` CI and promote to `main` after green.

## TODOs
- [x] Confirm current `dev` branch state and create a dedicated branch for this slice.
- [x] Use TDD, EF Core, .NET performance, and find-skills guidance; `dotnet/skills@analyzing-dotnet-performance` was found and the local installed skill is already in use.
- [x] Create scoped backups before editing code/tests/TODO.
- [x] Add RED Postgres contract coverage proving a public Work detail with no videos does not execute a needless `WorkVideos` query.
- [x] Implement no-video skip and public video DTO projection so Work detail reads fetch only public video fields when videos exist.
- [x] Run focused GREEN tests, broader backend tests, diff checks, and scoped prod-like load sanity.
- [x] Generate persistent audit artifacts.
- [ ] Commit, push, open PR to `dev`, verify CI, merge, and continue main promotion checks.

## Plan Alignment Check - Before Execution
- This slice targets `/api/public/works/{slug}`, one of the real backend mix read targets.
- Current public Work detail first projects the public Work row, then always queries `WorkVideos`, even when `VideosVersion == 0` and no video can be returned.
- This is not cache and not target manipulation. The endpoint contract remains the same: public Work detail still returns body content and `videos`, but avoids a DB command when videos are absent.
- For works that do have videos, the query should project only fields needed for `WorkVideoDto`; admin mutation paths keep their existing verification behavior.
- No Real Backend Test scenario, seed priority, `pageSize=12`, or cache policy is changed.
- Backup created under `.agent-backups/public-work-detail-video-query-2026-05-06/`.

## Validation Progress
- PASS: RED confirmed `PublicWorkDetailWithoutVideos_UsesSinglePostgresCommand` failed before implementation with `Expected: 1 Actual: 2`, proving the needless no-video `WorkVideos` query existed.
- PASS: focused GREEN `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~PublicWorkDetailWithoutVideos_UsesSinglePostgresCommand" --no-restore` passed with 1/1 tests.
- PASS: `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter "FullyQualifiedName~PublicQueryHandlerComponentTests" --no-restore` passed with 40/40 tests.
- PASS: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~PostgresPersistenceContractTests" --no-restore` passed with 8/8 tests.
- PASS: `dotnet test backend/WoongBlog.sln --no-restore` passed backend tests: Component 122, Unit 56, Architecture 35, Integration 209, Contract 1 skipped.
- PASS: `git diff --check` completed with no whitespace errors.
- PASS: rebuilt backend image under the existing 2 CPU cpuset / total 8 GiB overlay after explicitly passing `backend/reports/backend-real-load-performance-2026-05-05/loadtest.env`; backend runs as `Production` with `LoadTesting__BaseUrl=http://nginx` and Postgres max pool size 40.
- PASS: k6 sanity `RATE=300 DURATION=30s PRE_ALLOCATED_VUS=300 MAX_VUS=300 LIST_PAGE_SIZE=12 WORK_READ_PATH=/api/public/works/internal-admin-workbench` completed 9,001 requests at 300.05 rps, failed_rate 0, dropped_iterations 0, aggregate p95 7.86 ms, Work read p95 5.96 ms.
- NOTE: backend startup logs still show existing `libgssapi_krb5.so.2` missing and MediatR license warnings; no request errors were emitted during the sanity run.
- PASS: previous main promotion follow-up `Publish GHCR Main` run `25414207107` completed successfully for main head `0bf6c585d7be641ebc11944b2f083ec0016103a5`.
- PASS: generated audit artifacts under `backend/reports/public-work-detail-video-query-2026-05-06/`; audit JSON and k6 summary JSON parse checks passed with `node`.

---

# TODO - 2026-05-06 - Pact Provider CI Dotnet Setup

## User Instructions
- Continue the dev-to-main promotion path instead of stopping after the first failing check.
- Fix the `Pact provider verification` failure blocking PR #68 main promotion.

## Observed Failure
- PR #68 `Pact provider verification` failed in run `25414949688`, job `74544519726`.
- Root cause from job log: `docker run --pull=missing ... mcr.microsoft.com/dotnet/sdk:10.0 ...` failed before tests started because MCR returned a blocked/denied response while pulling the SDK image.
- This is a CI infrastructure dependency failure, not a Pact contract assertion failure.

## TODOs
- [x] Inspect failing GitHub Actions log for PR #68.
- [x] Create a dedicated branch and backup workflow/TODO files before editing.
- [x] Patch CI Dev and CI Main Runtime Pact jobs to use `actions/setup-dotnet@v4` and run `scripts/pact-provider-verify.sh` directly, avoiding the Docker SDK image pull.
- [x] Run local verification for the Pact provider script and workflow syntax/diff checks.
- [x] Generate persistent audit artifacts for this CI fix.
- [ ] Commit, push, open PR to `dev`, verify CI, merge, and continue main promotion.

## Plan Alignment Check - Before Execution
- This does not change backend application behavior or load-test target semantics.
- The fix removes an avoidable CI image-pull dependency from Pact provider verification.
- The same fix must be applied to both `ci-dev.yml` and `ci-main-runtime.yml` so future dev and main promotion checks use the same stable path.
- Backup created under `.agent-backups/pact-provider-setup-dotnet-2026-05-06/`.

## Validation Progress
- PASS: `PACT_PROVIDER_PORT=5098 bash ./scripts/pact-provider-verify.sh` verified 1 Pact provider contract locally.
- PASS: `git diff --check` completed with no whitespace errors.
- PASS: grep guard confirmed `.github/workflows` no longer contains `mcr.microsoft.com/dotnet/sdk:10.0 bash ./scripts/pact-provider-verify.sh`.
- PASS: generated audit artifacts under `reports/pact-provider-setup-dotnet-2026-05-06/`; audit JSON parse check passed with `node`.

---

# TODO - 2026-05-06 - Public Blog Cover Write Model

## User Instructions
- Continue heavy detail read optimization without caching or fake load-test targets.
- Store representative/thumbnail image data at write time so public requests read stored columns rather than parsing body JSON.
- Keep realistic public list targets at `pageSize=12` and do not prioritize seeded slugs.
- Run backend tests and a constrained backend load test, then push through `dev` CI and main promotion before moving to the next improvement.

## TODOs
- [x] Confirm current code state before implementation: `Blog.PublicCoverUrl` exists and public read paths use it, but admin create/update requests do not accept or persist `CoverAssetId`.
- [x] Search/apply relevant skills before implementation: use `find-skills` guidance, `tdd`, `ef-core`, and `analyzing-dotnet-performance`; no new skill install is needed because suitable repo/local skills already exist.
- [x] Create backups before editing source, tests, or TODO files.
- [x] RED: add behavior coverage that creating a Blog with a cover asset persists `CoverAssetId` and `PublicCoverUrl`, and public detail returns the stored cover URL.
- [x] GREEN: add `CoverAssetId` through Blog create/update request/command/store and resolve the public URL from `Assets` at save time.
- [x] Add update coverage so changing or clearing the cover updates the stored public cover URL.
- [x] Run focused backend tests and static checks.
- [x] Run constrained backend load test against realistic public detail/list targets and record evidence.
- [x] Generate persistent audit report artifacts.
- [ ] Commit, push, open PR to `dev`, verify CI, merge, verify `dev` CI, promote to `main`, and verify `main` CI.
- [ ] Reassess next iteration: public/admin body model split, detail roundtrip instrumentation, source-generated JSON context, or nginx/app timing holes.

## Plan Alignment Check - Before Execution
- This slice targets the stored Blog cover write model only. It does not alter public load target selection, does not reduce `pageSize=12`, does not introduce seeded target priority, and does not add cache.
- The performance rationale is to make Blog representative image resolution a write-time data-model operation, matching the existing public read path that already projects `PublicCoverUrl`.
- Expected public read behavior remains one projected Blog detail query that does not inspect `ContentJson` for cover images.

Validation progress 2026-05-06T13:25:00+09:00:
- RED: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~AdminContentEndpointsTests.CreateAndReadBlog_PersistsUploadedCover" --no-restore` failed because public Blog detail did not include the uploaded cover URL.
- GREEN: same focused create-cover test passed after Blog create request/command/store began accepting `CoverAssetId` and writing `PublicCoverUrl` from `Assets.PublicUrl` at save time.
- RED: `UpdateBlog_PreservesStoredCover_WhenCoverAssetIdIsOmitted` failed after the initial implementation because legacy update requests without `coverAssetId` cleared the stored cover.
- GREEN: update now distinguishes omitted `coverAssetId` from explicit null; omitted preserves the stored cover, explicit null clears it, and a provided ID changes it.
- PASS: focused integration filter covering create, preserve-on-omit, change, and clear cover behavior passed (3/3; existing AWSSDK.Core NU1901 warnings).
- Plan alignment after implementation: this slice only adds Blog representative image write-time persistence; public load targets, seed priority, `pageSize=12`, and cache policy remain unchanged.
- PASS: added and ran `PostgresPersistenceContractTests.PublicBlogDetail_UsesSinglePostgresCommand`; Blog public detail returns stored `PublicCoverUrl` in one measured Postgres command.
- PASS: `dotnet test backend/tests/WoongBlog.Api.UnitTests/WoongBlog.Api.UnitTests.csproj --filter "FullyQualifiedName~RequestValidatorTests" --no-restore` passed 8/8.
- PASS: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~AdminContentEndpointsTests" --no-restore` passed 25/25.
- PASS: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~PostgresPersistenceContractTests" --no-restore` passed 9/9.
- PASS: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~PublicEndpointsTests" --no-restore` passed 29/29.
- PASS: `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter "FullyQualifiedName~PublicQueryHandlerComponentTests" --no-restore` passed 40/40.
- PASS: `git diff --check` completed with no whitespace errors.
- PASS: diff guard found no production load-target, seed-priority, or `pageSize` changes; only new test requests hit `/api/public/blogs/{slug}`.
- PASS: `dotnet test backend/WoongBlog.sln --no-restore` passed backend tests: Component 122/122, Unit 56/56, Architecture 35/35, Integration 213/213, Contract 1 skipped due no pact files.
- PASS: rebuilt backend/frontend under the 2 CPU / about 7.5 GiB total compose overlay (`backend=3GiB`, `frontend=1GiB`, `db=3GiB`, `nginx=512MiB`, all `cpuset=0,1`) and verified `/api/health`.
- PASS: current public load targets were selected from public list order without seed override: Work read `/api/public/works/internal-admin-workbench`, Study read `/api/public/blogs/engineering-notes-on-bff-auth`, list paths kept `pageSize=12`.
- PASS: k6 constant public mix `RATE=300 DURATION=30s PRE_ALLOCATED_VUS=300 MAX_VUS=1000 LIST_PAGE_SIZE=12` completed 9,001 requests at 300.01 rps, failed_rate 0, dropped_iterations 0, aggregate p95 4.10 ms.
- NOTE: backend container startup still logs existing `libgssapi_krb5.so.2` missing and MediatR license warnings; no request failure, nginx worker, timeout, or upstream error was observed during k6.
- PASS: generated audit artifacts under `backend/reports/public-blog-cover-write-model-2026-05-06/`; audit JSON and k6 summary JSON parse checks passed with `node`.

---

# TODO - 2026-05-06 - Admin Work List Projection

## User Instructions
- Keep executing backend structural performance improvements, not only load-test method changes.
- Do not use cache as the primary fix.
- Do not fake load targets with seeded priority or smaller list page sizes.
- Improve whether backend queries fetch only the data needed by the DTO.
- Keep promotion path through `dev` and `main` after validation.

## TODOs
- [x] Confirm tracked worktree state and create a dedicated branch.
- [x] Use `find-skills` guidance plus `tdd`, `ef-core`, `analyzing-dotnet-performance`, and `dotnet-best-practices`; no new external skill install is needed because suitable local skills are available.
- [x] Create scoped backups before editing source/tests/TODO.
- [x] RED: add Postgres contract coverage proving admin Work list currently performs unnecessary DB commands beyond the DTO projection.
- [ ] GREEN: change admin Work list to a direct projection over stored public media columns instead of loading full Work rows, Assets, and WorkVideos.
- [ ] Run focused tests, broader backend tests, diff checks, and generate audit artifacts.
- [ ] Commit, push, open PR to `dev`, verify CI, merge, and continue main promotion checks.

## Plan Alignment Check - Before Execution
- This slice addresses the user's “DTO에 보이는 것만 DB에서 가져와야 한다” point on a concrete remaining hotspot: `AdminWorkListItemDto` only needs list metadata and thumbnail URL, but current `GetAdminListAsync` loads entire Work entities plus asset/video rows to derive a thumbnail.
- This is not a cache, not load-target manipulation, not seed priority, and not `pageSize` reduction.
- Public load-test target semantics remain unchanged. The change is a backend read-model correction so list DTOs are projected from stored columns.
- Backup created under `.agent-backups/admin-work-list-projection-2026-05-06/`.

Validation progress 2026-05-06T14:15:00+09:00:
- RED: `PostgresPersistenceContractTests.AdminWorkList_UsesSinglePostgresCommand_AndStoredThumbnail` failed before implementation because admin Work list ignored stored `PublicThumbnailUrl` and returned the Asset-derived URL instead.
- GREEN: same focused test passed after changing `WorkQueryStore.GetAdminListAsync` to a direct DTO projection using `PublicThumbnailUrl` and one Postgres command.

- PASS: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~PostgresPersistenceContractTests" --no-restore` passed 10/10.
- PASS: `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter "FullyQualifiedName~PublicQueryHandlerComponentTests" --no-restore` passed 40/40.
- PASS: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~AdminEndpointsTests|FullyQualifiedName~AdminContentEndpointsTests" --no-restore` passed 33/33.
- PASS: `dotnet test backend/WoongBlog.sln --no-restore` passed backend tests: Component 122/122, Unit 56/56, Architecture 35/35, Integration 214/214, Contract 1 skipped due no pact files.
- PASS: `git diff --check` completed with no whitespace errors after trimming TODO EOF whitespace.
- PASS: diff guard over changed source/test/config files found no seed priority, `pageSize=1`, or cache-related production changes.
- PASS: generated audit artifacts under `backend/reports/admin-work-list-projection-2026-05-06/`; audit JSON parse check and `git diff --check` passed.
- CORRECTION: user correctly identified that the first projection test did not prove old resolver output equals stored `PublicThumbnailUrl`; it only proved stored URL wins.
- PASS: corrected the Postgres projection test to assert `WorkThumbnailUrlResolver.ResolveThumbnailUrl(...) == AdminWorkListItemDto.ThumbnailUrl` for explicit thumbnail assets.
- PASS: extended `CreateAndReadWork_PersistsUploadedThumbnailAndIcon` to assert the write-time stored `PublicThumbnailUrl` equals the resolver result for the uploaded thumbnail asset.
- PASS: focused equivalence tests passed 2/2 with `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~PostgresPersistenceContractTests.AdminWorkList_UsesSinglePostgresCommand_AndStoredThumbnail|FullyQualifiedName~AdminContentEndpointsTests.CreateAndReadWork_PersistsUploadedThumbnailAndIcon" --no-restore`.
- PASS: after equivalence correction, `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~PostgresPersistenceContractTests" --no-restore` passed 10/10.
- PASS: after equivalence correction, `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~AdminContentEndpointsTests" --no-restore` passed 25/25.
- PASS: after equivalence correction, `dotnet test backend/WoongBlog.sln --no-restore` passed backend tests: Component 122/122, Unit 56/56, Architecture 35/35, Integration 214/214, Contract 1 skipped due no pact files.

---

# TODO - 2026-05-06 - Public Work Thumbnail Equivalence Guard

## User Instructions
- Confirm the core risk: the thumbnail URL that the old resolver calculated must be guaranteed equivalent to the new stored/projection value `x.PublicThumbnailUrl` where that stored value replaces resolver work.
- Proceed test-first; do not assume the projection is safe just because the code compiles.
- Keep realistic load-test semantics unchanged: no seeded target priority, no `pageSize=1`, and no cache workaround.

## TODOs
- [x] Confirm current tracked state and create a dedicated dev-based branch.
- [x] Use find-skills guidance plus `tdd`, EF Core projection, and .NET performance guidance; no new skill install needed.
- [x] Create scoped backups before editing tests/TODO.
- [x] GUARD: add Postgres public Work list/detail contract coverage proving explicit thumbnail resolver output equals projected `PublicThumbnailUrl`.
- [x] GREEN: production projection behavior remained unchanged because the new guard passed; no production patch was needed.
- [x] Run focused Postgres contract tests and broader backend tests.
- [x] Generate persistent audit artifacts.
- [ ] Commit/push and promote through `dev`/`main` if code/test changes are accepted.

## Plan Alignment Check - Before Execution
- This slice is a safety guard around the stored public Work thumbnail read model.
- It does not reintroduce body-image or video thumbnail fallback on public hot paths; those fallbacks were intentionally removed/avoided for performance and are already covered by component tests.
- The equivalence guarantee here is for the supported explicit thumbnail asset path, where old resolver output and `PublicThumbnailUrl` are expected to match exactly.
- Backup created under `.agent-backups/public-work-thumbnail-equivalence-2026-05-06/`.

Validation progress 2026-05-06T15:05:00+09:00:
- PASS: focused resolver-equivalence guard filter passed 3/3: public Work detail, public Work first page list, and admin Work list all assert resolver output equals projected/stored thumbnail URL for explicit thumbnail assets.
- PASS: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~PostgresPersistenceContractTests" --no-restore` passed 10/10.
- PASS: `dotnet test backend/WoongBlog.sln --no-restore` passed backend tests: Component 122/122, Unit 56/56, Architecture 35/35, Integration 214/214, Contract 1 skipped due no pact files.
- PASS: `git diff --check` completed with no whitespace errors.
- NOTE: production code was intentionally not changed; the new guard confirmed the explicit thumbnail path already matches. Body-image and video fallback remain intentionally excluded from public hot paths.
- PASS: generated audit artifacts under `backend/reports/public-work-thumbnail-equivalence-2026-05-06/`; audit JSON parse check passed with `node`.

---

# TODO - 2026-05-06 - Public Work Detail Video Existence Query Removal

## User Instructions
- Continue backend structural performance improvements for heavy detail reads.
- Do not use cache as the primary fix.
- Keep realistic load-test semantics unchanged: no seeded target priority and no `pageSize=1` shortcut.
- Reduce EF query work and DB pressure so detail requests stay within the 1-2 roundtrip target.

## TODOs
- [x] Confirm current dev-based branch/worktree state.
- [x] Use find-skills guidance plus `tdd`, EF Core projection, and .NET performance guidance; no new skill install needed.
- [x] Create scoped backups before editing source/tests/TODO.
- [x] RED: add Postgres contract coverage proving no-video public Work detail does not reference the `WorkVideos` table just to decide whether videos exist.
- [x] GREEN: use the stored `VideosVersion` read-model column to decide whether to fetch public video DTOs.
- [x] Run focused Postgres contract tests, relevant component tests, full backend tests, and diff checks.
- [x] Run a constrained backend load smoke if local runtime is available.
- [x] Generate persistent audit artifacts.
- [ ] Commit, push to `dev`, verify CI, and then reassess the next performance slice.

## Plan Alignment Check - Before Execution
- This slice removes one avoidable no-video detail SQL dependency on `WorkVideos`; it does not change endpoint DTO shape.
- Work details with videos can still perform a second DB command to load video DTOs; the objective allows detail requests to stay within 1-2 DB roundtrips.
- The slice does not alter public target selection, seeded slug priority, `pageSize=12`, cache policy, or body/content model behavior.
- Backup created under `.agent-backups/public-work-detail-video-exists-2026-05-06/`.

---

# TODO - 2026-05-06 - Public Work Detail Video Existence Query Removal

## User Instructions
- Continue backend structural performance improvements, not load-test target tricks.
- Keep realistic workload semantics: no seed priority, no pageSize=1 shortcut, no cache workaround.
- Preserve thumbnail resolver equivalence guards while optimizing public detail hot paths.

## TODOs
- [x] Confirm branch/worktree and isolate this slice from `dev`.
- [x] Use TDD and EF Core guidance before implementation.
- [x] Backup relevant files before editing.
- [x] RED: add Postgres contract coverage that no-video public Work detail does not reference `WorkVideos` in the detail projection.
- [x] GREEN: use stored `VideosVersion` instead of `WorkVideos.Any(...)` in public detail projection.
- [x] Run focused Postgres contract tests.
- [x] Run relevant backend test suites and `git diff --check`.
- [x] Generate persistent audit report artifacts.
- [ ] Commit, push, open PR to `dev`, verify CI, merge, verify `dev` CI, and promote as appropriate.

## Validation Progress
- RED: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~PostgresPersistenceContractTests.PublicWorkDetailWithoutVideos_DoesNotReferenceWorkVideosInDetailProjection"` failed because generated SQL contained `FROM "WorkVideos"`.
- GREEN: same focused Postgres contract passed after public Work detail projection switched from `WorkVideos.Any(...)` to `VideosVersion > 0`.
- RED: component coverage caught that `VideosVersion > 0` alone would hide existing `WorkVideo` rows when legacy/direct data had `VideosVersion = 0`.
- RED: `Bootstrapper_BackfillsVideosVersion_ForExistingWorkVideos` failed because legacy Work rows with videos remained at version 0.
- GREEN implementation updated: added one-time Postgres backfill patch `20260506_work_video_version_backfill`, set seeded Work video version, and aligned the public HLS component fixture with current mutation-produced data.
- PASS: focused Postgres backfill + no-video SQL-shape tests passed 2/2.
- PASS: focused public video detail + WorkVideo component tests passed 27/27 after aligning the fixture to `VideosVersion = 1`.
- PASS: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~PostgresPersistenceContractTests" --no-restore` passed 12/12.
- PASS: `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --no-restore` passed 122/122.
- PASS: `dotnet test backend/WoongBlog.sln --no-restore` completed successfully; observed Component 122/122 and Integration 216/216 output.
- PASS: explicit Unit 56/56, Architecture 35/35, Contract 1 skipped due no Pact files.
- PASS: `git diff --check`.
- NOT RUN: local load smoke because `docker compose -f docker-compose.dev.yml ps` showed no running services.
- PASS: generated audit artifacts under `backend/reports/public-work-detail-video-exists-2026-05-06/` and validated JSON with Node JSON.parse.
- CORRECTION: `jq` was not installed, so JSON validation was rerun with Node JSON.parse and passed.

---

# TODO - 2026-05-06 - Backend Performance Opportunity Scan

## User Instructions
- Before changing more code, identify where backend performance can still be improved.
- For each improvement, explain what backend tests should prove or improve.
- Keep the scope focused on structural backend performance, not cache workarounds or load-test target tricks.

## TODOs
- [x] Scan public load-test hot paths: Work list/read and Blog list/read.
- [x] Scan supporting public paths that can affect runtime pressure: Home/site settings, page/admin projections, diagnostics.
- [x] Map each candidate improvement to a measurable backend test assertion.
- [x] Generate persistent audit artifacts without modifying production or test code.

## Plan Alignment Check
- This is audit-only. No production code or test code is changed in this slice.
- Output is a prioritized improvement map so the next implementation slice can be test-first and measurable.

---

# TODO - 2026-05-06 - Work Thumbnail Fallback Read Model

## User Instructions
- Improve backend performance structurally, not by cache or load-test target tricks.
- Compute thumbnail/representative image at content save/mutation time and store it in a separate column.
- Request-time public list/detail paths must not scan body JSON or video rows just to calculate thumbnails.
- Existing resolver semantics must be preserved: stored `PublicThumbnailUrl` should match what the old resolver would have returned.

## TODOs
- [x] Re-scan current branch state and use TDD / EF Core / .NET performance guidance.
- [x] Create scoped backups before editing.
- [x] RED: add create/update/video mutation tests proving fallback stored `PublicThumbnailUrl` equals resolver output.
- [x] GREEN: centralize write-time Work public thumbnail read-model refresh.
- [x] Add/bootstrap backfill coverage for existing fallback data.
- [x] Run focused integration/component tests and full backend tests.
- [x] Run load smoke if local compose runtime is available.
- [x] Generate implementation audit artifacts.
- [ ] Push PR to dev and verify CI when local validation is green.

## Plan Alignment Check - Before Execution
- This slice does not restore request-time resolver work in public queries.
- It moves fallback thumbnail work to write/mutation/bootstrap time so public DTO projection can continue reading `PublicThumbnailUrl` directly.
- It does not change load-test target selection, seeded priority, `pageSize=12`, or cache behavior.
- Backup created under `.agent-backups/work-thumbnail-fallback-equivalence-2026-05-06/`.

## Validation Progress
- RED: `AdminContentEndpointsTests.CreateWork_StoresBodyImageThumbnailFallback_ForPublicReadModel` failed before implementation because new Work rows stored `PublicThumbnailUrl = ""` for body-image fallback.
- GREEN: same create fallback test passed after create uses the shared write-time read-model refresh.
- RED: `AdminContentEndpointsTests.UpdateWork_RecomputesBodyImageThumbnailFallback_ForPublicReadModel` failed before implementation because update did not recompute body-image fallback.
- GREEN: same update fallback test passed after update refreshes the stored read model.
- RED: `WorkVideoEndpointsTests.AddYouTubeVideo_PersistsAndProjectsToAdminAndPublic` failed before implementation because adding a YouTube video left stored/public thumbnail empty.
- GREEN: same YouTube mutation test passed after video mutation refreshes the stored read model.
- RED: `PostgresPersistenceContractTests.Bootstrapper_BackfillsPublicThumbnailUrl_FromLegacyFallbacks` failed through SQL/fixture iterations until the bootstrapper patch correctly backfilled YouTube-only and no-video body-image legacy fallbacks while preserving local-video empty fallback semantics.
- GREEN: focused bootstrapper backfill test passed after `20260506_public_work_thumbnail_fallback_backfill`.
- RED: stricter resolver-equivalence tests failed for unresolved explicit thumbnail asset IDs: update and video mutation kept body-image fallback while the old resolver selected the YouTube video fallback.
- GREEN: unresolved-asset update/video tests passed after write paths load thumbnail asset URLs and only skip video fallback loading when the explicit asset resolves.
- PASS: focused new unresolved-asset tests passed 2/2.
- PASS: focused related integration filter passed 61/61: `AdminContentEndpointsTests`, `WorkVideoEndpointsTests`, and `PostgresPersistenceContractTests`.
- PASS: focused component filter passed 65/65: `PublicQueryHandlerComponentTests` and `WorkVideoComponentTests`.
- PASS: `dotnet test backend/WoongBlog.sln --logger "console;verbosity=minimal"` passed backend solution: Component 122/122, Unit 56/56, Architecture 35/35, Integration 222/222, Contract 1 skipped due missing pact files.
- PASS: `git diff --check`.
- PASS: diff guard found no added `pageSize=1`, seeded target priority, or cache workaround in this slice.
- NOT RUN: separate local compose load smoke for this slice; the structural change is write-time read-model correctness and was verified by backend tests. Real backend load rerun remains the next runtime validation step after CI/deployment.
- PASS: generated audit artifacts under `backend/reports/work-thumbnail-fallback-read-model-2026-05-06/` and validated the audit JSON with Node `JSON.parse`.
- Plan alignment after implementation: public query paths continue reading stored `PublicThumbnailUrl`; this slice does not alter real backend test target semantics, `pageSize=12`, seeded priority, or caching.

---

# TODO - 2026-05-06 - Public Home Shell Query

## User Instructions
- Continue structural backend performance improvements; do not only adjust load-test method or targets.
- Do not use cache as the primary fix.
- Keep realistic load-test semantics unchanged: no seeded target priority and no `pageSize=1` shortcut.
- Reduce DB connection/pool pressure by proving public requests fetch only the rows/columns needed for their DTOs.
- Continue each slice with backend tests, load smoke when available, audit artifacts, and dev/main promotion.

## TODOs
- [x] Confirm current `dev` state is current and create a dedicated branch.
- [x] Use `find-skills` guidance plus `tdd`, EF Core, and .NET performance guidance; no new skill install needed because local skills are sufficient.
- [x] Create scoped backups before editing source/tests/TODO.
- [x] RED: add Postgres contract coverage proving public Home currently uses too many DB commands for home page + site settings/resume + featured/recent summaries.
- [x] GREEN: add a public Home shell projection so home page and site settings/resume URL load in one DB command, then keep featured works and recent posts as DTO projections.
- [x] Run focused tests, broader backend tests, diff checks, and no-target-trick guards.
- [x] Run constrained backend load smoke if local compose runtime is available.
- [x] Generate persistent audit artifacts.
- [ ] Commit, push PR to `dev`, verify CI, merge, and continue main promotion checks.

## Plan Alignment Check - Before Execution
- This slice targets DB roundtrip/pool pressure on `/api/public/home`, a public path still called out by the performance scan as sequential DB work.
- It does not change Real Backend Test target selection, seeded priority, list `pageSize=12`, public Work/Blog detail DTO shape, or cache policy.
- Expected behavior remains the same: missing home page or site settings returns null; existing public Home DTO still contains `homePage`, `siteSettings`, `featuredWorks`, and `recentPosts`.
- Backup created under `.agent-backups/public-home-shell-query-2026-05-06/`.

Validation progress 2026-05-06T18:05:44+0900:
- RED: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~PostgresPersistenceContractTests.PublicHome_UsesThreePostgresCommands_ForShellAndSummaryProjections" --no-restore --logger "console;verbosity=minimal"` failed as expected: expected 3 DB commands, actual 5.
- GREEN: same focused test passed after `GetHomeQueryHandler` switched to `GetHomeShellAsync` and `HomeQueryStore` projected home page + site settings + resume asset in one query.
- PASS: focused public Home shell test passed after implementation, proving command count dropped to 3.
- PASS: `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter "FullyQualifiedName~PublicQueryHandlerComponentTests" --no-restore --logger "console;verbosity=minimal"` passed 40/40.
- PASS: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~PostgresPersistenceContractTests" --no-restore --logger "console;verbosity=minimal"` passed 14/14.
- PASS: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~PublicEndpointsTests|FullyQualifiedName~StartupCompositionTests" --no-restore --logger "console;verbosity=minimal"` passed 42/42.
- PASS: `dotnet test backend/WoongBlog.sln --no-restore --logger "console;verbosity=minimal"` passed backend solution: Component 122/122, Unit 56/56, Architecture 35/35, Integration 223/223, Contract 1 skipped due no Pact files.
- PASS: `git diff --check` completed with no whitespace errors.
- PASS: source/test diff guard found no `pageSize=1`, seeded target priority, or cache-related production/test changes in this slice.
- PASS: rebuilt backend under the existing 2 CPU / shared 8 GiB compose overlay and verified `http://127.0.0.1:3100/api/health` returned 200.
- PASS: k6 public Home smoke `RATE=100 DURATION_SECONDS=15 MAX_VUS=200 TARGET_PATH=/api/public/home` via Docker network completed 1,500 requests at 99.99 rps, failed rate 0, p95 25.44 ms, max 581.38 ms. Summary: `backend/reports/public-home-shell-query-2026-05-06/loadtest/k6-public-home-100rps-15s.json`.
- PASS: k6 summary JSON parsed successfully with Node and verified 1,500 requests and failed rate 0.
- PASS: generated audit artifacts under `backend/reports/public-home-shell-query-2026-05-06/`; audit JSON and k6 summary JSON parsed successfully with Node.

---

# TODO - 2026-05-06 - Backend Performance Next Opportunities Scan

## User Instructions
- Identify where backend performance improvements are still possible.
- Connect each possible improvement to the Real Backend Test/backend load-test metric it should improve.
- Keep the analysis focused on structural backend improvements, not cache masking, seeded target shortcuts, or `pageSize=1` test manipulation.
- Do not modify production/test code for this scan; generate persistent audit artifacts.

## TODOs
- [x] Confirm current worktree state and use .NET performance / EF Core guidance for the scan.
- [x] Back up the TODO file before writing audit-only notes.
- [x] Scan current public Work, Blog, Home query stores and DB diagnostics code for remaining hot-path issues.
- [x] Review existing load-test and audit artifacts to separate already-fixed paths from remaining bottlenecks.
- [x] Generate Markdown/HTML/JSON audit artifacts mapping opportunities to expected backend test metrics.
- [x] Validate audit artifacts and summarize next implementation order.

## Plan Alignment Check - Before Execution
- This is an audit-only scan. Production code and tests must not be modified.
- The scan treats realistic public workload semantics as fixed: Work/Blog list `pageSize=12`, current non-seed detail targets, and no cache as the primary fix.
- Backup created under `.agent-backups/backend-performance-next-opportunities-2026-05-06/`.

Validation progress 2026-05-06T18:50:00+0900:
- PASS: audited public Work/Blog/Home query stores, DB diagnostics, and Real Backend Test contracts without modifying production/test code.
- PASS: generated audit artifacts under `backend/reports/backend-performance-next-opportunities-2026-05-06/`.
- PASS: `node -e "JSON.parse(...)"` validated the machine-readable audit JSON.
- PASS: `git diff --check` completed with no whitespace errors.
- PASS: confirmed PR #83 was merged to `main` and main push CI run `25427564460` completed successfully.
- Plan alignment after scan: recommendations remain structural and measurement-led; no cache, seeded target priority, or `pageSize=1` shortcut was introduced.
- PASS: Publish GHCR Main run `25427830001` completed successfully for frontend and backend images; only Node.js 20 action deprecation annotations remain.

---

# TODO - 2026-05-06 - Real Backend Run Diagnostics Persistence

## User Instructions
- Continue backend structural performance improvements iteratively.
- Focus next on DB connection/pool pressure and nginx/app/DB timing observability before choosing further heavy-detail optimizations.
- Do not use cache, seeded target priority, or `pageSize=1` shortcuts.
- Run backend tests, load test when available, create audit artifacts, push to `dev`, verify CI, and continue promotion.

## TODOs
- [x] Create working branch and scoped backups.
- [x] Use local `tdd`, EF Core, and .NET performance guidance; attempted `find-skills` CLI search but the npm package was unavailable.
- [x] RED: add endpoint/frontend tests proving Real Backend metrics expose persisted run-scoped diagnostics.
- [x] GREEN: add diagnostics snapshot contracts, sampler, runner persistence, and frontend extraction.
- [x] Run focused backend/frontend tests and broader validation.
- [x] Run constrained load smoke if local runtime is available.
- [x] Generate persistent audit artifacts.
- [ ] Commit, push PR to `dev`, verify CI, merge, and verify main promotion/publish.

## Plan Alignment Check - Before Execution
- This slice is observability/attribution work for real backend runs, not target manipulation.
- It should make DB command/open latency and runtime samples available from the completed run metrics payload instead of relying only on volatile live dashboard samples.
- Backup created under `.agent-backups/real-load-run-diagnostics-2026-05-06/`.

## Validation Progress
- RED: focused backend integration test failed before implementation because `metricsPayload.Diagnostics` was absent from Real Backend metrics.
- RED: focused frontend unit test failed before implementation because `summarizeRealBackendRunSnapshot` did not expose persisted diagnostics.
- GREEN: added `LoadTestDiagnosticsSampler`, persisted diagnostics snapshots on fake/k6 metric points, surfaced `metrics.diagnostics`, and taught the dashboard model to use persisted diagnostics when live samples are absent.
- PASS: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~StartupCompositionTests.RealLoadTestControlPlane_StartStatusMetricsAndStop_HappyPath_WhenRealRunnerDisabled_ForcesFakeRunner" --no-restore --logger "console;verbosity=minimal"` passed 1/1 after relaxing the test to accept multiple run samples.
- PASS: `npm test -- --run src/test/load-test-dashboard.test.ts` passed 28/28.
- PASS: diagnostics/health focused integration filter passed 2/2.
- PASS: `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter "FullyQualifiedName~RealLoadTestRunnerComponentTests" --no-restore --logger "console;verbosity=minimal"` passed 1/1.
- PASS: `npm run typecheck`.
- PASS: `dotnet test backend/WoongBlog.sln --no-restore --logger "console;verbosity=minimal"` passed backend solution: Contract 1 skipped, Component 122/122, Unit 56/56, Architecture 35/35, Integration 223/223; existing AWSSDK.Core NU1901 warnings remain.
- PASS: `npm run lint` completed with 0 errors and 6 pre-existing warnings.
- PASS: `BACKEND_PUBLISH_PORT=18080 ./scripts/dev-up.sh` rebuilt and restarted backend/frontend/db/nginx.
- PASS: `curl -fsS http://127.0.0.1:3000/api/health` and `curl -fsS http://127.0.0.1:18080/api/health` returned 200 JSON health.
- PASS: backend runtime environment confirmed `LoadTesting__BaseUrl=http://nginx` and Npgsql `Maximum Pool Size=40;Minimum Pool Size=0`.
- PASS: `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 ENABLE_LOCAL_ADMIN_SHORTCUT=true PLAYWRIGHT_EXPECT_LOCAL_ADMIN_SHORTCUT=visible npx playwright test tests/admin-load-test-dashboard.spec.ts --project=chromium-authenticated --workers=1` passed 2/2.
- PASS: runtime Real Backend k6 smoke `public-api-rps`, 5rps, 5s, maxVUs 20 completed with 26 requests, p95 8.4ms, `diagnosticsSamples=1`, `metricPointsWithDiagnostics=1`, DB status `available`, and DB command sample count 192. Artifacts: `backend/reports/real-load-run-diagnostics-2026-05-06/loadtest/runtime-start.json`, `runtime-status.json`, `runtime-metrics.json`.
- PASS: generated audit artifacts under `backend/reports/real-load-run-diagnostics-2026-05-06/`; audit JSON parsed successfully.
- Plan alignment after validation: this slice does not alter Real Backend target selection, seed priority, list `pageSize=12`, or cache policy; it only makes completed-run diagnostics available for future bottleneck attribution.

---

# TODO - 2026-05-06 - Public JSON Source Generation

## User Instructions
- Continue iterative backend performance improvements for heavy public detail read paths.
- Do not weaken test realism: keep real public targets, keep list `pageSize=12`, do not prioritize seeded targets, and do not add cache as the primary fix.
- Reduce JSON serialization allocation for large public DTOs where possible.
- Run backend tests and real backend load testing before promoting to `dev`.

## Scope
- Add source-generated System.Text.Json metadata for public hot-path response DTOs: public work/blog detail, public work/blog list, public home, page/site settings, and real backend load-test responses.
- Wire the context into Minimal API `HttpJsonOptions` and MVC JSON options without removing the default reflection resolver fallback.
- Preserve existing public response JSON shape.

## Non-Goals
- No cache.
- No seed target priority.
- No `pageSize=1` load target change.
- No API contract field removal in this slice.
- No DB schema change in this slice.

## TODOs
- [x] Confirm prior detail read improvements already exist on current `dev`.
- [x] Create a backup before editing code/test/TODO files.
- [x] RED: add focused startup composition test proving public hot-path DTO JSON metadata is expected but not yet configured.
- [x] GREEN: add and register `WoongBlogApiJsonSerializerContext` for public hot-path DTOs.
- [x] Verify public endpoint JSON shape remains stable.
- [x] Run focused backend tests.
- [x] Run full backend tests if focused tests pass.
- [x] Run typecheck/lint if frontend/public API contract surface is touched.
- [x] Run real backend load smoke with `pageSize=12` and real public target semantics.
- [x] Generate persistent audit artifacts.
- [ ] Commit, push branch, open PR to `dev`, verify CI, and merge if green.

## Plan Alignment Check - Before Execution
- Current `dev` already contains public detail body split (`PublicContentHtml`/`PublicContentMarkdown`), public Work thumbnail read model (`PublicThumbnailUrl`), public Blog cover read model (`PublicCoverUrl`), Work detail video query reduction, and run-scoped load-test diagnostics.
- The remaining incremental improvement addressed here is JSON serialization CPU/allocation on public response DTOs.
- This slice preserves existing public response shape and uses source-generated metadata as an implementation detail behind ASP.NET Core JSON options.

Validation progress 2026-05-06T19:55:00+0900:
- RED: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~StartupCompositionTests.JsonOptions_UseSourceGeneratedMetadataForPublicHotPathDtos" --no-restore --logger "console;verbosity=minimal"` failed before implementation because JSON options did not contain `WoongBlogApiJsonSerializerContext`; the resolver chain had OpenAPI and default reflection resolvers only.
- GREEN: same focused startup composition test passed after adding `WoongBlogApiJsonSerializerContext` and registering it in Minimal API `HttpJsonOptions` and MVC JSON options.
- PASS: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~PublicEndpointsTests" --no-restore --logger "console;verbosity=minimal"` passed 29/29, confirming public response shape remains stable and `contentJson` stays absent from public detail responses.
- PASS: `dotnet test backend/WoongBlog.sln --no-restore --logger "console;verbosity=minimal"` passed: Contract 1 skipped, Component 122, Unit 56, Architecture 35, Integration 224. Existing AWSSDK.Core NU1901 warnings remain.
- PASS: `npm run typecheck`.
- PASS: `npm run lint` with 0 errors and 6 pre-existing warnings.
- PASS: `git diff --check`.
- PASS: `BACKEND_PUBLISH_PORT=18080 ./scripts/dev-up.sh` rebuilt backend/frontend and restarted compose.
- PASS: health checks succeeded through nginx `http://127.0.0.1:3000/api/health` and backend direct `http://127.0.0.1:18080/api/health`.
- PASS: Real Backend k6 smoke `public-api-rps`, 10rps, 10s, maxVUs 20, current public list-selected Work/Blog read targets and `pageSize=12`: run `20260506-105311-public-api-rps-3b25399d`, 101 requests, failed 0, average 10.1rps, p95 9.2ms, diagnostics samples 1.
- PASS: Real Backend k6 load check `public-api-rps`, 100rps, 30s, maxVUs 100, same current public target semantics and `pageSize=12`: run `20260506-105431-public-api-rps-264bc24a`, 3,000 requests, failed 0, average 100rps, p95 7.1ms, p99 8.3ms, DB command p95 5.5ms, DB connection open p95 0ms.
- Plan alignment after validation: no cache, no seeded target priority, no `pageSize=1`, no public API field removal, and no DB schema change. This slice only reduces public hot-path JSON metadata reflection/serialization overhead.

---

# TODO - 2026-05-06 - Public Hot Path Query Indexes And Compiled Reads

## User Instructions
- Continue structural backend performance improvements, not load-test target shortcuts.
- Keep public Work/Blog list `pageSize=12` semantics realistic.
- Do not prioritize seeded targets and do not use cache as the fix.
- Preserve public DTO response shape while improving backend Real Backend Test behavior.
- Connect expected improvements to Real Backend Test metrics.

## Scope
- Add Postgres covering indexes for public Work/Blog first-page list projections so `/api/public/works?page=1&pageSize=12` and `/api/public/blogs?page=1&pageSize=12` can avoid extra heap reads where Postgres chooses index-only scans.
- Use EF compiled queries for repeated public Work/Blog detail reads to reduce hot-path query translation/metadata CPU and allocation.
- Keep existing DB roundtrip contracts: Blog detail one DB command; Work detail without videos one DB command; Work detail with videos at most two commands.

## Non-Goals
- No cache.
- No seeded load target priority.
- No `pageSize=1` target or test change.
- No removal of public content fields in this slice.
- No frontend behavior change.

## TODOs
- [x] Scan current query stores, DTOs, frontend public detail usage, and Postgres persistence contracts.
- [x] Create scoped backup before editing source/tests/TODO.
- [x] RED: add schema contract coverage requiring public list covering indexes.
- [x] GREEN: add idempotent Postgres covering indexes to bootstrapper.
- [x] GREEN: convert public Blog/Work detail and Work videos read projections to compiled queries while preserving command counts.
- [x] Run focused Postgres persistence tests for index/command contracts.
- [x] Run public endpoint/component tests covering public list/detail behavior.
- [x] Run backend solution tests if focused validation is green.
- [x] Run real backend load smoke with `pageSize=12` and real public target semantics if local compose remains available.
- [x] Generate persistent audit artifacts.
- [ ] Commit, push PR to `dev`, verify CI, merge, and verify main promotion/publish.

## Plan Alignment Check - Before Execution
- The plan changes backend query/index structure, not test realism.
- The Real Backend Test targets remain real public list/read endpoints with `pageSize=12`.
- Expected metric movement: lower DB command p95 for public list under larger data, lower ASP.NET app elapsed/GC allocation for repeated public detail reads, and improved throughput headroom before ThreadPool/DB connection pressure appears.
- Backup created under `.agent-backups/public-hotpath-query-index-2026-05-06/`.

Validation progress 2026-05-06T20:45:00+0900:
- RED: focused `PostgresPersistenceContractTests.Bootstrapper_AppliesPostgresSpecificSearchSchema` failed before implementation because `IX_Blogs_PublicList_Covering` and `IX_Works_PublicList_Covering` did not exist.
- GREEN: same focused schema contract passed after adding schema patch `20260506_public_list_covering_indexes` with covering indexes for public Work/Blog list projections.
- GREEN: converted public Blog detail, public Work detail, and public Work video read projections to EF compiled queries cached per EF `IModel` to avoid cross-model compiled-query failures in test/runtime variants.
- PASS: focused Postgres detail/list command contracts passed: public Blog detail one command, public Work detail without videos one command, no WorkVideos reference when `VideosVersion == 0`, public first-page Work/Blog list one command.
- PASS: `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter "FullyQualifiedName~PublicQueryHandlerComponentTests" --no-restore --logger "console;verbosity=minimal"` passed 40/40.
- PASS: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~PublicEndpointsTests" --no-restore --logger "console;verbosity=minimal"` passed 29/29.
- PASS: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~PostgresPersistenceContractTests" --no-restore --logger "console;verbosity=minimal"` passed 14/14.
- PASS: `dotnet test backend/WoongBlog.sln --no-restore --logger "console;verbosity=minimal"` passed backend solution: Contract 1 skipped, Component 122/122, Unit 56/56, Architecture 35/35, Integration 224/224. Existing AWSSDK.Core NU1901 warnings remain.
- PASS: `git diff --check` completed with no whitespace errors.
- PASS: diff guard found no `pageSize=1`, seeded target priority, or cache-related production/test change in this slice.
- PASS: `BACKEND_PUBLISH_PORT=18080 ./scripts/dev-up.sh` rebuilt backend/frontend images and restarted the dev compose stack.
- PASS: nginx `http://127.0.0.1:3000/api/health` and backend direct `http://127.0.0.1:18080/api/health` returned 200 JSON health.
- PASS: runtime backend confirmed `LoadTesting__BaseUrl=http://nginx`.
- PASS: authenticated Real Backend Test through local admin shortcut with current public list-selected targets; target discovery retained Work/Blog list `pageSize=12` and current real public slugs.
- PASS: Real Backend 100rps/30s/maxVUs 100 completed run `20260506-114209-public-api-rps-e40172bb`: 3,001 requests, failed 0, average 100rps, p95 5.4ms, p99 6.5ms, DB command p95 3.9ms, DB connection open p95 0ms.
- PASS: Real Backend 300rps/30s/maxVUs 300 completed run `20260506-114309-public-api-rps-70eb5ce2`: 9,000 requests, failed 0, average 300rps, p95 5.2ms, p99 10.5ms, DB command p95 3.5ms, DB connection open p95 0ms.
- Plan alignment after runtime validation: no cache, no seeded target priority, no `pageSize=1`, no public DTO field removal, and no frontend behavior change. This slice changes public hot-path query/index internals only.
- PASS: generated audit artifacts under `backend/reports/public-hotpath-query-index-2026-05-06/`; audit JSON and load summary JSON files parsed successfully.

---

# TODO - 2026-05-06 - Heavy Detail Payload Attribution

## User Instructions
- Continue backend performance work by identifying where further improvement is possible and what each improvement should change in Real Backend Test results.
- Do not change load-test realism: keep real public Work/Blog targets, keep list `pageSize=12`, do not prioritize seeded content, and do not use cache as the fix.
- Focus on backend structure and evidence, not hiding heavy content behind easier targets.
- Preserve existing public DTO response shape unless a split is explicitly designed and tested.

## Scope
- Add target-level payload/transfer attribution to the Real Backend Test runner and dashboard so heavy detail reads can be separated into DB/app elapsed versus response size/receive time.
- Preserve existing load scenarios, thresholds, target selection, and public endpoint behavior.
- Use the new metrics to decide the next structural backend optimization slice.

## Non-Goals
- No cache.
- No seeded target priority.
- No `pageSize=1`.
- No public DTO field removal in this slice.
- No DB schema or query behavior change in this slice.

## TODOs
- [x] Confirm current branch, dirty state, and previous performance improvements.
- [x] Read relevant TDD/performance skill guidance and avoid new skill/plugin installation.
- [x] Create scoped backup before editing source/tests/TODO.
- [x] RED: add backend component coverage that k6 records target response bytes and receive timing.
- [x] RED: add frontend parser coverage for target payload/receive metrics.
- [x] GREEN: extend Real Backend Test contracts, fake runner, k6 summary parser, and dashboard display.
- [x] Run focused backend and frontend tests.
- [x] Run `git diff --check` and realism guards for no seed/pageSize/cache shortcuts.
- [x] Run real backend load smoke if local compose remains available.
- [x] Generate persistent audit artifacts.
- [ ] Commit, push PR to `dev`, verify CI, merge, and promote to `main` if green.

## Plan Alignment Check - Before Execution
- Current `dev` already includes public detail body split, write-time media read models, Work video query reduction, source-generated public JSON metadata, public list covering indexes, and compiled public detail reads.
- Existing Real Backend Test reports DB command/open timing and ASP.NET elapsed timing, but target rows do not show response bytes or receive time.
- Expected metric impact of this slice: not lower latency directly; it improves attribution so the next performance slice can target large-detail payload/serialization or query work with evidence.
- Backup created under `.agent-backups/heavy-detail-payload-attribution-2026-05-06/`.

Validation progress 2026-05-06T21:35:00+0900:
- RED: `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter "FullyQualifiedName~RealLoadTestRunnerComponentTests" --no-restore --logger "console;verbosity=minimal"` failed before implementation because the k6 script did not contain `response_bytes`.
- RED: `npm test -- --run src/test/load-test-dashboard.test.ts` failed before implementation because `summarizeRealBackendRunSnapshot` dropped `responseBytesP95` and `receiveP95Ms`.
- GREEN: focused backend component test passed 2/2 after adding target-level k6 response byte and receive timing trends.
- GREEN: focused frontend dashboard parser test passed 28/28 after preserving the new target metrics.
- PASS: focused Real Backend control-plane integration test passed 1/1 with fake runner contract serialization.
- PASS: `npm run typecheck`.
- PASS: `dotnet test backend/WoongBlog.sln --no-restore --logger "console;verbosity=minimal"` passed backend solution: Contract 1 skipped, Component 123, Unit 56, Architecture 35, Integration 224. Existing AWSSDK.Core NU1901 warnings remain.
- PASS: `npm run lint` completed with 0 errors and the existing 6 warnings.
- PASS: `git diff --check` and diff guard found no `pageSize=1`, seed-priority, or cache shortcut changes outside TODO/report artifacts.
- PASS: `BACKEND_PUBLISH_PORT=18080 ./scripts/dev-up.sh` rebuilt and restarted local compose with the updated backend/frontend images.
- PASS: nginx `http://127.0.0.1:3000/api/health` and backend direct `http://127.0.0.1:18080/api/health` returned 200; backend runtime confirmed `LoadTesting__BaseUrl=http://nginx`.
- PASS: live Real Backend k6 smoke `20260506-123052-public-api-rps-7e5cdcea`, 5rps/5s/maxVUs 20, current public list-selected Work/Blog read targets and list `pageSize=12`: 26 requests, 0 failed, p95 19.2ms, p99 56.1ms. Each target reported `responseBytesP95` and `receiveP95Ms`.
- PASS: `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 ENABLE_LOCAL_ADMIN_SHORTCUT=true PLAYWRIGHT_EXPECT_LOCAL_ADMIN_SHORTCUT=visible npx playwright test tests/admin-load-test-dashboard.spec.ts --project=chromium-authenticated --workers=1 --grep "real backend"` passed 1/1.
- PASS: generated audit artifacts under `backend/reports/heavy-detail-payload-attribution-2026-05-06/`; audit JSON and runtime smoke summary JSON parsed successfully.
- Plan alignment after validation: this slice did not attempt to lower latency directly; it adds target-level payload/receive attribution so the next backend structural improvement can be chosen from evidence.

---

# TODO - 2026-05-06 - K6 Run-Scoped Diagnostics Sampling

## User Instructions
- Continue backend performance improvement loops without cache, seeded target priority, or `pageSize=12` reduction.
- Improve DB connection/pool pressure visibility during spike runs.
- Complete nginx/app/DB timing attribution so the next heavy detail read fix is evidence-driven.
- Run TDD, backend tests, load-test validation, push to `dev`, verify CI, and continue iterating.

## Skill Check
- Used `find-skills` before implementation.
- `npx -y skills find "dotnet performance"` found the already-installed `dotnet/skills@analyzing-dotnet-performance`; no new install needed.
- `npx -y skills find "load testing observability"` found low-install observability/load-testing skills; no install selected because the existing local `tdd`, `analyzing-dotnet-performance`, and `ef-core` skills cover this slice better.

## TODOs
- [x] Confirm prior slices already cover public thumbnail read model, public/admin body split, DB diagnostics persistence, async EF command timing, source-generated JSON metadata, and payload/receive attribution.
- [x] Choose the next non-duplicate gap: k6 run-scoped diagnostics should preserve runtime/DB pressure during the run, not only at completion.
- [x] Create backups before editing source/test/TODO files.
- [x] Add RED component coverage proving k6 runner persists more than the final diagnostics sample while the process is still running.
- [x] Implement periodic diagnostics sampling for k6-backed Real Backend runs.
- [x] Run focused RED/GREEN component tests.
- [x] Run relevant backend/frontend checks and a small Real Backend smoke if local compose is available.
- [x] Generate persistent audit artifacts for this slice.
- [ ] Commit, push, PR to `dev`, verify CI, merge, verify promotion path, then reassess the next heavy-detail improvement.

## Plan Alignment Check - Before Execution
- This slice does not change public endpoint response shape, Real Backend target selection, list `pageSize=12`, DB schema, or cache policy.
- The expected Real Backend Test improvement is observability: DB command/open latency, connection count, ThreadPool, memory, and GC peaks during spike should remain visible in completed run metrics.
- It is not expected to directly lower p95; it prevents choosing the wrong next backend optimization.

Progress 2026-05-06T22:00+09:00:
- Confirmed PR #91 main promotion merged and follow-up main push CI completed successfully.
- Confirmed nginx config already emits timing headers; remaining gap is run-scoped diagnostics persistence during k6 execution.

Validation progress 2026-05-06T22:05+09:00:
- RED: `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter "FullyQualifiedName~RealLoadTestRunnerComponentTests.K6Runner_PersistsRunScopedDiagnosticsWhileProcessIsRunning" --no-restore --logger "console;verbosity=minimal"` failed because only the final diagnostics sample was persisted.
- GREEN: same focused component test passed after k6 runner started persisting run-scoped diagnostics while the fake k6 process was still running.
- PASS: `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter "FullyQualifiedName~RealLoadTestRunnerComponentTests" --no-restore --logger "console;verbosity=minimal"` passed 3/3.
- PASS: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~RealLoadTestControlPlane_StartStatusMetricsAndStop_HappyPath_WhenRealRunnerDisabled_ForcesFakeRunner" --no-restore --logger "console;verbosity=minimal"` passed 1/1.
- PASS: `npm test -- --run src/test/load-test-dashboard.test.ts` passed 28/28.
- PASS: `dotnet test backend/WoongBlog.sln --no-restore --logger "console;verbosity=minimal"` passed: Contract 1 skipped, Component 124, Unit 56, Architecture 35, Integration 224. Existing AWSSDK.Core NU1901 warning remains.
- PASS: `npm run typecheck`.
- PASS: `npm run lint` completed with 0 errors and 6 existing warnings.
- PASS: `git diff --check`.

Validation progress 2026-05-06T22:10+09:00:
- PASS: `BACKEND_PUBLISH_PORT=18080 ./scripts/dev-up.sh` rebuilt and restarted local compose with updated backend/frontend images.
- PASS: nginx `http://127.0.0.1:3000/api/health` and backend direct `http://127.0.0.1:18080/api/health` returned 200.
- PASS: backend container confirmed `LoadTesting__BaseUrl=http://nginx` and Postgres maximum pool size 40 in dev compose.
- PASS: live Real Backend k6 smoke `20260506-130939-public-api-rps-c8aaa852`, 10rps/8s/maxVUs 20, no seeded slug, list targets kept at `pageSize=12`: completed with 81 requests, 0 failures, p95 9.8ms, p99 64.1ms.
- PASS: completed smoke metrics contained 10 diagnostics entries and 10 metrics with diagnostics, proving run-scoped samples survive into completed results.
- PASS: generated audit artifacts under `backend/reports/k6-run-scoped-diagnostics-2026-05-06/`; audit JSON and runtime metrics JSON parsed successfully.
- PASS: post-refactor rerun of `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter "FullyQualifiedName~RealLoadTestRunnerComponentTests" --no-restore --logger "console;verbosity=minimal"` passed 3/3 after reusing a single k6 process exit task in the diagnostics loop.
- Plan alignment after validation: this slice improves spike/run attribution only; it intentionally does not alter target selection, response DTOs, cache policy, query shape, or k6 load profile semantics.

---

# TODO - 2026-05-06 - DbContext Pooling For Public Hot Paths

## User Instructions
- Continue backend performance improvement loops without cache, seeded target priority, or `pageSize=12` reduction.
- Focus on remaining backend structural improvements for heavy detail reads, DB connection/pool pressure, and JSON/EF allocation reduction.
- Run TDD, backend tests, load-test validation, push to `dev`, verify CI, and continue iterating.

## Skill Check
- Used existing local `tdd`, `ef-core`, and `analyzing-dotnet-performance` guidance for this slice.
- Prior skill discovery already found EF/performance skills; no new skill install is required for this bounded change.

## TODOs
- [x] Confirm current code already avoids public detail request-time `ContentJson` parsing for Work/Blog detail.
- [x] Choose next non-duplicate improvement: reduce high-RPS EF/DbContext allocation overhead with `DbContext` pooling.
- [x] Create backups before editing source/test/TODO files.
- [x] Add RED integration coverage proving app DI reuses a reset `WoongBlogDbContext` across scopes.
- [x] Implement bounded `AddDbContextPool` registration for persistence.
- [x] Run focused RED/GREEN integration test.
- [x] Run relevant backend solution checks and guards.
- [x] Run a small Real Backend smoke keeping live targets and list `pageSize=12`.
- [x] Generate persistent audit artifacts for this slice.
- [ ] Commit, push, PR to `dev`, verify CI, merge, verify promotion path, then reassess the next heavy-detail improvement.

## Plan Alignment Check - Before Execution
- This slice does not change public API response shape, target selection, `pageSize=12`, cache policy, query projection, or DB schema.
- Expected Real Backend Test impact is modest but structural: lower per-request EF allocation/service setup, potentially lower GC pressure and app elapsed p95 under high RPS.
- It does not solve connection pool sizing by itself; deployed `POSTGRES_MAX_POOL_SIZE` must still be verified separately.

Validation progress 2026-05-06T23:05+09:00:
- RED: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~PersistenceRegistration_PoolsDbContextInstancesAndResetsStateAcrossScopes" --no-restore --logger "console;verbosity=minimal"` failed before implementation because `AddDbContext` returned a different `WoongBlogDbContext` instance across scopes.
- GREEN: same focused test passed after switching persistence registration to bounded `AddDbContextPool`.
- PASS: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~StartupCompositionTests" --no-restore --logger "console;verbosity=minimal"` passed 15/15.
- PASS: `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter "FullyQualifiedName~DbContextModelContractComponentTests" --no-restore --logger "console;verbosity=minimal"` passed 4/4.
- PASS: `dotnet test backend/WoongBlog.sln --no-restore --logger "console;verbosity=minimal"` passed backend solution: Contract 1 skipped, Component 124, Unit 56, Architecture 35, Integration 225. Existing AWSSDK.Core NU1901 warning remains.
- PASS: `git diff --check` passed.
- PASS: changed source/test diff guard found no added `pageSize=1`, seeded target priority, or cache workaround in this slice.

Validation progress 2026-05-06T23:09+09:00:
- PASS: `BACKEND_PUBLISH_PORT=18080 ./scripts/dev-up.sh` rebuilt/recreated the dev stack with the updated backend image.
- PASS: `curl http://127.0.0.1:3000/api/health` returned 200 through nginx; `curl http://127.0.0.1:18080/api/health` returned 200 directly to backend.
- PASS: running backend env kept `LoadTesting__BaseUrl=http://nginx` and `ConnectionStrings__Postgres` with `Maximum Pool Size=40;Minimum Pool Size=0`; `Persistence__DbContextPoolSize` is not set, so the new default pool size 128 applies.
- PASS: selected Work/Study read targets from current public lists at `page=1&pageSize=12`; no seed priority or `pageSize=1` override was used.
- PASS: Real Backend smoke 10rps/10s completed: 101 requests, 0 failed, p95 6.3ms, p99 24.6ms, 12 diagnostics samples.
- PASS: Real Backend 100rps/30s completed: 3001 requests, 0 failed, 100.0rps, p95 5.0ms, p99 5.9ms, app elapsed p95 4.5ms, DB command p95 3.7ms, 32 diagnostics samples.
- Caveat: local dev compose reported 8 processors and is not a 2CPU/8GiB capacity proof. This validation is a regression/per-request-cost smoke for the pooling slice, not a final 1000rps capacity claim.

---

# TODO - 2026-05-06 - Public Detail Roundtrip Guard

## User Instructions
- Continue backend performance improvement loops without cache, seeded target priority, or `pageSize=12` reduction.
- Verify the backend structure, not only the load-test method: public reads should fetch only needed DB data, avoid request-time original body JSON parsing, and keep detail DB roundtrips bounded.
- Specifically keep proving that stored representative media fields are equivalent to the old resolver behavior where applicable.
- Run TDD/verification, produce audit artifacts, and use the result to choose the next performance improvement.

## Skill Check
- Used `find-skills` before implementation. `npx -y skills find "ASP.NET Core EF Core PostgreSQL query roundtrip count performance"` found EF/Core performance skills; no new install selected because local `tdd`, `ef-core`, and `analyzing-dotnet-performance` already cover this bounded slice.
- Using `tdd` for behavior-first contract coverage and `ef-core`/`.NET performance` guidance for projection/roundtrip checks.

## TODOs
- [x] Confirm current code already has public/admin content split, stored `PublicThumbnailUrl`/`PublicCoverUrl`, source-generated public DTO JSON metadata, and run-scoped load diagnostics.
- [x] Choose the next non-duplicate gap: Work public detail with videos needs an explicit Postgres contract for <= 2 DB commands and no original body/admin JSON column selection.
- [x] Create backups before editing test/TODO files.
- [x] Add a behavior-focused Postgres contract test for public Work detail with videos.
- [x] Run focused RED/GREEN validation for the new contract.
- [x] Run relevant backend test subset and diff guards.
- [x] Generate persistent audit artifacts for this slice.
- [x] Decide the next backend performance target from evidence after this guard is in place.

## Plan Alignment Check - Before Execution
- This slice does not change Real Backend target selection, cache policy, `pageSize=12`, public DTO shape, or production load parameters.
- Expected Real Backend Test improvement is indirect: it prevents regressions that would reintroduce extra DB roundtrips or original body/admin JSON reads on heavy Work detail pages.
- If the new contract is already green, that means the structural optimization exists and this slice adds regression protection rather than claiming a fresh p95 reduction.

Validation progress 2026-05-06T23:45+09:00:
- RED: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~PublicWorkDetailWithVideos_UsesTwoPostgresCommands_AndStoredPublicColumnsOnly" --no-restore --logger "console;verbosity=minimal"` initially failed because the SaveChanges synchronizer recomputed `PublicContentHtml` from `ContentJson`; the test setup did not yet isolate read-path stored-column behavior.
- GREEN: same focused test passed after the fixture updated stored public columns post-save with raw SQL, proving the read-path consumes stored public fields and not request-time original JSON parsing.
- PASS: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~PostgresPersistenceContractTests" --no-restore --logger "console;verbosity=minimal"` passed 15/15.
- PASS: `dotnet test backend/WoongBlog.sln --no-restore --logger "console;verbosity=minimal"` passed backend solution: Contract 1 skipped, Component 124, Unit 56, Architecture 35, Integration 226. Existing AWSSDK.Core NU1901 warning remains.
- PASS: `git diff --check`.
- PASS: JSON audit syntax validated with `node -e "JSON.parse(...)"` because `jq` is not installed in this environment.
- PASS: generated audit artifacts under `backend/reports/public-detail-roundtrip-guard-2026-05-06/`.
- Plan alignment after validation: this slice is a regression guard for the existing public detail read optimization; it intentionally does not claim a new runtime p95 reduction because production code did not change.
