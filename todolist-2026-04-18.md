# TODO List - 2026-04-18

## User Instructions Captured

- Always leave a `todolist-YYYY-MM-DD.md` file once a work plan is set.
- Always confirm that the execution matches the work plan before and after TODO progress.
- Record every TODO carefully, including user instructions.
- Match user instructions to TODO items and complete all TODOs without omissions.
- Verify each TODO immediately after it is completed.
- Actively use the TDD skill for development work.
- Add tests for developed features that lack coverage, and remove tests for removed features.
- Ensure every newly written function or method has test coverage.
- Use Playwright for frontend validation.
- Prepare a backup before code changes.
- Use subagents only when the user triggers them.
- For specialist knowledge, use the `find-skills` skill; search, register/install, and actively use suitable skills, preferring popular/high-star options when possible.
- Even when a needed skill is missing, use `find-skills` to search for a related skill, install a suitable one, and use it during development when feasible.
- On dev-related branches, run Docker on `http://127.0.0.1` and run Playwright tests.
- Add these instructions to `AGENTS.md` in harmony with the existing content.

## Plan And TODO Mapping

| ID | TODO | User instruction coverage | Status | Verification |
| --- | --- | --- | --- | --- |
| T1 | Re-check current repository state and `AGENTS.md` contents before editing. | Plan/execution alignment before TODO progress. | Completed | `git status --short` and `sed -n '1,240p' AGENTS.md` were run before edits. |
| T2 | Back up `AGENTS.md` before modifying it. | Prepare a backup before code changes. | Completed | Created `AGENTS.md.bak-2026-04-18`. |
| T3 | Create this dated TODO file with user instructions and instruction-to-TODO mapping. | Always leave dated TODO file; record all TODOs and user instructions; map instructions to TODOs. | Completed | `todolist-2026-04-18.md` records user instructions, TODOs, mappings, and alignment checks. |
| T4 | Update `AGENTS.md` with workflow, backup, TODO, TDD, testing, Playwright, subagent, skill, and dev-branch Docker requirements. | Add all requested operating rules to `AGENTS.md`. | Completed | `diff -u AGENTS.md.bak-2026-04-18 AGENTS.md` shows the requested workflow, testing, skill, and dev-branch additions. |
| T5 | Verify post-edit plan/execution alignment and TODO completion. | Always confirm execution matches the work plan after TODO progress; verify each completed TODO. | Completed | Post-edit review confirmed the changes match the proposed plan and captured user instructions. |

## Pre-Progress Alignment Check

- Planned work: documentation-only update to root `AGENTS.md`, plus required dated TODO file and backup.
- Execution so far: repository state checked, `AGENTS.md` read, date confirmed as 2026-04-18, and backup created before documentation edits.
- Alignment: current execution matches the plan.

## Post-Progress Alignment Check

- Planned work completed: root `AGENTS.md` was updated, backup was created, and this dated TODO file records instructions, TODO mappings, and verification.
- Verification completed: file existence was checked with `ls -l`, current content was reviewed with `sed`, and `diff -u AGENTS.md.bak-2026-04-18 AGENTS.md` confirmed the intended documentation additions.
- Alignment: final execution matches the plan.

## Remote Push TODO - 2026-04-18

### User Instructions Captured

- Push the current contents to remote `dev`.
- Continue preserving the dated TODO record and plan/execution alignment checks.

### Plan And TODO Mapping

| ID | TODO | User instruction coverage | Status | Verification |
| --- | --- | --- | --- | --- |
| P1 | Confirm current branch, remote, and worktree state before pushing. | Push to remote `dev`; check plan/execution alignment before progress. | Completed | Current branch is `dev`, remote is `origin`, and `dev` tracks `origin/dev`. |
| P2 | Confirm whether `AGENTS.md` is tracked or ignored so it can be included correctly. | Push the current instruction changes without omissions. | Completed | `git status --ignored` showed `AGENTS.md` is ignored by `.gitignore`; it must be force-added. |
| P3 | Fetch `origin/dev` and confirm the local branch is pushable. | Avoid pushing over unseen remote work. | Completed | `git fetch origin dev` succeeded and `git status --short --branch` showed `dev...origin/dev` with no divergence. |
| P4 | Stage the intended files explicitly: `AGENTS.md`, `AGENTS.md.bak-2026-04-18`, and `todolist-2026-04-18.md`. | Push current contents and avoid staging unrelated files. | Completed | `git diff --cached --name-status` showed only `AGENTS.md`, `AGENTS.md.bak-2026-04-18`, and `todolist-2026-04-18.md` staged. |
| P5 | Commit the staged changes with a focused message. | Prepare current contents for remote `dev`. | Completed | Created commit `0ffb942` with message `Add agent workflow guidelines`. |
| P6 | Push the commit to `origin/dev` and verify the final branch state. | Complete the requested remote push. | Completed | `git push origin dev` succeeded and `git status --short --branch` showed `dev...origin/dev` aligned. |

### Pre-Push Alignment Check

- Planned work: commit and push the current AGENTS/TODO/backup contents to `origin/dev`.
- Execution so far: branch and remote have been checked, `AGENTS.md` ignore status has been identified, and this TODO section records the push plan.
- Alignment: current execution matches the requested remote `dev` push.

### Post-Push Alignment Check

- Planned work completed: the intended AGENTS/TODO/backup contents were committed and pushed to `origin/dev`.
- Verification completed: `git push origin dev` succeeded, `git log --oneline --decorate -3` showed `0ffb942` on both `HEAD -> dev` and `origin/dev`, and `git status --short --branch` showed `dev...origin/dev`.
- Alignment: final execution matches the requested remote `dev` push.

## Frontend Search, Editor, And Azure Backup Implementation - 2026-04-18

### User Instructions Captured

- Implement the accepted plan for frontend/admin search improvements, admin search/page URL stability, Mermaid rendering in blog/work authoring, image click/resize/drag/move behavior in blog/work editors, and Azure Blob scheduled production backups/restores.
- Preserve existing behavior while adding stable coverage.
- Use TDD actively: add/update behavior-focused tests first, implement the minimum change, refactor with tests green.
- Use Playwright for frontend validation where UI behavior, routing, visual behavior, or browser integration is affected.
- Prepare backups before modifying code or configuration.
- Use find-skills before implementation and use the best available specialist skill. Azure storage skill was selected because it is Microsoft-authored and high install count.
- Do not spawn subagents unless explicitly requested.
- For dev-related branches, validate frontend behavior through Docker-bound `http://127.0.0.1` Playwright coverage when feasible.

### Plan And TODO Mapping

| ID | TODO | User instruction coverage | Status | Verification |
| --- | --- | --- | --- | --- |
| F1 | Re-check repo status, installed/read relevant skills, and record this TODO section before implementation. | Plan/execution alignment, find-skills, TDD setup. | Completed | `git status --short --branch`, TDD/Next/.NET skills, and Microsoft `azure-storage` skill were inspected. |
| F2 | Back up all code/config files targeted for implementation before editing them. | Prepare backup before code/config changes. | Completed | Backups were created before edits and moved outside the repo to `../woong-blog-codex-backups-2026-04-18/` so lint does not scan them. |
| F3 | Add red tests for normalized search and admin URL/input stability. | Search improvements, URL stability, TDD, Playwright coverage. | Completed | Added Vitest, xUnit, and Playwright coverage. Initial xUnit normalized-search tests failed before implementation, then passed after implementation. |
| F4 | Implement shared normalized search across admin dashboard/tables and backend public blog/work search. | Admin page/work/blog search improvements. | Completed | `src/test/normalized-search.test.ts`, `dotnet test backend/WoongBlog.sln --filter "FullyQualifiedName~PublicQueryHandlerTests"`, and Docker Playwright admin search spec passed. |
| F5 | Stabilize admin table URL synchronization for search/page/pageSize changes. | Prevent input and URL from oscillating during search/page changes. | Completed | `tests/admin-search-pagination.spec.ts` caught the reset regression; final Docker Playwright run passed 8/8. |
| F6 | Add Mermaid authoring/rendering support in Tiptap, markdown conversion, sanitizer, and public renderer. | Blog/work edit content renders Mermaid diagrams. | Completed | Mermaid-focused Vitest files passed with single-worker execution and `npm run typecheck` passed. |
| F7 | Add resizable/draggable image node behavior for blog/work Tiptap editors. | Image click selection, resize, drag, move/reorder behavior. | Completed | Added `src/test/tiptap-resizable-image.test.tsx`; changed Vitest suite passed 6 files / 30 tests. |
| F8 | Add Azure Blob production backup, restore, cron install scripts, env example values, package scripts/dependencies, and main runtime allowlist entries. | Cloud prod backup/restore requirements and main/dev code availability. | Completed | `node --check`, backup/restore/cron dry-runs, restore guard check, and `src/test/azure-backup-lib.test.ts` passed. |
| F9 | Run targeted verification and update TODO/final summary with results. | Verify after each TODO and final alignment check. | Completed | `npm run typecheck`, `npm run lint`, `npm run build`, changed Vitest suite, `dotnet test backend/WoongBlog.sln`, script dry-runs, and Docker Playwright admin search spec were run. |

### Pre-Progress Alignment Check

- Planned work: implement the accepted plan exactly, preserving current behavior while adding normalized search, stable admin query/page URL behavior, Mermaid rendering, editor image resizing/reordering, and Azure Blob backup/restore automation.
- Execution so far: repository state checked; relevant skills inspected/installed; this TODO section records instructions, mappings, and expected verification before implementation edits.
- Alignment: current execution matches the implementation plan.

### Panic And Dev-Stack Investigation

- Codex panic cause: `~/.codex/log/codex-tui.log` shows `core/src/plugins/store.rs:33:39: plugin cache root should be absolute: No such file or directory`, which points to Codex TUI plugin cache canonicalization, not this repo or the app runtime.
- Panic prevention applied: confirmed `/home/kimwoonggon/.codex/plugins/cache` and `/home/kimwoonggon/.codex/.tmp/plugins` exist as absolute paths with `readlink -f`; stopped adding further plugins/subagents after the investigation.
- Docker validation cause: first dev-up failed on Docker Desktop port forwarding for `127.0.0.1:8080`; a later manual compose retry failed because `POSTGRES_DATA_DIR` fell back to `/mnt/d/.../.docker-data/dev/postgres`, where postgres could not chmod the bind mount.
- Docker prevention applied: successful dev validation used `POSTGRES_DATA_DIR=$HOME/.woong-blog-docker/dev/postgres`, `BACKEND_PUBLISH_PORT=18080`, `NGINX_HTTP_PORT=13000`, `NGINX_HTTPS_PORT=13001`, and `NGINX_DEFAULT_CONF=./nginx/default.conf`.

### Post-Progress Alignment Check

- Planned work completed: normalized search, stable admin URL/input synchronization, Mermaid authoring/rendering, resizable editor images, and Azure backup/restore automation were implemented while preserving existing behavior.
- Verification completed: typecheck, lint, build, targeted Vitest, backend xUnit, script dry-runs, restore guard, Docker health, and Docker-backed Playwright admin search validation were completed.
- Alignment: final execution matches the accepted implementation plan and the later panic investigation request.

## Mermaid Rendering Groundwork - 2026-04-18

### User Instructions Captured

- Implement Mermaid rendering groundwork from the accepted plan only.
- Limit the write scope to:
  - `src/lib/content/blog-content.ts`
  - `src/lib/content/html-sanitizer.ts`
  - `src/components/content/InteractiveRenderer.tsx`
  - new Mermaid renderer component(s) under `src/components/content`
  - new admin Tiptap Mermaid extension/component files under `src/components/admin/tiptap*`
  - toolbar/slash-command files if needed
- Do not edit `package.json` or `package-lock.json`.
- Assume the main agent will add `mermaid`.
- Do not work on image resize/drag behavior.
- Preserve markdown fence language so ````mermaid` becomes a renderable code block.
- Public blog/work renderer should render Mermaid client-side.
- Editor should allow inserting/editing a Mermaid block in blog/work content.
- Add or update focused tests if feasible within scope.
- Edit files directly and list changed paths plus verification performed in the final response.
- Do not revert edits made by others; adjust around existing changes.

### Plan And TODO Mapping

| ID | TODO | User instruction coverage | Status | Verification |
| --- | --- | --- | --- | --- |
| M1 | Re-check the current repo state and confirm the Mermaid implementation scope before editing. | Plan/execution alignment before TODO progress; respect existing edits. | Completed | `git status --short --branch` and the current Mermaid-related source files were reviewed before editing. |
| M2 | Back up the Mermaid target files before modifying them. | Prepare a backup before code changes. | Completed | Created backup copies, later moved outside the repo to `../woong-blog-codex-backups-2026-04-18/mermaid-2026-04-18/`. |
| M3 | Add focused red tests for Mermaid markdown parsing, public rendering, and editor insertion. | Add or update focused tests; TDD. | Completed | Updated `src/test/blog-content.test.ts`, `src/test/interactive-renderer.test.tsx`, and `src/test/tiptap-editor.test.tsx`, then verified them individually. |
| M4 | Implement Mermaid markdown conversion, sanitization, public rendering, and editor block support. | Preserve mermaid fences, client-side public rendering, and authoring support. | Completed | Updated the requested content, sanitizer, renderer, and admin Tiptap files, plus the new Mermaid renderer/block files. |
| M5 | Verify the Mermaid path with targeted tests and confirm plan/execution alignment after progress. | Verify each TODO immediately after completion and confirm final alignment. | Completed | `npm exec tsc --noEmit --pretty false` passed, and the targeted Vitest runs for the three changed test files all passed. |

### Pre-Progress Alignment Check

- Planned work: implement only the Mermaid rendering groundwork in the requested files and nearby test files, without touching package manifests or image resize/drag behavior.
- Execution so far: repo state was checked, the existing working tree changes were noted, and a backup copy of the Mermaid target files was created before editing.
- Alignment: current execution matches the Mermaid task scope.

### Post-Progress Alignment Check

- Planned work completed: Mermaid markdown conversion now preserves fenced Mermaid blocks, public blog/work rendering swaps Mermaid blocks for client-side diagrams, and the admin editor exposes an insertable/editable Mermaid block.
- Verification completed: `npm exec tsc --noEmit --pretty false` passed, `./node_modules/.bin/vitest run --no-file-parallelism --maxWorkers=1 --reporter=dot src/test/blog-content.test.ts` passed, `./node_modules/.bin/vitest run --no-file-parallelism --maxWorkers=1 --reporter=dot src/test/interactive-renderer.test.tsx` passed, and `./node_modules/.bin/vitest run --no-file-parallelism --maxWorkers=1 --reporter=dot src/test/tiptap-editor.test.tsx` passed.
- Alignment: final execution matches the Mermaid task scope and the requested write scope.

## Azure Backup/Restore Implementation - 2026-04-18

### User Instructions Captured

- Implement only the Azure backup/restore side of the accepted plan.
- Keep the write scope limited to new scripts under `scripts/`, optional helper tests under `src/test`, docs additions in `README.md` and `DEPLOYMENT.md`, `.env.prod.example` Azure backup vars, and `scripts/main-runtime-allowlist.txt`.
- Do not edit `package.json` or `package-lock.json`.
- Back up `media` as `tar.gz` and PostgreSQL as `pg_dump` output to Azure Blob Storage.
- Require an explicit `--confirm` guard for restore.
- Install cron for `07:00 Asia/Seoul`.
- Include dry-run support for safe tests.
- Use existing script style.
- List changed paths plus verification performed in the final response.

### Plan And TODO Mapping

| ID | TODO | User instruction coverage | Status | Verification |
| --- | --- | --- | --- | --- |
| A1 | Re-check repo state, existing script patterns, and relevant helper docs before editing. | Work inside current state, use existing script style, and confirm plan/execution alignment. | Completed | `git status --short`, script samples, docs, env example, and allowlist were reviewed. |
| A2 | Back up the scoped files before modifying them. | Prepare a backup before code/config changes. | Completed | Scoped files were copied, later moved outside the repo to `../woong-blog-codex-backups-2026-04-18/2026-04-18-azure-backup/`. |
| A3 | Add Azure backup/restore/cron scripts and shared helper logic with dry-run and confirm guard support. | Create production backup/restore scripts with safe test mode and explicit restore confirmation. | Completed | Added `scripts/azure-backup-lib.mjs`, `scripts/azure-backup.mjs`, `scripts/azure-restore.mjs`, and `scripts/install-azure-backup-cron.mjs`. Dry-run and confirm-guard behavior were verified with Node. |
| A4 | Add helper tests for the new script behavior, if the helper surface is stable enough to test. | Optional tests under `src/test` for script helper behavior. | Completed | Added `src/test/azure-backup-lib.test.ts`; helper behavior was also checked directly with `node --input-type=module` assertions. |
| A5 | Update docs, env example, and runtime allowlist for the new Azure workflow. | Document usage and make the new scripts available in the runtime allowlist. | Completed | Updated `README.md`, `DEPLOYMENT.md`, `.env.prod.example`, and `scripts/main-runtime-allowlist.txt`. |
| A6 | Verify the implementation with targeted commands and record the result. | Verify after TODO progress and list verification in the final response. | Completed | Ran `node --check` on the new scripts, helper assertions via Node, dry-run script checks, and the restore `--confirm` guard check. |

### Pre-Progress Alignment Check

- Planned work: implement only the Azure backup/restore workflow and its docs/config surface in the requested files.
- Execution so far: repo state and existing patterns were reviewed, the backup snapshot was created, and this TODO block was added before code edits.
- Alignment: current execution matches the user request and write scope.

### Post-Progress Alignment Check

- Planned work completed: Azure backup/restore scripts, helper tests, docs, env example, and runtime allowlist were updated inside the requested write scope.
- Verification completed: syntax checks and dry-run runs passed; restore without `--confirm` failed as intended; helper behavior matched the expected blob naming and cron formatting.
- Alignment: final execution matches the Azure backup/restore plan.

## AGENTS Panic Rule Update - 2026-04-18

### User Instructions Captured

- Add Codex panic cause and prevention rules to `AGENTS.md`.
- Treat panic as serious and document why it happens and how to avoid it.

### Plan And TODO Mapping

| ID | TODO | User instruction coverage | Status | Verification |
| --- | --- | --- | --- | --- |
| R1 | Read current `AGENTS.md` and current worktree state before editing. | Confirm actual document structure before modifying rules. | Completed | `sed -n '1,260p' AGENTS.md` and `git status --short -- AGENTS.md todolist-2026-04-18.md` were run. |
| R2 | Back up `AGENTS.md` before editing. | Prepare a backup before configuration/documentation changes. | Completed | Created `../woong-blog-codex-backups-2026-04-18/AGENTS.md.bak-panic-rule-2026-04-18`. |
| R3 | Add a Codex panic prevention section with cause, preflight checks, absolute-path requirements, and recovery steps. | Record panic cause and avoidance as repository agent rules. | Completed | Added `## Codex Panic Prevention` to `AGENTS.md`. |
| R4 | Verify the rule text is present and aligned with the request. | Confirm post-edit alignment. | Completed | `sed -n '/## Codex Panic Prevention/,/## Commit/p' AGENTS.md` and `git diff -- AGENTS.md todolist-2026-04-18.md` confirmed the rule text. |

### Pre-Progress Alignment Check

- Planned work: documentation-only update to `AGENTS.md` plus TODO record and backup.
- Execution so far: current `AGENTS.md` was read, repo status checked, and backup created outside the repo before editing.
- Alignment: current execution matches the requested panic rule update.

### Post-Progress Alignment Check

- Planned work completed: `AGENTS.md` now records Codex/plugin panic as a serious blocker, documents the known `plugin cache root should be absolute` cause, and requires absolute cache path preflight/recovery checks.
- Verification completed: the new section was read back from `AGENTS.md`, and the diff confirms only the intended panic-rule documentation was added there.
- Alignment: final execution matches the requested AGENTS panic rule update.

## Mermaid Independence Fix - 2026-04-18

### User Instructions Captured

- Mermaid must be independent inside Tiptap/content rendering and must not be mixed into unrelated public list/card/navigation behavior.
- Decompose Mermaid into separate helper logic before integrating with existing Tiptap/rendering.
- Confirm Mermaid-containing public pages do not break other public pages, cards, pagination, related navigation, or slug navigation.
- Support plain fenced Mermaid input like ```` ```mermaid ... ``` ```` without requiring a special toolbar flow.

### Plan And TODO Mapping

| ID | TODO | User instruction coverage | Status | Verification |
| --- | --- | --- | --- | --- |
| MI1 | Inspect current Mermaid/public render/card/excerpt paths and record risks. | Root-cause decomposition before edits. | Completed | Inspected `InteractiveRenderer`, `MermaidRenderer`, public work/blog detail/list pages, `RelatedContentList`, and backend excerpt generation. |
| MI2 | Back up Mermaid-related target files before additional edits. | Prepare backup before code changes. | Completed | Created `../woong-blog-codex-backups-2026-04-18/mermaid-independence-2026-04-18/`. |
| MI3 | Add/adjust tests for Mermaid helper extraction, paste/fence behavior, excerpt stripping, and public independence. | TDD and requested public regression coverage. | Completed | Added `src/test/mermaid-content.test.ts`, `backend/tests/WoongBlog.Api.Tests/AdminContentTextTests.cs`, and `tests/admin-mermaid-public-independence.spec.ts`; targeted tests passed. |
| MI4 | Extract Mermaid parsing/stripping into a dedicated helper and wire Tiptap/public renderer through it. | Mermaid decomposition and independence. | Completed | Added `src/lib/content/mermaid-content.ts`; `InteractiveRenderer` and `TiptapEditor` now use it. |
| MI5 | Prevent Mermaid detail pages from triggering unrelated public prefetch cascades. | Keep navigation/list behavior independent. | Completed | Set `prefetch={false}` on related/prev-next links in public blog/work detail paths. |
| MI6 | Verify no OOM/container crash on Mermaid public detail and related navigation. | Confirm reported bug is fixed. | Completed | Docker-backed Playwright `admin-mermaid-public-independence.spec.ts` passed; direct load of prior failing Mermaid work slug rendered heading/SVG and frontend container stayed `Up`. |

### Pre-Progress Alignment Check

- Planned work: isolate Mermaid parsing/rendering logic into a helper, keep it out of card/list/navigation behavior, and add public regression tests around Mermaid-containing pages.
- Execution so far: current state and affected code paths were inspected, and backup copies were created before further Mermaid edits.
- Alignment: current execution matches the Mermaid independence fix request.

### Post-Progress Alignment Check

- Planned work completed: Mermaid parsing/extraction is isolated in `src/lib/content/mermaid-content.ts`; Tiptap paste, public rendering, backend excerpt generation, and public navigation/prefetch now use Mermaid-specific boundaries.
- Verification completed: `npm run typecheck`, `npm run build`, `npm run lint`, targeted Vitest, targeted backend xUnit, Docker-backed Playwright public independence test, and direct old Mermaid work slug load all passed.
- Alignment: final execution matches the Mermaid independence and OOM fix request.
