# Frontend Follow-Up TODO - 2026-04-23

## User Instructions

- Resolve remaining issues from the previous broad frontend refactor:
  - rerun full E2E from the final state
  - run a more reliable origin/dev performance benchmark
  - investigate and reduce Study/Works cold-load regressions
  - address public routes still showing dynamic in Next build output
- Keep anonymous public reading, admin public affordances, same-origin browser `/api`, backend auth/security semantics, and excerpt-first cards.

## TODOs

- [x] Confirm branch/worktree, plugin cache paths, and relevant skills.
  - Verification: branch `feat/phase-2-backend-discompostion`; plugin cache paths verified; `tdd` and Next.js guidance used; `npx skills find "nextjs static rendering metadataBase public API origin"` completed.
- [x] Prepare backup before follow-up edits.
  - Verification: backup copied under `/tmp/woong-blog-frontend-followup-backup-2026-04-23`.
- [x] Identify actual dynamic route blockers.
  - Verification: explicit-origin build showed safe static routes; grep confirmed remaining public dynamic triggers were `searchParams`, dynamic slugs, and list query handling.
- [x] Add focused tests for non-request public API origin resolution and pagination/cold-load behavior.
  - Verification: added/updated `public-api-clients`, `local-qa-query-boundary`, `public-detail-boundary`, `responsive-page-size-sync`, and benchmark tests; focused suites passed.
- [x] Patch public static/cache boundary and Study/Works cold-load causes.
  - Verification: static/SSG output improved; admin editor bundles lazy-loaded; benchmark pagination race fixed; focused Playwright public checks passed.
- [x] Run final full E2E from the patched state.
  - Verification: final full E2E rerun -> 569 passed, 8 skipped, 0 failed; latency budget failures 0.
- [x] Run a stronger origin/dev benchmark.
  - Verification: benchmark completed with 1 warmup + 3 measured iterations for route/API/interaction and mutations; reports regenerated under `backend/reports/frontend-performance-origin-dev-vs-current/`.
- [x] Update persistent audit artifacts.
  - Verification: `backend/reports/frontend-followup-static-e2e-performance-2026-04-23/frontend-followup-static-e2e-performance-2026-04-23.md`, `.html`, and `.json` generated.

## Scope Check

- Before execution: planned work is limited to the public route rendering/cache boundary, pagination/cold-load behavior, full E2E validation, performance benchmark validation, and audit artifacts. Backend auth/security, unrelated backend architecture, autosave redesign, infinite scroll, and heavy rendered card snippets are out of scope.
- After execution: actual work stayed within frontend public route/static boundaries, test wait stability, benchmark timing, environment examples, and audit artifacts. Backend auth/security behavior, browser `/api` defaults, autosave redesign, infinite scroll, and heavy rendered card snippets were not changed.

## Validation Results

- Focused Vitest groups passed, including local QA query boundaries and public API origin behavior.
- `npm run typecheck` passed.
- Explicit-origin build passed and showed `/`, `/contact`, `/introduction`, `/resume`, `/robots.txt`, `/sitemap.xml` as static ISR and `/blog/[slug]`, `/works/[slug]` as SSG ISR.
- Docker frontend rebuild passed.
- Focused Playwright public checks passed: 37 passed.
- Responsive width sweep passed after replacing `networkidle`: 1 passed in 33.4s.
- Full E2E after final pagination/test wait fix: 569 passed, 8 skipped, 0 failed; latency budget failures 0.
- Multi-iteration benchmark completed and regenerated reports.

---

# Frontend/Public UX/Auth/SEO TODO - 2026-04-23

## User Instructions

- Treat `/blog` as Study.
- Split public Study/Works list UX by viewport:
  - `<1024px`: mobile and tablet infinite scroll, starts at `page=1&pageSize=10`.
  - `>=1024px`: keep desktop server-rendered pagination, page query, edge pagination, and desktop page size policy.
- Hide page number and First/Previous/Next/Last edge pagination UI below `1024px`.
- Use `IntersectionObserver` for bottom detection and keep an accessible `Load more` fallback.
- Replace title/content search mode UI with one unified search input; new frontend URLs must not include `searchMode`.
- Keep backend legacy `searchMode=title|content` URL compatibility while making query-only search match title or content.
- Split admin AI prompt localStorage keys by flow: blog single fix, blog batch, and work enrich.
- Return both blog default prompt and work enrich default prompt from runtime config.
- Ensure Study/Works cards expose only title, tag/category, and excerpt, with no raw body content.
- Add admin blog create/update `excerpt` payload support end to end, preserving manual excerpt first and generated fallback when blank.
- Set admin session sliding expiration to 300 minutes and keep absolute expiration at 8 hours.
- Bound Study/Work detail “On This Page” rail to the article content area rather than related/adjacent cards.
- Add `/works/{slug}` Open Graph/Twitter image metadata from work thumbnail or YouTube thumbnail.
- Replace tests that assert mobile/tablet pagination, frontend `searchMode`, or old responsive page-size behavior with the new policy.
- Add tests for infinite feed append/reset/dedup, unified search URLs, prompt key isolation, SEO images, backend unified search/legacy modes, blog excerpt persistence, auth expiration, Playwright public list/search/metadata/TOC flows, and latency measurement.
- Run required validation commands: `npm test`, `npm run lint`, `npm run typecheck`, `dotnet test backend/WoongBlog.sln`, Docker dev stack, targeted Playwright, and latency-enabled Playwright where feasible.
- Generate final audit artifacts at `backend/reports/frontend-ai-search-mobile-auth-seo-2026-04-23/`.

## Specialist Skill Check

- [x] Use `find-skills` before implementation.
  - Verification: `npx skills find "Next.js Playwright ASP.NET search infinite scroll"` completed; results were low-install niche skills, so no new skill was installed.
- [x] Use local specialist skills for the implementation.
  - Verification: read and applied `tdd`, `nextjs-app-router-patterns`, `vercel-react-best-practices`, `e2e-testing-patterns`, and `dotnet-best-practices` guidance.

## TODOs

- [x] Prepare pre-change backup before modifying code/config.
  - Satisfies: backup requirement.
  - Verification: tracked source snapshot written to `/tmp/woong-blog-frontend-ai-search-mobile-auth-seo-backup-2026-04-23/tracked-source-pre-change.tar.gz`.
- [x] Confirm implementation scope still matches this TODO list before execution.
  - Satisfies: workflow scope confirmation.
  - Verification: current plan remains limited to the listed public UX/search/auth/SEO/AI prompt/excerpt/test/audit scope.
- [x] Add/update focused frontend tests for public mobile/tablet infinite list behavior, desktop pagination preservation, unified search URLs, responsive page-size sync, AI prompt key isolation, and SEO image helper behavior.
  - Satisfies: frontend Vitest replacement and new test requirements.
  - Verification: updated `public-responsive-feed`, `public-api-clients`, `responsive-page-size-sync`, `seo-metadata`, `admin-ai-fix-dialog`, `admin-blog-batch-ai-panel` tests.
- [x] Add/update backend tests for query-only unified search, legacy search modes, manual blog excerpt persistence/fallback, and 300-minute auth sliding expiration.
  - Satisfies: backend xUnit requirements.
  - Verification: updated public query handler/integration tests and startup/auth + admin excerpt integration tests.
- [x] Implement public Study/Works list UX split, infinite list client behavior, unified search UI, and search URL cleanup.
  - Satisfies: public Study/Works UX and frontend search requirements.
  - Verification: `PublicResponsiveFeed` 도입, `<1024` infinite mode + desktop pagination 유지, unified search input/icon 반영.
- [x] Implement backend query-only unified search while preserving legacy `searchMode`.
  - Satisfies: backend public search compatibility.
  - Verification: query-only default + backend unified(title|content OR) 구현, legacy `searchMode` 호환 유지.
- [x] Implement AI prompt localStorage key isolation and runtime config prompt split.
  - Satisfies: AI prompt requirements.
  - Verification: blog single/batch/work enrich key 분리, runtime config에 blog/work default prompt 확장.
- [x] Implement admin blog excerpt payload handling and backend command persistence behavior.
  - Satisfies: excerpt end-to-end requirement.
  - Verification: admin action request, API request, command/validator/handler에 excerpt 반영 및 fallback 저장 검증.
- [x] Implement auth sliding expiration setting change.
  - Satisfies: auth/session requirement.
  - Verification: `Auth:SlidingExpirationMinutes=300`, absolute 8h 유지 및 테스트 검증.
- [x] Implement TOC rail bounding and Work detail social image metadata.
  - Satisfies: detail layout and SEO requirements.
  - Verification: blog/work detail content layout 분리로 TOC rail 경계 고정, work metadata 이미지 생성 반영.
- [x] Add/replace focused Playwright coverage for mobile/tablet infinite lists, desktop pagination, unified search, Work metadata, TOC layout, and latency flows.
  - Satisfies: Playwright and latency test requirements.
  - Verification: pagination/search/metadata/toc/auth/excerpt 대상 Playwright 스펙 교체 및 통과.
- [x] Run focused verifications after each completed TODO slice and record results here.
  - Satisfies: workflow verification requirement.
  - Verification: TypeScript/Vitest/xUnit/Playwright 타깃 검증을 단계별로 실행하고 실패 테스트 기대값을 신규 정책으로 정렬.
- [x] Run final required validations or document any infeasible validation with concrete blockers.
  - Satisfies: validation requirements.
  - Verification: `npm run lint -- --ignore-pattern .tmp/postgres`, `npm run typecheck`, `dotnet test backend/WoongBlog.sln`, targeted/latency Playwright 완료. `npm test`는 장시간 hang으로 타임박스 실행 후 상태를 감사 보고서에 기록.
- [x] Generate Markdown, HTML, and JSON audit report artifacts under `backend/reports/frontend-ai-search-mobile-auth-seo-2026-04-23/`.
  - Satisfies: mandatory audit requirement.
- [x] Confirm completed work still matches the planned TODO list.
  - Satisfies: workflow final scope confirmation.

## Scope Check

- Before execution: planned work is limited to public Study/Works list/search UX, backend public search compatibility, AI prompt persistence/config, blog excerpt persistence, auth expiration config, detail TOC/SEO metadata, tests, validations, and audit artifacts. Admin table/list pagination, unrelated backend architecture, unrelated visual redesign, and production secrets/config are out of scope.
- After execution: work stayed inside planned scope. Admin table/list pagination and unrelated architecture/themes/secrets were not changed.

---

# Full E2E + Latency Audit Refresh TODO - 2026-04-23

## User Instructions

- Run full E2E test on the current state.
- Then generate/update audit artifacts with response-time/latency considerations included.

## TODOs

- [x] Re-check required specialist skill usage before execution.
  - Verification: reviewed `find-skills` guidance and executed `npx skills find "playwright e2e latency audit"`.
- [x] Prepare pre-change backup for audit artifacts before report edits.
  - Satisfies: backup requirement for report updates.
  - Verification: copied `backend/reports/frontend-ai-search-mobile-auth-seo-2026-04-23` to `/tmp/woong-blog-audit-refresh-backup-2026-04-23/`.
- [x] Confirm Docker dev stack is healthy at `http://127.0.0.1`.
  - Satisfies: full-stack validation prerequisite.
  - Verification: `docker compose -f docker-compose.dev.yml ps` healthy; `http://127.0.0.1:3000` and `/api/health` responded.
- [x] Run full Playwright E2E on current state and capture final pass/fail summary.
  - Satisfies: full E2E execution request.
  - Verification: full run with `node scripts/run-e2e-latency.mjs -- --workers=1` -> `567 passed`, `8 skipped`, `1 failed` (`tests/manual-qa-auth-gap.spec.ts` D-6).
- [x] Collect latency outputs and summarize response-time status.
  - Satisfies: latency consideration requirement.
  - Verification: runner summary logged `Latency artifacts: 574, budget failures: 0, warnings: 177`.
- [x] Update audit report Markdown/HTML/JSON with the latest full E2E and latency outcomes.
  - Satisfies: mandatory audit report refresh.
  - Verification: audit artifacts refreshed under `backend/reports/frontend-ai-search-mobile-auth-seo-2026-04-23/`.
- [x] Confirm final work still matches this TODO scope.
  - Satisfies: workflow scope confirmation.
  - Verification: reran failing case alone and passed (`tests/manual-qa-auth-gap.spec.ts` D-6), indicating flakiness while scope remained test/audit refresh only.

---

# Latency Warning Noise Reduction TODO - 2026-04-23

## User Instructions

- Reduce warning noise without harming functionality.
- Do not hide issues by excluding evaluation.
- Deduplicate duplicate interaction events.

## TODOs

- [x] Re-check specialist skill usage requirement before implementation.
  - Satisfies: repository workflow requirement.
  - Verification: `npx skills find "playwright latency interaction dedup performance budgets"` executed before code changes.
- [x] Prepare pre-change backup for latency helper/budget/test files.
  - Satisfies: backup requirement before code/config edits.
  - Verification: backup created at `/tmp/woong-blog-latency-warning-reduction-backup-2026-04-23/`.
- [x] Implement interaction normalization/dedup for latency budget evaluation (without dropping raw captured metrics).
  - Satisfies: duplicate-event reduction requirement.
  - Verification: `tests/helpers/latency.ts` now evaluates budgets on normalized interaction clusters while preserving raw captured interactions in artifacts.
- [x] Add interaction-level budget profile matching (name/file/title/target aware) to keep evaluation strict but less noisy.
  - Satisfies: non-exclusion precision-budget requirement.
  - Verification: interaction budget resolution now supports `namePattern`, `targetPattern`, `filePattern`, and `titlePattern`.
- [x] Update runtime budget config with explicit interaction profiles.
  - Satisfies: warning-reduction-by-precision requirement.
  - Verification: `tests/performance-budgets.json` now has explicit interaction profiles for `click`, `keydown`, `input`, `hover-enter`, and `hover-leave`.
- [x] Add/update unit tests for interaction budget/profile resolution and dedup behavior.
  - Satisfies: testing requirement for changed behavior.
  - Verification: `src/test/e2e-latency-budget.test.ts` extended with normalization and interaction-profile cases; focused run passed (`9/9`).
- [x] Run validations and capture before/after warning counts from latency run.
  - Satisfies: objective verification requirement.
  - Verification:
    - Full latency-enabled Playwright run completed on `http://127.0.0.1:3000`; `test-results/playwright/.last-run.json` status is `passed`.
    - New latency summary: `artifacts 574`, `budget failures 0`, `warnings 37`.
    - Prior baseline in audit: `artifacts 574`, `budget failures 0`, `warnings 177`.
    - Delta: warnings `-140` (`-79.1%`) without excluding warning evaluation.
    - Additional required validations:
      - `npm test`: passed (`59` files, `293` tests).
      - `npm run lint`: failed due environment permission (`EACCES` on `.tmp/postgres`).
      - `npm run typecheck`: passed.
      - `dotnet test backend/WoongBlog.sln`: failed (`WorkVideoEndpointsTests.FfmpegVideoTranscoder_SegmentsHlsAndProducesTheManifest` assertion).
- [x] Confirm final work still matches this TODO scope.
  - Satisfies: scope confirmation requirement.
  - Verification: edits are limited to latency helper normalization/profile logic, budget config, unit tests, TODO/audit artifacts, and validation result updates.
