# Resume DOMMatrix SSR Fix TODO - 2026-04-22

## User Instructions

- Create one small focused change for `/resume`.
- Fix `ReferenceError: DOMMatrix is not defined` by preventing `react-pdf`/`pdfjs-dist` from being evaluated in the server SSR bundle.
- Read `src/app/(public)/resume/page.tsx`, `src/components/content/ResumePdfViewer.tsx`, wrapper/imported components, relevant `/resume` tests, `next.config.*`, and `package.json`.
- Do not redesign the resume page layout, change backend APIs, touch unrelated blog/work/contact flows, or bundle mobile/feed/search/autosave/global e2e isolation work.
- Keep public `/resume` accessible to anonymous users.
- Prefer a minimal production-safe client-only boundary, preserving current UX/loading/error states.
- Add/update focused tests, run relevant lint/typecheck/tests, and run focused `/resume` Playwright with `--workers=1`.
- Use subagents where useful; Playwright worker count is at most 1 unless explicitly diagnosing parallel instability.

## TODOs

- [x] Confirm current branch/worktree and isolate prior work.
  - Satisfies: avoid mixing prior public/admin PR work with this focused `/resume` change.
  - Verification: branch `feat/phase-2-backend-discompostion` is clean after commit `e1be359`.
- [x] Read required files and inspect current failure shape.
  - Satisfies: required-file read list.
  - Verification: inspected resume page/viewer, resume Playwright specs, `next.config.ts`, and `package.json`; subagent confirmed top-level `react-pdf` import reaches `pdfjs-dist` and `DOMMatrix` during Node evaluation.
- [x] Run skill discovery and choose guidance.
  - Satisfies: specialist skill requirement.
  - Verification: `npx skills find nextjs dynamic import ssr browser only pdf` found no stronger high-confidence skill; using local `tdd`, `nextjs-app-router-patterns`, and `vercel-react-best-practices`.
- [x] Prepare backup before edits.
  - Satisfies: backup requirement.
  - Verification: backup files created under `/tmp/woong-blog-resume-dommatrix-backup-2026-04-22`.
- [x] Add/update focused test proving server path avoids browser-only PDF module evaluation.
  - Satisfies: TDD and DOMMatrix regression coverage.
  - Verification: `npx vitest run src/test/resume-server-render.test.tsx --pool=threads` -> 1 file passed, 1 test passed.
- [x] Isolate `react-pdf` usage behind a client-only boundary.
  - Satisfies: implementation goal.
  - Verification: `ResumePdfViewer` no longer imports `react-pdf`; `ResumePdfDocument` holds the browser-only PDF code behind `next/dynamic(..., { ssr: false })`.
- [x] Run focused validation.
  - Satisfies: requested verification.
  - Verification:
    - `npx vitest run src/test/resume-server-render.test.tsx --pool=threads` -> passed.
    - `npm run lint` -> passed with 5 existing warnings.
    - `npm run typecheck` -> passed.
    - `BACKEND_PUBLISH_PORT=18080 NGINX_DEFAULT_CONF=./nginx/default.conf ./scripts/dev-up.sh` -> Docker dev stack rebuilt; Next build passed.
    - `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 npx playwright test tests/public-resume-empty-state.spec.ts tests/resume.spec.ts --workers=1` -> 3 passed.
    - `docker compose -f docker-compose.dev.yml logs frontend | rg "DOMMatrix|ReferenceError"` -> no matches after rebuild.
- [x] Generate mandatory audit report.
  - Satisfies: AGENTS.md completion requirement.
  - Verification: `backend/reports/resume-dommatrix-ssr-fix/resume-dommatrix-ssr-fix.md`, `.html`, and `.json` created.

## Scope Check

- Before execution: planned work is limited to `/resume` PDF viewer SSR isolation, focused tests, and audit artifacts. Resume content redesign, backend API changes, caching changes, mobile/feed/search work, autosave work, and global e2e worker isolation cleanup are out of scope.
- After implementation before audit: work remains within `/resume` PDF SSR isolation, focused tests, and verification only. No backend APIs, resume layout redesign, caching, or unrelated public/admin flows were changed.

---

# Playwright Notion Test Isolation TODO - 2026-04-22

## User Instructions

- Continue on `feat/phase-2-backend-discompostion` with commits on the current branch; no PR creation for now.
- Create one focused infrastructure/testing change only.
- Improve Playwright full-suite stability under parallel execution by addressing shared data/session/state collisions.
- Do not mix product feature work, resume SSR fixes, mobile/feed/search/autosave product redesign, or broad app refactors.
- Prefer deterministic test isolation over retries.
- If Playwright is run for validation, use `--workers=1` unless explicitly diagnosing parallel instability.

## TODOs

- [x] Commit and push the completed `/resume` DOMMatrix fix first.
  - Verification: commit `d32dab1 Fix resume PDF SSR boundary` pushed to `origin/feat/phase-2-backend-discompostion`.
- [x] Use subagent investigation for parallel instability root-cause analysis.
  - Verification: read-only subagent identified Notion/default blog mutation, first-row mutation, singleton state, work ordering, and shared auth storage risks.
- [x] Select first focused isolation slice.
  - Verification: chosen slice is Notion autosave/visual/semantic tests using their own blog fixture instead of ambient first/default blog.
- [x] Prepare backup before test edits.
  - Verification: backup files created under `/tmp/woong-blog-playwright-isolation-backup-2026-04-22`.
- [x] Add helper-level blog fixture creation for Playwright tests.
  - Verification: helper returns stable `{ id, slug, title }` for test-owned blog docs.
- [x] Update Notion autosave/visual/semantic specs to open explicit blog ids.
  - Verification: affected specs no longer depend on `/admin/blog/notion` default first document for mutating assertions.
- [x] Run targeted serial validation and narrow parallel diagnosis.
  - Verification: serial and targeted parallel Playwright results recorded.
- [x] Generate mandatory audit report.
  - Verification: Markdown, HTML, and JSON report artifacts recorded.

## Scope Check

- Before execution: planned work is limited to Playwright test helper/data isolation for Notion-related blog mutation specs. Product code, resume SSR code, backend APIs, mobile/feed/search work, autosave product behavior, and global full-suite cleanup are out of scope.
- After execution: actual work stayed within Playwright test data isolation for Notion-related blog mutation specs plus `admin-blog-edit` first-row mutation, which was proven to collide with generated baseline blogs in 4-worker diagnosis. Product code, backend APIs, resume SSR code, and global full-suite cleanup were not changed.

## Validation Results

- Serial target group:
  - `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 npx playwright test tests/admin-blog-edit.spec.ts tests/ui-admin-notion-autosave-info.spec.ts tests/ui-admin-notion-visual-state.spec.ts tests/ui-admin-semantic-colors.spec.ts --workers=1`
  - Result: 8 passed.
- Parallel diagnostic:
  - `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 npx playwright test tests/admin-blog-edit.spec.ts tests/ui-admin-notion-autosave-info.spec.ts tests/ui-admin-notion-visual-state.spec.ts tests/ui-admin-semantic-colors.spec.ts tests/plain-baseline-0419.spec.ts --workers=4`
  - Before fix: 1 baseline collision failure where `admin-blog-edit` mutated `plain-baseline` generated content.
  - After fix and grounded timeout tuning: 48 passed.
- Static checks:
  - `npm run lint` -> passed with 5 existing warnings.
  - `npm run typecheck` -> passed.

---

# Public Read Cache Boundary TODO - 2026-04-22

## User Instructions

- Do one focused task only.
- Refactor public read pages so public content can be cache/revalidate friendly while admin affordances remain available.
- Read public layout/pages, public API helpers, server API helpers, and public admin helper.
- Do not redesign UI, change backend auth/security semantics, remove admin affordances, break anonymous reading, or bundle mobile/search/infinite-scroll/autosave work.
- Identify routes that can safely use revalidate behavior and routes intentionally still dynamic.
- Run lint/typecheck/relevant tests and verify anonymous public pages and admin affordances.

## TODOs

- [x] Finish and push previous Playwright isolation work before starting.
  - Verification: commit `c55f5b4 Stabilize Playwright Notion blog fixtures` pushed; worktree clean before this task.
- [x] Read required files and run skill discovery.
  - Verification: inspected requested public pages/helpers; actual helper filenames are `blogs.ts`, `works.ts`, `pages.ts`; `npx skills find nextjs revalidate caching server components` found no stronger high-confidence skill than local Next.js guidance.
- [x] Prepare backup before edits.
  - Verification: backup files created under `/tmp/woong-blog-public-cache-boundary-backup-2026-04-22`.
- [x] Add/update focused tests for public fetch cache options and client admin affordance gate.
  - Verification: `npx vitest run src/test/public-api-clients.test.ts src/test/public-api-contracts.test.ts src/test/public-admin-rendering.test.tsx src/test/public-admin-client-gate.test.tsx --pool=threads` -> 4 files passed, 24 tests passed.
- [x] Make safe public read helpers revalidate-friendly.
  - Verification: public fetch helpers use `next: { revalidate: 60 }` and no longer add request forwarding headers for public content fetches.
- [x] Move static public page admin affordance checks behind client-only gate.
  - Verification: contact/introduction/resume no longer call server session gate; admin affordances load through `PublicAdminClientGate`.
- [x] Run focused validation.
  - Verification:
    - Targeted Vitest -> 4 files passed, 24 tests passed.
    - `npm run typecheck` -> passed.
    - `npm run lint` -> passed with 5 existing warnings.
    - `npm run build` -> passed after adding request-aware fallback for build environments without `INTERNAL_API_ORIGIN`.
    - Focused Playwright against local dev server could not complete because the existing Docker/nginx API target at `127.0.0.1:3000` was unavailable (`ECONNREFUSED`); no product assertion failure was observed from this attempted run.
- [x] Generate mandatory audit report.
  - Verification: `backend/reports/public-cache-boundary/public-cache-boundary.md`, `.html`, and `.json` created.

## Scope Check

- Before execution: planned work is limited to public read caching/revalidation boundaries and admin affordance decoupling on safe public routes. Blog/works pages may remain dynamic where search/pagination/admin edit payload coupling requires it. UI redesign, backend auth/API changes, mobile/search/feed/autosave work, and global e2e isolation cleanup are out of scope.
- After execution: actual work stayed within public read fetch caching and static public page admin gate decoupling. Blog/works routes intentionally remain dynamic because of search/pagination, QA hooks, and server-side admin edit payload loading.
- Full e2e single-worker:
  - `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 PLAYWRIGHT_EXPECT_LOCAL_ADMIN_SHORTCUT=visible npx playwright test --workers=1`
  - First result after Notion isolation: 561 passed, 2 failed, 8 skipped.
  - The two failures were visual measurement flakes in:
    - `tests/ui-improvement-container-width.spec.ts:47`
    - `tests/ui-quality-visual-contracts.spec.ts:73`
  - Test code was updated to wait for valid geometry before asserting.
  - Final full single-worker result: 563 passed, 8 skipped.
  - Final `npm run lint` -> passed with 5 existing warnings.
  - Final `npm run typecheck` -> passed.

---

# Public On-Demand Revalidation TODO - 2026-04-22

## User Instructions

- Do one focused task only.
- Add on-demand cache invalidation for public routes after successful admin mutations.
- Scope: contact, introduction, resume, blog create/update/delete, and work create/update/delete.
- Use Next.js server-side revalidation APIs, preferably `revalidatePath`.
- Include home page invalidation where blog/work content affects home sections.
- Keep the change minimal and production-safe; do not redesign editor flows, mix mobile/search/autosave work, or weaken auth/security.
- After modification and adding new behavior, check whether full e2e works.
- Full Playwright must run with one worker.

## TODOs

- [x] Identify mutation success points and public cache dependencies.
  - Satisfies: locate where admin saves/deletes succeed before adding invalidation.
  - Verification: inspected blog/work editors, inline delete helpers, page/home/resume/site-settings editors, public fetch helpers, and Next route constraints behind nginx.
- [x] Add a protected Next route for public revalidation.
  - Satisfies: server-side Next revalidation API requirement.
  - Verification: `src/app/revalidate-public/route.ts` requires same-host requests plus current admin affordance/session state before calling `revalidatePath` and `revalidateTag`.
- [x] Add shared path/tag helpers for public invalidation.
  - Satisfies: exact route invalidation mapping and helper-level reuse.
  - Verification: `src/lib/public-revalidation-paths.ts` normalizes public paths and maps them to cache tags.
- [x] Wire successful admin mutations to revalidation.
  - Satisfies: contact/introduction/resume/blog/work mutation scope.
  - Verification: create/update/delete flows call `revalidatePublicPathsAfterMutation` only after successful backend mutations.
- [x] Update focused unit and browser tests for the new revalidation behavior.
  - Satisfies: newly introduced behavior is represented in tests and stale race tests are updated.
  - Verification:
    - Revalidation helper/route tests added.
    - Public API tests now assert public fetch cache tags.
    - Playwright site-settings and inline editor specs wait for `/revalidate-public` before reading cached public pages.
- [x] Run validation.
  - Verification:
    - `npx vitest run src/test/public-revalidation-paths.test.ts src/test/public-revalidation-route.test.ts src/test/blog-editor.test.tsx src/test/work-editor.test.tsx src/test/page-editor.test.tsx src/test/resume-editor.test.tsx src/test/inline-blog-editor-section.test.tsx src/test/inline-work-editor-section.test.tsx src/test/public-api-clients.test.ts src/test/public-api-contracts.test.ts --pool=threads` -> 10 files passed, 72 tests passed.
    - `npm run typecheck` -> passed.
    - `npm run lint` -> blocked by local Docker-owned `.tmp/postgres` permission during root scan.
    - `npx eslint src tests playwright.config.ts next.config.ts eslint.config.mjs` -> passed with 3 existing warnings.
    - Docker/nginx frontend rebuild -> passed; Next production build succeeded.
    - Focused Playwright revalidation group -> 16 passed.
    - First full e2e single-worker after implementation -> 562 passed, 1 failed, 8 skipped; failure was a test race waiting only for `POST` while the editor still had `/revalidate-public` and router navigation pending.
    - Targeted rerun after test wait update -> `tests/public-inline-editors-unsaved-warning.spec.ts` 2 passed.
    - Final full e2e single-worker -> 563 passed, 8 skipped.
    - `npm test -- --run` -> 51 files passed, 259 tests passed.
- [x] Generate mandatory audit report.
  - Verification: `backend/reports/public-on-demand-revalidation/public-on-demand-revalidation.md`, `.html`, and `.json` created.

## Scope Check

- Before execution: planned work was limited to public route invalidation after admin mutations and associated tests. UI redesign, backend API/auth changes, mobile/search/feed/autosave work, and broad refactors were out of scope.
- After execution: work remained within public revalidation helpers, admin mutation success wiring, cache tag support for public fetches, and tests that wait for the new revalidation completion signal. No backend APIs or auth semantics were changed.

## Invalidation Map

- Blog create/update/delete: `/`, `/blog`, `/blog/<nextSlug>`, and `/blog/<previousSlug>` when applicable.
- Work create/update/delete: `/`, `/works`, `/works/<nextSlug>`, and `/works/<previousSlug>` when applicable.
- Contact page save: `/contact`.
- Introduction page save: `/introduction`.
- Home page save: `/`.
- Resume upload/delete: `/resume`.
- Site settings save: `/`, `/blog`, `/works`, `/contact`, `/introduction`, and `/resume`.
- Public fetch cache tags are also invalidated for the same affected data groups so external public API fetch caches refresh immediately.

---

# Pagination And AI Provider Hotfix TODO - 2026-04-22

## User Instructions

- Fix broken/very slow Work and Study pagination.
- Remove the unwanted top save button/action area in the blog editor.
- Restore AI Fix provider options so OpenAI and Codex both appear when OpenAI API access is configured.
- Diagnose the reported main syntax error.
- Keep the fix focused and avoid unrelated redesign work.

## TODOs

- [x] Confirm branch and starting state.
  - Verification: branch `feat/phase-2-backend-discompostion` matches origin before edits.
- [x] Run skill discovery and read relevant local skills.
  - Verification: used local `tdd`, `nextjs-app-router-patterns`, and `vercel-react-best-practices`; `npx skills find nextjs app router pagination cache revalidation ai provider editor workflow` found external Next.js skills but no installed replacement needed.
- [x] Prepare backup before edits.
  - Verification: files backed up under `/tmp/woong-blog-pagination-ai-hotfix-backup-2026-04-22`.
- [x] Decouple public Study/Works pagination from server admin session checks and unnecessary header reads.
  - Verification: `src/test/public-admin-rendering.test.tsx` covers Study/Works list rendering without server session checks; focused Playwright pagination specs passed.
- [x] Remove BlogEditor top save/action bar while keeping bottom submit and keyboard save.
  - Verification: `src/test/blog-editor.test.tsx` and `tests/ui-admin-blog-excerpt.spec.ts` assert the top save action is absent and the bottom submit remains.
- [x] Restore OpenAI/Codex provider dropdown behavior.
  - Verification: backend runtime config returns `availableProviders:["openai","codex"]` when Codex command is configured; `src/test/admin-ai-fix-dialog.test.tsx` covers both options.
- [x] Run focused tests and syntax/type validation.
  - Verification:
    - `npx vitest run src/test/blog-editor.test.tsx src/test/public-admin-rendering.test.tsx src/test/admin-ai-fix-dialog.test.tsx --pool=threads` -> 3 files passed, 19 tests passed.
    - `dotnet test backend/WoongBlog.sln --filter BlogAiFixServiceCodexRuntimeComponentTests` -> component target passed, 4 tests passed; existing NU1901 warnings remain.
    - `npm run typecheck` -> passed.
    - `npm run build` -> initially failed on invalid segment config because list pages exported `revalidate` through an imported constant; fixed to literal `60`; rerun passed.
    - Docker compose frontend/backend rebuild -> passed.
    - Runtime config curl with admin cookie -> `availableProviders:["openai","codex"]`.
    - Focused Playwright pagination/editor/AI group -> 13 passed.
    - `npx eslint src tests playwright.config.ts next.config.ts eslint.config.mjs` -> 0 errors, 3 existing warnings.
    - `git diff --check` -> passed.
- [x] Generate mandatory audit report.
  - Verification: `backend/reports/pagination-ai-provider-hotfix/pagination-ai-provider-hotfix.md`, `.html`, and `.json` created.

## Scope Check

- Before execution: planned work is limited to public list pagination responsiveness, BlogEditor top save UI removal, AI provider availability, syntax/type validation, focused tests, and audit artifacts. Mobile redesign, feed redesign, autosave redesign, backend auth changes, and broad refactors are out of scope.
- After execution: actual work stayed within the hotfix scope. Public list server session fetching was removed, the BlogEditor top save bar was removed, AI provider choices now expose OpenAI and Codex when Codex command support exists, and the invalid segment config causing the build error was fixed.

---

# E2E Response-Time Measurement TODO - 2026-04-22

## User Instructions

- Redesign the full E2E response-time measurement and validation system.
- Every Playwright scenario should record timing data: total test duration, navigation duration, document navigation timing, `/api/**` response duration, browser interaction latency, max interaction latency, and slow interactions.
- Add explicit latency budgets for core user actions such as pagination, search, save/delete, public refresh after admin mutation, and AI dialog open.
- Attach per-test metrics with `testInfo.attach('e2e-latency.json', ...)`.
- Add `tests/helpers/performance-test.ts`, `tests/helpers/latency.ts`, `tests/performance-budgets.json`, `scripts/summarize-e2e-latency.mjs`, and a dedicated `tests/e2e-response-time.spec.ts`.
- Switch Playwright specs from `@playwright/test` to the local performance fixture.
- Split npm scripts for `test:e2e`, `test:e2e:latency`, and `test:e2e:exhaustive`, keeping single-worker execution.
- Use Docker/nginx compose with `PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000` for full validation where feasible.
- Run `npm run typecheck`, `npm test -- --run`, `npm run build`, `npm run test:e2e:latency`, `npm run test:e2e`, and `npm run test:e2e:exhaustive` before finalizing thresholds where feasible.

## TODOs

- [x] Confirm current repo state, applicable skills, and existing Playwright import/helper patterns.
  - Satisfies: fresh-context implementation and specialist skill requirement.
  - Verification: plugin cache paths verified; `tdd`, `nextjs-app-router-patterns`, `vercel-react-best-practices`, and installed `e2e-testing-patterns` skill read; `npx skills find "playwright performance"` and `npx skills find "e2e testing"` completed; existing Playwright imports and target specs inspected.
- [x] Prepare backup before code/config edits.
  - Satisfies: backup requirement.
  - Verification: backup files created under `/tmp/woong-blog-e2e-latency-backup-2026-04-22`.
- [x] Add behavior-focused unit tests for budget lookup, metric attachment helpers, and summary aggregation before implementation.
  - Satisfies: TDD requirement and new helper coverage.
  - Verification: `npx vitest run src/test/e2e-latency-budget.test.ts src/test/e2e-latency-summary.test.ts --pool=threads` -> 2 files passed, 9 tests passed.
- [x] Implement automatic Playwright latency collection and per-test attachment.
  - Satisfies: all scenarios record metrics.
  - Verification: `npm run test:e2e:latency` produced one `e2e-latency.json` artifact per selected test and summary artifacts.
- [x] Implement `measureStep` with hard/warn budgets and artifact participation.
  - Satisfies: core action latency enforcement.
  - Verification: focused latency run failed on hard budget during development, then passed after grounded readiness waits; final latency run -> 12 passed, 0 budget failures.
- [x] Add budget configuration and summary script.
  - Satisfies: declared budgets plus machine/human summaries.
  - Verification: `tests/performance-budgets.json`, `scripts/summarize-e2e-latency.mjs`, and `scripts/run-e2e-latency.mjs` added; summary files generated under `test-results/playwright/`.
- [x] Route Playwright specs through the performance fixture.
  - Satisfies: all specs collect automatic metrics.
  - Verification: `npx playwright test --list` listed 577 tests in 186 files with specs importing `./helpers/performance-test`; `npm run typecheck` passed.
- [x] Add explicit measured core flows for public pagination, AI dialog, and one admin save/public refresh path.
  - Satisfies: plan's core latency coverage.
  - Verification: measured Study/Works pagination, Works search, AI dialog open/provider readiness, site/page/resume/admin blog/work create/update/delete representative flows.
- [x] Add/update npm e2e scripts.
  - Satisfies: official full, latency, and exhaustive definitions.
  - Verification: `test:e2e`, `test:e2e:latency`, and `test:e2e:exhaustive` scripts updated to run through the latency summary wrapper with one worker where applicable.
- [x] Run targeted and feasible full validation, then record results.
  - Satisfies: completion validation requirements.
  - Verification:
    - `npm run typecheck` -> passed.
    - `npm test -- --run` -> 53 files passed, 270 tests passed.
    - `npm run build` -> passed.
    - `npx eslint src tests playwright.config.ts next.config.ts eslint.config.mjs` -> 0 errors, 3 existing warnings.
    - `git diff --check` -> passed.
    - `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 npm run test:e2e:latency` -> 12 passed, 12 latency artifacts, 0 budget failures, 0 warnings.
    - Full `npm run test:e2e` was run against compose. First full run after instrumentation: 566 passed, 3 failed, 8 skipped, 575 latency artifacts, 0 budget failures; failures were auth-policy expectation/env plus one import-rewrite typo. Targeted rerun after fixes: 7 passed, 1 skipped.
    - Second full run after auth fixes: 565 passed, 4 failed, 8 skipped, 575 latency artifacts, 0 budget failures; observed failures were order/timing-sensitive visitor works, home editor, renovation home reload, and public loading-state assertions. Targeted rerun after scoped stability fixes: visitor/home/renovation passed; loading-state was adjusted and targeted rerun passed.
    - Final full `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 npm run test:e2e` -> 569 passed, 8 skipped, 0 failed, 575 latency artifacts, 0 budget failures, 89 warnings.
- [x] Generate mandatory audit report.
  - Satisfies: AGENTS.md completion requirement.
  - Verification: `backend/reports/e2e-latency-instrumentation/e2e-latency-instrumentation.md`, `.html`, and `.json` created.

## Scope Check

- Before execution: planned work is limited to Playwright latency instrumentation, budget helpers/config, summary artifacts, core measured specs, package scripts, tests, validation, and audit artifacts. Product behavior/UI redesign, backend API changes, auth/security changes, and unrelated performance optimization are out of scope.
- After execution: actual work remained in Playwright/test infrastructure, latency budget helpers/config/scripts, targeted E2E specs, package scripts, validation stability fixes, and audit artifacts. No production app behavior or backend API semantics were intentionally changed.

---

# Frontend Performance Origin/Dev vs Current TODO - 2026-04-22

## User Instructions

- Benchmark `origin/dev` against the current working-tree version for runtime/load performance only.
- Do not estimate engineering time to implement.
- Measure user-facing load time, API response time, interaction readiness, and admin mutation-to-public-refresh time for changed feature areas.
- Compare baseline `origin/dev` in a temporary git worktree against current `feat/phase-2-backend-discompostion`, including uncommitted changes.
- Run each candidate through Docker/nginx on `http://127.0.0.1:3000`, Chromium only, one worker, and local admin shortcut enabled.
- Use equivalent seed data with at least 2 pages of Study posts, at least 2 pages of Works, editable blog/work, resume fixture, page records, and site settings.
- Persist Markdown, HTML, and JSON reports under `backend/reports/frontend-performance-origin-dev-vs-current/`.
- Classify improved/neutral/regression/correctness-only scenarios using median and p95 thresholds.
- Update this TODO/audit record with commands, report paths, summary result, and regressions or inconclusive paths.

## TODOs

- [x] Confirm repo state, relevant skills, and current benchmark/test conventions.
  - Satisfies: fresh-context implementation, specialist skill discovery, and repo workflow requirements.
  - Verification: plugin cache paths verified; `find-skills`, `tdd`, and `e2e-testing-patterns` read; `npx skills find playwright performance benchmark` found no stronger high-confidence skill to install.
- [x] Prepare backup before edits.
  - Satisfies: backup requirement.
  - Verification: backup directory `/tmp/woong-blog-frontend-performance-benchmark-backup-2026-04-22` created; `todolist-2026-04-22.md` and `.dockerignore` were copied there before edits.
- [x] Add behavior-focused test coverage for benchmark classification/report output.
  - Satisfies: TDD requirement for new benchmark logic.
  - Verification: `npx vitest run src/test/frontend-performance-benchmark.test.ts --pool=threads` -> 1 file passed, 1 test passed.
- [x] Implement external Docker/nginx benchmark runner.
  - Satisfies: baseline/current orchestration, seed data, Playwright/Chromium measurements, API timings, admin mutation timings, and report generation.
  - Verification: `scripts/benchmark-frontend-performance.mjs` added; `.dockerignore` now excludes local `.tmp` runtime data so Docker build contexts do not fail on local Postgres permissions.
- [x] Run focused validation and feasible benchmark execution.
  - Satisfies: verify runner and reports before completion.
  - Verification:
    - `node --check scripts/benchmark-frontend-performance.mjs` -> passed.
    - `npx eslint scripts/benchmark-frontend-performance.mjs src/test/frontend-performance-benchmark.test.ts` -> passed.
    - `git diff --check` -> passed.
    - `npm run typecheck` -> passed.
    - Current-only smoke: `node scripts/benchmark-frontend-performance.mjs --current-only --no-docker --warmups 0 --iterations 1 --mutation-warmups 0 --mutation-iterations 1 --base-url http://127.0.0.1:3000 --report-dir backend/reports/frontend-performance-origin-dev-vs-current-smoke` -> 45 scenarios, 0 failed after runner hardening.
    - Comparative Docker run: `node scripts/benchmark-frontend-performance.mjs --warmups 0 --iterations 1 --mutation-warmups 0 --mutation-iterations 1 --base-url http://127.0.0.1:32080 --http-port 32080 --https-port 32081 --backend-port 18180 --report-dir backend/reports/frontend-performance-origin-dev-vs-current` -> baseline and current built, served, seeded, and measured through Docker/nginx; 45 comparisons written.
- [x] Generate mandatory audit report.
  - Satisfies: AGENTS.md completion requirement.
  - Verification: `backend/reports/frontend-performance-origin-dev-vs-current/frontend-performance-origin-dev-vs-current-audit.md`, `.html`, and `.json` created.

## Scope Check

- Before execution: planned work is limited to benchmark tooling, tests for benchmark report behavior, generated benchmark/audit artifacts, and TODO updates. Production frontend/backend behavior, UI design, auth semantics, caching implementation, and feature code are out of scope.
- After execution: actual work stayed within benchmark tooling, one test for runner report behavior, `.dockerignore` hygiene for local `.tmp` build-context exclusion, generated benchmark/audit artifacts, and TODO updates. Production frontend/backend behavior, UI design, auth semantics, and feature code were not changed.

## Benchmark Summary

- Primary benchmark report paths:
  - `backend/reports/frontend-performance-origin-dev-vs-current/frontend-performance-origin-dev-vs-current.md`
  - `backend/reports/frontend-performance-origin-dev-vs-current/frontend-performance-origin-dev-vs-current.html`
  - `backend/reports/frontend-performance-origin-dev-vs-current/frontend-performance-origin-dev-vs-current.json`
- Audit report paths:
  - `backend/reports/frontend-performance-origin-dev-vs-current/frontend-performance-origin-dev-vs-current-audit.md`
  - `backend/reports/frontend-performance-origin-dev-vs-current/frontend-performance-origin-dev-vs-current-audit.html`
  - `backend/reports/frontend-performance-origin-dev-vs-current/frontend-performance-origin-dev-vs-current-audit.json`
- Feasible run used isolated Docker ports `32080/32081/18180` because the existing dev stack already occupied `127.0.0.1:3000`.
- Feasible run used `0` warmups and `1` measured iteration per scenario to keep runtime bounded after full Docker build/serve validation; this is a smoke comparison, not the full requested 5/25 and 2/10 statistical run.
- Candidate coverage: baseline `origin/dev` and current working tree both built, served, seeded, and produced 45 scenario entries.
- Summary result from the one-sample report:
  - Public cache/revalidation: 8 regressions, 9 improved, 11 neutral.
  - Resume PDF SSR: 1 regression, 3 improved, 2 neutral.
  - Study/Works pagination: 2 regressions, 1 improved, 3 neutral; current Study Next click had one unresolved timeout sample.
  - Admin editor UX: 3 regressions, 1 neutral.
  - AI Fix dialog: neutral latency; baseline correctness note records only `codex`, current records `openai, codex`.
- Yellow flags: one-sample timing is noisy and should not be treated as a release-blocking statistical conclusion; run the default benchmark without overriding iterations when a longer window is available and `127.0.0.1:3000` can be dedicated to the benchmark.

---

# Broad Frontend Refactor TODO - 2026-04-22

## User Instructions

- Implement a broad but disciplined frontend refactor on branch `feat/phase-2-backend-discompostion`.
- Preserve anonymous public reading, admin public affordances, centralized public-admin affordance direction, same-origin browser `/api` defaults, excerpt-first cards, fast public visibility after admin save/upload, and backend auth/security semantics.
- Work in priority order P0 through P6: runtime correctness, public/admin decoupling, on-demand invalidation, performance cleanup, reading UX, SEO metadata, and low-risk mobile/tablet polish.
- Do not stop after analysis; implement code first, then report.
- Run lint, typecheck, build, relevant Vitest, focused Playwright, full E2E, and performance comparison where feasible; be explicit about environment blockers.
- Generate persistent audit artifacts before completion.

## TODOs

- [x] Confirm current branch/worktree, plugin cache paths, relevant skills, and prior completed work so this refactor builds on the branch instead of reverting it.
  - Satisfies: repo workflow, specialist skill requirement, panic prevention, and no-revert constraints.
  - Verification: branch `feat/phase-2-backend-discompostion`; cache paths verified; local `find-skills`, `tdd`, `nextjs-app-router-patterns`, and `vercel-react-best-practices` used; `npx skills find "nextjs performance caching"` completed with no stronger installed replacement selected.
- [x] Prepare backup before production/test code edits.
  - Satisfies: backup requirement.
  - Verification: backup files copied under `/tmp/woong-blog-broad-frontend-refactor-backup-2026-04-22`.
- [x] Add/update focused tests for remaining route boundary, pagination, revalidation, metadata, and card/readability behavior.
  - Satisfies: TDD and feature coverage requirements.
  - Verification: added/updated `src/test/public-detail-boundary.test.tsx`, `src/test/responsive-page-size-sync.test.tsx`, `src/test/public-admin-client-gate.test.tsx`, `src/test/related-content-list.test.tsx`, and `src/test/seo-metadata.test.ts`; focused suites passed.
- [x] P0: verify `/resume` client-only PDF boundary, study/blog page 2 pagination, and public/admin affordances on list/detail routes.
  - Satisfies: runtime correctness/unblockers.
  - Verification: focused Playwright resume/pagination/public-admin tests passed; full E2E first run covered these flows; targeted reruns for affected failures passed.
- [x] P1: decouple public read data from admin detail edit payloads for blog/work detail routes without weakening admin affordances.
  - Satisfies: public caching/rendering cleanup and duplicate fetch avoidance.
  - Verification: public detail boundary tests prove blog/work detail server render avoids `fetchServerSession` and `fetchAdmin*ById`; new lazy admin action components load admin detail only after client gate/open.
- [x] P2: verify and preserve immediate public cache invalidation after contact/introduction/resume/blog/work mutations.
  - Satisfies: mutation-to-public-visible behavior.
  - Verification: existing public revalidation unit/editor tests passed in the 16-file focused group; full E2E mutation/public refresh paths were exercised.
- [x] P3: reduce grounded frontend regressions in study/blog/works cold loads, public API usage, and editor save/navigation where safe.
  - Satisfies: performance cleanup.
  - Verification: removed server admin/session fetches from public detail routes, deduped client admin session fetches, normalized list page sizes, and ran origin/dev benchmark smoke; benchmark still reports cold-load/API regressions for follow-up.
- [x] P4: improve public reading UX while keeping excerpt-first lightweight cards.
  - Satisfies: card/related section/readability requirements.
  - Verification: related-content unit tests and Playwright related/visual specs passed after fixes.
- [x] P5: normalize metadata/SEO basics and sitemap/robots coverage for key public routes.
  - Satisfies: SEO consistency requirement.
  - Verification: `seo-metadata` unit tests and `public-seo-metadata.spec.ts` passed; `robots.txt` curl returned 200.
- [x] P6: add only low-risk mobile/tablet polish after P0-P5 are stable.
  - Satisfies: mobile/tablet polish without risky redesign.
  - Verification: low-risk responsive behavior was limited to pagination/page-size and TOC overflow polish; broader mobile search/infinite-scroll work deferred.
- [x] Run final validation and generate mandatory audit report.
  - Satisfies: completion requirement.
  - Verification: validation results recorded in `backend/reports/broad-frontend-refactor-2026-04-22/broad-frontend-refactor-2026-04-22.md`, `.html`, and `.json`.

## Scope Check

- Before execution: planned work is limited to frontend runtime boundaries, public/admin decoupling, cache invalidation verification, grounded performance cleanup, public reading UX, metadata/SEO consistency, low-risk responsive polish, tests, validation, benchmark comparison, and audit artifacts. Backend auth/security semantics, backend architecture work, same-origin browser API defaults, heavy rendered card previews, broad infinite scroll/autosave redesigns, and unrelated folder-only churn are out of scope.
- After execution: actual work remained in frontend public routes/components/helpers/tests plus audit and benchmark artifacts. Backend auth/security semantics, browser `/api` defaults, excerpt-first cards, and the existing revalidation security model were preserved. Infinite scroll, autosave redesign, backend architecture, and heavy rendered card previews were not implemented.

## Validation Results

- Focused Vitest public/admin/revalidation/editor group: 16 files passed, 90 tests passed.
- Additional focused Vitest after fixes: blog editor, related content, responsive page-size, SEO, and public admin rendering groups passed.
- `npm run typecheck` -> passed.
- `npm run build` -> passed locally; Docker frontend rebuild passed.
- `npm run lint` -> blocked by existing `.tmp/postgres` permission error.
- `npx eslint src tests playwright.config.ts next.config.ts eslint.config.mjs` -> passed with 3 existing warnings.
- Full E2E single-worker latency run -> 560 passed, 8 failed, 9 skipped on the first full pass; all failed specs were targeted-rerun after fixes and passed, including `tests/public-edge-nav.spec.ts`.
- Origin/dev benchmark smoke regenerated Markdown/HTML/JSON reports under `backend/reports/frontend-performance-origin-dev-vs-current/`.
- Mandatory broad refactor audit generated under `backend/reports/broad-frontend-refactor-2026-04-22/`.
