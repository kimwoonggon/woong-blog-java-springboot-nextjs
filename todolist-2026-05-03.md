# TODO 2026-05-03 - Codex GPT-5.5 runtime env hotfix

## User instructions
- Fix AI provider/Codex model dropdown still showing only up to GPT-5.4.
- Restore visible AI Enrich/AI Fix execution buttons.
- Ensure GPT-5.5 appears in the runtime provider model options.
- Restart Docker dev stack after the fix.

## TODOs
- [x] Confirm whether code defaults or Docker runtime env is overriding GPT-5.5.
- [x] Update runtime env files so CODEX_MODEL defaults to gpt-5.5 and CODEX_ALLOWED_MODELS includes gpt-5.5 first.
- [x] Restart Docker dev stack so backend runtime config reloads the env.
- [x] Keep GPT-5.5 in the AI Fix/AI Enrich model list even when stale runtime/localStorage values are present.
- [x] Add a persistent header action button so AI Fix/AI Enrich can be executed without scrolling to the preview empty state.
- [x] Record audit artifacts for the hotfix.

## Verification notes
- Inspection found backend defaults already include gpt-5.5.
- Inspection found .env and env examples still set CODEX_MODEL/CODEX_ALLOWED_MODELS to gpt-5.4-only values.
- Updated .env, .env.example, .env.staging.example, and .env.prod.example.
- Docker dev stack rebuilt and restarted successfully after UI and env updates.

---

# TODO - 2026-05-03 - Home Works Mobile Overflow

## User Instructions

- Fix the mobile home page where the Works card area extends to the right beyond the navigation width and makes the ratio feel mismatched.

## Plan Alignment Check - Before Execution

- Target is public home only, specifically the featured Works section on mobile.
- Preserve existing desktop/tablet card layout unless the same width alignment issue is present there.
- Use TDD: add a behavior-focused Playwright regression before changing production code.
- Prepare backup before code/config edits.
- Generate persistent audit artifacts before completion.

## TODOs

- [x] Inspect current worktree and relevant home/navigation/Works files.
  - Verification: `git status --short`, `rg`, and targeted `sed` reads completed.
- [x] Search for relevant specialist skills before implementation.
  - Verification: `npx skills find nextjs responsive layout` found only low-install candidates; using existing installed Next.js/TDD skills instead.
- [x] Prepare backups for files expected to change.
  - Verification: `.codex-backups/home-works-mobile-overflow-2026-05-03/` contains backups of the home page and featured works Playwright spec.
- [x] Add a mobile regression test proving the Works section/cards stay within the same horizontal content width as the mobile navbar.
  - Verification: added `Works stays aligned to the mobile home content rail` in `tests/ui-improvement-featured-works-grid.spec.ts`.
- [x] Run the new targeted test and confirm it fails before implementation.
  - Verification: attempted `PLAYWRIGHT_E2E_PROFILE=exhaustive npx playwright test tests/ui-improvement-featured-works-grid.spec.ts --project=chromium-public --grep "Works stays aligned" --workers=1`; blocked before assertion by dev server backend connection failure to `127.0.0.1:8080`.
- [x] Fix the mobile Works section/card width alignment without broad layout churn.
  - Verification: removed negative horizontal margin compensation from the home featured Works section, so it follows the parent content rail naturally at all breakpoints.
- [x] Run focused Playwright validation and a relevant static check.
  - Verification: `npx eslint 'src/app/(public)/page.tsx' tests/ui-improvement-featured-works-grid.spec.ts` passed. `docker compose -f docker-compose.dev.yml up -d --build` built successfully through frontend `next build`, but default `127.0.0.1:8080` and `3001` port publishing failed in Docker; reran with `BACKEND_PUBLISH_PORT=18080 NGINX_HTTPS_PORT=3002` and validated `http://127.0.0.1:3000`. Focused Playwright passed after the no-negative-margin adjustment: `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 PLAYWRIGHT_E2E_PROFILE=exhaustive npx playwright test tests/ui-improvement-featured-works-grid.spec.ts --project=chromium-public --grep "Works stays aligned|Works collapses" --workers=1`.
- [x] Generate Markdown, HTML, and JSON audit reports.
  - Verification: generated `backend/reports/home-works-mobile-overflow-2026-05-03/home-works-mobile-overflow-2026-05-03.md`, `.html`, and `.json`.

## Progress Notes

- Existing unrelated worktree state observed: modified `image.png`; untracked `.hotfix-backup-*` and prior `backend/reports/*` directories. These will not be modified.
- Follow-up hardcoding cleanup: removed the remaining `md:-mx-6` compensation and named the representative phone viewport fixture in the Playwright spec.

---

# TODO - 2026-05-03 - Issue 29 Load Test Dashboard

## User Instructions

- Read the GitHub issue from `dev`.
- Create a branch from `dev`.
- Fix the issue, then run E2E tests.
- Push the result to the remote `dev` flow, then promote/push to `main`.

## Issue

- GitHub issue `#29`: "부하 테스트 측정"
- Requirement: provide a dedicated load-test dashboard to observe Work/Study read speed as simulated users increase by 100-user intervals up to 1000.

## Plan Alignment Check - Before Execution

- Branch: `feature/issue-29-load-test-dashboard` from `origin/dev`.
- Target an admin-only dashboard so load testing is not public-facing.
- Use a synthetic read-load runner against public Work/Study list and detail pages.
- Keep default scenario aligned with the issue: 100 to 1000 users in 100-user steps.
- Keep E2E validation small and deterministic by reducing the runtime scenario during the test.
- Preserve unrelated local dirty state (`image.png`, old hotfix/report directories).

## TODOs

- [x] Read issue and confirm the current open GitHub issue.
  - Verification: `gh issue view 29 --json ...` confirmed issue #29 requests load-test measurement for Work/Study read speed.
- [x] Search relevant skills before implementation.
  - Verification: `npx skills find playwright e2e github issue` found lower-confidence Playwright skills; using existing installed TDD, E2E, and Next.js skills.
- [x] Create issue branch from `origin/dev`.
  - Verification: `git switch -c feature/issue-29-load-test-dashboard origin/dev` succeeded.
- [x] Prepare backups for files expected to change.
  - Verification: `.codex-backups/issue-29-load-test-dashboard-2026-05-03/` contains backups for `AdminSidebarNav.tsx` and this TODO file before production code edits.
- [x] Add behavior-focused tests for load-test planning/statistics and admin dashboard flow.
  - Verification: initial `npx vitest run src/test/load-test-dashboard.test.ts` failed because `@/lib/load-test-dashboard` does not exist yet, establishing the RED state for the utility behavior.
  - User clarification: load testing must be real-time, so Playwright coverage must assert in-progress live results before the run completes.
  - Verification: `tests/admin-load-test-dashboard.spec.ts` initially failed because `/admin/load-test` did not exist, then passed after implementation with delayed responses proving `Running` and `1 / 2` appear before final completion.
  - Verification: `src/test/admin-sidebar-nav.test.tsx` now covers the Load Test nav item and active state.
- [x] Implement the admin load-test dashboard and navigation entry.
  - Verification: added `/admin/load-test`, `LoadTestDashboard`, shared load-test planning/statistics utilities, and `Load Test` admin sidebar nav entry.
- [x] Run focused unit tests, Playwright E2E, lint/type/build checks.
  - Verification: `npx vitest run src/test/load-test-dashboard.test.ts src/test/admin-sidebar-nav.test.tsx` passed 8/8.
  - Verification: focused ESLint for touched files passed.
  - Verification: Docker compose rebuild passed and included `/admin/load-test`; focused Playwright `tests/admin-load-test-dashboard.spec.ts` passed 1/1.
  - Verification: `npm run typecheck` passed.
  - Verification: `git diff --check` passed.
- [x] Generate persistent audit artifacts.
  - Verification: generated `backend/reports/issue-29-load-test-dashboard-2026-05-03/issue-29-load-test-dashboard-2026-05-03.md`, `.html`, and `.json`; JSON parsed successfully and final `git diff --check` passed.
- [ ] Push branch/dev and monitor CI.
- [ ] Promote to main after dev succeeds and monitor main CI.

---

# TODO - 2026-05-03 - Public Article Code And Mermaid GitHub Styling

## User Instructions

- In Study and Work reading pages, make code and Mermaid attachment/rendering areas look closer to GitHub.
- Make both light mode and dark mode readable.
- Fix Mermaid in light mode because it is currently hard to see.
- Remove the floating mac-style window dots/chrome above code blocks.

## Plan Alignment Check - Before Execution

- Target public article rendering surfaces shared by Study/blog and Work detail pages.
- Avoid content-specific hardcoding; use semantic CSS variables and renderer theme settings.
- Keep the editor/content data model unchanged.
- Use TDD with a public detail Playwright regression and component tests.
- Generate persistent audit artifacts before completion.

## TODOs

- [x] Inspect current prose code and Mermaid rendering paths.
  - Verification: found code block pseudo chrome in `src/app/globals.css` and Mermaid default/dark theme detection in `src/components/content/MermaidRenderer.tsx`.
- [x] Search for relevant specialist skills before implementation.
  - Verification: `npx skills find "markdown code mermaid github theme playwright"` found `microsoft/playwright@playwright-dev` and a low-install Mermaid skill; using existing installed TDD/UI/Next.js/E2E skills instead of installing new low-confidence skills.
- [x] Prepare backups for files expected to change.
  - Verification: `.codex-backups/public-article-code-mermaid-github-2026-05-03/` contains backups for CSS, Mermaid renderer/types, tests, and this TODO.
- [x] Add failing tests for GitHub-like code/Mermaid public detail styling.
  - Verification: `npx vitest run src/test/mermaid-renderer.test.tsx` failed before implementation because Mermaid still initialized with `default`/`dark` themes and lacked `mermaid-diagram-shell`; focused Playwright `DM-18` failed on the existing 16px/mac-chrome code block styling and Mermaid light surface mismatch.
- [x] Update code block CSS to remove mac chrome and use GitHub-like light/dark surfaces.
  - Verification: `DM-18` now asserts code block radius, GitHub light/dark colors, inline code background, contrast, and `::before`/`::after` chrome removal; focused Playwright passed after implementation.
- [x] Update Mermaid renderer theme variables so diagrams stay readable in light and dark mode.
  - Verification: `src/test/mermaid-renderer.test.tsx` confirms Mermaid initializes with `theme: 'base'` plus GitHub-readable light/dark theme variables; `DM-18b` confirms public Study and Work Mermaid surfaces in light/dark mode.
- [x] Run focused unit/Playwright checks and static validation.
  - Verification: Docker compose frontend rebuild completed through `next build`; `npx vitest run src/test/mermaid-renderer.test.tsx src/test/interactive-renderer.test.tsx` passed 13/13; focused Playwright `DM-18` passed 2/2; `npx eslint src/components/content/MermaidRenderer.tsx src/test/mermaid-renderer.test.tsx tests/dark-mode.spec.ts` passed; `git diff --check` passed.
- [x] Generate Markdown, HTML, and JSON audit reports.
  - Verification: generated `backend/reports/public-article-code-mermaid-github-2026-05-03/public-article-code-mermaid-github-2026-05-03.md`, `.html`, and `.json`; JSON parsed successfully and final `git diff --check` passed.

---

# TODO - 2026-05-03 - Home Works Mobile Overflow Follow-up

## User Instructions

- The Works cards still overflow to the right in mobile mode.
- Make Works fit the screen like the Study section.
- Even if the mobile viewport height is extremely reduced, keep the Works card width stable and inside the screen.

## Plan Alignment Check - Before Execution

- Target remains the public home page Works section.
- The likely issue is mobile grid sizing and unbroken card text, not only the outer section margin.
- Keep the fix content-responsive, avoiding viewport-specific hardcoded widths.
- Use Playwright to reproduce overflow and validate the fixed mobile behavior.

## TODOs

- [x] Re-check source, running compose output, and remote branch source for Works section classes.
  - Verification: source and `origin/dev`/`origin/main` have no `md:-mx-6`; the currently running compose frontend was stale and still rendered the old class.
- [x] Reproduce the real overflow failure with mobile Playwright metrics.
  - Verification: injecting a long unbroken Works title/category at 320px and 375px expanded the Works card to 2110px and `documentElement.scrollWidth` to 2143px.
- [x] Search for relevant specialist skills before implementation.
  - Verification: `npx skills find "responsive grid playwright"` found low-install responsive/grid options; using existing installed TDD, UI/UX, E2E, and Next.js skills instead.
- [x] Prepare backups for files expected to change.
  - Verification: `.codex-backups/home-works-mobile-overflow-followup-2026-05-03/` contains backups of the home page, Playwright spec, and TODO file.
- [x] Add a Playwright regression for long unbroken Works content on mobile.
  - Verification: added `Works cards do not overflow on mobile with long unbroken content`; it fails before implementation with `documentWidth` 2143px at a 320px viewport.
- [x] Fix Works mobile grid/card/text shrink behavior without hardcoded viewport widths.
  - Verification: Works grid now uses `grid-cols-1` on mobile and `min-w-0`/wrapping on the card text stack so long content cannot widen the grid track.
- [x] Rebuild or restart the dev frontend so Playwright validates the current source, not the stale container image.
  - Verification: `BACKEND_PUBLISH_PORT=18080 NGINX_HTTPS_PORT=3002 docker compose -f docker-compose.dev.yml up -d --build frontend nginx` rebuilt and restarted frontend/backend/nginx; `curl` confirmed current Works classes render from `http://127.0.0.1:3000`.
- [x] Run targeted Playwright and static checks.
  - Verification: `npx eslint 'src/app/(public)/page.tsx' tests/ui-improvement-featured-works-grid.spec.ts` passed. Focused Playwright grep for `Works cards do not overflow|Works stays aligned|Works collapses` passed 3/3. Full `tests/ui-improvement-featured-works-grid.spec.ts` passed 14/14. Manual Playwright metrics confirmed 320px/375px/768px document widths match viewport widths after injecting long Works content.
- [x] Add and validate a Playwright regression for extremely short mobile viewport heights.
  - Verification: added `Works card width stays stable when mobile viewport height is extremely short`; targeted Playwright passed at 320x240 and 375x260 height cases, and the full Works spec passed 15/15.
- [x] Generate updated persistent audit artifacts.
  - Verification: generated `backend/reports/home-works-mobile-overflow-followup-2026-05-03/home-works-mobile-overflow-followup-2026-05-03.md`, `.html`, and `.json`.
