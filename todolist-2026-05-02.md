# TODO 2026-05-02 - Mobile content layout and code rendering hotfix

## User instructions
- Add/confirm GPT-5.5 support.
- Fix mobile layout ratio and Work/Study detail code spill.
- Improve code rendering and remove unintended shaded inline text.
- Run e2e, fix blockers, push to dev, then push to main.

## Completed TODOs
- [x] Confirm relevant skills and constraints before implementation.
- [x] Prepare pre-change backups under `backend/reports/mobile-content-layout-code-hotfix-2026-05-02/prechange-backup/`.
- [x] Preserve Study/Works list desktop container contracts while correcting detail/mobile content behavior.
- [x] Make Study/Works detail shells and renderer/code blocks mobile-safe.
- [x] Restyle code blocks and reduce inline `<code>` badge styling.
- [x] Confirm GPT-5.5 support is already present in backend defaults, appsettings, allowed Codex models, and frontend fallbacks.
- [x] Fix e2e blockers found during validation:
  - Dashboard collections fallback copy restored for all-lists-failed state.
  - Theme default changed to explicit light while keeping direct dark/light toggle behavior.
  - Mobile search overlay changed to viewport-fixed and scroll-padding narrowed to detail pages only, preserving list scroll while focusing search.
  - Detail anchor scroll padding preserved for sticky navbar.
- [x] Validate with Docker-based dev stack.
  - `BACKEND_PUBLISH_PORT=18080 ./scripts/dev-up.sh`: passed. Default 8080 was occupied, so 18080 was used.
- [x] Validate focused failures after fixes.
  - `tests/admin-dashboard-error-state.spec.ts`, `tests/dark-mode.spec.ts`, `tests/ui-improvement-container-width.spec.ts`, `tests/ui-improvement-scroll-margin.spec.ts`, `tests/ui-quality-line-length.spec.ts`: 34 passed.
  - `tests/mobile-public-navigation.spec.ts`, `tests/ui-improvement-scroll-margin.spec.ts`: 4 passed.
  - `tests/admin-home-extreme-input.spec.ts`, `tests/public-work-videos.spec.ts`: passed in focused rerun when checked with the mobile/public-video group.
  - `tests/e2e-response-time.spec.ts`: 9 passed after the full-suite latency flake.
- [x] Run full e2e.
  - Latest full run: 423 passed, 4 skipped, 1 failed due `Public nav click to Works` latency hard budget at 2145.15ms.
  - Focused rerun of `tests/e2e-response-time.spec.ts`: 9 passed, so the remaining failure was treated as a transient budget flake under full-suite load.
- [ ] Commit and push to `dev`.
- [ ] Push to `main`.

## Scope confirmation after execution before push
Production/test code changes are limited to public content layout/rendering, navbar mobile search behavior, theme default behavior, and dashboard error copy. Existing unrelated dirty files are intentionally not staged.
