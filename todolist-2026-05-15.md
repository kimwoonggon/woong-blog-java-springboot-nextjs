# TODO: Public Detail UI And Promotion - 2026-05-15

## User Instructions Captured

- Make the `On This Page` right-side table of contents wider because it is too narrow.
- In light mode, make the reading area background for Work and Study detail pages plain white.
- Fix Study detail `More Studies` pagination so it is not limited to pages `1, 2`; it should page around the current item with left/right context.
- Run full E2E after the fixes.
- If local validation is clean, push to `dev`.
- If `dev` CI passes, promote to `main`.
- If `main` CI passes, complete deployment/publish through the repository flow.

## Constraints And Scope

- Work from a clean worktree based on `origin/dev`, not the dirty primary worktree.
- Keep edits scoped to public Work/Study detail UI, related content pagination/context, tests, TODO, and audit artifacts.
- Do not include unrelated dirty changes from the primary worktree.
- Use TDD-style focused tests before implementation where feasible.
- Run the repository's Docker-backed full E2E before promotion.
- Generate persistent audit artifacts before final completion.

## Skill Search And Specialist Guidance

- Used local skills: `find-skills`, `tdd`, `nextjs-app-router-patterns`, `e2e-testing-patterns`, `ui-ux-pro-max`.
- External skill search: `npx --yes skills find "nextjs ui pagination playwright"` returned low/directly less relevant pagination/Playwright skills; no new external skill was installed.

## Work Items

- [x] Create a clean worktree and confirm baseline.
  - Verification: worktree `/mnt/d/woong-blog/woong-blog-backend-architecture-renovate-0419/woong-blog-aspcore-nextjs-ui-fixes-20260515` created from `origin/dev` at `d0a4e42`; `git status --short --branch` is clean on `codex/public-detail-ui-pagination-20260515`.
- [x] Back up target files before code/test edits.
  - Verification: copied target frontend/backend/test files under `.agent-backups/public-detail-ui-pagination-2026-05-15/` before implementation edits.
- [x] Add/update focused tests for wider TOC rail, white light-mode detail reading surface, and current-centered related pagination.
  - Verification: focused Vitest RED showed 6 failures for new expected limit/order/current-centered behavior; component xUnit RED showed both detail context tests still returned newest-first rather than current-adjacent related items.
- [x] Update public blog/work detail layout so the TOC rail has a real wide right column.
  - Verification: changed blog/work detail XL grids from a flexible right `1fr` column to a `minmax(20rem,22rem)` right rail and removed the rail padding that consumed width.
- [x] Update detail reading surfaces so light mode is pure white in the Work and Study reading areas.
  - Verification: added white light-mode detail page shells for blog/work and kept reading body/content roots white, with dark-mode background preserved.
- [x] Update Study/Work detail related context and pagination so current-centered windows have enough left/right items.
  - Verification: raised detail context request/cap to 24, changed backend related context to balanced newer/older neighbors, sorted frontend related items by public chronology with current in-place, and fixed centered-window math for page sizes beyond 5.
- [x] Run focused frontend/backend tests for the changed behavior.
  - Verification: focused Vitest passed 4 files / 16 tests; focused component xUnit passed 2 tests; focused integration xUnit passed 2 tests.
- [x] Run lint, typecheck, and relevant backend tests.
  - Verification: targeted ESLint passed; `npm run typecheck` passed; focused backend integration passed with existing NU1901 AWSSDK.Core warning only; full `npm test` passed 93 files / 654 tests; full `dotnet test backend/WoongBlog.sln` passed component 133, unit 61, architecture 40, integration 234, contract 1 skipped.
- [x] Run Docker-backed full E2E.
  - Verification: Docker dev stack started with `BACKEND_PUBLISH_PORT=18082 NGINX_HTTP_PORT=13002 NGINX_HTTPS_PORT=13003 NGINX_DEFAULT_CONF=./nginx/default.conf ./scripts/dev-up.sh` after confirming default `18080` and nginx `3000/3002` host ports were unavailable in this local environment.
  - Verification: readiness passed for Docker daemon, backend `http://127.0.0.1:18082/api/health`, and frontend `http://127.0.0.1:13002`.
  - Verification: first full E2E attempt found a real contract failure in `tests/ui-improvement-related-content-width.spec.ts`; fixed by widening the detail page shell from `max-w-7xl` to `max-w-[82rem]` so the wider TOC rail does not squeeze the reading column.
  - Verification: focused Playwright rerun passed `tests/ui-improvement-related-content-width.spec.ts` and `tests/ui-quality-blog-detail-visuals.spec.ts` together: 7 passed.
  - Verification: final Docker-backed core full E2E passed: 432 passed, 4 skipped, 0 failed, 15.5m. Latency artifacts 436, budget failures 0, warnings 81.
- [x] Generate Markdown/HTML/JSON audit artifacts.
  - Verification: wrote `backend/reports/public-detail-ui-pagination-2026-05-15/public-detail-ui-pagination-2026-05-15.md`, `.html`, and `.json`.
- [ ] Commit, push, open PR to `dev`, and wait for dev CI.
  - Verification pending.
- [ ] Promote the green dev state to `main`, wait for main CI, and verify publish/deploy gate.
  - Verification pending.

## Plan Match Check Before Implementation

- The current plan matches the user's requested UI and pagination fixes plus the full promotion flow.
- Implementation has not started yet.
- The dirty primary worktree is intentionally excluded from this task.

## Plan Match Check After Local Validation

- The completed local work still matches the requested scope: public Work/Study detail UI width, light-mode white surfaces, Study/Work related pagination context, and tests only.
- No production secrets, local certs, Playwright storage state, Docker runtime artifacts, or unrelated primary-worktree changes are included.
- Local validation is sufficient to proceed to audit artifact generation and then the dev PR/CI gate.
