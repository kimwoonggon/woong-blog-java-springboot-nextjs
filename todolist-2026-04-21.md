# Phase 2 Stabilization TODO - 2026-04-21

## User Instructions

- Strengthen architecture tests so physical Application/Domain/Infrastructure/API boundaries cannot regress.
- Clean up DI/composition with clearer `AddApplication()` and `AddInfrastructure()` style boundaries when appropriate.
- Review and safely reduce remaining split-weakening coupling without behavior redesign.
- Preserve public/admin endpoint contracts, routes, DTO JSON shape, EF design-time behavior, and runtime behavior.
- Update stale README/backend path docs if low-risk.
- Reclassify or note tests that behave more like component/integration tests than true unit tests.
- Do not split per module, redesign handlers/stores, change endpoint routes/DTO shape, or reintroduce broad facade services.
- Use subagents strategically.

## TODOs

- [x] Prepare backup before code/config/doc edits.
  - Satisfies: Agent workflow backup requirement.
  - Verification: Backup files created under `/tmp/woong-blog-phase2-stabilization-backup-2026-04-21`.
- [x] Run skill discovery and select implementation guidance.
  - Satisfies: Specialist skill requirement.
  - Verification: `npx skills find dotnet architecture tests dependency injection` found only low-install third-party skills; proceeding with local `tdd` and `dotnet-best-practices`.
- [x] Use subagents strategically for bounded review.
  - Satisfies: User request to use subagents strategically.
  - Verification: Read-only subagents reviewed DI/composition cautions and active doc/test taxonomy updates; edits remained local to avoid conflicts in the dirty worktree.
- [x] Add explicit architecture boundary tests.
  - Satisfies: no Application -> Infrastructure, no Domain -> Application/Infrastructure, no HTTP result types in Application, no service locator/scope usage in Application, HTTP-agnostic `AiActionResult<T>` and `WorkVideoResult<T>`.
  - Verification: `dotnet test backend/tests/WoongBlog.Api.ArchitectureTests/WoongBlog.Api.ArchitectureTests.csproj` -> 24 passed.
- [x] Introduce clearer Application and Infrastructure composition entry points.
  - Satisfies: cleaner DI/composition and clearer `Program.cs`.
  - Verification: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName!~PostgresPersistenceContractTests"` -> 92 passed.
- [x] Safely clean DbContext search synchronization coupling.
  - Satisfies: review `WoongBlogDbContext` Application support-helper usage without redesign.
  - Verification: `SaveChanges_PopulatesContentSearchFields` covered by filtered integration suite; Postgres persistence contract also passed separately.
- [x] Update stale active backend docs.
  - Satisfies: README/backend path docs low-risk update.
  - Verification: README and `docs/quality-verification-matrix.md` reviewed in final diff.
- [x] Note component-style tests currently living in unit test project.
  - Satisfies: reclassify/note integration/component-like tests without moving suites.
  - Verification: README taxonomy note added for `PublicQueryHandlerTests`, `AuthRecorderTests`, `BlogAiFixServiceCodexRuntimeTests`, and `CodexRuntimeEnvironmentTests`.
- [x] Run targeted backend verification and record results.
  - Satisfies: tests for every developed feature and preservation of runtime behavior.
  - Verification:
    - `dotnet test backend/tests/WoongBlog.Api.ArchitectureTests/WoongBlog.Api.ArchitectureTests.csproj` -> 24 passed.
    - `dotnet test backend/tests/WoongBlog.Api.UnitTests/WoongBlog.Api.UnitTests.csproj` -> 38 passed.
    - `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName!~PostgresPersistenceContractTests"` -> 92 passed.
    - `dotnet test backend/tests/WoongBlog.Api.ContractTests/WoongBlog.Api.ContractTests.csproj` -> 1 passed.
    - `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~PostgresPersistenceContractTests"` -> 1 passed.
    - All dotnet test runs still report existing NU1901 low-severity `AWSSDK.Core` warning.
- [x] Fix full e2e stack failures.
  - Satisfies: user request to catch the failed e2e tests.
  - Verification:
    - Initial full e2e stack run: `558 passed`, `5 failed`, `8 skipped`.
    - Focused rerun through package script: `12 passed`.
    - Final full e2e stack run: `563 passed`, `8 skipped`.
    - Fix: `npm run test:e2e:stack` now sets `PLAYWRIGHT_EXPECT_LOCAL_ADMIN_SHORTCUT=visible`, matching `docker-compose.dev.yml` and `./scripts/dev-up.sh`.

## Scope Check

- Before execution: planned work matches the TODO list above. Behavior redesign, route changes, DTO JSON changes, per-module physical splitting, and broad facade services are out of scope.
- After execution: actual changes still match the TODO list. No endpoint routes, DTO JSON shapes, handler/store behavior, per-module physical split, or broad facade services were changed.
- After e2e follow-up: failure fix was limited to Playwright dev-stack environment configuration. Product UI/API code was not changed for the e2e failures.

---

# Public Session + Blog Editor QoL TODO - 2026-04-21

## User Instructions

- Implement one coherent PR on `feat/phase-2-backend-discompostion`.
- Focus only on public/admin session rendering consistency, blog editor quality-of-life improvements, and removing the contact-page authored-content override.
- Keep backend auth/security semantics, role checks, session checks, and `/auth/session` response compatibility intact.
- Keep public read pages accessible to anonymous users.
- Do not introduce a broad rewrite, mobile infinite scroll, mobile/feed redesign, rendered-content card snippets, broad card density tuning, deeper related-content refactors, or autosave debounce redesign.
- Preserve excerpt-first blog card and related-card presentation.
- Use TDD with focused tests, run relevant frontend validation, and generate a mandatory audit report.

## TODOs

- [x] Confirm plan still matches the user scope before execution.
  - Satisfies: one coherent PR scope control and explicit deferrals.
  - Verification: scope check recorded below before production/test edits.
- [x] Prepare backup before code/test/config edits.
  - Satisfies: Agent workflow backup requirement.
  - Verification: backup files created under `/tmp/woong-blog-public-session-blog-editor-qol-backup-2026-04-21`.
- [x] Add or update focused tests first.
  - Satisfies: TDD and coverage for anonymous rendering, admin affordance visibility, contact fallback removal, excerpt preservation, and quick-save smoke behavior.
  - Verification: initial targeted RED run `npx vitest run src/test/blog-editor.test.tsx src/test/public-admin-rendering.test.tsx --pool=threads` failed as expected on missing top quick-save/workspace, redundant public layout session fetch, async `PublicAdminLink`, and contact fallback/page gate behavior.
- [x] Centralize public admin affordance visibility.
  - Satisfies: remove redundant public layout session fetch and reduce scattered session checks across blog list/detail, works list/detail, contact, introduction, and resume.
  - Verification: targeted Vitest passed; `rg "fetchServerSession|session\\.authenticated|role === 'admin'|role !== 'admin'" -n 'src/app/(public)' src/components/admin/PublicAdminLink.tsx src/lib/auth/public-admin.ts src/components/layout/Navbar.tsx` shows session/role checks only in `src/lib/auth/public-admin.ts`.
- [x] Remove contact fallback direct email injection.
  - Satisfies: authored contact content is respected as-is while keeping inline edit entry point.
  - Verification: targeted Vitest passed; `rg "Direct email|woong@example.com|__qaNoMailto|hasMailto|fallbackEmail" -n src tests` shows those strings only in tests that assert absence.
- [x] Improve BlogEditor workflow ergonomics.
  - Satisfies: top quick save/update, bottom submit retained, edit back/delete shell behavior preserved, horizontal editor resizing, excerpt-aware save/update and inline data flow.
  - Verification: targeted Vitest passed for top quick-save, excerpt update payloads, inline update routing, and resizable workspace.
- [x] Run relevant frontend validation.
  - Satisfies: requested lint/typecheck/test verification.
  - Verification:
    - `npx vitest run src/test/api-base.test.ts src/test/auth-csrf.test.ts src/test/auth-login-url.test.ts src/test/page-editor.test.tsx src/test/blog-editor.test.tsx src/test/public-admin-rendering.test.tsx --pool=threads` -> 6 files passed, 38 tests passed.
    - `npm run lint` -> passed with 5 existing warnings.
    - `npm run typecheck` -> passed.
    - `npm test -- --run` -> 47 files passed, 247 tests passed.
    - `BACKEND_PUBLISH_PORT=18080 NGINX_DEFAULT_CONF=./nginx/default.conf ./scripts/dev-up.sh` -> Docker dev stack rebuilt and started; Next build passed.
    - Focused Playwright subset -> 14 passed, 1 existing skipped.
    - Full Playwright with 4 workers -> 552 passed, 10 failed, 9 skipped.
    - Serial rerun of the failed full-e2e lines -> 29 passed.
- [x] Generate mandatory audit report.
  - Satisfies: AGENTS.md completion requirement.
  - Verification: `backend/reports/public-session-blog-editor-qol-audit/public-session-blog-editor-qol-audit.md`, `.html`, and `.json` updated.

## Scope Check

- Before execution: planned work is limited to public admin session rendering cleanup, contact authored-content fidelity, and BlogEditor QoL/excerpt preservation. Backend endpoint contracts, auth semantics, public anonymous access, mobile/feed/search redesign, card-content rendering, broad density tuning, related-content rewrites, and autosave redesign are out of scope.
- After execution: actual work stayed within the planned public/admin session cleanup, contact authored-content fidelity, BlogEditor QoL/excerpt data flow, and the small same-origin browser API base fix needed for admin browser mutations under CSP. Backend auth/session contracts, public read access, excerpt-first cards, mobile/feed/search redesign, card density tuning, related-content refactors, and autosave debounce redesign were not changed.

---

# Test Rebalancing TODO - 2026-04-21

## User Instructions

- Rebalance backend tests toward architecture tests, integration tests, component-style integration tests, and focused unit tests.
- Move or relabel `WoongBlog.Api.UnitTests` tests that instantiate Infrastructure, EF InMemory, ASP.NET runtime objects, filesystem/process fakes, or `HttpClient`.
- Preserve production behavior and physical boundaries.
- Do not split modules further, redesign handlers/stores, change routes/DTO JSON shape, reintroduce broad facades, normalize namespaces, split AI options, relocate middleware, or change EF migration workflow.
- Use subagents if needed.

## TODOs

- [x] Prepare backup before test/project edits.
  - Satisfies: Agent workflow backup requirement.
  - Verification: Backup files created under `/tmp/woong-blog-test-rebalancing-backup-2026-04-21`.
- [x] Run skill discovery and select implementation guidance.
  - Satisfies: Specialist skill requirement.
  - Verification: `npx skills find dotnet test taxonomy component integration architecture` found only low-install third-party skills; proceeding with local `tdd` and `dotnet-best-practices`.
- [x] Use subagent for bounded read-only test taxonomy review.
  - Satisfies: user request to use subagents if needed.
  - Verification: subagent requested to classify backend tests and recommend safe moves.
- [x] Create a clearly named backend component test project.
  - Satisfies: reduce ambiguity for tests that are not true unit tests and not full endpoint integration tests.
  - Verification: `WoongBlog.Api.ComponentTests` added to `backend/WoongBlog.sln`; `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj` -> 26 passed.
- [x] Move Infrastructure/EF/ASP.NET/filesystem/HttpClient-backed tests out of UnitTests.
  - Satisfies: test taxonomy rebalancing.
  - Verification: `rg` found no Infrastructure, ASP.NET, EF InMemory, HttpClient, filesystem/process, or `DefaultHttpContext` usage under `backend/tests/WoongBlog.Api.UnitTests`; UnitTests now references only Application.
- [x] Keep architecture boundary tests green and strengthen only if needed.
  - Satisfies: preserve Phase 2 boundaries.
  - Verification: `UnitTestProject_DoesNotReference_Infrastructure_AspNetCore_Or_EfInMemory` added; `dotnet test backend/tests/WoongBlog.Api.ArchitectureTests/WoongBlog.Api.ArchitectureTests.csproj` -> 25 passed.
- [x] Add or confirm representative integration/component coverage.
  - Satisfies: Content command, Content query, AI flow, WorkVideo flow, composition root sanity.
  - Verification: inventory recorded in `backend/reports/test-rebalancing/test-inventory.md`; existing IntegrationTests cover Content command, AI endpoint, WorkVideo endpoint, and composition root flows; ComponentTests cover Content query handlers and infrastructure-backed service behavior.
- [x] Run targeted backend test verification and record results.
  - Satisfies: ensure moved/rebalanced tests still pass.
  - Verification:
    - `dotnet test backend/tests/WoongBlog.Api.UnitTests/WoongBlog.Api.UnitTests.csproj` -> 12 passed.
    - `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj` -> 26 passed.
    - `dotnet test backend/tests/WoongBlog.Api.ArchitectureTests/WoongBlog.Api.ArchitectureTests.csproj` -> 25 passed.
    - `dotnet test backend/WoongBlog.sln` -> Contract 1, Unit 12, Component 26, Architecture 25, Integration 93 passed.
    - `dotnet test backend/WoongBlog.sln --filter "Category=Unit"` -> Unit 12 passed.
    - `dotnet test backend/WoongBlog.sln --filter "Category=Component"` -> Component 26 passed.
    - Existing NU1901 low-severity `AWSSDK.Core` warning remains.
- [x] Generate mandatory audit report.
  - Satisfies: AGENTS.md completion requirement for non-trivial PR-sized changes.
  - Verification: `backend/reports/test-rebalancing-audit/test-rebalancing-audit.md`, `.html`, and `.json` created.

## Scope Check

- Before execution: planned work is test/project taxonomy only. No production behavior redesign, route/DTO changes, module split, namespace normalization, AI options split, middleware relocation, or EF migration changes are in scope.
- After execution: actual work stayed within test/project taxonomy. The only production-code change was adding `InternalsVisibleTo("WoongBlog.Api.ComponentTests")` so moved infrastructure component tests retain prior internal access.

---

# WorkVideo Chapter 2.5 Deepening TODO - 2026-04-21

## User Instructions

- Target WorkVideo for the next selective chapter2.5 modular deepening PR.
- Improve WorkVideo internal modularity and ownership clarity without changing runtime behavior.
- Review `StartWorkVideoHlsJobCommandHandler` and extract only clearly separable orchestration helpers/support where low-risk.
- Review `IWorkVideoCommandStore` responsibilities and split only if small and appropriate.
- Preserve endpoint routes, request/response JSON shape, upload/confirm/reorder/delete/HLS/playback behavior, and API result mapping.
- Do not split WorkVideo into its own csproj, redesign subsystem-wide behavior, change public behavior, reintroduce facades, normalize namespaces, or touch unrelated modules.

## TODOs

- [x] Prepare backup before WorkVideo code edits.
  - Satisfies: Agent workflow backup requirement.
  - Verification: Backup files created under `/tmp/woong-blog-workvideo-deepening-backup-2026-04-21`.
- [x] Run skill discovery and select implementation guidance.
  - Satisfies: Specialist skill requirement.
  - Verification: `npx skills find dotnet refactoring architecture testing` found only low-install third-party skills; proceeding with local `tdd`, `dotnet-best-practices`, and `refactor-method-complexity-reduce`.
- [x] Extract low-risk HLS job planning support from `StartWorkVideoHlsJobCommandHandler`.
  - Satisfies: reduce handler density without behavior redesign.
  - Verification: `WorkVideoHlsJobPlanTests` added; UnitTests passed.
- [x] Split background cleanup persistence responsibility from `IWorkVideoCommandStore`.
  - Satisfies: reduce mixed command/session/cleanup responsibilities.
  - Verification: `IWorkVideoCleanupStore` and `WorkVideoCleanupStore` added; WorkVideo endpoint and startup composition tests passed.
- [x] Keep current handler -> store/port -> adapter direction intact.
  - Satisfies: preserve Phase 2/2.5 architecture direction.
  - Verification: ArchitectureTests passed with a new WorkVideo command-store cleanup boundary guard.
- [x] Run targeted and full backend verification.
  - Satisfies: preserve runtime behavior.
  - Verification:
    - `dotnet test backend/tests/WoongBlog.Api.UnitTests/WoongBlog.Api.UnitTests.csproj` -> 13 passed.
    - `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~WorkVideoEndpointsTests|FullyQualifiedName~StartupCompositionTests"` -> 9 passed.
    - `dotnet test backend/tests/WoongBlog.Api.ArchitectureTests/WoongBlog.Api.ArchitectureTests.csproj` -> 26 passed.
    - `dotnet test backend/WoongBlog.sln` -> Contract 1, Unit 13, Component 26, Architecture 26, Integration 93 passed.
    - `npm run test:e2e:stack` -> 561 passed, 8 skipped, 2 failed. WorkVideo e2e flows passed; failures were `/blog` mobile overflow and mermaid batch prompt save.
    - Existing NU1901 low-severity `AWSSDK.Core` warning remains.
- [x] Generate mandatory audit report.
  - Satisfies: AGENTS.md completion requirement for non-trivial PR-sized changes.
  - Verification: `backend/reports/workvideo-deepening-audit/workvideo-deepening-audit.md`, `.html`, and `.json` created.

## Scope Check

- Before execution: planned work is limited to WorkVideo HLS planning support, WorkVideo cleanup store responsibility split, focused tests, and audit artifacts.
- After execution: actual code changes stayed within WorkVideo Application/Infrastructure/Api registration plus tests; endpoint routes, DTO JSON shape, API result mapping, and external behavior were not changed.

---

# UI E2E Fix + AI Batch Deepening TODO - 2026-04-21

## User Instructions

- First fix the two known unrelated full-e2e failures: `/blog` mobile horizontal overflow and mermaid batch prompt textarea value not updating.
- Then proceed to AI batch selective chapter2.5 deepening without further user intervention.
- Use subagents if useful.
- Preserve backend architecture, endpoint routes, public API contracts, DTO JSON shape, and current behavior.
- Do not split modules into separate csproj projects, reintroduce broad facades, normalize namespaces, relocate middleware, or redesign unrelated areas.

## TODOs

- [x] Verify plugin cache paths and prepare backup.
  - Satisfies: panic prevention and backup requirement.
  - Verification: plugin cache paths resolved; backup files created under `/tmp/woong-blog-ui-fix-ai-batch-backup-2026-04-21`.
- [x] Run skill discovery and choose guidance.
  - Satisfies: specialist knowledge requirement.
  - Verification: `npx skills find playwright responsive textarea state testing ai batch dotnet refactor` found no suitable high-confidence skill; using local `ui-ux-pro-max`, `tdd`, and backend refactoring guidance.
- [x] Use subagents for bounded read-only investigation.
  - Satisfies: user permission to use subagents.
  - Verification: one subagent assigned UI/test failure investigation, one assigned AI batch boundary review.
- [x] Fix `/blog` mobile horizontal overflow.
  - Satisfies: known e2e failure fix.
  - Verification: `npm run test:e2e:stack -- tests/ui-pub-overflow.spec.ts:6 tests/mermaid-batch-prompt-0419.spec.ts:126` -> 9 passed.
- [x] Fix mermaid batch prompt textarea update failure.
  - Satisfies: known e2e failure fix.
  - Verification: `npm run test:e2e:stack -- tests/ui-pub-overflow.spec.ts:6 tests/mermaid-batch-prompt-0419.spec.ts:126` -> 9 passed.
- [x] Re-run focused e2e failures and full e2e if feasible.
  - Satisfies: prove branch is ready for AI batch deepening.
  - Verification:
    - Focused e2e -> 9 passed.
    - Initial full e2e -> 561 passed, 8 skipped, 2 failed.
    - Final full e2e after fixes and rebuild -> 563 passed, 8 skipped.
- [x] Deepen AI batch persistence boundaries conservatively.
  - Satisfies: AI batch chapter2.5 goal.
  - Verification: narrow target/job query/command interfaces added; direct aggregate store usage remains only in compatibility/DI/concrete store.
- [x] Add/update AI batch architecture/integration/unit tests.
  - Satisfies: boundary and behavior coverage.
  - Verification: Architecture guard added; startup composition resolves new AI batch store interfaces; Admin AI endpoint focused tests passed.
- [x] Run backend verification.
  - Satisfies: preserve behavior and architecture.
  - Verification:
    - `npx vitest run src/test/admin-blog-batch-ai-panel.test.tsx --pool=threads` -> 10 passed.
    - `npm run typecheck` -> passed.
    - `npm run test:e2e:stack -- tests/ui-pub-overflow.spec.ts:6 tests/mermaid-batch-prompt-0419.spec.ts:126` -> 9 passed.
    - `dotnet test backend/tests/WoongBlog.Api.UnitTests/WoongBlog.Api.UnitTests.csproj` -> 13 passed.
    - `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~AdminAiEndpointsTests|FullyQualifiedName~StartupCompositionTests"` -> 16 passed.
    - `dotnet test backend/tests/WoongBlog.Api.ArchitectureTests/WoongBlog.Api.ArchitectureTests.csproj` -> 27 passed.
    - `dotnet test backend/WoongBlog.sln` -> Contract 1, Unit 13, Component 26, Architecture 27, Integration 93 passed.
    - `npm run test:e2e:stack` -> 563 passed, 8 skipped.
- [x] Generate mandatory audit report.
  - Satisfies: AGENTS.md completion requirement.
  - Verification: `backend/reports/ui-e2e-fix-ai-batch-deepening-audit/ui-e2e-fix-ai-batch-deepening-audit.md`, `.html`, and `.json` created.

## Scope Check

- Before execution: planned work is limited to two known UI/test e2e fixes, AI batch persistence-boundary deepening, tests, and audit artifacts. No routes, DTO shape, module csproj split, broad redesign, namespace normalization, middleware relocation, or unrelated module work is in scope.
- After execution: actual work stayed within the two UI/test fixes plus AI batch store interface segregation. No routes, DTO shape, module csproj split, broad facade, namespace normalization, middleware relocation, or unrelated backend module redesign was introduced.

---

# Phase 2.6 Ownership & Composition Consolidation TODO - 2026-04-21

## User Instructions

- Reduce leftover compatibility and ownership friction after selective deepening.
- Improve composition ownership/readability.
- Preserve runtime behavior, endpoint routes, DTO JSON shape, and current physical boundaries.
- Prepare branch for review/merge rather than starting another large redesign.
- Include AI aggregate cleanup, Infrastructure-owned module registration cleanup, conservative AI options review, and Identity naming/path clarity cleanup.
- Do not do atomic claim/DB concurrency redesign, per-module csproj split, AI batch behavior redesign, WorkVideo behavior redesign, broad namespace normalization, middleware relocation, EF migrations workflow, route/DTO changes, or broad facades.

## TODOs

- [x] Audit remaining `IAiBlogFixBatchStore` usage.
  - Satisfies: AI compatibility aggregate cleanup.
  - Verification: `rg` now finds no production source usage of `IAiBlogFixBatchStore`; aggregate interface removed.
- [x] Replace aggregate usage with narrower interfaces.
  - Satisfies: reduce compatibility friction.
  - Verification: AI handlers/orchestrators use `IAiBatchTargetQueryStore`, `IAiBatchJobQueryStore`, and/or `IAiBatchJobCommandStore`.
- [x] Move Infrastructure-owned module registrations behind Infrastructure helpers.
  - Satisfies: composition ownership cleanup.
  - Verification: Api module registration files now delegate to `Add*Infrastructure` helpers instead of importing concrete Persistence/Storage/Policies providers.
- [x] Review AI options ownership and defer risky split.
  - Satisfies: AI options ownership review.
  - Verification: no options split made; recorded as deferred because it touches runtime config, validation, provider availability, and infrastructure provider settings.
- [x] Clean up Identity infrastructure naming/path clarity.
  - Satisfies: local identity ownership cleanup.
  - Verification: `IIdentityInteractionService` and `IdentityInteractionService` moved from Infrastructure `Application` path/namespace to Infrastructure `Infrastructure` path/namespace.
- [x] Add/update architecture/integration tests.
  - Satisfies: protect cleaned boundaries.
  - Verification: architecture guards added for aggregate removal and Api module registration concrete adapter imports; startup composition resolves narrow AI batch stores.
- [x] Run backend, frontend, focused e2e, and full e2e verification.
  - Satisfies: behavior preservation.
  - Verification:
    - `npx vitest run src/test/admin-blog-batch-ai-panel.test.tsx --pool=threads` -> 10 passed.
    - `npm run typecheck` -> passed.
    - Focused e2e -> 9 passed.
    - `dotnet test backend/tests/WoongBlog.Api.UnitTests/WoongBlog.Api.UnitTests.csproj` -> 13 passed.
    - `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~AdminAiEndpointsTests|FullyQualifiedName~StartupCompositionTests"` -> 16 passed.
    - `dotnet test backend/tests/WoongBlog.Api.ArchitectureTests/WoongBlog.Api.ArchitectureTests.csproj` -> 28 passed.
    - `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~StartupCompositionTests|FullyQualifiedName~AdminAiEndpointsTests|FullyQualifiedName~AuthEndpointsTests|FullyQualifiedName~AdminMembersEndpointsTests|FullyQualifiedName~UploadsControllerTests|FullyQualifiedName~WorkVideoEndpointsTests"` -> 35 passed.
    - `dotnet test backend/WoongBlog.sln` -> Contract 1, Unit 13, Component 26, Architecture 28, Integration 93 passed.
    - `npm run test:e2e:stack` -> 563 passed, 8 skipped.
- [x] Generate mandatory audit report.
  - Satisfies: AGENTS.md completion requirement.
  - Verification: `backend/reports/phase2-6-ownership-composition-audit/phase2-6-ownership-composition-audit.md`, `.html`, and `.json` created.

## Scope Check

- Before execution: planned work is limited to ownership/composition consolidation, UI/e2e known failure cleanup, AI batch narrow-interface cleanup, tests, and audit artifacts.
- After execution: actual work preserved routes, DTO JSON shape, physical project boundaries, AI/WorkVideo runtime behavior, and existing endpoint contracts. No atomic claim redesign, per-module csproj split, broad namespace normalization, middleware relocation, EF migrations workflow, or broad facade was introduced.

---

# Frontend CI Pact Fix TODO - 2026-04-21

## User Instructions

- Explain and fix the failing `Frontend lint, types, and unit tests` CI check on PR #13.
- Keep the fix scoped and do not change backend architecture or production behavior.
- Commit and push so the normal origin/PR CI flow reruns.

## TODOs

- [x] Verify plugin cache paths and prepare backup.
  - Satisfies: panic prevention and backup requirement.
  - Verification: `/home/kimwoonggon/.codex/plugins/cache` and `/home/kimwoonggon/.codex/.tmp/plugins` resolved; backup copied to `/tmp/codex-backups/woong-blog/2026-04-21-ci-pact-fix/public-api-consumer.pact.test.ts.before`.
- [x] Identify the failing CI error.
  - Satisfies: user request to explain the error.
  - Verification: `gh run view 24705473556 --job 72257705668 --log` showed one failed test: `src/test/pact/public-api-consumer.pact.test.ts` site settings Pact case rejected because `next/headers` was called outside a request scope.
- [x] Apply the smallest safe test fix.
  - Satisfies: keep production/backend architecture untouched.
  - Verification: `withServerApi` now mocks `next/headers` with the Pact mock server host/proto/cookie context; unused Pact matcher import removed.
- [x] Run CI-equivalent frontend verification.
  - Satisfies: prove the failed check should pass after push.
  - Verification:
    - `npx vitest run src/test/pact/public-api-consumer.pact.test.ts --pool=threads` -> 6 passed.
    - `npm run lint` -> 0 errors, 5 existing warnings.
    - `npm run typecheck` -> passed.
    - `npm test -- --run` -> 46 files passed, 239 tests passed.
- [x] Generate a persistent audit artifact for the CI fix.
  - Satisfies: AGENTS.md report requirement for non-trivial work.
  - Verification: `backend/reports/frontend-ci-pact-fix-audit/frontend-ci-pact-fix-audit.md`, `.html`, and `.json` created.

## Scope Check

- Before execution: planned work was limited to the frontend Pact test failure and CI verification.
- After execution: actual changes stayed within one frontend test file plus TODO/report artifacts. No production code, backend architecture, routes, DTOs, Dockerfiles, CI files, or configuration files were changed.
