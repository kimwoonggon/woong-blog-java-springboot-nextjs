# TODO List - 2026-04-20

## Responsive Fail-Zero Stabilization

### User Instructions Captured

- Make the responsive/frontend test suite fail-zero so backend/frontend refactoring can continue safely.
- Fix the direction, not just individual assertions: Playwright should determine expected counts and controls from viewport width/height.
- Prioritize Android small phone and iPad/tablet UX while preserving PC desktop UX.
- Avoid the current behavior where item count drops too sharply when desktop width is slightly reduced.
- Use suitable skills and subagents where useful.

### Plan And TODO Mapping

| ID | TODO | Status | Verification |
| --- | --- | --- | --- |
| RFZ1 | Preflight Codex plugin panic paths and current worktree. | Completed | `plugins=false`; plugin cache paths resolve; no Playwright process remained before restart. |
| RFZ2 | Back up frontend/tests before edits. | Completed | Created `../woong-blog-codex-backups-2026-04-20/responsive-fail-zero/pre-responsive-fail-zero-20260420.tar.gz`. |
| RFZ3 | Replace height-interpolated page size policy with deterministic width buckets. | Completed | `npx vitest run src/test/responsive-page-size.test.ts`: 6 passed. |
| RFZ4 | Add Playwright responsive oracle/helper for viewport class, nav mode, theme toggle, page size. | Completed | Added `tests/helpers/responsive-policy.ts`; focused responsive/theme/admin/public run: 52 passed. |
| RFZ5 | Fix theme/navigation tests to use desktop toggle or drawer toggle according to viewport. | Completed | `tests/dark-mode.spec.ts`, `tests/ui-quality-z-index.spec.ts`, `tests/ui-quality-visual-metrics.spec.ts` included in 52-pass focused run. |
| RFZ6 | Remove hard-coded pageSize expectations in public/admin pagination and return URL tests. | Completed | Public/admin pagination focused run: 52 passed; full-run hard-coded/data assumptions patched as discovered. |
| RFZ7 | Scope Mermaid SVG tests to Mermaid containers, not global SVGs. | Completed | Subagent added `tests/helpers/mermaid.ts`; Mermaid focused run: 84 passed. |
| RFZ8 | Stabilize upload/image drag/paste tests by separating real app failures from wait conditions. | Completed | Upload/image focused run: 10 passed; C-3/C-4 now wait for inserted inline image and enabled save. |
| RFZ9 | Run focused unit/Playwright checks and record results. | Completed | `npm test -- --run`: 233 passed; `dotnet test backend/WoongBlog.sln`: 139 passed; `npm run typecheck`: pass; `npm run lint`: 0 errors, 5 existing warnings; repeated full e2e found remaining brittle tests which were fixed and re-run in targeted groups. |

### Post-Progress Verification Notes

- Responsive/theme/pagination/admin/Mermaid independence focused Playwright: `52 passed`.
- Mermaid worstcase/batch/recording focused Playwright: `84 passed`.
- Upload/image/drag/paste focused Playwright: `10 passed`.
- Regressions fixed from full e2e attempts: admin blog edit link timing, auth helper networkidle timeout, recent posts boundingBox visibility, video-only upload global `video` locator, optimistic work delete row removal, home hero long-word overflow at 350px, batch prompt save state race, home page save response matching, spacing-system invalid row sample, toast region over-scoping, visitor pagination data assumption, admin editor/dialog first-row click timing, featured works grid column parser, recent post card visibility, batch cancel toast dependency.
- Latest targeted re-runs for the last discovered failures passed:
  - `tests/admin-ai-batch-cancel.spec.ts --grep "cancel a running"`: 1 passed.
  - `tests/ui-admin-tiptap-sticky-toolbar.spec.ts tests/ui-quality-z-index.spec.ts`: 3 passed.
  - `tests/ui-improvement-featured-works-grid.spec.ts tests/ui-improvement-recent-posts-cards.spec.ts tests/ui-admin-tiptap-sticky-toolbar.spec.ts tests/ui-quality-z-index.spec.ts --grep "two columns|vertically aligned|sticky|dialogs render"`: 4 passed.
  - `tests/e2e-visitor-pagination-journey.spec.ts tests/mermaid-batch-prompt-0419.spec.ts tests/admin-home-extreme-input.spec.ts tests/ui-quality-spacing-system.spec.ts tests/ui-quality-toast-accessibility.spec.ts --grep "paginated reading path|save batch ai prompt|home page editor persists|spacing rhythm|admin toasts"`: 5 passed.
- A final uninterrupted full e2e pass has not yet been achieved in this session; the last full attempts were used to discover and remove additional brittle/data-coupled assertions.

### Pre-Progress Alignment Check

- Planned work starts with responsive policy because it is the root cause of many tablet/mobile failures.
- Desktop remains `>=1280`; tablet/iPad and Android get drawer/touch-friendly behavior.
- Failure fixes will distinguish app issues from brittle test assumptions.

## Backend Architecture Test Plan Expansion

### User Instructions Captured

- `docs/test260419-feature-flow-map.md` and `docs/backend-cqrs-refactor-2026-04-19.md` have a good direction, but the improvement appears too limited to blog/work.
- Extend the same responsibility-based service decomposition and handler usage to other backend areas.
- Plan backend tests as separate unit, integration, and contract test layers.
- Introduce contract testing through Pact.NET (`pack.net` interpreted as Pact.NET because the requested context is contract testing).
- Initially considered SonarQube static analysis and quality gates; later corrected to SonarAnalyzer.CSharp compile-time analyzer usage.
- Use relevant skills and search when helpful, but prefer proven choices.

### Skill And Source Check

- Used local `find-skills` workflow. Search results for .NET testing, Pact/contract testing, and Sonar-related workflows were low-install or generic; no new skill was installed.
- Used local `tdd` guidance for behavior-focused vertical test slices.
- Used local `dotnet-best-practices` guidance for narrow interfaces, handler ownership, DI, and service testability.
- Checked official Pact and SonarSource documentation before writing the strategy:
  - Pact.NET official docs/README: current Pact.NET supports Pact specification V4 and uses consumer/provider verification flows.
  - NuGet `pact-foundation/PactNet`: current package owner and latest version confirmed from NuGet.
  - SonarSource docs: .NET scanner should wrap build/test between `begin` and `end`; coverage is generated by third-party tooling and imported.

### Backup

- Created `.codex-backups/2026-04-20-docs-architecture-test-plan/test260419-feature-flow-map.md.bak`.
- Created `.codex-backups/2026-04-20-docs-architecture-test-plan/backend-cqrs-refactor-2026-04-19.md.bak`.
- Created `.codex-backups/2026-04-20-docs-architecture-test-plan/todolist-2026-04-20.md.bak`.

### Plan And TODO Mapping

| ID | TODO | Status | Verification |
| --- | --- | --- | --- |
| BTP1 | Capture instructions, skills, source checks, and backup paths in this TODO section. | Completed | TODO updated after backup creation; backup files created under `.codex-backups/2026-04-20-docs-architecture-test-plan/`. |
| BTP2 | Extend CQRS notes with a cross-module responsibility decomposition roadmap beyond Blog/Work/Page. | Completed | Added `Follow-up direction - cross-module handler/service split`; `rg` found the heading in `docs/backend-cqrs-refactor-2026-04-19.md`. |
| BTP3 | Add backend test split plan: unit, integration, Pact.NET contract, persistence contracts, architecture boundaries. | Completed | Added `Backend test strategy expansion`, `Pact.NET contract tests`, and architecture-test sections; checked current test project shape before writing. |
| BTP4 | Add initial static-analysis plan. | Superseded | Initial SonarQube scanner plan was later superseded by SonarAnalyzer.CSharp-only guidance. |
| BTP5 | Add feature-flow map interpretation for non-Content modules and handler/store migration candidates. | Completed | Added `Backend Refactor Interpretation`, `Cross-Module Migration Candidates`, and test layer mapping to `docs/test260419-feature-flow-map.md`; `rg` found the headings. |
| BTP6 | Run lightweight documentation verification and record results. | Completed | `git diff --check -- docs/test260419-feature-flow-map.md docs/backend-cqrs-refactor-2026-04-19.md todolist-2026-04-20.md`: passed with no output; `git diff --stat` shows only these 3 files changed for this task. |

### Pre-Progress Alignment Check

- Planned work still matches the user's request: document the broader architecture/test direction, not implement the refactor yet.
- Scope stays on documentation and planning; existing backend auth/test/video artifacts in the dirty worktree remain untouched.
- Blog/Work/Page changes remain as the reference implementation; AI, Media, Identity, Site, Composition, and WorkVideo are treated as follow-up candidates.

### Post-Progress Alignment Check

- Completed work still matches the planned TODO list: no backend implementation or config was changed.
- The two requested docs now cover service decomposition beyond Blog/Work/Page, Pact.NET contract testing, test layer splitting, and the corrected SonarAnalyzer.CSharp direction.
- Pact.NET provider verification guidance was corrected to require a real local TCP socket instead of in-memory `WebApplicationFactory`/`TestServer` hosting, matching Pact.NET's native verifier requirement.
- Verification was documentation-focused because this turn changed markdown planning files only; no code test suite was run.

## Backend CQRS Slice Implementation

### User Instructions Captured

- Implement the agreed backend plan.
- Keep the implementation aligned with the first concrete slice: test classification groundwork plus Site, Composition, and Identity query/store responsibility splits.
- Use TDD and .NET best-practice skills; avoid unverified skill installs.
- Preserve existing dirty worktree changes and keep edits scoped.

### Backup

- Created `.codex-backups/2026-04-20-backend-cqrs-slice/todolist-2026-04-20.md.bak`.
- Created backups for Site, Composition, Identity modules, `PersistenceServiceCollectionExtensions.cs`, and `ArchitectureBoundaryTests.cs` under `.codex-backups/2026-04-20-backend-cqrs-slice/`.

### Plan And TODO Mapping

| ID | TODO | Status | Verification |
| --- | --- | --- | --- |
| BCSI1 | Preflight plugin cache paths, inspect worktree, read relevant skills, and check skill search results. | Completed | Cache paths resolved; worktree is dirty with unrelated existing changes; `npx skills find dotnet cqrs testing` returned low-install skills so none installed. |
| BCSI2 | Add/refine architecture tests so Site, Composition, and Identity handler/store splits are enforced. | Completed | `dotnet test backend/tests/WoongBlog.Api.UnitTests, WoongBlog.Api.IntegrationTests, WoongBlog.Api.ArchitectureTests/WoongBlog.Api.UnitTests, WoongBlog.Api.IntegrationTests, WoongBlog.Api.ArchitectureTests.csproj --filter Category=Architecture`: 14 passed. |
| BCSI3 | Refactor Site settings services into command/query stores with handler-owned update orchestration. | Completed | Site handlers now use `ISiteSettingsCommandStore`/`ISiteSettingsQueryStore`; full backend suite passed. |
| BCSI4 | Refactor Composition dashboard/home services into query stores with handler-owned read orchestration. | Completed | Composition handlers now use `IAdminDashboardQueryStore`/`IHomeQueryStore`; `Category=Unit` and integration checks passed. |
| BCSI5 | Refactor Identity admin member listing into a query store while keeping auth interaction service intact. | Completed | `GetAdminMembersQueryHandler` now uses `IAdminMemberQueryStore`; `Category=Integration` passed. |
| BCSI6 | Move low-risk module registrations from central persistence registration into module extensions. | Completed | Store registrations moved into Site, Composition, and Identity module extensions; full backend suite passed. |
| BCSI7 | Run focused backend tests, then full backend test suite if focused checks pass. | Completed | `Category=Architecture`: 14 passed; `Category=Unit`: 11 passed; `Category=Integration`: 26 passed; `dotnet test backend/WoongBlog.sln`: 141 passed with existing package vulnerability warnings; scoped `git diff --check`: passed. |

### Pre-Progress Alignment Check

- The implementation starts with the lowest-risk plan slice and intentionally leaves AI, Media, WorkVideo, Pact.NET, and static-analysis packaging for later slices.
- External API paths and JSON shapes should remain unchanged.
- The work will not revert or overwrite existing unrelated modified files.

### Post-Progress Alignment Check

- Completed work matches the first implementation slice: test classification groundwork plus Site, Composition, and Identity responsibility splits.
- AI, Media, WorkVideo, Pact.NET, and static-analysis packaging are intentionally untouched for this pass.
- Existing unrelated dirty worktree changes remain preserved.

## Frontend E2E Gate Before Next Backend Slice

### User Instructions Captured

- Before moving to the next backend refactor step, validate the frontend e2e flows affected by the current backend slice.
- If the e2e gate has no blocking issues, proceed to the next backend slice later.

### Plan And TODO Mapping

| ID | TODO | Status | Verification |
| --- | --- | --- | --- |
| FE2E1 | Identify e2e specs covering Site, Composition, and Identity-admin-member flows touched by the backend slice. | Completed | Selected home/resume/admin-dashboard/admin-menus/admin-members/admin-pages-settings/admin-site-settings-extreme-input/admin-home-extreme-input/admin-resume-upload/admin-resume-validation specs. |
| FE2E2 | Rebuild/restart Docker dev compose so e2e runs against the updated backend implementation. | Completed | Rebuilt backend/frontend images; restarted compose on `127.0.0.1:18080` and `127.0.0.1:13000` using `/tmp/woong-postgres-dev` and `nginx/default.conf`; `/api/health` returned ok. |
| FE2E3 | Run targeted Playwright e2e subset against compose at `http://127.0.0.1:13000`. | Completed | Targeted Site/Composition/Identity subset: 20 passed. Official admin subset initially hit stale DB data collision, then fresh DB run passed 27/27. Official public subset passed 28/28. |
| FE2E4 | Record e2e result and decide whether the next backend slice is unblocked. | Completed | Frontend e2e gate is green on fresh compose DB; next backend slice is unblocked. |

### Pre-Progress Alignment Check

- This gate checks browser-facing behavior for the backend CQRS slice before AI/Media/WorkVideo work.
- Existing unrelated dirty worktree changes remain out of scope.

### Post-Progress Alignment Check

- The backend CQRS slice has backend unit/integration/architecture coverage and frontend e2e coverage through targeted, admin, and public Playwright subsets.
- The only failed e2e attempt was caused by stale persistent DB data containing duplicate `afdfad` works; fresh compose DB passed the same admin subset.
- The next backend refactor slice can start from the AI module or the next selected priority.

## Backend Test Project Split - Integration Migration

### User Instructions Captured

- Move host, endpoint, and persistence integration tests into `backend/tests/WoongBlog.Api.IntegrationTests`.
- Move the shared integration-test helpers as needed so the new project owns the integration slice.
- Preserve namespaces unless compile requires a minimal change.
- Add `xunit.runner.json` to the integration project if needed.
- Do not edit the solution or CI files.
- Run `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj` if possible.

### Backup

- Created `.codex-backups/2026-04-20-integration-tests-move/backend-tests-backup-20260420.tar.gz`.

### Plan And TODO Mapping

| ID | TODO | Status | Verification |
| --- | --- | --- | --- |
| BIT1 | Confirm the integration test project layout and any helper dependencies from the current unit test project. | Completed | Reviewed `WoongBlog.Api.IntegrationTests.csproj`, mapped the helper/test dependencies, and identified `StartupOptionsValidationTests` as the last host-based unit test. |
| BIT2 | Move the listed host/endpoint/persistence tests into `WoongBlog.Api.IntegrationTests`. | Completed | Moved the 15 listed test files plus `StartupOptionsValidationTests.cs` into `backend/tests/WoongBlog.Api.IntegrationTests/`. |
| BIT3 | Add or copy the shared helper/usings config needed by the new project. | Completed | Added `CustomWebApplicationFactory.cs`, `TestAuthHandler.cs`, `TestCategories.cs`, `Usings.cs`, and `xunit.runner.json` to the integration project; retained `Usings.cs`, `TestCategories.cs`, and the runner json in the unit project because the remaining unit tests still depend on them. |
| BIT4 | Run the targeted integration test project and record the result. | Completed | `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj`: 90 passed, 0 failed. |

### Pre-Progress Alignment Check

- The new integration project already exists, so this task is a relocation of test ownership rather than a bootstrap.
- `StartupOptionsValidationTests` depends on the shared host factory and needs to move with the integration slice to keep the remaining unit test project clean.
- The old unit test project still needs its `Usings.cs` until the remaining unit tests are updated, so that file may need to be retained there even if a copy is added to the integration project.

### Post-Progress Alignment Check

- The moved integration suite now builds and executes under `WoongBlog.Api.IntegrationTests`.
- Shared support files were duplicated only where the surviving unit project still needs them; the integration suite now owns its own copy.
- No solution or CI files were edited.

## AI CQRS Slice Implementation

### User Instructions Captured

- Continue without further user intervention.
- Start with AI module `IAiAdminService` decomposition.
- Use subagents for parallelizable work; Media, WorkVideo, and Pact/Sonar work are running in parallel.
- Judge when to run backend and frontend e2e checks before moving between slices.

### Backup

- Created `.codex-backups/2026-04-20-ai-cqrs-slice/AI.bak`.
- Created `.codex-backups/2026-04-20-ai-cqrs-slice/AdminAiEndpointsTests.cs.bak`.
- Created `.codex-backups/2026-04-20-ai-cqrs-slice/ArchitectureBoundaryTests.cs.bak`.
- Created `.codex-backups/2026-04-20-ai-cqrs-slice/todolist-2026-04-20.md.bak`.

### Plan And TODO Mapping

| ID | TODO | Status | Verification |
| --- | --- | --- | --- |
| AICQRS1 | Launch parallel subagents for Media, WorkVideo, and Pact/Sonar while local work handles AI. | Completed | Spawned Media worker, WorkVideo worker, and Pact/Sonar explorer. |
| AICQRS2 | Add architecture guard against `IAiAdminService`/application-layer `IResult` regression. | Completed | Focused architecture tests passed as part of `FullyQualifiedName~AdminAiEndpointsTests|Category=Architecture`: 29 passed. |
| AICQRS3 | Replace `IAiAdminService` direct endpoint calls with MediatR command/query handlers. | Completed | AI endpoints now call MediatR commands/queries instead of `IAiAdminService`; focused AI endpoint tests passed. |
| AICQRS4 | Introduce AI batch/blog persistence store and keep AI provider as infrastructure adapter. | Completed | Added `IAiBlogFixBatchStore`/`AiBlogFixBatchStore`; `IBlogAiFixService` remains the external AI adapter. |
| AICQRS5 | Preserve AI HTTP paths and JSON response shapes. | Completed | Existing `AdminAiEndpointsTests` passed without changing route expectations. |
| AICQRS6 | Run focused AI backend tests, architecture tests, full backend suite, and relevant AI e2e subset if backend is green. | Completed | Focused AI + architecture backend tests passed; `dotnet test backend/WoongBlog.sln`: 143 passed; AI/Media/WorkVideo targeted Playwright subset: 22 passed. |

### Parallel Subagent Results

- Media worker completed `IMediaAssetService` split into command handlers, store, storage adapter, and upload policy. Reported `UploadsControllerTests`: 4 passed.
- WorkVideo worker completed first safe split: extracted `IVideoTranscoder`, added `FfmpegVideoTranscoder`, and routed HLS job endpoint through MediatR. Reported `WorkVideoEndpointsTests`: 7 passed.
- Local follow-up completed the rest of the WorkVideo endpoint handler routing, so upload URL, local upload, confirm, YouTube add, reorder, delete, and HLS job now all go through MediatR command handlers.
- Pact/Sonar explorer completed implementation blueprint with PactNet 5.0.1, PactNet.Output.Xunit 1.0.0, dotnet-sonarscanner 11.2.1, dotnet-coverage 18.6.2, CI placement, and real TCP socket provider guidance.

### Pre-Progress Alignment Check

- This slice should not touch Media or WorkVideo code because subagents own those write scopes.
- Existing unrelated dirty worktree changes remain preserved.

### Post-Progress Alignment Check

- AI `IAiAdminService` is removed; AI endpoints now use MediatR handlers and `IAiBlogFixBatchStore`.
- Media and WorkVideo subagent slices are integrated in the working tree.
- Backend and targeted e2e checks are green, so Pact.NET setup and later SonarAnalyzer.CSharp test-project packaging are unblocked.
- Additional WorkVideo e2e after full endpoint handler routing passed 8/8.

## Pact.NET And Static Analysis Implementation

### User Instructions Captured

- Continue remaining plan items without user intervention.
- Introduce Pact.NET contract testing. Initial SonarQube CI setup was added here but later removed in favor of SonarAnalyzer.CSharp during the eventual test project split.
- Use verified tooling and avoid replacing existing tests.

### Backup

- Created `.codex-backups/2026-04-20-pact-sonar-slice/WoongBlog.sln.bak`.
- Created `.codex-backups/2026-04-20-pact-sonar-slice/ci-dev.yml.bak`.
- Created `.codex-backups/2026-04-20-pact-sonar-slice/ci-main-runtime.yml.bak`.
- Created `.codex-backups/2026-04-20-pact-sonar-slice/todolist-2026-04-20.md.bak`.

### Plan And TODO Mapping

| ID | TODO | Status | Verification |
| --- | --- | --- | --- |
| PS1 | Add a dedicated Pact.NET provider verification project to the backend solution. | Completed | Added `backend/tests/WoongBlog.Api.ContractTests` to `backend/WoongBlog.sln`; contract project test passed. |
| PS2 | Ensure Pact provider verification uses a real local TCP provider URL and skips cleanly when pact files are absent. | Completed | Added `scripts/pact-provider-verify.sh`; local run skipped cleanly because `tests/contracts/pacts` has no pact JSON files yet. |
| PS3 | Add CI jobs for contract verification and initial static-analysis exploration. | Superseded | Added `contract-tests`; the temporary `sonar-scan` jobs were later removed because the desired direction is SonarAnalyzer.CSharp, not SonarQube scanner/server. |
| PS4 | Run local contract project tests and backend solution tests. | Completed | `dotnet test backend/tests/WoongBlog.Api.ContractTests/WoongBlog.Api.ContractTests.csproj`: 1 passed; `dotnet test backend/WoongBlog.sln`: 144 passed total across both projects, with existing package vulnerability warnings. |

### Pre-Progress Alignment Check

- Pact starts as infrastructure for stable read JSON contracts; multipart uploads, HLS/video, and AI batch flows remain integration/e2e-tested.
- SonarQube scanner/server is no longer planned; static analysis will be added later via SonarAnalyzer.CSharp packages on split test projects.

### Post-Progress Alignment Check

- Pact.NET provider verification infrastructure is present, but actual consumer pact files still need to be generated by frontend/API-client contract tests or supplied through a Pact Broker.
- SonarQube scan CI was removed after clarification; Pact provider verification remains.
- `backend/Dockerfile` now copies all backend test projects before solution restore so the new contract test project does not break runtime image builds.

## CQRS Completion Before Test Project Split

### User Instructions Captured

- Do not perform the backend test project split yet.
- First complete remaining CQRS/service decomposition work, especially WorkVideo store split and AI batch processor cleanup.
- Use subagents to parallelize independent implementation work.
- SonarQube scanner/server is not intended; remove the earlier SonarQube CI job and keep SonarAnalyzer for the later test split.

### Backup

- Created `.codex-backups/2026-04-20-cqrs-completion/WorkVideos.Application.bak`.
- Created `.codex-backups/2026-04-20-cqrs-completion/WorkVideos.Api.bak`.
- Created `.codex-backups/2026-04-20-cqrs-completion/AI.bak`.
- Created `.codex-backups/2026-04-20-cqrs-completion/Infrastructure.Ai.bak`.
- Created `.codex-backups/2026-04-20-cqrs-completion/ci-dev.yml.bak`.
- Created `.codex-backups/2026-04-20-cqrs-completion/ci-main-runtime.yml.bak`.
- Created `.codex-backups/2026-04-20-cqrs-completion/todolist-2026-04-20.md.bak`.

## AI Batch Processor Cleanup Before Test Project Split

### User Instructions Captured

- Complete the AI batch processor cleanup before the test project split.
- `IAiAdminService` is already removed and endpoints already use MediatR.
- Refactor `AiBatchJobProcessor` so it reuses the AI batch store/policies where practical and reduces direct EF/state-transition duplication.
- Preserve external behavior and JSON shape.
- Strengthen focused tests if needed.
- Run `dotnet test backend/tests/WoongBlog.Api.UnitTests, WoongBlog.Api.IntegrationTests, WoongBlog.Api.ArchitectureTests/WoongBlog.Api.UnitTests, WoongBlog.Api.IntegrationTests, WoongBlog.Api.ArchitectureTests.csproj --filter AdminAiEndpointsTests` if possible.
- Keep edits scoped to `backend/src/WoongBlog.Api/Infrastructure/Ai/**`, `backend/src/WoongBlog.Api/Modules/AI/**`, and `backend/tests/WoongBlog.Api.UnitTests, WoongBlog.Api.IntegrationTests, WoongBlog.Api.ArchitectureTests/AdminAiEndpointsTests.cs`.

### Backup

- Created `.codex-backups/2026-04-20-ai-batch-processor-cleanup/AiBatchJobProcessor.cs`.
- Created `.codex-backups/2026-04-20-ai-batch-processor-cleanup/IAiBlogFixBatchStore.cs`.
- Created `.codex-backups/2026-04-20-ai-batch-processor-cleanup/AiBlogFixBatchStore.cs`.
- Created `.codex-backups/2026-04-20-ai-batch-processor-cleanup/BatchJobHandlers.cs`.
- Created `.codex-backups/2026-04-20-ai-batch-processor-cleanup/AdminAiEndpointsTests.cs`.
- Created `.codex-backups/2026-04-20-ai-batch-processor-cleanup/todolist-2026-04-20.md`.

### Plan And TODO Mapping

| ID | TODO | Status | Verification |
| --- | --- | --- | --- |
| AIBATCH1 | Confirm the current processor, store, and admin AI test coverage before editing. | Completed | Reviewed `AiBatchJobProcessor`, `IAiBlogFixBatchStore`, `AiBlogFixBatchStore`, `BatchJobHandlers`, and `AdminAiEndpointsTests` before implementation. |
| AIBATCH2 | Back up the targeted AI/backend files before making changes. | Completed | Copied the targeted files into `.codex-backups/2026-04-20-ai-batch-processor-cleanup/`. |
| AIBATCH3 | Refactor batch job state transitions and store access to remove direct EF duplication from `AiBatchJobProcessor`. | Completed | Processor now uses `IAiBlogFixBatchStore` plus `AiBatchJobProgressPolicy` instead of direct `WoongBlogDbContext` access. |
| AIBATCH4 | Keep the public AI batch API payloads unchanged while preserving current behavior. | Completed | Existing admin AI endpoint responses still pass the focused integration suite with no JSON shape changes. |
| AIBATCH5 | Strengthen focused tests only if the refactor exposes a gap. | Completed | No extra test file was needed because `AdminAiEndpointsTests` already covered the exercised batch paths. |
| AIBATCH6 | Run `dotnet test backend/tests/WoongBlog.Api.UnitTests, WoongBlog.Api.IntegrationTests, WoongBlog.Api.ArchitectureTests/WoongBlog.Api.UnitTests, WoongBlog.Api.IntegrationTests, WoongBlog.Api.ArchitectureTests.csproj --filter AdminAiEndpointsTests` and record the result. | Completed | `dotnet test backend/tests/WoongBlog.Api.UnitTests, WoongBlog.Api.IntegrationTests, WoongBlog.Api.ArchitectureTests/WoongBlog.Api.UnitTests, WoongBlog.Api.IntegrationTests, WoongBlog.Api.ArchitectureTests.csproj --filter AdminAiEndpointsTests`: 14 passed, 0 failed, 0 skipped. |

### Pre-Progress Alignment Check

- The current work is limited to the AI batch processor cleanup requested by the user.
- The planned edits stay within the allowed backend write scope and do not touch the unrelated dirty files already present in the worktree.
- The next step is to refactor the processor around the store and shared state-transition helpers, then verify the existing admin AI behavior.

### Post-Progress Alignment Check

- The AI batch processor now runs through the shared batch store and policy helpers, with direct EF access removed from the processor itself.
- The public admin AI endpoints still pass their focused integration coverage, so the response shape and behavior remain intact.
- No extra test file edits were required because the existing admin AI integration suite already exercised the relevant batch flows.

### Plan And TODO Mapping

| ID | TODO | Status | Verification |
| --- | --- | --- | --- |
| CQRSFIN1 | Split WorkVideo persistence operations into command/query stores while preserving endpoint routes and response shape. | Completed | WorkVideo endpoint command handlers now orchestrate stores/storage/transcoder directly; cleanup boundary remains narrow; focused WorkVideo/architecture tests passed. |
| CQRSFIN2 | Refactor `AiBatchJobProcessor` to reuse AI batch store/policies and reduce direct EF/state-transition ownership. | Completed | Processor now uses `IAiBlogFixBatchStore` and `AiBatchJobProgressPolicy`; focused AI/architecture tests passed. |
| CQRSFIN3 | Remove earlier SonarQube scanner CI job and keep Pact provider verification. | Completed | Removed `sonar-scan` jobs and `browser-smoke` dependency from dev/main workflows; `contract-tests` remains. |
| CQRSFIN4 | Run focused backend tests for WorkVideo, AI, architecture, then full backend solution. | Completed | Focused `WorkVideoEndpointsTests|AdminAiEndpointsTests|Category=Architecture`: 36 passed; `dotnet test backend/WoongBlog.sln`: contract 1 passed plus backend 143 passed; Pact verifier skipped cleanly because no pact JSON files exist yet. |
| CQRSFIN5 | Rebuild compose and run targeted AI/WorkVideo e2e gates. | Completed | Fresh compose rebuild and `/api/health` passed; targeted AI/WorkVideo/public/admin e2e gate passed 21/21. |

### Pre-Progress Alignment Check

- Test project physical split is intentionally deferred until CQRSFIN items are green.
- Existing unrelated dirty worktree changes remain preserved.

### Post-Progress Alignment Check

- WorkVideo endpoint command handlers now orchestrate command/query stores, storage selector, and transcoder directly instead of wrapping a broad service.
- WorkVideo cleanup remains a narrow background/delete-work boundary through `IWorkVideoCleanupService`.
- `AiBatchJobProcessor` no longer depends directly on `WoongBlogDbContext`; it uses `IAiBlogFixBatchStore` and shared progress policy.
- SonarQube scanner CI was removed; SonarAnalyzer.CSharp remains deferred to the later test-project split.
- Backend and targeted e2e gates are green, so the later test split can now be performed safely.

## Backend Test Project Split Implementation

### User Instructions Captured

- After CQRS/service decomposition is green, split backend tests into unit, integration, architecture, and contract projects.
- Do not use SonarQube scanner/server; add SonarAnalyzer.CSharp as compile-time analyzer package.
- Classify tests by actual dependency shape at move time, not by stale file names.
- Use subagents for parallelizable movement work.

### Backup

- Created `.codex-backups/2026-04-20-test-project-split/tests.bak`.
- Created `.codex-backups/2026-04-20-test-project-split/WoongBlog.sln.bak`.
- Created `.codex-backups/2026-04-20-test-project-split/ci-dev.yml.bak`.
- Created `.codex-backups/2026-04-20-test-project-split/ci-main-runtime.yml.bak`.
- Created `.codex-backups/2026-04-20-test-project-split/todolist-2026-04-20.md.bak`.

### Plan And TODO Mapping

| ID | TODO | Status | Verification |
| --- | --- | --- | --- |
| TPS1 | Create UnitTests, IntegrationTests, and ArchitectureTests projects with requested package sets and SonarAnalyzer.CSharp. | Completed | Created three projects with requested package sets. |
| TPS2 | Move architecture tests by dependency shape. | Completed | `WoongBlog.Api.ArchitectureTests`: 13 passed after narrowing the IResult boundary rule to actual `IResult`. |
| TPS3 | Move host/endpoint/persistence tests and shared integration helpers. | Completed | `WoongBlog.Api.IntegrationTests`: 90 passed. |
| TPS4 | Move remaining pure unit tests. | Completed | `WoongBlog.Api.UnitTests`: 38 passed. |
| TPS5 | Align ContractTests package versions and add SonarAnalyzer.CSharp. | Completed | `WoongBlog.Api.ContractTests`: 1 passed; Pact verifier still clean-skips without pact JSON. |
| TPS6 | Update solution and CI backend test commands; remove old WoongBlog.Api.UnitTests, WoongBlog.Api.IntegrationTests, WoongBlog.Api.ArchitectureTests after all tests are green. | Completed | Removed old test project from solution and filesystem; CI now runs unit, architecture, and integration projects explicitly. |
| TPS7 | Run project-by-project tests, full solution, Pact verifier, compose build, and e2e smoke gate. | Completed | UnitTests 38 passed; ArchitectureTests 13 passed; IntegrationTests 90 passed; ContractTests 1 passed; full solution passed; Pact verifier skipped cleanly with no pact JSON; compose build/health passed; Playwright smoke gate 13 passed. |

### Post-Progress Alignment Check

- Backend tests are now physically split into unit, integration, architecture, and contract projects.
- The old `WoongBlog.Api.UnitTests, WoongBlog.Api.IntegrationTests, WoongBlog.Api.ArchitectureTests` project was removed after all moved tests were green.
- SonarQube scanner/server is not used; SonarAnalyzer.CSharp is included in split test projects.
- Test split was performed only after CQRSFIN backend and e2e gates were green.

### Pre-Progress Alignment Check

- CQRSFIN gates are green, so test split can proceed.
- Existing unrelated dirty worktree changes remain preserved.

## WorkVideo CQRS Narrow Store Split

### User Instructions Captured

- Complete the WorkVideo CQRS split before the test project split.
- Introduce `IWorkVideoCommandStore` and `IWorkVideoQueryStore` or equivalent narrow stores.
- Move direct EF row operations out of `WorkVideoService` and its handlers.
- Preserve endpoint routes, request/response shape, and behavior.
- Keep `IVideoObjectStorage`, `IVideoTranscoder`, and `IWorkVideoPlaybackUrlBuilder` as adapters/support.
- Limit edits to the WorkVideo application/API files, module registration, and the focused WorkVideo endpoint tests.

### Backup

- Created `.codex-backups/2026-04-20-workvideo-cqrs-split/WorkVideoService.cs.bak`.
- Created `.codex-backups/2026-04-20-workvideo-cqrs-split/WorkVideoEndpoints.cs.bak`.
- Created `.codex-backups/2026-04-20-workvideo-cqrs-split/WorksModuleServiceCollectionExtensions.cs.bak`.
- Created `.codex-backups/2026-04-20-workvideo-cqrs-split/WorkVideoEndpointsTests.cs.bak`.
- Created `.codex-backups/2026-04-20-workvideo-cqrs-split/todolist-2026-04-20.md.bak`.

### Plan And TODO Mapping

| ID | TODO | Status | Verification |
| --- | --- | --- | --- |
| WV1 | Add WorkVideo command/query stores and move EF row operations behind them. | Completed | Added `IWorkVideoCommandStore`, `IWorkVideoQueryStore`, `WorkVideoCommandStore`, and `WorkVideoQueryStore`; `WorkVideoService` now coordinates through stores instead of `WoongBlogDbContext`. |
| WV2 | Keep WorkVideo endpoints, request/response shapes, and admin routes unchanged. | Completed | Route and payload behavior remained unchanged; focused endpoint suite passed without test updates. |
| WV3 | Register new WorkVideo stores in the Works module service collection. | Completed | Registered both stores plus `IVideoTranscoder` in `WorksModuleServiceCollectionExtensions`. |
| WV4 | Run focused backend tests for WorkVideo endpoints after the split. | Completed | `dotnet test backend/tests/WoongBlog.Api.UnitTests, WoongBlog.Api.IntegrationTests, WoongBlog.Api.ArchitectureTests/WoongBlog.Api.UnitTests, WoongBlog.Api.IntegrationTests, WoongBlog.Api.ArchitectureTests.csproj --filter WorkVideoEndpointsTests`: 7 passed, 0 failed. |

### Pre-Progress Alignment Check

- The current change is a narrow persistence refactor inside the WorkVideo module only.
- Existing unrelated dirty worktree changes remain preserved and untouched.

### Post-Progress Alignment Check

- The WorkVideo CQRS split is complete at the module level requested here.
- `IVideoObjectStorage`, `IVideoTranscoder`, and `IWorkVideoPlaybackUrlBuilder` remain adapter/support boundaries.
- Endpoint contracts stayed intact; the focused endpoint suite passed after the refactor.

## Test Project Split

### User Instructions Captured

- Move pure/non-host unit tests into `backend/tests/WoongBlog.Api.UnitTests`.
- Keep host/integration tests in `backend/tests/WoongBlog.Api.UnitTests, WoongBlog.Api.IntegrationTests, WoongBlog.Api.ArchitectureTests`.
- Preserve namespaces unless a minimal compile fix is required.
- Add local `Usings.cs` and `TestCategories.cs` in the unit-test project if needed.
- Do not edit the solution or CI files.
- Run `dotnet test backend/tests/WoongBlog.Api.UnitTests/WoongBlog.Api.UnitTests.csproj` if possible.

### Backup

- Created `/tmp/woongblog-backup-20260420/backend/tests/WoongBlog.Api.UnitTests, WoongBlog.Api.IntegrationTests, WoongBlog.Api.ArchitectureTests/` copies of the targeted host test files and shared helpers.
- Created `/tmp/woongblog-backup-20260420/backend/tests/WoongBlog.Api.UnitTests/WoongBlog.Api.UnitTests.csproj` as a project backup.

### Plan And TODO Mapping

| ID | TODO | Status | Verification |
| --- | --- | --- | --- |
| TPS1 | Confirm the candidate tests are pure/non-host and identify the one host-only test that must stay. | Completed | Reviewed the targeted files; `StartupOptionsValidationTests` stays in the host project, the rest are logic/service/query-validator tests. |
| TPS2 | Move the pure tests into `backend/tests/WoongBlog.Api.UnitTests` and add local helper files there. | Completed | Re-homed 8 pure test files plus local `Usings.cs` and `TestCategories.cs` into the unit-test project. |
| TPS3 | Leave host-only integration/startup validation tests in `backend/tests/WoongBlog.Api.UnitTests, WoongBlog.Api.IntegrationTests, WoongBlog.Api.ArchitectureTests`. | Completed | `StartupOptionsValidationTests.cs` remains in the host test project. |
| TPS4 | Run `dotnet test backend/tests/WoongBlog.Api.UnitTests/WoongBlog.Api.UnitTests.csproj` and record the result. | Completed | `dotnet test backend/tests/WoongBlog.Api.UnitTests/WoongBlog.Api.UnitTests.csproj`: 38 passed, 0 failed. |

### Pre-Progress Alignment Check

- This split must not broaden scope into solution or CI changes.
- Host-only startup validation should remain where `WebApplicationFactory` is available.

### Post-Progress Alignment Check

- The pure logic/service/query-validator tests now live in the dedicated unit-test project.
- The host project still retains the startup validation/integration-style test that depends on `WebApplicationFactory`.
- The targeted unit-test project run passed with 38 tests green.

## CI Testcontainers Docker Access Fix

### User Instructions Captured

- Fix the review finding that GitHub Actions backend integration tests cannot run Testcontainers inside a plain .NET SDK container without Docker daemon access.
- Apply the same treatment to both `.github/workflows/ci-dev.yml` and `.github/workflows/ci-main-runtime.yml`.
- Preserve the already-green full Playwright e2e result and avoid committing generated Playwright artifacts.

### Skill And Source Check

- Used local `tdd` guidance for a small behavior-preserving CI change followed by workflow/backend verification.
- Used local `find-skills` workflow. `npx skills find "github actions testcontainers dotnet"` returned only low-install skills for this exact area, so no skill was installed.

### Backup

- Created `.codex-backups/2026-04-20-ci-testcontainers/ci-dev.yml.bak`.
- Created `.codex-backups/2026-04-20-ci-testcontainers/ci-main-runtime.yml.bak`.
- Created `.codex-backups/2026-04-20-ci-testcontainers/todolist-2026-04-20.md.bak`.

### Plan And TODO Mapping

| ID | TODO | Status | Verification |
| --- | --- | --- | --- |
| CITC1 | Add TODO entry and backup CI workflow files before editing. | Completed | Backup files created under `.codex-backups/2026-04-20-ci-testcontainers/`. |
| CITC2 | Update dev and main runtime backend CI so Testcontainers integration tests run with host Docker access. | Completed | Both workflows now use `actions/setup-dotnet@v4`, check `docker version`, and run backend `dotnet test` from the host runner instead of inside a plain SDK container. |
| CITC3 | Keep unit and architecture backend test commands explicit and preserve browser-smoke dependencies. | Completed | Backend job still runs UnitTests, ArchitectureTests, and IntegrationTests as separate commands; browser-smoke still depends on frontend-quality, backend-tests, contract-tests, and compose-verify. |
| CITC4 | Verify workflow syntax/diff and run the local Testcontainers integration test. | Completed | `git diff --check -- .github/workflows/ci-dev.yml .github/workflows/ci-main-runtime.yml todolist-2026-04-20.md`: passed; `npx --yes js-yaml` parsed both workflow files; host backend test flow passed with UnitTests 38, ArchitectureTests 14, IntegrationTests 91. |

### Pre-Progress Alignment Check

- This task is limited to CI execution environment wiring for backend tests.
- The local dev compose code path already passed full Playwright e2e with `563 passed / 8 skipped`; this CI fix should not alter runtime app behavior.
- Existing unrelated dirty worktree changes remain preserved.

### Post-Progress Alignment Check

- The review finding is addressed by removing plain SDK-container execution from `backend-tests` for the split backend projects.
- Testcontainers integration tests now run in the same host-Docker shape that GitHub-hosted Ubuntu runners support.
- Runtime compose, Pact contract, and browser-smoke behavior were not changed beyond preserving the existing CI dependency chain.

## Backend CQRS Docs - AI And WorkVideo Current-State Update

### User Instructions Captured

- Update `docs/backend-cqrs-refactor-2026-04-19.md` with what was actually reflected in the CQRS refactor.
- From the CQRS viewpoint, document which interfaces disappeared and which interfaces/classes were added.
- Explain what improved, especially which services and handlers changed in the AI and WorkVideo areas.

### Backup

- Created `.codex-backups/2026-04-20-docs-ai-workvideo-cqrs/backend-cqrs-refactor-2026-04-19.md.bak`.
- Created `.codex-backups/2026-04-20-docs-ai-workvideo-cqrs/todolist-2026-04-20.md.bak`.

### Plan And TODO Mapping

| ID | TODO | Status | Verification |
| --- | --- | --- | --- |
| DAWV1 | Inspect current AI and WorkVideo implementation to avoid stale documentation. | Completed | Reviewed AI application/API/persistence files and WorkVideo command handlers, stores, cleanup service, result type, and architecture tests. |
| DAWV2 | Add a current-state AI CQRS section listing removed/added interfaces, handlers, policies, stores, and service boundaries. | Completed | Added `2026-04-20 implementation update - AI and WorkVideo` with `IAiAdminService`/`AiAdminService` removal, `IAiBlogFixBatchStore`, policies, handlers, and batch runner/item processor split. |
| DAWV3 | Add a current-state WorkVideo CQRS section listing removed/added interfaces, handlers, support adapters, result mapping, and service boundary changes. | Completed | Documented `IWorkVideoService`/`WorkVideoServiceResult<T>` removal, `IWorkVideoCleanupService`, command/query stores, command handlers, support adapters, and `WorkVideoResult<T>`. |
| DAWV4 | Update module registration notes and run markdown diff checks. | Completed | Works module registration notes now list WorkVideo stores/support adapters; stale `IWorkVideoService` cleanup and `WorkVideoServiceResult<T>` follow-up text was corrected; `git diff --check` passed. |

### Pre-Progress Alignment Check

- This is a documentation-only update.
- The implementation has already been verified with backend tests and full Playwright e2e; this pass should only make the doc match the code.

### Post-Progress Alignment Check

- The document now records actual AI and WorkVideo CQRS implementation state, not only future candidates.
- No runtime code changed in this doc pass.

### Follow-up Critical Notes Added Before Commit

- Added explicit cautions that `AiBatchJobRunner` can become too large and still owns worker queue drain, cancellation checks, item dispatch, count refresh, and finalize orchestration.
- Documented that `AiBatchJobProcessor` still owns queued job pick/mark-running orchestration and needs concurrency review if multiple workers/processes become possible.
- Documented that repeated `IServiceScopeFactory` resolution makes flow tracing harder and may need smaller runner/finalizer/cancellation boundaries later.
- Documented that `IAiBlogFixBatchStore` is currently a combined batch boundary and can split into target query, job command, and job query stores if it grows.
- Documented that WorkVideo command handlers, especially HLS, are still heavy even though broad service delegation is removed.
- Documented WorkVideo follow-up candidates: move `WorkVideoPolicy`, split command store if needed, and consider command handlers for cleanup background use cases.

## CQRS Bloat And Scope Dependency Cleanup

### User Instructions Captured

- Start resolving the documented cautions instead of only noting them.
- Detect similar bloat in newly changed features.
- Detect direct `IServiceScopeFactory`/scope resolve flows and reduce dependent/tracing-heavy patterns.

### Skill And Source Check

- Used local `tdd` skill guidance: add an architecture guard first, then refactor while preserving behavior.
- Used local `refactor-method-complexity-reduce` guidance for extracting focused helpers/classes and verifying zero failures.
- Used local `dotnet-best-practices` guidance for DI boundaries and focused interfaces.
- `npx skills find "dotnet dependency injection scope factory refactor"` returned low-install skills; no new skill was installed.

### Detection Summary

- Production `IServiceScopeFactory` direct usage:
  - `AiBatchJobProcessor`
  - `AiBatchJobRunner` in `AiBatchProcessingServices.cs`
  - `VideoStorageCleanupWorker`
- Test `CreateScope` usages are integration-test setup/verification and are not refactor targets in this slice.
- Largest changed-feature files:
  - `WorkVideoCommandHandlers.cs`: 455 lines, multiple command handlers plus `WorkVideoPolicy`
  - `BatchJobHandlers.cs`: 287 lines, multiple AI batch handlers
  - `AiBatchProcessingServices.cs`: 229 lines, runner, item processor, apply policy, worker policy, interfaces
  - `AiBlogFixBatchStore.cs`: 179 lines, combined batch persistence boundary

### Backup

- Created `.codex-backups/2026-04-20-bloat-scope-cleanup/AiBatchJobProcessor.cs.bak`.
- Created `.codex-backups/2026-04-20-bloat-scope-cleanup/AiBatchProcessingServices.cs.bak`.
- Created `.codex-backups/2026-04-20-bloat-scope-cleanup/AiModuleServiceCollectionExtensions.cs.bak`.
- Created `.codex-backups/2026-04-20-bloat-scope-cleanup/WorkVideoCommandHandlers.cs.bak`.
- Created `.codex-backups/2026-04-20-bloat-scope-cleanup/ArchitectureBoundaryTests.cs.bak`.
- Created `.codex-backups/2026-04-20-bloat-scope-cleanup/todolist-2026-04-20.md.bak`.

### Plan And TODO Mapping

| ID | TODO | Status | Verification |
| --- | --- | --- | --- |
| CQSCOPE1 | Add architecture guard so AI application types do not directly use `IServiceScopeFactory`, `CreateScope`, or service locator resolution. | Completed | Red test failed on `AiBatchProcessingServices.cs`; after refactor, architecture run passed with `total: 15, failed: 0`. |
| CQSCOPE2 | Move AI scope creation into one infrastructure boundary and make processor/runner use named methods instead of repeated scope/resolve. | Completed | Added `IAiBatchJobScheduler` and `IAiBatchJobItemDispatcher`; `AiBatchJobProcessor` now resolves only scheduler per background scope, and `AiBatchJobRunner` depends on DI services instead of `IServiceScopeFactory`. |
| CQSCOPE3 | Split `AiBatchProcessingServices.cs` into focused runner/item processor/policy/scope files. | Completed | Replaced the combined file with `AiBatchJobContracts`, `AiBatchJobScheduler`, `AiBatchJobRunner`, `AiBatchJobItemProcessor`, `AiBatchWorkerPolicy`, and `BlogFixApplyPolicy`. |
| CQSCOPE4 | Split WorkVideo command handlers and `WorkVideoPolicy` out of the 455-line combined file. | Completed | Replaced `WorkVideoCommandHandlers.cs` with separate handler files and `WorkVideoPolicy.cs`; largest WorkVideo handler file is now the HLS handler at 94 lines. |
| CQSCOPE5 | Run architecture, AI integration, WorkVideo integration, and full backend tests as needed. | Completed | Architecture tests: `total: 15, failed: 0`; AI/WorkVideo integration filter: `total: 21, failed: 0`; full backend solution: `total: 145, failed: 0`. |
| CQSCOPE6 | Update CQRS refactor docs to reflect the reduced scope dependency and file-bloat cleanup. | Completed | `docs/backend-cqrs-refactor-2026-04-19.md` now documents `IAiBatchJobScheduler`, `IAiBatchJobItemDispatcher`, AI application scope-factory guard, handler file split, and `WorkVideoPolicy` extraction. |

### Pre-Progress Alignment Check

- This slice targets maintainability and dependency tracing only.
- Runtime behavior and public API shapes should remain unchanged.
- `VideoStorageCleanupWorker` is noted but not the first target because it is a narrow background worker; AI batch has the repeated scope/resolve flow that currently hurts traceability most.

### Post-Progress Alignment Check

- The AI application layer no longer directly references `IServiceScopeFactory`, `CreateScope`, or `GetRequiredService`.
- Direct scope creation remains only in infrastructure/background boundaries: `AiBatchJobProcessor`, `AiBatchJobItemDispatcher`, and `VideoStorageCleanupWorker`.
- WorkVideo and AI batch file bloat were reduced by splitting combined handler/service files into focused files.
- Behavior was preserved by integration tests and full backend solution tests.

## Backend CQRS Docs - Korean Flow Rewrite

### User Instructions Captured

- The AI/WorkVideo CQRS documentation is hard to understand because English and Korean are mixed and the structure is scattered.
- Rewrite it like the earlier Blog/Work/Page section: previous flow, problem, changed flow, command side, query/support side, and remaining cautions.

### Backup

- Created `.codex-backups/2026-04-20-docs-flow-rewrite/backend-cqrs-refactor-2026-04-19.md.bak`.
- Created `.codex-backups/2026-04-20-docs-flow-rewrite/todolist-2026-04-20.md.bak`.

### Plan And TODO Mapping

| ID | TODO | Status | Verification |
| --- | --- | --- | --- |
| DOCFLOW1 | Rewrite the AI section into Korean flow-oriented structure. | Completed | AI section now uses previous flow, problem, changed flow, command side, query side, store side, policy/support side, background side, and remaining cautions. |
| DOCFLOW2 | Rewrite the WorkVideo section into Korean flow-oriented structure. | Completed | WorkVideo section now uses previous flow, problem, changed flow, command side, store side, support/adapter side, result mapping, file structure cleanup, and remaining cautions. |
| DOCFLOW3 | Keep the caveats clear but separate from implemented changes. | Completed | Remaining cautions are separated under `남은 주의점`; implemented changes are no longer mixed with speculative warnings. |
| DOCFLOW4 | Run markdown diff check. | Completed | `git diff --check -- docs/backend-cqrs-refactor-2026-04-19.md todolist-2026-04-20.md` passed. |

## Full E2E Gate After Scope Cleanup

### User Instructions Captured

- Run the full Playwright e2e suite after the backend CQRS/scope cleanup changes.

### Backup

- Created `.codex-backups/2026-04-20-e2e-flake-stabilization/ui-loading-states.spec.ts.bak`.
- Created `.codex-backups/2026-04-20-e2e-flake-stabilization/ui-quality-layout-rhythm.spec.ts.bak`.
- Created `.codex-backups/2026-04-20-e2e-flake-stabilization/todolist-2026-04-20.md.bak`.

### Plan And TODO Mapping

| ID | TODO | Status | Verification |
| --- | --- | --- | --- |
| E2EGATE1 | Rebuild dev compose on `127.0.0.1:13000` so full e2e runs against current backend/frontend images. | Completed | Compose rebuilt and `/api/health` returned ok. |
| E2EGATE2 | Run full Playwright e2e. | Completed | First full run after rebuild: `560 passed`, `9 skipped`, `2 failed`. |
| E2EGATE3 | Re-run failed specs to distinguish runtime regression from timing/flaky assertions. | Completed | `tests/ui-loading-states.spec.ts tests/ui-quality-layout-rhythm.spec.ts`: `5 passed`. |
| E2EGATE4 | Stabilize timing-sensitive assertions and re-run focused specs. | Completed | Added unique slow-route query and section visibility waits; focused run `tests/ui-loading-states.spec.ts tests/ui-quality-layout-rhythm.spec.ts`: `5 passed`. |
| E2EGATE5 | Re-run full Playwright e2e after stabilization. | Completed | Final full run: `563 passed`, `8 skipped`, `0 failed`, duration `14.6m`. |


---

# TODO - 2026-04-20 - Phase 2 Backend Multi-Csproj Split

## User Instructions

- Implement PR "Phase 2 - move the backend to a real multi-csproj structure".
- Preserve external behavior, endpoint paths, request/response JSON shapes, AI batch behavior, WorkVideo behavior, and seed/bootstrap/schema behavior.
- Do a physical 4-project layer split only: Domain, Application, Infrastructure, Api.
- Do not split per module, redesign use cases, move HTTP concepts into Application/Domain, put DbContext in Application, or reintroduce broad facade services.
- Use TDD slices, update tests, enforce architecture boundaries, create a backup, and verify after each completed TODO.
- Search for specialist skills before implementation.

## Pre-Implementation Checks

- [x] Plugin cache directories verified:
  - `/home/kimwoonggon/.codex/plugins/cache`
  - `/home/kimwoonggon/.codex/.tmp/plugins`
- [x] Git worktree checked clean before changes.
- [x] Backup prepared at `/tmp/woongblog-phase2-prechange-20260420.patch`.
- [x] Skill search performed with `npx -y skills find "dotnet architecture tests"`.
  - Result: only low-install related skills were found; no new skill installed. Proceeding with local `tdd` and `.NET` guidance.
- [x] Confirmed this TODO matches the planned work before starting implementation.

## Implementation TODOs

- [x] Add/update architecture tests first so the desired four-project boundaries are executable.
  - Satisfies: acceptance criteria 2, 8, 9, 10; TDD red slice.
  - Verification: `dotnet test backend/tests/WoongBlog.Api.ArchitectureTests/WoongBlog.Api.ArchitectureTests.csproj --no-restore` failed as expected because `WoongBlog.Domain`, `WoongBlog.Application`, and `WoongBlog.Infrastructure` do not exist yet.

- [x] Create the new backend projects and solution entries.
  - Satisfies: tasks A, F, project reference graph.
  - Verification: `dotnet sln backend/WoongBlog.sln list` shows `WoongBlog.Domain`, `WoongBlog.Application`, `WoongBlog.Infrastructure`, and `WoongBlog.Api`.

- [x] Move pure domain entities/constants into `WoongBlog.Domain`.
  - Satisfies: task B and Domain hard rules.
  - Verification: `dotnet build backend/WoongBlog.sln --no-restore` builds `WoongBlog.Domain`; architecture tests confirm no WoongBlog/ASP.NET/EF/MediatR references.

- [x] Move handlers, commands/queries, validators, application DTO/results, policies, and abstractions into `WoongBlog.Application`.
  - Satisfies: task C; Content/AI/WorkVideo Phase 1 boundaries.
  - Verification: Application builds; `rg` found no Infrastructure/API/ASP.NET/EF/HTTP-result/service-locator tokens in `backend/src/WoongBlog.Application`; architecture tests enforce this.

- [x] Move DbContext, persistence stores, bootstrap/seeding/schema support, AI provider/background infrastructure, auth persistence, media/video storage, WorkVideo HLS/transcoding/cleanup infrastructure into `WoongBlog.Infrastructure`.
  - Satisfies: task D and infrastructure hard rules.
  - Verification: Infrastructure builds with Application + Domain references only; architecture tests confirm no Infrastructure -> Api reference.

- [x] Keep API host, endpoints, HTTP request binding, request DTOs, endpoint validators, result mapping, middleware, auth startup, and static file wiring in `WoongBlog.Api`.
  - Satisfies: task E and external behavior preservation.
  - Verification: Api builds with Application + Infrastructure references; integration tests pass against existing routes.

- [x] Rework DI composition.
  - Satisfies: task G.
  - Verification: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj` passed 91 tests.

- [x] Preserve EF bootstrap/design-time behavior.
  - Satisfies: task H.
  - Verification: persistence/bootstrap integration tests pass; `WoongBlogDbContextDesignTimeFactory` exists in Infrastructure and runtime Npgsql registration sets the migrations assembly.

- [x] Update test project references and strengthen architecture tests.
  - Satisfies: task I.
  - Verification: architecture, unit, integration, and contract test projects compile and run through `dotnet test backend/WoongBlog.sln --no-build`.

- [x] Run build/tests and record results.
  - Satisfies: acceptance criteria 1, 7 and final output requirements.
  - Verification:
    - `dotnet build backend/src/WoongBlog.Api/WoongBlog.Api.csproj`: passed.
    - `dotnet test backend/tests/WoongBlog.Api.ArchitectureTests/WoongBlog.Api.ArchitectureTests.csproj`: passed 20 tests.
    - `dotnet test backend/tests/WoongBlog.Api.UnitTests/WoongBlog.Api.UnitTests.csproj`: passed 38 tests.
    - `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj`: passed 91 tests.
    - `dotnet test backend/WoongBlog.sln`: passed all backend tests.
    - `dotnet build backend/WoongBlog.sln --no-restore`: passed.
    - `dotnet test backend/WoongBlog.sln --no-build`: passed all backend tests.
    - Non-blocking warning observed: NU1901 low-severity advisory for transitive `AWSSDK.Core` 4.0.0.17.

## Additional E2E Verification - 2026-04-20

- [x] Rebuilt Docker dev stack after the project split.
  - Initial rebuild exposed `backend/Dockerfile` only copied `WoongBlog.Api`; fixed it to copy `WoongBlog.Domain`, `WoongBlog.Application`, and `WoongBlog.Infrastructure`.
  - Compose rebuild then succeeded.
- [x] Counted Playwright suite size.
  - Command: `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:13000 npx playwright test --workers=1 --list`
  - Result: 571 tests in 185 files.
- [x] Ran full Playwright suite against rebuilt dev compose stack.
  - Command: `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:13000 npx playwright test --workers=1`
  - Result: 560 passed, 9 skipped, 2 failed.
  - Both failures were local-admin shortcut policy assertions because dev compose intentionally exposes `Continue as Local Admin`; rerun needed `PLAYWRIGHT_EXPECT_LOCAL_ADMIN_SHORTCUT=visible`.
- [x] Reran full Playwright suite with the correct dev shortcut expectation.
  - Command: `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:13000 PLAYWRIGHT_EXPECT_LOCAL_ADMIN_SHORTCUT=visible npx playwright test --workers=1`
  - Result: 561 passed, 8 skipped, 2 failed.
  - One failure was state contamination from the previous full run; one was the public loading skeleton timing assertion.
- [x] Started a separate clean compose project with fresh Postgres data and reran the full suite.
  - Clean stack: `COMPOSE_PROJECT_NAME=woongbloge2eclean`, `PLAYWRIGHT_BASE_URL=http://127.0.0.1:13100`.
  - Command: `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:13100 PLAYWRIGHT_EXPECT_LOCAL_ADMIN_SHORTCUT=visible npx playwright test --workers=1`
  - Result: 560 passed, 9 skipped, 2 failed.
  - The previously state-contaminated admin home test passed on clean data.
  - Remaining failures:
    - `tests/ui-loading-states.spec.ts:4` expected a transient public route skeleton (`.animate-pulse`) that the production compose route resolved before Playwright observed.
    - `tests/ui-quality-visual-contracts.spec.ts:73` failed once but passed on targeted rerun.
- [x] Reran failed tests on the clean stack.
  - Command: `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:13100 PLAYWRIGHT_EXPECT_LOCAL_ADMIN_SHORTCUT=visible npx playwright test tests/ui-loading-states.spec.ts:4 tests/ui-quality-visual-contracts.spec.ts:73 --project=chromium-authenticated --workers=1`
  - Result: visual-contract test passed; loading-skeleton timing test failed reproducibly.

## Progress Notes

- Completed implementation still matches the planned TODO list. No module-per-project split, endpoint redesign, DTO shape redesign, or broad facade service reintroduction was added.
