# Backend Test Coverage Audit TODO - 2026-04-25

## Backend Code Coverage Reporting TODO - 2026-04-25

### User Instructions

- Scope backend only.
- Add backend code coverage reporting.
- Goal: generate readable coverage reports without chasing meaningless coverage numbers.
- Add coverage collection for backend tests using the existing .NET test tooling style.
- Generate coverage reports for:
  - UnitTests
  - ComponentTests
  - IntegrationTests
  - full backend solution
- Add documentation explaining:
  - how to generate coverage locally
  - where coverage output is written
  - why coverage percentage is not the only quality metric
  - which backend areas are intentionally covered by integration/component tests instead of unit tests
- Do not enforce a strict high coverage threshold yet.
- If adding a threshold, start with a conservative baseline and document it.
- Update the backend test audit/report with:
  - current line coverage
  - current branch coverage if available
  - low coverage production areas
  - high-risk low-coverage areas
  - recommended next coverage targets
- Run `dotnet test backend/WoongBlog.sln`.

### Scope Check Before TODO Execution

- Actual planned work matches backend-only coverage-reporting scope.
- Planned production changes: none.
- Existing test projects already reference `coverlet.collector`, so coverage collection can use the current `dotnet test` style plus `XPlat Code Coverage`.
- Planned tooling/documentation write set:
  - backend coverage runsettings
  - backend local ReportGenerator tool manifest
  - backend coverage runner script under `scripts/`
  - backend testing documentation
  - backend architecture/tooling coverage test
  - existing backend test coverage audit artifacts
  - mandatory final audit artifacts for this task
  - this TODO file
- Coverage output will be generated under ignored local `coverage/backend/` artifacts; persistent audit reports will summarize current metrics.
- Specialist skill search completed with `npx skills find dotnet coverage`; no external skill installed because available coverage skills had low install counts and the repo already has local `tdd` and .NET guidance.

### TODOs

- [x] Prepare backups before modifying tooling, docs, reports, and TODO files.
  - Satisfies: backup requirement.
  - Verification: backups created under `backend/reports/backend-code-coverage-reporting-2026-04-25/prechange-backup/`; pre-change diff written to `backend/reports/backend-code-coverage-reporting-2026-04-25/prechange-backup.patch`.
- [x] Confirm planned work still matches this TODO list before implementation.
  - Satisfies: workflow requirement and scoped-change requirement.
  - Verification: reviewed existing backend test projects, runner scripts, `backend/TESTING.md`, coverage collector references, and current audit report before first tooling edit.
- [x] Add a behavior-focused architecture test for backend coverage tooling contracts.
  - Satisfies: TDD requirement and developed-feature test coverage.
  - Verification:
    - Added `backend/tests/WoongBlog.Api.ArchitectureTests/BackendCoverageToolingTests.cs`.
    - Red: `dotnet test backend/tests/WoongBlog.Api.ArchitectureTests/WoongBlog.Api.ArchitectureTests.csproj --filter FullyQualifiedName~BackendCoverageToolingTests` failed for missing coverage script, runsettings, tool manifest, and docs.
    - Green: the same targeted command passed 4 architecture tests after implementation.
- [x] Add backend coverage collection/report generation tooling.
  - Satisfies: tasks 1 and 2.
  - Verification:
    - Added `backend/coverage.runsettings`, `backend/.config/dotnet-tools.json`, and `scripts/run-backend-coverage.sh`.
    - `bash -n scripts/run-backend-coverage.sh` passed.
    - `node -e "JSON.parse(...backend/.config/dotnet-tools.json...)"` passed.
- [x] Update backend coverage documentation.
  - Satisfies: task 3 and task 4.
  - Verification:
    - Updated `backend/TESTING.md` with local coverage commands, output locations, coverage quality guidance, and areas intentionally covered by Component/Integration tests instead of Unit tests.
    - `rg -n "run-backend-coverage|coverage/backend|coverage percentage|Component|Integration|Unit" backend/TESTING.md` confirmed the documented topics.
- [x] Generate current backend coverage reports and capture metrics.
  - Satisfies: task 2 and task 5.
  - Verification:
    - `./scripts/run-backend-coverage.sh unit -v minimal` passed: Unit 14; line 11.0%, branch 4.0%; report at `coverage/backend/unit/report/index.html`.
    - `./scripts/run-backend-coverage.sh component -v minimal` passed: Component 76; line 40.3%, branch 30.0%; report at `coverage/backend/component/report/index.html`.
    - `./scripts/run-backend-coverage.sh integration -v minimal` passed: Integration 194; line 85.0%, branch 51.5%; report at `coverage/backend/integration/report/index.html`.
    - `./scripts/run-backend-coverage.sh full -v minimal` passed: Contract 1 skipped, Unit 14, Component 76, Architecture 35, Integration 194; line 90.1%, branch 60.9%; report at `coverage/backend/full/report/index.html`.
- [x] Run required backend solution test command.
  - Satisfies: task 6.
  - Verification: `dotnet test backend/WoongBlog.sln` passed: Contract 1 skipped, Unit 14, Component 76, Architecture 35, Integration 194; existing NU1901 low-severity `AWSSDK.Core` advisory warning remains.
- [x] Update backend test coverage audit/report artifacts.
  - Satisfies: task 5 and mandatory persistent audit requirement.
  - Verification:
    - Updated `backend/reports/backend-test-coverage-audit-2026-04-24/backend-test-coverage-audit-2026-04-24.md`.
    - Updated `backend/reports/backend-test-coverage-audit-2026-04-24/backend-test-coverage-audit-2026-04-24.html`.
    - Updated `backend/reports/backend-test-coverage-audit-2026-04-24/backend-test-coverage-audit-2026-04-24.json`.
    - Added final task audit artifacts under `backend/reports/backend-code-coverage-reporting-2026-04-25/`.
- [x] Re-check actual work still matches this TODO list after completion.
  - Satisfies: workflow requirement and scoped-change requirement.
  - Verification:
    - `node -e "JSON.parse(...)"` passed for the updated backend test coverage audit JSON, the new task audit JSON, and the backend tool manifest.
    - `git diff --check` passed.
    - `git status --short` shows only expected backend testing docs, coverage tooling, architecture tooling test, TODO, backend reports, and backend runner script changes.

---

## Backend Test Execution Strategy And CI Ergonomics TODO - 2026-04-25

### User Instructions

- Scope backend only.
- Improve backend test execution strategy and CI ergonomics.
- Inspect existing backend test projects and test category traits.
- Ensure all backend tests have correct category traits:
  - `Unit`
  - `Component`
  - `Integration`
  - `Architecture`
  - `Contract`
- Add or update backend test runner scripts if appropriate:
  - `run-unit-tests`
  - `run-component-tests`
  - `run-integration-tests`
  - `run-backend-tests`
- Use the existing project style. Prefer simple scripts or documented `dotnet` commands.
- Update backend README or testing documentation with:
  - what each test category means
  - when to run each category
  - recommended local commands
  - recommended CI commands
  - which tests require external services or Testcontainers
- If GitHub Actions exists, update it so backend tests are split into logical jobs:
  - unit
  - component
  - architecture
  - integration
- Do not make the pipeline unnecessarily slow.
- Do not add new feature tests in this step unless required to fix taxonomy problems.
- Run:
  - `dotnet test backend/WoongBlog.sln --filter "Category=Unit"`
  - `dotnet test backend/WoongBlog.sln --filter "Category=Component"`
  - `dotnet test backend/WoongBlog.sln --filter "Category=Architecture"`
  - `dotnet test backend/WoongBlog.sln --filter "Category=Integration"`
  - `dotnet test backend/WoongBlog.sln`
- Update the backend test audit/report with the new execution strategy and command results.

### Scope Check Before TODO Execution

- Actual planned work matches backend-only execution-strategy scope.
- Planned production changes: none.
- Planned code/config/documentation write set:
  - backend test taxonomy constants and ContractTests trait consistency
  - backend test runner shell scripts under `scripts/`
  - backend testing documentation and README test-command pointers
  - GitHub Actions backend test job split in existing CI workflows
  - persistent backend audit artifacts and this TODO file
- Existing test inventory shows every executable backend test file already has a category trait; the main taxonomy cleanup is consistency for the `Contract` category constant and future command ergonomics.
- Specialist skill search completed with `npx skills find dotnet testing ci`; no external skill installed because returned skills had low install counts and repo-local `tdd` plus .NET guidance are sufficient.
- Backups were prepared under `.agent-backups/backend-test-execution-strategy-2026-04-25/` and a pre-change diff was written to `backend/reports/backend-test-execution-strategy-2026-04-25/prechange-backup.patch`.

### TODOs

- [x] Inspect backend test projects, category traits, docs, scripts, and CI workflows.
  - Satisfies: tasks 1, 2, and 5 inspection requirements.
  - Verification: `dotnet sln backend/WoongBlog.sln list`, `rg -l "\[(Fact|Theory|PactProviderFact)" backend/tests -g "*.cs"`, `rg -n "\[Trait\(" backend/tests -g "*.cs"`, and workflow/doc/script reads completed.
- [x] Confirm planned work still matches this TODO list before implementation.
  - Satisfies: workflow requirement and scoped-change requirement.
  - Verification: confirmed after inspection and before first source/config/documentation edit; no production code changes are planned.
- [x] Standardize backend category taxonomy constants and ContractTests trait usage.
  - Satisfies: task 2.
  - Verification:
    - Added `Contract` to each backend test project's local `TestCategories`.
    - Added `backend/tests/WoongBlog.Api.ContractTests/TestCategories.cs`.
    - Updated `ProviderContractVerificationTests` to use `[Trait(TestCategories.Key, TestCategories.Contract)]`.
    - `comm -23 <(rg -l "\[(Fact|Theory|PactProviderFact)" backend/tests -g "*.cs" | sort) <(rg -l "\[Trait\(" backend/tests -g "*.cs" | sort)` returned no executable backend test files without a trait.
- [x] Add backend test runner scripts for category-filtered and full backend runs.
  - Satisfies: task 3.
  - Verification:
    - Added executable scripts: `scripts/run-unit-tests.sh`, `scripts/run-component-tests.sh`, `scripts/run-architecture-tests.sh`, `scripts/run-integration-tests.sh`, and `scripts/run-backend-tests.sh`.
    - Category scripts target the owning test project for CI speed; `scripts/run-backend-tests.sh` runs the full backend solution.
    - `bash -n scripts/run-unit-tests.sh scripts/run-component-tests.sh scripts/run-architecture-tests.sh scripts/run-integration-tests.sh scripts/run-backend-tests.sh` passed.
- [x] Update backend testing documentation and README pointers.
  - Satisfies: task 4.
  - Verification:
    - Added `backend/TESTING.md` with category meanings, when to run each category, local commands, CI commands, and external-service/Testcontainers notes.
    - Updated `README.md` backend test section with runner commands and a pointer to `backend/TESTING.md`.
    - `rg -n "Category|run-unit-tests|Testcontainers|PACT_PROVIDER_BASE_URL|CI Commands|backend/TESTING" backend/TESTING.md README.md` confirmed the documented topics.
- [x] Split GitHub Actions backend tests into unit, component, architecture, and integration jobs.
  - Satisfies: task 5.
  - Verification:
    - Updated `.github/workflows/ci-dev.yml` and `.github/workflows/ci-main-runtime.yml`.
    - Added `backend-unit-tests`, `backend-component-tests`, `backend-architecture-tests`, and `backend-integration-tests` jobs in both workflows.
    - Kept the Docker availability check only in the integration job.
    - Updated `contract-tests` and `browser-smoke` dependencies to use the split backend jobs.
    - `python3 -c 'import sys, yaml; [yaml.safe_load(open(path, encoding="utf-8")) for path in sys.argv[1:]]; print("yaml ok")' .github/workflows/ci-dev.yml .github/workflows/ci-main-runtime.yml` passed.
    - `rg -n "backend-(unit|component|architecture|integration)-tests|run-(unit|component|architecture|integration)-tests|backend-tests" .github/workflows/ci-dev.yml .github/workflows/ci-main-runtime.yml` confirmed the new job IDs and no remaining `backend-tests` job.
- [x] Run required backend test commands and record results.
  - Satisfies: task 7.
  - Verification:
    - `dotnet test backend/WoongBlog.sln --filter "Category=Unit"` passed: Unit 14.
    - `dotnet test backend/WoongBlog.sln --filter "Category=Component"` passed: Component 76.
    - `dotnet test backend/WoongBlog.sln --filter "Category=Architecture"` passed: Architecture 31.
    - `dotnet test backend/WoongBlog.sln --filter "Category=Integration"` passed: Integration 194.
    - `dotnet test backend/WoongBlog.sln` passed: Contract 1 skipped, Unit 14 passed, Component 76 passed, Architecture 31 passed, Integration 194 passed.
    - Additional taxonomy check `dotnet test backend/WoongBlog.sln --filter "Category=Contract"` passed with 1 skipped Pact provider test because `PACT_PROVIDER_BASE_URL` and pact files are not configured.
    - All `dotnet test` runs emitted the existing NU1901 low-severity `AWSSDK.Core` advisory warning.
- [x] Update backend test audit/report artifacts with execution strategy and command results.
  - Satisfies: task 8 and mandatory audit report requirement.
  - Verification:
    - Added Markdown, HTML, and JSON artifacts under `backend/reports/backend-test-execution-strategy-2026-04-25/`.
    - Report includes changed scope, intentional non-changes, category policy, goal checks, validation commands/results, risks, and final recommendation.
    - `node -e "JSON.parse(require('fs').readFileSync('backend/reports/backend-test-execution-strategy-2026-04-25/backend-test-execution-strategy-2026-04-25.json','utf8')); console.log('json ok')"` passed.
    - `rg` confirmed report command results are present in Markdown, HTML, and JSON.
- [x] Re-check actual work still matches this TODO list after completion.
  - Satisfies: workflow requirement and scoped-change requirement.
  - Verification:
    - `git diff --check` passed.
    - `git status --short` shows only expected workflow, README, backend test taxonomy, TODO, backup, backend testing doc, backend report, and backend runner script changes.
    - No production source files were modified.

---

## Final Backend Test Cleanup And Audit Update TODO - 2026-04-25

### User Instructions

- Scope backend only.
- Use the existing backend test coverage audit report.
- Review all backend test projects:
  - `WoongBlog.Api.UnitTests`
  - `WoongBlog.Api.ComponentTests`
  - `WoongBlog.Api.IntegrationTests`
  - `WoongBlog.Api.ArchitectureTests`
  - `WoongBlog.Api.ContractTests`
- Look for duplicate tests, tests in the wrong project, wrong category traits, brittle assertions, implementation-detail assertions, unused helpers, repeated fixture setup, misleading names, and tests that pass without meaningful behavior assertions.
- Fix only safe cleanup items.
- Do not rewrite production code or change API behavior.
- Do not remove tests unless truly duplicated or meaningless.
- Preserve test taxonomy.
- Prefer small helper extraction only when it reduces obvious duplication.
- Update the audit report final section with current suite summary, tests added across all 8 pushes, remaining gaps, recommended future work, commands run, and final result.
- Run:
  - `dotnet test backend/WoongBlog.sln`
  - `dotnet test backend/WoongBlog.sln --filter "Category=Unit"`
  - `dotnet test backend/WoongBlog.sln --filter "Category=Component"`
  - `dotnet test backend/WoongBlog.sln --filter "Category=Integration"`
  - `dotnet test backend/WoongBlog.sln --filter "Category=Architecture"`
- If ContractTests require external/provider setup and cannot run locally, do not fake success; document the reason clearly in the audit report.

### Scope Check Before TODO Execution

- Actual planned work matches backend-only final cleanup/audit scope.
- Planned production changes: none.
- Safe cleanup candidates identified:
  - Add missing `Category=Integration` traits to pre-existing integration test classes so category-filtered runs match the integration taxonomy.
  - Change the Pact provider contract test from a passing early return to an explicit skip when `PACT_PROVIDER_BASE_URL` or pact files are missing, and tag it as `Category=Contract`.
- Existing dirty files from previous backend test pushes are preserved and treated as prior work.
- Specialist skill search completed with `npx skills find dotnet testing`; no external skill installed because returned skills had low install counts and repo-local `tdd` plus .NET guidance are sufficient.

### TODOs

- [x] Prepare backups before modifying test/report/TODO files.
  - Satisfies: backup requirement.
  - Verification: backups created under `.agent-backups/final-backend-test-cleanup-2026-04-25/` for targeted tests, TODO, and audit artifacts.
- [x] Confirm planned work still matches this TODO list before changing tests.
  - Satisfies: workflow requirement and scoped-change requirement.
  - Verification: reviewed backend test project taxonomy, category trait coverage, assertion/no-op candidates, duplicate risk, and existing audit report; planned write set remains backend tests, TODO, and audit artifacts only.
- [x] Add missing safe category traits and explicit ContractTests skip behavior.
  - Satisfies: wrong category trait cleanup and "do not fake success" contract-test requirement.
  - Verification:
    - Added `Category=Integration` traits to `AdminContentEndpointsTests`, `AuthEndpointsTests`, `AuthSecurityTests`, and `UploadsControllerTests`.
    - Added `Category=Contract` and discovery-time setup skip behavior to `ProviderContractVerificationTests`.
    - `rg --files-without-match "\[Trait\(TestCategories\.Key" backend/tests/WoongBlog.Api.IntegrationTests -g '*Tests.cs'` returned no untagged integration test classes.
    - `dotnet test backend/tests/WoongBlog.Api.ContractTests/WoongBlog.Api.ContractTests.csproj` passed with 1 skipped contract test because `PACT_PROVIDER_BASE_URL` and pact files are not configured.
- [x] Update the existing audit report final section and machine-readable/static variants.
  - Satisfies: requested final audit update and mandatory audit artifact requirement.
  - Verification:
    - Markdown, HTML, and JSON artifacts updated under `backend/reports/backend-test-coverage-audit-2026-04-24/`.
    - `rg` confirmed final cleanup sections, Docker/Testcontainers notes, Pact setup notes, and final result content in Markdown/HTML.
    - `node -e "JSON.parse(...)"` passed for the updated JSON.
- [x] Run required backend test commands and record results.
  - Satisfies: requested validation commands.
  - Verification:
    - `dotnet test backend/WoongBlog.sln` failed locally: Contract 1 skipped, Unit 14 passed, Component 76 passed, Architecture 31 passed, Integration 191 passed and 3 failed because Docker/Testcontainers is unavailable for `PostgresPersistenceContractTests`.
    - `dotnet test backend/WoongBlog.sln --filter "Category=Unit"` passed: Unit 14.
    - `dotnet test backend/WoongBlog.sln --filter "Category=Component"` passed: Component 76.
    - `dotnet test backend/WoongBlog.sln --filter "Category=Integration"` failed locally: Integration 191 passed and 3 Postgres/Testcontainers tests failed because Docker is unavailable; the filter now selects all 194 integration cases.
    - `dotnet test backend/WoongBlog.sln --filter "Category=Architecture"` passed: Architecture 31.
    - `docker ps` and `docker compose -f docker-compose.dev.yml ps -a` both failed because Docker is not available in this WSL distro.
- [x] Re-check actual work still matches this TODO list after completion.
  - Satisfies: workflow requirement and scoped-change requirement.
  - Verification: `git diff --check` passed; `git status --short` shows backend test/report/TODO changes plus expected `.agent-backups/` artifacts and no production source changes.

---

## Persistence / Startup Test TODO - 2026-04-25

### User Instructions

- Scope backend only.
- Use the existing backend test coverage audit report.
- Implement tests for persistence, EF model behavior, startup composition, DI, options, and middleware registration.
- Focus areas:
  - EF model constraints, required fields, unique constraints, cascade/delete behavior, seeding/bootstrap behavior, DbContext model contract, and Postgres/Testcontainers contract if already present.
  - Startup composition, DI service registration, options binding/validation, middleware ordering or registration if testable, and health/metrics registration if present.
- Do not test relational constraints with EF InMemory when behavior depends on relational database semantics; use existing Postgres/Testcontainers or another relational pattern.
- Use ArchitectureTests for dependency/layer rules.
- Use IntegrationTests for full startup/composition behavior.
- Use ComponentTests for persistence contracts when full host is unnecessary.
- Include cases for required services/handlers/stores resolving, missing required options failing clearly when production validates them, EF constraints/indexes, duplicate slug/key violations with a relational provider, seeding/bootstrap idempotence, startup not requiring unavailable external services in tests, and metrics/health registration when present.
- Do not change production composition unless a real bug is found; explain any production change in the audit report.
- Run:
  - `dotnet test backend/WoongBlog.sln`
  - `dotnet test backend/WoongBlog.sln --filter "Category=Architecture"`
  - `dotnet test backend/WoongBlog.sln --filter "Category=Component"`
  - `dotnet test backend/WoongBlog.sln --filter "Category=Integration"`
- Update the audit report with tests added, persistence/startup coverage improved, remaining gaps, and command results.

### Scope Check Before TODO Execution

- Actual planned work matches backend-only persistence/startup scope.
- Planned production changes: none.
- Planned test taxonomy:
  - Component: DbContext model metadata contracts where a full HTTP host is unnecessary.
  - Integration: Postgres/Testcontainers relational behavior and full startup/DI/options/middleware/health behavior.
  - Architecture: dependency/layer/composition rules only where they fit existing ArchitectureTests.
- Existing local skill search found no more specific EF/Testcontainers skill; using repo-local `tdd` workflow and .NET guidance.

### TODOs

- [x] Prepare backups before modifying test/report/TODO files.
  - Satisfies: backup requirement.
  - Verification: backups created under `.agent-backups/persistence-startup-tests-2026-04-25/` for targeted tests, TODO, and audit artifacts.
- [x] Confirm planned work still matches this TODO list before changing tests.
  - Satisfies: workflow requirement and scoped-change requirement.
  - Verification: reviewed existing audit P1 persistence/startup gaps, DbContext configuration, Postgres Testcontainers pattern, startup/options tests, and service registration code; planned write set remains backend tests, TODO, and audit artifacts only.
- [x] Add component tests for DbContext model metadata contracts.
  - Satisfies: EF model constraints, required fields, indexes, JSON column contracts, cascade/delete metadata, and ComponentTests taxonomy.
  - Verification: `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter FullyQualifiedName~DbContextModelContractComponentTests` passed: 4 tests; existing NU1901 `AWSSDK.Core` advisory warning emitted.
- [x] Add Postgres/Testcontainers integration tests for relational persistence contracts.
  - Satisfies: relational unique constraints, required fields, cascade/delete behavior, duplicate slug/key violation, and Postgres contract coverage.
  - Verification: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter FullyQualifiedName~PostgresPersistenceContractTests` passed: 3 tests; existing NU1901 `AWSSDK.Core` advisory warning emitted.
- [x] Add integration tests for seeding/bootstrap idempotence and schema contracts.
  - Satisfies: seeding/bootstrap behavior, DbContext relational schema contract, and startup not relying on external services beyond the test database.
  - Verification: included in targeted `PostgresPersistenceContractTests` pass above; the test class now covers Postgres search schema, bootstrap idempotence, runtime data preservation, relational required/unique violations, and database-side cascade behavior.
- [x] Add integration tests for startup composition, DI, options, middleware, health/OpenAPI registration, and hosted service registration.
  - Satisfies: required service registrations, Application handlers, Infrastructure stores/services, options binding/validation, middleware/registration behavior, health/metrics registration if present.
  - Verification: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter FullyQualifiedName~StartupCompositionTests` passed: 7 tests; `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter FullyQualifiedName~StartupOptionsValidationTests` passed: 8 tests; existing NU1901 `AWSSDK.Core` advisory warning emitted.
- [x] Add architecture tests only for dependency/layer rules that fit ArchitectureTests.
  - Satisfies: ArchitectureTests rule.
  - Verification: `dotnet test backend/tests/WoongBlog.Api.ArchitectureTests/WoongBlog.Api.ArchitectureTests.csproj --filter FullyQualifiedName~Program_ComposesServicesMiddlewareAndEndpoints_InExpectedBoundaryOrder` passed: 1 test; existing NU1901 `AWSSDK.Core` advisory warning emitted.
- [x] Run required backend test commands and record results.
  - Satisfies: requested validation commands.
  - Verification:
    - `dotnet test backend/WoongBlog.sln` passed: Contract 1, Unit 14, Component 76, Architecture 31, Integration 194.
    - `dotnet test backend/WoongBlog.sln --filter "Category=Architecture"` passed: Architecture 31.
    - `dotnet test backend/WoongBlog.sln --filter "Category=Component"` passed: Component 76.
    - `dotnet test backend/WoongBlog.sln --filter "Category=Integration"` passed: Integration 160.
    - All runs emitted the existing NU1901 low-severity `AWSSDK.Core` advisory warning.
- [x] Update the audit report with tests added, coverage improved, remaining gaps, command results, and final recommendation.
  - Satisfies: requested audit update and completion audit requirement.
  - Verification: Markdown, HTML, and JSON artifacts updated under `backend/reports/backend-test-coverage-audit-2026-04-24/`; `node -e "JSON.parse(...)"` passed for the machine-readable JSON; `git diff --check` passed.
- [x] Re-check actual work still matches this TODO list after completion.
  - Satisfies: workflow requirement and scoped-change requirement.
  - Verification: `git status --short` shows only intended backend test/report/TODO changes plus `.agent-backups/persistence-startup-tests-2026-04-25/`; no production source files are modified.

---

## User Instructions

- Scope backend only.
- Do not implement or modify tests.
- Do not modify production code.
- Create a strict backend test coverage audit report at `backend/reports/backend-test-coverage-audit-2026-04-24/backend-test-coverage-audit-2026-04-24.md`.
- Preserve existing backend test taxonomy:
  - `WoongBlog.Api.UnitTests`: pure Application/helper/validator tests only.
  - `WoongBlog.Api.ComponentTests`: Application + Infrastructure + EF InMemory/fakes/filesystem/HttpClient style tests, but not full HTTP host.
  - `WoongBlog.Api.IntegrationTests`: full ASP.NET test host and endpoint behavior.
  - `WoongBlog.Api.ArchitectureTests`: project/layer/dependency boundary tests.
  - `WoongBlog.Api.ContractTests`: provider/contract verification tests.
- Build a feature-by-feature matrix grouped by the requested feature areas.
- Be strict and pessimistic; do not claim coverage unless a test file and asserted behavior can be named.
- Recommend an 8-step direct origin push plan, not a PR plan.
- After creating the report, run `dotnet test backend/WoongBlog.sln` and update the report with the command result.

## Backup / Scope Check

- Backup requirement reviewed. No production code, test code, or configuration files are planned for modification.
- Target TODO and report artifacts did not exist before this work.
- Audit-only scope allows generating persistent audit artifacts only.

## TODOs

- [x] Confirm planned work still matches the requested audit-only backend scope before scanning.
  - Satisfies: backend-only, no production/test modifications.
  - Verification: `git status --short` was clean before audit artifact creation; write set remains TODO/report artifacts only.
- [x] Inventory backend production files by module and feature area.
  - Satisfies: backend feature inventory grouped by requested categories.
  - Verification: `rg --files backend/src backend/tests` and `find backend/src/WoongBlog.Api/Modules backend/src/WoongBlog.Application/Modules backend/src/WoongBlog.Infrastructure/Modules -type f`.
- [x] Inventory backend test projects and summarize existing suites.
  - Satisfies: existing backend test suite summary and taxonomy preservation.
  - Verification: inspected test project files and test method names/assertions with `rg` and `sed`.
- [x] Map features to existing test files and asserted behaviors.
  - Satisfies: strict coverage matrix with named evidence.
  - Verification: inspected endpoint, component, persistence, architecture, contract, and unit test assertions before assigning classifications.
- [x] Write the Markdown audit report at the requested path.
  - Satisfies: persistent audit report requirement and requested report content.
  - Verification: `rg` confirmed suite summary, feature sections, direct push plan, and validation row are present.
- [x] Run `dotnet test backend/WoongBlog.sln`.
  - Satisfies: requested validation command.
  - Verification: passed with exit code 0; Contract 1, Unit 14, Component 34, Architecture 30, Integration 98.
- [x] Update the report with the `dotnet test` command result.
  - Satisfies: final report must include validation performed.
  - Verification: report validation log records pass counts and NU1901 `AWSSDK.Core` warning.
- [x] Re-check no production or test files were modified and actual work matches this TODO list.
  - Satisfies: audit-only constraint and workflow requirement.
  - Verification: `git status --short` shows only untracked audit report directory and `todolist-2026-04-25.md`.

---

# WorkVideo Test Implementation TODO - 2026-04-25

## User Instructions

- Scope backend only.
- Use the existing backend test coverage audit report.
- Implement tests for WorkVideo behavior.
- Focus areas:
  - Work video upload.
  - Work video metadata persistence.
  - Work video ordering/reordering.
  - Work video delete.
  - File/storage side effects.
  - Cleanup behavior.
  - Public work video rendering/query behavior.
  - Invalid upload request handling.
  - Missing work/video not found behavior.
- Use IntegrationTests for endpoint behavior:
  - auth requirement.
  - status code.
  - request/response shape.
  - database persistence.
  - public response shape.
- Use ComponentTests for storage/service behavior:
  - temp file/fake storage side effects.
  - cleanup behavior.
  - ordering logic.
  - failure handling.
- Do not use real external storage; use existing fake storage, temp directories, or test-specific file providers.
- Include edge cases:
  - unauthenticated upload is rejected.
  - upload to missing work returns not found.
  - invalid file or missing file returns validation error.
  - successful upload persists metadata.
  - successful upload stores file or fake file as expected.
  - deleting video removes database record.
  - deleting video removes or schedules cleanup of associated files as production behavior defines.
  - reorder updates order deterministically.
  - reorder with invalid IDs returns expected failure.
  - public query includes only visible/published video data if applicable.
- Be careful with filesystem tests:
  - use temp directories.
  - cleanup after test.
  - make tests deterministic.
- After implementing tests, run:
  - `dotnet test backend/WoongBlog.sln`
  - `dotnet test backend/WoongBlog.sln --filter "Category=Component"`
  - `dotnet test backend/WoongBlog.sln --filter "Category=Integration"`
- Update the audit report with:
  - tests added.
  - WorkVideo coverage improved.
  - remaining storage/video gaps.
  - command results.

## Scope Check Before TODO Execution

- Actual planned work matches backend-only WorkVideo scope.
- Planned production changes: none unless a new test exposes a clear WorkVideo defect.
- Planned test taxonomy:
  - Integration: WorkVideo endpoint auth/status/request-response/DB/public projection behavior.
  - Component: local storage side effects, cleanup service job processing, deterministic reorder handler/store behavior, and failure paths using EF InMemory/temp directories/fakes.
  - Unit: no pure helper/validator work identified for this slice.
- Public visibility is represented by the owning `Work.Published` flag; video rows do not have a separate published/private flag.
- Specialist skill search completed with `npx skills find dotnet xunit integration testing`; no external skill installed because returned skills had low install counts and the repo-local `tdd` workflow plus existing .NET guidance are sufficient.

## TODOs

- [x] Prepare backups before modifying test/report files.
  - Satisfies: backup requirement.
  - Verification: backups created under `.agent-backups/workvideo-tests-2026-04-25/` for the TODO, WorkVideo integration tests, and audit report artifacts.
- [x] Confirm planned work still matches this TODO list before changing tests.
  - Satisfies: workflow requirement and scoped-change requirement.
  - Verification: reviewed WorkVideo endpoints, handlers, stores, local storage, cleanup service, and existing audit matrix; planned write set remains backend tests, TODO, and audit artifacts only.
- [x] Add integration tests for WorkVideo upload auth, missing work, invalid/missing file request handling, successful local upload metadata persistence, storage side effect, and public projection.
  - Satisfies: upload, metadata persistence, auth requirement, status code, request/response shape, database persistence, public response shape, invalid upload handling, missing work behavior.
  - Verification: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter FullyQualifiedName~WorkVideoEndpointsTests` passed: 17 tests. Initial red result exposed an invalid empty multipart test request; adjusted to a valid multipart form without a `file` part.
- [x] Add integration tests for WorkVideo delete and reorder edge behavior.
  - Satisfies: delete DB effect, cleanup scheduling, deterministic reorder, invalid IDs, missing video not found.
  - Verification: included in targeted `WorkVideoEndpointsTests` pass above.
- [x] Add component tests for local storage and cleanup service side effects/failure handling.
  - Satisfies: temp-directory file behavior, cleanup behavior, fake/no external storage, deterministic cleanup outcomes.
  - Verification: `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter FullyQualifiedName~WorkVideoComponentTests` passed: 8 tests.
- [x] Add component tests for WorkVideo ordering logic through handler/store behavior.
  - Satisfies: ordering logic and deterministic reorder behavior without a full HTTP host.
  - Verification: included in targeted `WorkVideoComponentTests` pass above.
- [x] Run required backend test commands and record results.
  - Satisfies: requested validation commands.
  - Verification:
    - `dotnet test backend/WoongBlog.sln` passed: Contract 1, Unit 14, Component 60, Architecture 30, Integration 182.
    - `dotnet test backend/WoongBlog.sln --filter "Category=Component"` passed: Component 60.
    - `dotnet test backend/WoongBlog.sln --filter "Category=Integration"` passed: Integration 133.
    - All runs emitted the existing NU1901 low-severity `AWSSDK.Core` advisory warning.
- [x] Update the audit report with tests added, WorkVideo coverage improved, remaining storage/video gaps, command results, and final recommendation.
  - Satisfies: requested audit update and completion audit requirement.
  - Verification: Markdown, HTML, and JSON artifacts updated under `backend/reports/backend-test-coverage-audit-2026-04-24/`; `rg` confirmed WorkVideo sections and validation rows; JSON parsed successfully with `node -e "JSON.parse(...)"`.
- [x] Re-check actual work still matches this TODO list after completion.
  - Satisfies: workflow requirement and scoped-change requirement.
  - Verification: `git diff --check` passed; `git status --short` shows only the intended backend test/report/TODO changes plus `.agent-backups/workvideo-tests-2026-04-25/`.

---

# Priority 1 Auth Test TODO - 2026-04-25

## User Instructions

- Scope backend only.
- Use the existing backend test coverage audit report at `backend/reports/backend-test-coverage-audit-2026-04-24/backend-test-coverage-audit-2026-04-24.md`.
- Implement only Priority 1 tests related to auth, session, login, logout, CSRF, auth redirect behavior, auth-required admin endpoint rejection, and unauthorized/forbidden behavior.
- Do not implement unrelated tests.
- Preserve existing test taxonomy:
  - Pure validator/helper logic goes to UnitTests.
  - Auth/session behavior using Infrastructure, EF InMemory, DefaultHttpContext, fakes, or HttpClient-like collaborators goes to ComponentTests.
  - Full HTTP endpoint behavior goes to IntegrationTests.
- Do not use real Google OAuth; use existing test auth handler or fake auth infrastructure.
- Add both success and failure tests for unauthenticated admin rejection, authenticated admin success where appropriate, logout auth/session state, CSRF missing/invalid/valid behavior, invalid sessions, and expired/revoked behavior where present.
- Avoid production auth rewrites unless a very small testability seam is required.
- Run:
  - `dotnet test backend/WoongBlog.sln`
  - `dotnet test backend/WoongBlog.sln --filter "Category=Unit"`
  - `dotnet test backend/WoongBlog.sln --filter "Category=Component"`
  - `dotnet test backend/WoongBlog.sln --filter "Category=Integration"`
- Update the audit report with tests added, files changed, behavior covered, remaining auth/session/CSRF gaps, and test command results.

## Scope Check Before TODO Execution

- Actual planned work matches backend-only scope.
- Planned production changes: none, except a test-only seam in `CustomWebApplicationFactory` if fake OIDC metadata is needed to test login redirects without network OAuth.
- Planned test taxonomy:
  - Integration: full HTTP endpoint auth/session/login/logout/CSRF/admin authorization tests.
  - Component: `AuthRecorder` invalid session edge tests with EF InMemory and `DefaultHttpContext`.
  - Unit: no pure validator/helper auth work identified.
- Specialist skill search completed with `npx --yes skills find dotnet testing`; no external skill installed because local `tdd` and `dotnet-best-practices` skills are already more directly applicable.

## TODOs

- [x] Prepare backups before modifying test/report files.
  - Satisfies: backup requirement.
  - Verification: backups created under `.agent-backups/priority1-auth-tests-2026-04-25/` for `CustomWebApplicationFactory.cs`, `TestAuthHandler.cs`, `AuthRecorderComponentTests.cs`, the audit Markdown report, and this TODO file.
- [x] Add integration tests for session payload shape, non-admin session role, login redirect challenge, logout POST CSRF/success behavior, and representative admin 401/403/200 behavior.
  - Satisfies: auth, session, login, logout, auth redirect behavior, auth-required admin endpoint rejection, unauthorized/forbidden behavior, success/failure coverage, no real Google OAuth.
  - Verification: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter FullyQualifiedName~AuthFlowIntegrationTests` passed: 31 tests. Initial red result exposed missing test OIDC scheme; fixed with `TestOidcChallengeHandler`.
- [x] Add integration tests for CSRF invalid-token rejection and valid-token acceptance on protected mutations without expanding into unrelated content semantics.
  - Satisfies: CSRF missing/invalid/valid behavior and backend-owned CSRF coverage.
  - Verification: included in `AuthFlowIntegrationTests` targeted pass above; covered missing logout CSRF, invalid admin mutation token with no persistence, valid admin mutation token, and valid-token auth rejection for anonymous/non-admin principals.
- [x] Add component tests for invalid auth session claims/profile/absolute-expiration behavior around `AuthRecorder`.
  - Satisfies: session behavior taxonomy, invalid session, expired/revoked behavior.
  - Verification: `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter FullyQualifiedName~AuthRecorderComponentTests` passed: 12 tests.
- [x] Run required backend test commands and record results.
  - Satisfies: user validation commands.
  - Verification:
    - `dotnet test backend/WoongBlog.sln` passed: Contract 1, Unit 14, Component 37, Architecture 30, Integration 129.
    - `dotnet test backend/WoongBlog.sln --filter Category=Unit` passed: Unit 14.
    - `dotnet test backend/WoongBlog.sln --filter Category=Component` passed: Component 37.
    - `dotnet test backend/WoongBlog.sln --filter Category=Integration` passed: Integration 73.
    - All runs emitted the existing NU1901 low-severity `AWSSDK.Core` advisory warning.
- [x] Update the audit report with added tests, files changed, behavior covered, remaining gaps, command results, and final recommendation.
  - Satisfies: requested audit update and completion audit requirement.
  - Verification: Markdown, HTML, and JSON artifacts updated under `backend/reports/backend-test-coverage-audit-2026-04-24/`; `rg` confirmed required Markdown sections; JSON parsed successfully with `node -e "JSON.parse(...)"` after `jq` was unavailable.
- [x] Re-check actual work still matches this TODO list after completion.
  - Satisfies: workflow requirement and scoped-change requirement.
  - Verification: `git diff --check` passed; `git status --short` shows only the intended modified test/report/TODO files plus new report/test artifacts and `.agent-backups/priority1-auth-tests-2026-04-25/`.

---

# Priority 1 Admin Mutation Test TODO - 2026-04-25

## User Instructions

- Scope backend only.
- Use the existing backend test coverage audit report.
- Implement only Priority 1 tests related to destructive or state-changing admin commands.
- Focus areas:
  - Admin content create/update/delete.
  - Admin page create/update/delete.
  - Admin blog create/update/delete/publish/unpublish if present.
  - Admin work create/update/delete/publish/unpublish if present.
  - Admin member create/update/delete/update role if present.
  - Site settings update if present.
  - Any admin mutation that changes database state.
- Do not implement public read tests yet.
- Do not implement WorkVideo-specific tests yet unless the audit report says the command is part of general admin destructive behavior.
- Use IntegrationTests for endpoint-level route/status/auth/request-response JSON/persistence behavior.
- Use ComponentTests only where direct handler/service/store coverage is more appropriate.
- Test both success and failure cases.
- Prefer checking database state after mutation.
- Do not assert implementation details that are not part of behavior.
- Run:
  - `dotnet test backend/WoongBlog.sln`
  - `dotnet test backend/WoongBlog.sln --filter "Category=Integration"`
  - `dotnet test backend/WoongBlog.sln --filter "Category=Component"`
- Update the audit report with tests added, admin mutation coverage improved, remaining gaps, and command results.

## Scope Check Before TODO Execution

- Actual planned work matches backend-only scope.
- Planned production changes: none.
- Planned test taxonomy:
  - Integration: page update, site settings update, blog create/update/delete, work create/update/delete endpoint behavior and DB state.
  - Component: no direct handler/store-only case identified for this slice.
  - Unit: no pure helper/validator work identified.
- Existing audit confirms there are no admin member create/update/delete/update-role endpoints; member mutations are out of scope unless production endpoints are found.
- Existing audit marks WorkVideo as its own P1 area; WorkVideo-specific mutation tests are intentionally deferred.
- Specialist skill search completed with `npx --yes skills find dotnet integration testing`; no external skill installed because the available external skills had low install counts and the repo-local `tdd` workflow is sufficient.

## TODOs

- [x] Prepare backups before modifying test/report files.
  - Satisfies: backup requirement.
  - Verification: backups created under `.agent-backups/priority1-admin-mutation-tests-2026-04-25/` for the TODO, Markdown/HTML/JSON audit report artifacts, and existing `AdminContentEndpointsTests.cs`.
- [x] Add integration tests for admin mutation auth rejection with valid CSRF.
  - Satisfies: unauthenticated request is rejected and endpoint-level auth requirement.
  - Verification: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter FullyQualifiedName~AdminMutationEndpointsTests` passed: 32 tests.
- [x] Add integration tests for admin page update success/failure persistence behavior.
  - Satisfies: admin page update, invalid/missing/success behavior, unrelated record preservation.
  - Verification: included in targeted `AdminMutationEndpointsTests` pass.
- [x] Add integration tests for admin blog create/update/delete/publish/unpublish persistence behavior.
  - Satisfies: admin blog create/update/delete and publish/unpublish where represented by `Published`.
  - Verification: included in targeted `AdminMutationEndpointsTests` pass.
- [x] Add integration tests for admin work create/update/delete/publish/unpublish persistence behavior, excluding WorkVideo-specific endpoints.
  - Satisfies: admin work create/update/delete and publish/unpublish where represented by `Published`.
  - Verification: included in targeted `AdminMutationEndpointsTests` pass.
- [x] Add integration tests for site settings update persistence behavior.
  - Satisfies: site settings update and partial field preservation.
  - Verification: included in targeted `AdminMutationEndpointsTests` pass.
- [x] Run required backend test commands and record results.
  - Satisfies: user validation commands.
  - Verification:
    - `dotnet test backend/WoongBlog.sln` passed: Contract 1, Unit 14, Component 37, Architecture 30, Integration 161.
    - `dotnet test backend/WoongBlog.sln --filter Category=Integration` passed: Integration 105.
    - `dotnet test backend/WoongBlog.sln --filter Category=Component` passed: Component 37.
    - All runs emitted the existing NU1901 low-severity `AWSSDK.Core` advisory warning.
- [x] Update the audit report with tests added, improved mutation coverage, remaining gaps, command results, and final recommendation.
  - Satisfies: requested audit update and completion audit requirement.
  - Verification: Markdown, HTML, and JSON artifacts updated under `backend/reports/backend-test-coverage-audit-2026-04-24/`; `rg` confirmed required Markdown sections; JSON parsed successfully with `node -e "JSON.parse(...)"`.
- [x] Re-check actual work still matches this TODO list after completion.
  - Satisfies: workflow requirement and scoped-change requirement.
  - Verification: `git diff --check` passed; `git status --short` shows only the intended modified report/TODO files, new `AdminMutationEndpointsTests.cs`, and `.agent-backups/priority1-admin-mutation-tests-2026-04-25/`.

---

# Public Read Query Test TODO - 2026-04-25

## User Instructions

- Scope backend only.
- Use the existing backend test coverage audit report.
- Implement tests for public read/query behavior.
- Focus areas:
  - Public home query.
  - Public site settings query.
  - Public page by slug query.
  - Public blog list/detail query.
  - Public work list/detail query.
  - Published/unpublished filtering.
  - Sorting by published date/order.
  - Pagination if present.
  - Slug not found behavior.
  - Asset/media fields included in public DTOs if present.
- Do not implement admin mutation tests in this step.
- Do not implement WorkVideo upload/order/delete tests in this step.
- Use ComponentTests for query handler/store behavior where the goal is domain/query correctness.
- Use IntegrationTests for endpoint-level HTTP status, route, serialization shape, not found behavior, and anonymous access.
- Include edge cases:
  - unpublished content is not visible publicly.
  - draft/private content is excluded.
  - missing slug returns expected not found behavior.
  - empty database returns stable empty response.
  - ordering is deterministic.
  - pagination boundaries behave correctly if implemented.
  - related assets are included only as intended.
- Avoid overfitting to exact JSON formatting unless it is a public API contract.
- Run:
  - `dotnet test backend/WoongBlog.sln`
  - `dotnet test backend/WoongBlog.sln --filter "Category=Component"`
  - `dotnet test backend/WoongBlog.sln --filter "Category=Integration"`
- Update the audit report with tests added, public query coverage improved, remaining public read gaps, and command results.

## Scope Check Before TODO Execution

- Actual planned work matches backend-only public read/query scope.
- Planned production changes: none unless a new test exposes a clear public query defect.
- Planned test taxonomy:
  - Component: handler/store correctness for home, site settings, page by slug, blog list/detail, work list/detail, filtering, ordering, paging, empty results, and asset DTO fields.
  - Integration: public endpoint status, route, serialization shape, not-found behavior, and anonymous client access.
  - Unit: no pure validator/helper work identified for this slice.
- Admin mutation tests and WorkVideo upload/order/delete tests are intentionally out of scope.
- Draft/private content is represented by `Published = false`; no separate private content field exists in the current domain model.
- Specialist skill search completed with `npx -y skills find "dotnet testing"`; no external skill installed because results were low-install and the repo-local `tdd` and `.NET` guidance are sufficient.

## TODOs

- [x] Prepare backups before modifying test/report files.
  - Satisfies: backup requirement.
  - Verification: backups created under `.agent-backups/public-read-query-tests-2026-04-25/` for the TODO, public component/integration tests, and audit report artifacts.
- [x] Confirm planned work still matches this TODO list before changing tests.
  - Satisfies: workflow requirement and scoped-change requirement.
  - Verification: reviewed production endpoints/stores and current test taxonomy before edits; planned write set remains public read/query tests, TODO, and audit artifacts only.
- [x] Add component tests for public home, site settings, and page-by-slug query behavior.
  - Satisfies: public home query, public site settings query, public page by slug query, assets where present, missing slug behavior, and empty/missing data behavior.
  - Verification: `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter FullyQualifiedName~PublicQueryHandlerComponentTests` passed: 32 tests; existing NU1901 `AWSSDK.Core` warning emitted.
- [x] Add component tests for public blog list/detail query behavior.
  - Satisfies: public blog list/detail query, published/draft filtering, published-date ordering, pagination boundaries, missing slug behavior, stable empty responses, and cover asset fields.
  - Verification: `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter FullyQualifiedName~PublicQueryHandlerComponentTests` passed: 32 tests; existing NU1901 `AWSSDK.Core` warning emitted.
- [x] Add component tests for public work list/detail query behavior.
  - Satisfies: public work list/detail query, published/draft filtering, published-date ordering, pagination boundaries, missing slug behavior, stable empty responses, thumbnail/icon/video media fields, and intentionally excluded draft assets.
  - Verification: `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter FullyQualifiedName~PublicQueryHandlerComponentTests` passed: 32 tests; existing NU1901 `AWSSDK.Core` warning emitted.
- [x] Add integration tests for public endpoint status, routes, serialization shape, anonymous access, and not-found behavior.
  - Satisfies: endpoint-level HTTP status, route, serialization shape, not-found behavior, anonymous access allowed, and stable empty list responses without overfitting JSON formatting.
  - Verification: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter FullyQualifiedName~PublicEndpointsTests` passed: 27 tests after aligning the site-settings shape assertion to the existing `gitHubUrl`/`linkedInUrl` JSON contract; existing NU1901 `AWSSDK.Core` warning emitted.
- [x] Run required backend test commands and record results.
  - Satisfies: user validation commands.
  - Verification:
    - `dotnet test backend/WoongBlog.sln` passed: Contract 1, Unit 14, Component 52, Architecture 30, Integration 172.
    - `dotnet test backend/WoongBlog.sln --filter "Category=Component"` passed: Component 52.
    - `dotnet test backend/WoongBlog.sln --filter "Category=Integration"` passed: Integration 116.
    - All runs emitted the existing NU1901 low-severity `AWSSDK.Core` advisory warning.
- [x] Update the audit report with tests added, coverage improved, remaining gaps, command results, and final recommendation.
  - Satisfies: requested audit update and completion audit requirement.
  - Verification: Markdown, HTML, and JSON artifacts updated under `backend/reports/backend-test-coverage-audit-2026-04-24/`; `rg` confirmed public read sections and command results; JSON parsed successfully with `node -e "JSON.parse(...)"`; `git diff --check` passed.
- [x] Re-check actual work still matches this TODO list after completion.
  - Satisfies: workflow requirement and scoped-change requirement.
  - Verification: `git status --short` shows only public read/query test files, audit report artifacts, this TODO file, and `.agent-backups/public-read-query-tests-2026-04-25/`; `git diff --name-only` matches the planned write set.

---

# AI Batch Runtime Test TODO - 2026-04-25

## User Instructions

- Scope backend only.
- Use the existing backend test coverage audit report.
- Implement tests for AI/Codex/batch/runtime config behavior.
- Focus areas:
  - Admin AI endpoints.
  - Blog AI fix behavior.
  - Batch job creation.
  - Batch job status transitions.
  - Batch target selection.
  - Runtime config loading.
  - Codex process/runtime invocation using existing fakes.
  - Failure handling.
  - Partial failure behavior.
  - Job result persistence.
- Do not call real external AI services.
- Do not call real Codex or shell tools unless the existing test suite already uses a fake executable/script pattern.
- Use existing fake process/runtime helpers where possible.
- Use ComponentTests for runtime/service/job behavior.
- Use IntegrationTests for endpoint-level admin API behavior.
- Include cases such as unauthenticated admin AI request rejection, invalid AI request validation, valid batch request job records, status changes, failed runtime calls, partial failures where supported, runtime defaults, invalid config safe failure, and deterministic duplicate/repeated requests where relevant.
- Do not rewrite the AI architecture.
- Do not implement frontend tests.
- After implementing tests, run:
  - `dotnet test backend/WoongBlog.sln`
  - `dotnet test backend/WoongBlog.sln --filter "Category=Component"`
  - `dotnet test backend/WoongBlog.sln --filter "Category=Integration"`
- Update the audit report with tests added, AI/batch/runtime coverage improved, remaining gaps, and command results.

## Scope Check Before TODO Execution

- Actual planned work matches backend-only AI/Codex/batch/runtime config scope.
- Planned production changes: none.
- Planned test taxonomy:
  - Integration: Admin AI endpoint auth/validation/duplicate job behavior through the ASP.NET host.
  - Component: batch scheduler/runner/item processor/store state transitions, partial/failure persistence, runtime config defaults/fallbacks, options validation, and Codex fake-process invocation.
  - Unit: no pure helper-only AI test is required for this slice.
- External AI calls will be avoided by replacing `IBlogAiFixService` with fakes in endpoint tests and by using fake shell scripts only for Codex process tests, matching existing component-test patterns.
- Specialist skill search completed with `npx skills find dotnet testing`; no external skill installed because results were low-install and the repo-local `tdd` and `.NET` guidance are sufficient.

## TODOs

- [x] Prepare backups before modifying test/report files.
  - Satisfies: backup requirement.
  - Verification: backups created under `.agent-backups/ai-batch-runtime-tests-2026-04-25/` for the TODO, existing AI integration/component tests, and audit report artifacts.
- [x] Confirm planned work still matches this TODO list before changing tests.
  - Satisfies: workflow requirement and scoped-change requirement.
  - Verification: reviewed existing AI endpoint tests, batch handlers/stores, runtime config handler/options, Codex fake-script component tests, and audit AI section; planned write set remains backend tests, TODO, and audit artifacts only.
- [x] Add integration tests for Admin AI endpoint auth rejection, validation failures, and deterministic duplicate/repeated batch-job creation.
  - Satisfies: Admin AI endpoints, unauthenticated rejection, invalid request validation, duplicate/repeated request behavior, no real AI service calls.
  - Verification: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter FullyQualifiedName~AdminAiEndpointsTests` passed: 17 tests; existing NU1901 `AWSSDK.Core` warning emitted.
- [x] Add component tests for batch creation target selection and job record persistence.
  - Satisfies: batch job creation, batch target selection, result/job persistence.
  - Verification: `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter FullyQualifiedName~AiBatchRuntimeComponentTests` passed: 10 tests after resolving scheduler from a DI scope.
- [x] Add component tests for batch status transitions, failure handling, and partial failure behavior.
  - Satisfies: pending/running/succeeded/failed behavior using current `queued/running/completed/failed` job states and `pending/running/succeeded/failed` item states.
  - Verification: included in the targeted `AiBatchRuntimeComponentTests` pass; covers running-job reset, queued-to-running-to-completed success, all-failed final `failed`, partial failure final `completed`, failed item error persistence, and successful auto-apply preservation of unrelated data.
- [x] Add component tests for runtime config defaults, invalid config safe failure, and Codex fake-process invocation failure/success details.
  - Satisfies: runtime config loading, stable defaults, missing/invalid config failure, Codex process/runtime invocation through existing fake script pattern.
  - Verification: `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter "FullyQualifiedName~AiBatchRuntimeComponentTests|FullyQualifiedName~BlogAiFixServiceCodexRuntimeComponentTests"` passed: 16 tests; existing NU1901 `AWSSDK.Core` warning emitted.
- [x] Run required backend test commands and record results.
  - Satisfies: user validation commands.
  - Verification:
    - `dotnet test backend/WoongBlog.sln` passed: Contract 1, Unit 14, Component 72, Architecture 30, Integration 185.
    - `dotnet test backend/WoongBlog.sln --filter "Category=Component"` passed: Component 72.
    - `dotnet test backend/WoongBlog.sln --filter "Category=Integration"` passed: Integration 136.
    - All runs emitted the existing NU1901 low-severity `AWSSDK.Core` advisory warning.
- [x] Update the audit report with tests added, AI/batch/runtime coverage improved, remaining gaps, command results, and final recommendation.
  - Satisfies: requested audit update and mandatory audit artifact requirement.
  - Verification: Markdown, HTML, and JSON artifacts updated under `backend/reports/backend-test-coverage-audit-2026-04-24/`; `rg` confirmed AI batch/runtime sections and command results; JSON parsed successfully with `node -e "JSON.parse(...)"`; `git diff --check` passed.
- [x] Re-check actual work still matches this TODO list after completion.
  - Satisfies: workflow requirement and scoped-change requirement.
  - Verification: `git status --short` shows only the intended AI integration/component tests, audit report artifacts, this TODO file, new `AiBatchRuntimeComponentTests.cs`, and `.agent-backups/ai-batch-runtime-tests-2026-04-25/`.

---

# Origin Dev Addenda Preserved During Merge Prep

# TODO List (2026-04-25)

## Fresh Pass Revalidation
- Context:
  - The worktree already contains partial edits from a prior attempt.
  - This pass revalidates only the user-approved follow-up scope from the latest plan: Works TOC rail behavior, uploaded/HLS preview hover behavior, Study restore across viewport changes, Mermaid centering, and Study card excerpt/tag rendering.
- Scope check before implementation:
  - [x] F1 Keep `TableOfContents` generic again and move Works sticky/clamp behavior into a Works-specific rail wrapper.
  - [x] F2 Remove the Works-internal TOC scroll assumption and verify `On This Work` remains the Works-only label.
  - [x] F3 Rework uploaded/HLS preview hover so it follows lower control-band hover from the video frame, with no standalone strip and no resize cursor.
  - [x] F4 Preserve preview clear behavior and desktop-only gating for hover previews.
  - [x] F5 Restore `/blog` mobile reading state even after resizing to desktop width and returning via browser back, while leaving fresh desktop navigation on normal pagination.
  - [x] F6 Center Mermaid diagrams in the shared renderer for both public and admin/editor usage.
  - [x] F7 Keep Study cards rendering saved excerpts only, with no blank-excerpt fallback and all saved tags visible.
  - [x] F8 Re-run focused verification and write fresh audit artifacts that describe this pass accurately.

## Follow-up Hotfix Addendum
- Context:
  - User confirmed the prior work is broadly acceptable, but reported remaining instability in the actual browser UX.
  - This addendum keeps the existing partial hotfix edits and only tightens the reported edge cases.
- Scope check before addendum implementation:
  - [x] A1 Prevent the Works `On This Work` rail from covering wide/theater uploaded videos by dynamically avoiding overlapping video frames.
  - [x] A2 Change the Works mobile feed from manual `Load more` to automatic infinite scroll and stop at the final page.
  - [x] A3 Stabilize Study browser-back behavior across mobile/desktop switches so desktop width does not render the mobile restored-reading layout.
  - [x] A4 Keep the manual-only Study excerpt policy and remove now-unused backend excerpt fallback variables.
  - [x] A5 Re-run focused unit/browser/backend verification and refresh the audit artifacts.

## Task
- Implement the Works / Study hotfix plan from the prior agent in a fresh pass.
- Keep scope to the planned hotfix: work-detail TOC behavior, uploaded/HLS video UX stability, Study mobile feed/back restoration, Study excerpt/tag policy, regression coverage, and the mandatory audit artifacts.

## Skills In Use
- `find-skills`: confirmed the repo-local skill set is sufficient; no external install was justified because the search hits were lower quality than the already-installed local skills.
- `tdd`: work in red-green-refactor slices with behavior-first tests around TOC, player behavior, feed restore, and backend excerpt/preview policy.
- `nextjs-app-router-patterns`: guide the App Router list/detail state changes and client/server boundaries.
- `dotnet-best-practices`: keep backend policy/query changes small and explicit.
- `e2e-testing-patterns`: keep Playwright additions focused on critical public flows only.

## User Instruction Mapping
- [x] I1 Works right rail:
  - [x] T1 Add configurable TOC title support so works render `On This Work` and blogs keep `On This Page`.
  - [x] T2 Make the works TOC rail range-aware so it sticks only through the work body range and releases before prev/next + `More Works`.
  - Verification: `src/test/table-of-contents.test.tsx`; `tests/public-work-toc.spec.ts`.
- [x] I2 Uploaded/HLS player behavior:
  - [x] T3 Restore native `<video controls>` for uploaded/HLS videos while keeping the paused center overlay affordance.
  - [x] T4 Replace the current custom primary control row with desktop-only preview behavior aligned to the native progress region.
  - [x] T5 Disable preview entirely on touch/mobile and clear preview state on leave/play/seek/source changes.
  - Verification: `src/test/work-video-player.test.tsx`; `tests/public-work-videos.spec.ts`.
- [x] I3 Resize-mode consistency for uploaded/HLS videos:
  - [x] T6 Ensure `fit / wide / theater` appears for lead, more-videos, and inline uploaded/HLS videos on public work detail.
  - [x] T7 Thread a works-detail uploaded-video presentation flag through `InteractiveRenderer`; do not add this to YouTube.
  - Verification: `src/test/interactive-renderer.test.tsx`; `tests/public-work-videos.spec.ts`.
- [x] I4 Upload / preview stability:
  - [x] T8 Treat HLS as the canonical uploaded-video public playback path.
  - [x] T9 Keep preview publication strict so preview URLs are exposed only when both preview assets actually exist.
  - [x] T10 Ensure delete-then-reupload on existing works rehydrates client video state from the latest mutation payload and refreshed public detail data.
  - Verification: `backend/tests/WoongBlog.Api.ComponentTests/PublicQueryHandlerComponentTests.cs`; `backend/tests/WoongBlog.Api.IntegrationTests/WorkVideoEndpointsTests.cs`.
- [x] I5 Study mobile feed + back restoration:
  - [x] T11 Change `/blog` responsive behavior to mobile auto infinite scroll, tablet manual `Load more`, desktop pagination.
  - [x] T12 Persist mobile Study feed state using session/history storage and restore appended pages + exact scroll on browser back.
  - Verification: `src/test/public-responsive-feed.test.tsx`; `tests/public-blog-pagination.spec.ts`.
- [x] I6 Study excerpt + tags:
  - [x] T13 Change blog create/update excerpt persistence to manual-only so blank excerpts stay blank.
  - [x] T14 Public `/blog` cards render only saved excerpts and hide the summary block when blank.
  - [x] T15 Public `/blog` cards show multiple saved tags; keep home recent posts and `More Studies` unchanged.
  - Verification: `backend/tests/WoongBlog.Api.IntegrationTests/AdminContentEndpointsTests.cs`; `src/test/public-responsive-feed.test.tsx`; `tests/public-blog-pagination.spec.ts`.
- [x] I7 Mandatory process requirements:
  - [x] T16 Prepare backups before modifying code/configuration.
  - [x] T17 Record verification outcomes here as slices complete and ensure final work still matches this TODO list.
  - [x] T18 Generate audit artifacts under `backend/reports/works-study-hotfix-2026-04-25/`.
- [x] I8 Follow-up hotfix details from latest user message:
  - [x] T19 Add video-overlap avoidance to the Works TOC rail.
  - [x] T20 Make Works mobile list auto-append with an end-of-list stop state instead of a button.
  - [x] T21 Make Study restore mobile-only for exact infinite-list restoration; desktop viewport keeps desktop pagination/layout.
  - [x] T22 Keep content text extraction only for search indexing after manual-only excerpt policy.

## Planned Red-Green Order
- [x] P1 Red: update frontend unit tests for TOC/player/inline renderer/feed/card behavior.
- [x] P2 Green: implement frontend behavior changes for TOC/player/inline renderer/feed/list cards.
- [x] P3 Red: update backend tests for excerpt policy and preview strictness.
- [x] P4 Green: implement backend excerpt/storage-query changes.
- [x] P5 Red/Green: add or update focused Playwright coverage for public work/video/blog flows.
- [x] P6 Run targeted validation commands and record outcomes here.
- [x] P7 Generate the Markdown/HTML/JSON audit report artifacts.

## Verification Log
- Backup created before code edits: `/tmp/woong-blog-backup-2026-04-25-works-study-hotfix`.
- Continuation backup confirmed before additional edits: `/tmp/woong-blog-backup-2026-04-25-followup`.
- Follow-up backup confirmed before addendum edits: `/tmp/woong-blog-backup-2026-04-25-followup-2`.
- Fresh pass frontend unit slice:
  - `npm test -- --run src/test/interactive-renderer.test.tsx src/test/table-of-contents.test.tsx src/test/work-video-player.test.tsx src/test/public-responsive-feed.test.tsx src/test/responsive-page-size-sync.test.tsx src/test/mermaid-renderer.test.tsx`
  - Result: passed (`43/43`).
- Fresh pass Study restore race guard:
  - Added `ResponsivePageSizeSync` coverage for pending Study browser-back restore so desktop page-size replacement cannot preempt restored-reading mode.
  - Result: covered by `src/test/responsive-page-size-sync.test.tsx` and `tests/public-blog-pagination.spec.ts`.
- Frontend unit slice:
  - `npm test -- --run src/test/table-of-contents.test.tsx src/test/work-video-player.test.tsx src/test/interactive-renderer.test.tsx src/test/public-responsive-feed.test.tsx`
  - Result: passed (`32/32`).
- Backend component slice:
  - `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter FullyQualifiedName~PublicQueryHandlerComponentTests`
  - Result: passed (`18/18`).
- Backend integration slice:
  - `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~AdminContentEndpointsTests|FullyQualifiedName~WorkVideoEndpointsTests"`
  - Result: passed (`30/30`).
- Targeted Playwright slice against rebuilt compose stack:
  - `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=https://localhost:3001 npx playwright test tests/public-work-toc.spec.ts tests/public-blog-pagination.spec.ts tests/public-work-videos.spec.ts --workers=1`
  - Result: passed (`10/10`).
- Docker rebuild used for fresh browser verification:
  - `BACKEND_PUBLISH_PORT=18080 docker compose -f docker-compose.dev.yml up -d --build frontend nginx`
  - Result: passed; frontend and nginx were rebuilt/recreated against the local dev stack.
- Frontend static check:
  - `npm run typecheck`
  - Result: passed.
- Frontend lint:
  - `npm run lint`
  - Result: passed with 6 pre-existing/non-blocking warnings (`no-img-element` in tests/backups, unused vars in scripts/tests).
- Patch hygiene:
  - `git diff --check`
  - Result: passed.
- Compose validation:
  - `docker compose -f docker-compose.dev.yml up -d --build` rebuilt current images, then `BACKEND_PUBLISH_PORT=18080 docker compose -f docker-compose.dev.yml up -d backend frontend nginx` restored the stack after Docker port/cert issues.
- Audit artifacts:
  - `backend/reports/works-study-hotfix-2026-04-25/works-study-hotfix-2026-04-25.md`
  - `backend/reports/works-study-hotfix-2026-04-25/works-study-hotfix-2026-04-25.html`
  - `backend/reports/works-study-hotfix-2026-04-25/works-study-hotfix-2026-04-25.json`
- Follow-up addendum frontend unit slice:
  - `npm test -- --run src/test/table-of-contents.test.tsx src/test/public-responsive-feed.test.tsx src/test/responsive-page-size-sync.test.tsx src/test/work-video-player.test.tsx src/test/interactive-renderer.test.tsx`
  - Result: passed (`44/44`).
- WorkEditor fallback unit slice:
  - `npm test -- --run src/test/work-editor.test.tsx`
  - Result: passed (`19/19`).
- Follow-up backend verification:
  - `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~AdminContentEndpointsTests|FullyQualifiedName~WorkVideoEndpointsTests"`
  - Result: passed (`30/30`) with existing `AWSSDK.Core` NU1901 warnings.
  - `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter FullyQualifiedName~PublicQueryHandlerComponentTests`
  - Result: passed (`18/18`) with existing `AWSSDK.Core` NU1901 warnings.
- Follow-up browser verification:
  - Docker port publish for nginx failed in this WSL/Docker Desktop session, so validation used compose backend/frontend plus an internal no-port nginx container on the compose network.
  - `docker run --rm --network woong-blog-aspcore-nextjs_default ... npx playwright test tests/public-work-videos.spec.ts --workers=1`
  - Result: passed (`5/5`).
  - `docker run --rm --network woong-blog-aspcore-nextjs_default ... npx playwright test tests/public-work-toc.spec.ts tests/public-works-pagination.spec.ts tests/public-blog-pagination.spec.ts --workers=1`
  - Result: passed (`8/8`).
- Follow-up static checks:
  - `npm run typecheck`
  - Result: passed.
  - `npm run lint`
  - Result: passed with 6 pre-existing/non-blocking warnings.
  - `git diff --check`
  - Result: passed.
- Notes:
  - Generated `.local-certs/localhost.pem` and `.local-certs/localhost-key.pem` so nginx could restart locally after recreate.

## Plan Checkpoints
- Before implementation: the scoped work above matches the prior hotfix plan and no extra product scope has been added.
- After each completed item: update verification evidence here or in the final summary before moving on.
- After completion: the implemented work still matches F1-F8 and the prior hotfix plan. The only additional frontend guard added was `ResponsivePageSizeSync.skipWhenStudyRestorePending`, which directly supports F5 by preventing desktop page-size sync from overriding browser-back restored-reading state.

## Crypto Hotfix Addendum
- User report: admin workspace can fail during Works video upload with `crypto.randomUUID is not a function`; study/works view sometimes appeared stale immediately after edit but later reflected correctly.
- Scope check before implementation: fix client ID generation robustness and verify WorkEditor loads/handles video UI when `crypto.randomUUID` is absent or not callable. Do not broaden product behavior unless a concrete stale-view repro appears.
- [x] C1 Add/adjust focused WorkEditor coverage for existing-work admin load with unavailable `crypto.randomUUID`.
- [x] C2 Move client ID generation behind a `globalThis.crypto` safe helper and update WorkEditor to use it.
- [x] C3 Run targeted WorkEditor tests and static checks.
- [x] C4 Update audit report artifacts with the crypto hotfix evidence and stale-view note.

### Crypto Hotfix Verification Log
- Backup created before edits: `/tmp/woong-blog-backup-2026-04-25-crypto-hotfix`.
- C1/C2: added WorkEditor coverage for existing Work edit screens when `crypto.randomUUID` is unavailable; moved client ID generation to `createClientId()` using `globalThis.crypto?.randomUUID` with a Date/Math fallback.
- C3: `npm test -- --run src/test/work-editor.test.tsx` passed (`20/20`).
- C3: `npm run typecheck` passed.
- C3: `npm run lint` passed with 6 existing non-blocking warnings.
- C3: browser smoke passed via Playwright with `window.crypto.randomUUID` removed; `/admin/works/new` rendered `Upload MP4 Video as HLS`.
- C3 note: `npm run build` was attempted and stopped after more than 4 minutes with no output on `/mnt/d`; local verification server is running via `next dev` on `0.0.0.0:3002` behind the `0.0.0.0:3000` proxy.
- C4: audit report Markdown/HTML/JSON updated with crypto-hotfix evidence and stale-view observation.

### Crypto Hotfix Completion Check
- [x] C1 Add/adjust focused WorkEditor coverage for existing-work admin load with unavailable `crypto.randomUUID`.
- [x] C2 Move client ID generation behind a `globalThis.crypto` safe helper and update WorkEditor to use it.
- [x] C3 Run targeted WorkEditor tests and static checks.
- [x] C4 Update audit report artifacts with the crypto hotfix evidence and stale-view note.

## Public Latency Measurement Addendum
- User report: public navigation/button interactions feel much slower after recent hotfix; Introduction, Works, Study, and detail-card opens need measured with updated e2e coverage.
- Observed before implementation: current local browser server is `next dev` on `0.0.0.0:3002` behind the `3000` proxy. Dev logs show cold route compilation dominates first-click latency, including `/blog/[slug]` around 6.8s and `/works/[slug]` around 8.7s.
- [x] L1 Add explicit e2e latency coverage for public nav clicks: Works, Study, Introduction, Contact.
- [x] L2 Add explicit e2e latency coverage for public Work/Study detail-card opens.
- [x] L3 Run the updated latency spec through `scripts/run-e2e-latency.mjs` against `http://127.0.0.1:3000` and record generated artifacts.
- [x] L4 Update audit artifacts with the latency diagnosis and validation result.

### Public Latency Verification Log
- Backup created before edits: `/tmp/woong-blog-backup-2026-04-25-latency-hotfix`.
- L1/L2: updated `tests/e2e-response-time.spec.ts` to measure desktop public nav clicks and Work/Study card detail opens.
- L1/L2: added `publicNavClick` and `publicDetailOpen` budgets in `tests/performance-budgets.json`.
- L3 first run: 8 passed, 1 selector failure on Introduction heading; measured first failing step at 5452.5ms because the selector waited for exact `Introduction` while real heading was `Introduction...`.
- L3 green run: `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 PLAYWRIGHT_EXPECT_LOCAL_ADMIN_SHORTCUT=visible node scripts/run-e2e-latency.mjs -- tests/e2e-response-time.spec.ts --workers=1` passed (`9/9`), generated `test-results/playwright/e2e-latency-summary.json` and `.md`, budget failures `0`.
- Key measured warm-state steps: Work card detail 1241.78ms, Study card detail 1055.98ms, Works nav 787.78ms, Introduction nav 620.27ms, Study nav 570.05ms, Contact nav 504.86ms.
- Full Playwright suite inventory: `589 tests in 189 files`; not run in full during this addendum because the current server is `next dev`, which would mix route compilation and video/test-data setup cost into app latency measurement.
- `npm run typecheck` passed after latency spec edits.
- `npm run lint` passed with 6 existing non-blocking warnings.
