# TODO 2026-05-07 - Heavy Detail Performance Completion Audit

## User Objective
- Continue the backend performance objective focused on heavy public detail reads.
- Verify that thumbnail/representative image values are computed at write/backfill time and read from separate columns, not by scanning body JSON at request time.
- Verify that large public detail body payloads are split from admin edit JSON and public detail returns only rendering fields.
- Verify DB connection/pool pressure work: Npgsql pool sizing, EF query counts, and detail roundtrip count target of 1-2 DB roundtrips per detail request.
- Verify JSON serialization allocation reduction work: smaller public DTOs and source-generated JSON metadata where applicable.
- Verify nginx/app/DB timing observability is complete enough for Real Backend Test attribution.
- Verify repeated slices went through backend tests/load tests, dev CI, main promotion, and GHCR publish where applicable.

## Plan
- [ ] Map each explicit objective item to concrete code/report/CI evidence.
- [ ] Inspect current query stores, DTOs, JSON metadata, diagnostics, and persistence contracts.
- [ ] Run targeted PostgreSQL persistence contract tests that cover public detail projection and roundtrip behavior.
- [ ] Run full backend solution tests as the final local backend gate.
- [ ] Inspect latest GitHub PR/CI/main publish state.
- [ ] Write persistent audit report artifacts: markdown, JSON, HTML.
- [ ] Decide whether the active goal is actually complete or whether another implementation slice is still required.

## Progress
- Pending verification.

## Slice: Public Video Null Payload Reduction

### Rationale
- Current heavy detail evidence shows Work/Study detail can carry large response payloads.
- Work detail videos serialize many optional fields as explicit `null` values even when the public frontend parser already treats missing optional fields as null-equivalent.
- Reducing null optional video fields is a DTO/payload reduction, not cache, not target manipulation, and not a `pageSize` shortcut.

### TODO Mapping
- [ ] RED: add public endpoint behavior test proving optional null video fields are omitted from public Work detail JSON.
- [ ] GREEN: annotate nullable `WorkVideoDto` properties with `JsonIgnoreCondition.WhenWritingNull` while preserving required video fields and existing snake_case names.
- [ ] Verify focused backend integration test.
- [ ] Verify full backend solution tests.
- [x] Generate persistent audit report artifacts for this slice.
- [ ] Push branch, open PR to `dev`, wait for CI, merge if green, then verify promotion path.

### Progress
- Branch created: `perf/public-video-null-payload-2026-05-07`.
- Backups created under `.agent-backups/public-video-null-payload-2026-05-07/`.
- RED: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~GetWorkBySlug_OmitsNullOptionalVideoFields" --no-restore --logger "console;verbosity=minimal"` failed before implementation because null optional video fields were serialized.
- GREEN: same focused test passed after adding `JsonIgnoreCondition.WhenWritingNull` to nullable `WorkVideoDto` fields.
- PASS: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~PublicEndpointsTests|FullyQualifiedName~PostgresPersistenceContractTests" --no-restore --logger "console;verbosity=minimal"` passed 45/45.
- PASS: `npm test -- --run src/test/public-api-clients.test.ts src/test/public-api-contracts.test.ts` passed 22/22.
- PASS: `dotnet test backend/WoongBlog.sln --no-restore --logger "console;verbosity=minimal"` passed: Contract 1 skipped, Component 124, Unit 56, Architecture 35, Integration 227.
- PASS: `git diff --check` passed.
- PASS: `npm run typecheck` passed.
- PASS: rebuilt dev compose with current branch via `BACKEND_PUBLISH_PORT=18080 docker compose -f docker-compose.dev.yml up -d --build backend nginx`.
- PASS: health check returned HTTP 200 and `/api/public/works/seeded-work` payload omitted optional null video fields.
- PASS: backend-container k6 smoke against `/api/public/works/seeded-work`, 50rps/10s/maxVUs100: 500 requests, failure rate 0, p95 3.43ms.

## Slice: Backend Performance Opportunity Map

### Rationale
- User asked to identify where further performance improvement is possible and what each improvement would change in Real Backend Test.
- This is an audit-only slice: scan code and test/report evidence, then produce actionable findings without changing production or test code.

### TODO Mapping
- [x] Inspect public Work/Blog list/detail hot paths and DTO payload shape.
- [x] Inspect EF query projections, DB roundtrip contracts, indexes, and connection diagnostics.
- [x] Inspect JSON serialization and load-test observability path.
- [x] Map each candidate improvement to expected Real Backend Test metrics: throughput, p95/p99 latency, HTTP failures, app elapsed, DB/open latency, GC/memory, ThreadPool queue.
- [x] Generate persistent audit report artifacts for this analysis.

### Progress
- Backup created under `.agent-backups/backend-performance-opportunity-map-2026-05-07/`.
- Re-confirmed actual work still matches this audit-only TODO list: production/test code was not changed in this slice.
- Completed code scan for public Work/Blog list/detail, Home summary, JSON context, DB diagnostics, nginx timing headers, and load-test runner metrics.
- Updated baseline to current `dev` commit `6a18e20` after PR #102 merge. The older note that Work detail with videos used 2 DB commands is now obsolete.
- Confirmed current public hot path shape: list page 1/no-search uses 1 DB command per Work/Blog list; Blog detail uses 1 command; Work detail without videos uses 1 command; Work detail with videos uses 1 command from `Work.PublicVideosJson` and does not query `WorkVideos`.
- Completed performance pattern scan on selected hot-path files: `AsNoTracking` 19, `EF.CompileAsyncQuery` 2, `SqlQuery<` 2, `CountAsync` 2, `JsonDocument.Parse` 7, `Include(` 0, blocking wait patterns 0.
- PASS: targeted PostgreSQL persistence contract tests passed 5/5 for Work detail with videos single-command contract, Work detail without videos single-command contract, Blog detail single command, Work first-page list single command, and Blog first-page list single command.
- PASS: PR #102 CI completed successfully, was marked ready, and merged to `dev` as `6a18e20`.
- Generated/updated report artifacts under `backend/reports/backend-performance-opportunity-map-2026-05-07/`.

## Slice: Public Work Video Read Model

### Rationale
- The next measured structural target is public Work detail with videos.
- Current contract intentionally allows 2 DB commands for Work detail with videos: one Work projection plus one `WorkVideos` projection.
- To reduce detail DB roundtrips without cache or target manipulation, store a small public video snapshot on `Work` at write/backfill time and read it from the Work row.

### TODO Mapping
- [ ] RED: add PostgreSQL contract proving public Work detail with stored public video snapshot should use 1 command and not reference `WorkVideos`.
- [ ] GREEN: add `Work.PublicVideosJson`, EF model mapping, bootstrap backfill, and public query-store mapping from the stored snapshot.
- [ ] Update video mutation paths to refresh the stored public video snapshot when videos are added, deleted, reordered, or HLS-confirmed.
- [ ] Verify public video behavior remains equivalent for YouTube/local/HLS preview fields.
- [ ] Run focused backend tests, full backend solution tests, compose/load smoke, audit report, PR to `dev`, CI, merge/promotion.

### Progress
- Branch created: `perf/public-work-video-read-model-2026-05-07`.
- Backups created under `.agent-backups/public-work-video-read-model-2026-05-07/`.
- RED: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~PublicWorkDetailWithVideos_UsesSinglePostgresCommand_AndStoredPublicColumnsOnly" --no-restore --logger "console;verbosity=minimal"` failed before implementation because `Work.PublicVideosJson` did not exist.
- GREEN: added `Work.PublicVideosJson`, jsonb model mapping, bootstrap patch/backfill `20260507_public_work_videos_read_model`, and public Work detail projection from the stored video snapshot.
- GREEN: updated add YouTube, confirm upload, HLS job start, delete, and reorder mutation handlers to refresh the stored public video snapshot at write time.
- PASS: focused PostgreSQL contracts passed 3/3 for public Work detail single command, schema patch presence, and legacy `WorkVideos` backfill to `PublicVideosJson`.
- PASS: component tests passed 69/69 for public query handlers, Work video mutations, and EF model contracts.
- PASS: endpoint tests passed 21/21 for Work video flows plus public null video field contract.
- PASS: `git diff --check` passed.
- PASS: `dotnet test backend/WoongBlog.sln --no-restore --logger "console;verbosity=minimal"` passed: Contract 1 skipped, Component 124, Unit 56, Architecture 35, Integration 228.
- WARN: `BACKEND_PUBLISH_PORT=18080 docker compose -f docker-compose.dev.yml up -d --build backend nginx` could not complete because Docker build DNS failed against NuGet and Google Fonts; existing containers remained running.
- PASS: local `dotnet publish --no-restore` output was mounted into an existing backend runtime image, health checked at `http://127.0.0.1:18081/api/health`, and `/api/public/works/seeded-work` returned successfully.
- PASS: k6 smoke against latest publish `/api/public/works/seeded-work`, 100rps/10s/maxVUs100: 1,001 requests, failure rate 0, p95 3.21ms, summary at `backend/reports/public-work-video-read-model-2026-05-07/loadtest/k6-public-work-detail-100rps-10s.json`.

## Slice: Public Work Video Empty JSON Fast Path

### Rationale
- `PublicVideosJson` removes the public detail `WorkVideos` DB query, but it introduces a small JSON deserialize step.
- Most Work detail reads have no videos and store the default `[]` snapshot.
- The no-video path should return the shared empty snapshot without invoking JSON deserialization, reducing allocation on the common public Work detail path.

### TODO Mapping
- [ ] RED: add unit contract proving `WorkPublicVideosReadModel.Deserialize("[]")` returns the shared empty snapshot.
- [ ] GREEN: add a normalized empty-array fast path before `JsonSerializer.Deserialize`.
- [ ] Verify focused unit test and targeted public Work detail contract.
- [ ] Run backend tests/load smoke as appropriate.
- [ ] Generate persistent audit report artifacts for this slice.
- [ ] Push branch, open PR to `dev`, wait for CI, and merge if green.

### Progress
- Branch created: `perf/public-work-video-empty-json-fast-path-2026-05-07`.
- Backups created under `.agent-backups/public-work-video-empty-json-fast-path-2026-05-07/`.
- RED: focused unit test failed because `Deserialize("[]")`, `Deserialize("  []  ")`, and `Deserialize("\n[]\t")` returned newly deserialized empty arrays.
- GREEN: added `AsSpan().Trim().SequenceEqual("[]")` fast path before JSON deserialization.
- PASS: focused unit test passed 3/3.
- PASS: focused PostgreSQL no-video public Work detail contract passed 1/1.
- PASS: full unit tests passed 59/59.
- PASS: `git diff --check` passed.
- PASS: `dotnet test backend/WoongBlog.sln --no-restore --logger "console;verbosity=minimal"` passed: Contract 1 skipped, Component 124, Unit 59, Architecture 35, Integration 228.
- PASS: latest-code k6 smoke against no-video Work detail `/api/public/works/seeded-work-extra-01`, 100rps/10s/maxVUs100: 1,001 requests, failure rate 0, p95 2.22ms.

## Slice: Public API Response Compression

### Rationale
- Public detail DB roundtrips are now at the target shape for the default Real Backend Test mix.
- Remaining heavy detail pressure can come from large public detail JSON transfer and serialization/network cost.
- Compression is not cache and does not change target realism or `pageSize=12`; it is a payload transport optimization that must be measured on the 2 CPU / 8 GiB shared host.

### TODO Mapping
- [x] RED: add an integration behavior test proving public detail JSON honors `Accept-Encoding: gzip` and returns compressed JSON with `Vary: Accept-Encoding`.
- [x] GREEN: configure ASP.NET Core response compression for JSON/text with fastest gzip and HTTPS enabled.
- [x] Verify public endpoint compression test and startup architecture order.
- [x] Verify focused public endpoint tests and backend solution tests as feasible.
- [x] Run or document a backend load smoke/A-B command for compressed public detail.
- [x] Generate persistent audit report artifacts for this slice.
- [ ] Push branch, open PR to `dev`, wait for CI, merge if green.

### Progress
- Branch created: `perf/public-api-response-compression-2026-05-07`.
- Backups created under `.agent-backups/public-api-response-compression-2026-05-07/`.
- RED: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~GetWorkBySlug_CompressesPublicDetailJson_WhenClientAcceptsGzip" --no-restore --logger "console;verbosity=minimal"` failed before implementation because `Content-Encoding: gzip` was absent.
- GREEN: added ASP.NET Core response compression for JSON/problem+JSON/text responses with HTTPS enabled and gzip fastest provider.
- PASS: same focused compression test passed 1/1 after implementation.
- PASS: focused architecture middleware order test passed 1/1.
- PASS: focused public endpoint integration suite passed 31/31.
- PASS: `dotnet test backend/WoongBlog.sln --no-restore --logger "console;verbosity=minimal"` passed: Contract 1 skipped, Component 124, Unit 59, Architecture 35, Integration 229.
- PASS: compose dev backend/nginx rebuilt current branch successfully on `BACKEND_PUBLISH_PORT=18080`.
- PASS: curl smoke against `/api/public/works/seeded-work` returned `Content-Encoding: gzip` and `Vary: Accept-Encoding`; plain 1195 B vs gzip 850 B for the seeded Work detail response.
- PASS: backend-container k6 compression smoke, 50rps/10s/maxVUs100 against `/api/public/works/seeded-work`: 500 requests, check rate 1.0, HTTP failure rate 0, p95 2.42 ms. Summary: `backend/reports/public-api-response-compression-2026-05-07/loadtest/k6-public-work-compression-50rps-10s.json`.
- PASS: persistent audit report artifacts generated under `backend/reports/public-api-response-compression-2026-05-07/`.
- PASS: PR #104 merged to `dev` as `a7c0217` and was promoted to `main`; publish workflow completed after rerun.

## Slice: Backend Performance Next Opportunities

### Rationale
- User asked to identify where backend performance can still be improved and what each improvement would change in Real Backend Test.
- Current `dev` already includes hot-path query, read-model, payload-null trimming, and response compression slices.
- This is an audit-only slice: production/test code is not changed; the output is a current-state opportunity map and recommended next TDD slice.

### TODO Mapping
- [x] Re-scan public Work/Blog list/detail hot paths on current `dev`.
- [x] Re-scan EF query shape, DB roundtrip contracts, source-generated JSON metadata, compression, and load-test diagnostics path.
- [x] Run targeted contract tests for current public DB command behavior and gzip public detail behavior.
- [x] Map remaining opportunities to Real Backend Test metrics: client p95/p99, throughput, HTTP 5xx, app elapsed, response bytes/receive time, DB command/open latency, GC/memory, ThreadPool queue.
- [x] Generate persistent audit report artifacts for this analysis.

### Progress
- Backup created under `.agent-backups/backend-performance-next-opportunities-2026-05-07/`.
- Re-confirmed actual work still matches this audit-only TODO list: production/test code was not changed in this slice.
- Current baseline is `dev` commit `a7c0217`.
- Hot-path scan counts on selected files: `AsNoTracking` 11, `EF.CompileAsyncQuery` 2, `SqlQuery<` 2, `CountAsync` 2, `ToListAsync` 8, `Include` 0, `JsonSerializer.` 3, `JsonDocument.Parse` 1, `RegexOptions.Compiled` 3, blocking wait patterns 0.
- PASS: targeted PostgreSQL contract tests passed 6/6 for public Work/Blog detail/list single-command behavior and async DB diagnostics interceptor.
- PASS: focused gzip public detail integration test passed 1/1.
- Generated report artifacts under `backend/reports/backend-performance-next-opportunities-2026-05-07/`.

## Slice: Runtime Pool Diagnostics

### Rationale
- User reported a 2 CPU / total 8 GiB production host shared by backend, frontend, nginx, and DB.
- Earlier Real Backend Test screenshots showed `Open connections` reaching 100 and `DB connection open P95` spiking, while compose defaults now target a lower Npgsql max pool.
- Before tuning pool sizes or further backend structure, the running app must expose effective runtime limits and pool configuration so stale deployment/config/resource mismatch can be detected.

### TODO Mapping
- [ ] RED: backend diagnostics endpoint must expose process CPU quota, memory limit, DbContext pool size, and Npgsql pool min/max without leaking secrets.
- [ ] RED: frontend diagnostics parser/summary must preserve and surface runtime limit and pool metadata.
- [ ] GREEN: implement runtime resource limit detection and persistence pool diagnostics in load-test diagnostics snapshots.
- [ ] GREEN: show CPU quota, memory limit, DbContext pool, and Npgsql pool cap in the Real Backend Test UI.
- [ ] Verify focused backend integration/component tests.
- [ ] Verify focused frontend load-test dashboard tests.
- [ ] Run backend solution tests and frontend type/lint tests as feasible.
- [ ] Run dev compose/load smoke or document blocker.
- [ ] Generate persistent audit report artifacts.
- [ ] Push branch, open PR to `dev`, wait for CI, merge if green, then decide the next performance slice.

### Progress
- Branch created: `perf/runtime-pool-diagnostics-2026-05-07`.
- Backups created under `.agent-backups/runtime-pool-diagnostics-2026-05-07/`.
- RED: backend component diagnostics parser test failed because `PersistenceRuntimeDiagnostics` did not exist.
- RED: backend diagnostics endpoint test failed because `process.memoryLimitBytes`/`process.cpuQuotaCores` were absent.
- RED: frontend load-test dashboard summary test failed because runtime limit and pool summary fields were absent.
- GREEN: added runtime resource limit detection, persistence pool diagnostics, load-test snapshot serialization, and UI parser/summary/display support.
- PASS: focused frontend load-test dashboard test passed 28/28.
- PASS: focused backend component tests passed 3/3 for pool diagnostics parsing and run-scoped diagnostics compatibility.
- PASS: focused backend integration tests passed 2/2 for diagnostics endpoint shape and collector failure payload.
- PASS: source-generated JSON resolver focused test passed 1/1 for `LoadTestDatabasePoolDiagnostics`.
- PASS: full backend solution test passed: Contract 1 skipped, Component 126, Unit 59, Architecture 35, Integration 229.
- PASS: `npm run typecheck` passed.
- PASS: `npm run lint` passed with 0 errors and 7 existing warnings.
- PASS: dev compose backend/nginx/frontend rebuild succeeded with `BACKEND_PUBLISH_PORT=18080`.
- PASS: health checks returned HTTP 200 via backend `http://127.0.0.1:18080/api/health` and nginx `http://127.0.0.1:3000/api/health`.
- PASS: Playwright `tests/admin-load-test-dashboard.spec.ts` passed 2/2 against the dev compose runtime and verifies Memory limit, CPU quota, DbContext pool, and Npgsql max pool are visible.

## Interrupting Slice: HLS Upload Request Limit Failure

### Rationale
- User reported critical `Failed to process HLS` during video upload.
- Live dev repro confirmed a 35 MiB HLS multipart upload returns HTTP 413 before HLS policy/transcoding runs.
- The actual backend error is `Request body too large. The max request body size is 30000000 bytes.`
- Existing domain policy allows video uploads up to 200 MiB, while ASP.NET Core defaults still cap Kestrel request bodies at about 30 MB and multipart bodies at 128 MiB unless configured.

### TODO Mapping
- [x] Reproduce the upload failure through the real nginx/backend path without changing production code.
- [x] Preserve current WIP and create targeted backups before edits.
- [ ] RED: add startup/config regression coverage proving API request/form upload limits are aligned with `WorkVideoPolicy.MaxVideoBytes`.
- [ ] GREEN: configure ASP.NET Core request body and multipart form limits so valid HLS uploads can reach app-level validation/transcoding.
- [ ] Verify focused startup/config tests.
- [ ] Verify live dev docker repro no longer fails at Kestrel 30,000,000-byte limit.
- [x] Generate persistent audit report artifacts for this interrupting fix.

### Progress
- Repro command created an authenticated admin session, created a draft Work, and posted a 35 MiB multipart MP4 to `/api/admin/works/{id}/videos/hls-job` through nginx at `http://127.0.0.1:3000`.
- FAIL reproduced: endpoint returned HTTP 413 with `Microsoft.AspNetCore.Server.Kestrel.Core.BadHttpRequestException: Request body too large. The max request body size is 30000000 bytes.`
- Backups created under `.agent-backups/hls-upload-limit-2026-05-07/`.
- RED: focused startup/config test failed as expected because Kestrel request limit was still 30,000,000 bytes while video policy allows 209,715,200 bytes.
- GREEN: configured Kestrel request body limit to video policy plus 1 MiB multipart overhead and multipart form body limit to the 200 MiB video policy.
- PASS: focused startup/config test passed 1/1 after implementation.
- PASS: focused HLS happy-path integration and upload-limit config tests passed 2/2.
- PASS: focused WorkVideo policy unit tests passed 39/39.
- PASS: `git diff --check` passed.
- PASS: dev compose backend rebuild completed and health check returned HTTP 200.
- PASS: live repro after rebuild posted the same 35 MiB multipart request through nginx/backend; response changed from HTTP 413 Kestrel limit to HTTP 400 app-level `Only valid MP4 files are supported.`, proving the 30,000,000-byte server cap no longer blocks HLS upload.
- PASS: `dotnet test backend/WoongBlog.sln --no-restore --logger "console;verbosity=minimal"` passed: Contract 1 skipped, Component 126, Unit 59, Architecture 35, Integration 230.
- PASS: persistent audit report artifacts generated under `backend/reports/hls-upload-limit-2026-05-07/`.
- PASS: current focused frontend load-test dashboard test passed 28/28.
- PASS: current `npm run typecheck` passed.
- PASS: current `npm run lint` passed with 0 errors and 7 existing warnings.
- PASS: current Playwright `tests/admin-load-test-dashboard.spec.ts` passed 2/2 against dev compose.
- PASS: current diagnostics endpoint smoke confirmed `process.memoryLimitBytes`, `process.cpuQuotaCores`, and `database.pool` fields are present; dev DB pool resolved as Postgres DbContext 128 / Npgsql min 0 / max 40 from connection string.

## Slice: Public JSON Serialization Source Generation

### Rationale
- Current public Work/Blog list/detail DTOs are registered in `WoongBlogApiJsonSerializerContext`, but the context is configured for `JsonSourceGenerationMode.Metadata` only.
- Metadata mode improves type metadata lookup, but it does not explicitly request generated serialization fast paths for response DTOs.
- The remaining Real Backend Test pressure after DB roundtrip and payload-shape work is mostly app elapsed, JSON serialization/allocation, GC heap, and client p95/p99 on heavy public detail reads.
- This slice must not change public API shape, test target realism, cache policy, `pageSize=12`, seeded-vs-real target selection, database schema, or query projection behavior.

### TODO Mapping
- [x] Confirm the actual work still matches this TODO before editing production/test code.
- [x] RED: extend startup composition coverage so the API JSON source-generation context must enable both metadata and serialization generation modes.
- [x] GREEN: configure `WoongBlogApiJsonSerializerContext` for `Metadata | Serialization` while preserving camelCase naming and registered DTO coverage.
- [x] Verify focused startup/config test.
- [x] Verify focused public Work/Blog endpoint behavior to ensure response shape is unchanged.
- [x] Verify backend solution tests as feasible.
- [x] Verify frontend public API parser/contract tests as feasible.
- [x] Generate persistent audit report artifacts for this slice.
- [ ] Push branch, open PR to `dev`, wait for CI, merge if green, then promote through `main` and confirm GHCR publish if required.

### Progress
- Branch created: `perf/public-json-serialization-sourcegen-2026-05-07`.
- Backups created under `.agent-backups/public-json-serialization-sourcegen-2026-05-07/`.
- Confirmed actual work still matches this TODO: the change is scoped to JSON serialization configuration and regression tests only.
- RED: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~JsonOptions_UseSourceGeneratedSerializationForPublicHotPathDtos" --no-restore --logger "console;verbosity=minimal"` failed before implementation because the API JSON context did not enable `JsonSourceGenerationMode.Serialization`.
- GREEN: configured `WoongBlogApiJsonSerializerContext` with `JsonSourceGenerationMode.Metadata | JsonSourceGenerationMode.Serialization`.
- PASS: focused startup/config serialization test passed 1/1 after implementation.
- PASS: focused startup/public endpoint tests passed 33/33, covering source-generated JSON configuration and public Work/Blog endpoint response shape.
- WARN: full `dotnet test backend/WoongBlog.sln --no-restore --logger "console;verbosity=minimal"` failed only because local Docker/Testcontainers is unavailable for `PostgresPersistenceContractTests`; non-Postgres projects had passed or were not implicated.
- PASS: `dotnet test backend/WoongBlog.sln --no-restore --filter "FullyQualifiedName!~PostgresPersistenceContractTests" --logger "console;verbosity=minimal"` passed: Contract 1 skipped, Component 126, Unit 59, Architecture 39, Integration 215.
- PASS: `npm test -- --run src/test/public-api-clients.test.ts src/test/public-api-contracts.test.ts` passed 22/22.
- PASS: `npm run typecheck` passed.
- PASS: `npm run lint` passed with 0 errors and 7 existing warnings.
- PASS: `git diff --check` passed.
- WARN: local Docker is unavailable in this WSL runtime, so Docker compose, Testcontainers PostgreSQL, and local k6/real backend load smoke cannot be run here.
- PASS: persistent audit report artifacts generated under `backend/reports/public-json-serialization-sourcegen-2026-05-07/`.

## Interrupting Slice: Main Runtime Nginx Config Allowlist

### Rationale
- PR #114 was merged to `dev`, but promotion to `main` is blocked by the existing runtime-only PR #113.
- PR #113 failed `CI Main Runtime / Backend architecture tests` because `NginxRuntimeConfigTests` checks `nginx/default.conf` and `nginx/local-https.conf`, while `scripts/main-runtime-allowlist.txt` only promotes the production nginx configs.
- This is not a backend performance code change, but it blocks the required `dev -> main -> GHCR` promotion chain.

### TODO Mapping
- [x] Confirm the actual work still matches this TODO before editing production/test code.
- [x] RED: add architecture coverage proving every nginx config consumed by `NginxRuntimeConfigTests` is included in `scripts/main-runtime-allowlist.txt`.
- [x] GREEN: include the missing nginx runtime configs in the main runtime allowlist.
- [x] Verify focused architecture tests.
- [x] Generate persistent audit report artifacts for this CI/promotion fix.
- [ ] Push branch, open PR to `dev`, wait for CI, merge if green.
- [ ] Confirm promotion PR `release/main-promote -> main` refreshes and passes `CI Main Runtime`.

### Progress
- Branch created: `fix/main-runtime-nginx-allowlist-2026-05-07`.
- Backups created under `.agent-backups/main-runtime-nginx-allowlist-2026-05-07/`.
- Confirmed actual work still matches this TODO: the change is scoped to the main runtime allowlist and architecture regression coverage only.
- RED: `dotnet test backend/tests/WoongBlog.Api.ArchitectureTests/WoongBlog.Api.ArchitectureTests.csproj --filter "FullyQualifiedName~MainRuntimeAllowlist_IncludesEveryNginxConfigUnderArchitectureCoverage" --no-restore --logger "console;verbosity=minimal"` failed because `nginx/default.conf` was missing from the main runtime allowlist.
- GREEN: added `nginx/default.conf` and `nginx/local-https.conf` to `scripts/main-runtime-allowlist.txt`.
- PASS: focused `NginxRuntimeConfigTests` architecture tests passed 5/5.
- PASS: full architecture test project passed 40/40.
- PASS: `git diff --check` passed.
- PASS: persistent audit report artifacts generated under `backend/reports/main-runtime-nginx-allowlist-2026-05-07/`.

## Interrupting Slice: HLS Upload Nginx Timeout Recheck

### Rationale
- User re-reported critical `failed to process HLS` during video upload.
- Current code already fixes the earlier 30,000,000-byte Kestrel request-body limit.
- Code scan found a separate proxy/runtime mismatch: backend HLS processing timeout is 300 seconds, but general nginx `/api/` locations did not set 300-second proxy timeouts.
- The HLS endpoint is `/api/admin/works/{id}/videos/hls-job`, which routes through general `/api/`, not the existing long-timeout `/api/admin/ai/` location.

### TODO Mapping
- [x] Re-scan frontend HLS error surfacing and backend HLS processing path.
- [x] Verify current backend HLS happy path and upload-limit regression tests still pass.
- [x] Verify current frontend HLS error surfacing tests still pass.
- [x] Preserve current files in `.agent-backups/hls-upload-nginx-timeout-2026-05-07/`.
- [x] RED: add nginx runtime config regression test proving `/api/` proxy locations allow long-running HLS jobs.
- [x] GREEN: align nginx `/api/` proxy timeouts with backend HLS timeout in dev/prod configs.
- [x] Verify focused architecture test and config syntax/grep checks.
- [ ] Generate persistent audit report artifacts for this interrupting fix.

### Progress
- PASS: focused backend HLS tests passed 2/2 for HLS happy path and upload-limit config.
- PASS: focused frontend HLS error tests passed 2/2.
- Backups created under `.agent-backups/hls-upload-nginx-timeout-2026-05-07/`.
- RED: focused `NginxRuntimeConfigTests` failed 4/4 because all runtime nginx configs lacked `proxy_connect_timeout 300s;` in general `/api/` locations.
- GREEN: added `proxy_connect_timeout 300s;`, `proxy_send_timeout 300s;`, and `proxy_read_timeout 300s;` to general `/api/` locations in `nginx/default.conf`, `nginx/local-https.conf`, `nginx/prod-bootstrap.conf`, and `nginx/prod.conf`.
- PASS: focused `NginxRuntimeConfigTests` passed 4/4.
- PASS: full architecture suite passed 39/39.
- PASS: static grep confirmed all `/api/` blocks in the four runtime nginx configs include the 300-second proxy timeout directives.
- PASS: `git diff --check` passed.
- BLOCKED: local `nginx -t`/compose validation could not run because this WSL environment does not have `nginx` or `docker` installed.
- PASS: persistent audit report artifacts generated under `backend/reports/hls-upload-failure-current-recheck-2026-05-07/`.

## Slice: Main Runtime Promotion For Runtime Diagnostics And HLS Upload Limit

### Rationale
- PR #106 merged runtime resource/pool diagnostics and the HLS upload request-limit fix to `dev`.
- The active performance objective requires verified CI promotion through `dev` and `main` where applicable so production/runtime images can actually use the improved diagnostics and upload-limit behavior.
- This slice is promotion/verification-only: no production or test code is changed locally.

### TODO Mapping
- [x] Verify current `dev` head and PR #106 CI status.
- [x] Verify runtime DB command diagnostics is currently available in the dev compose runtime after public API traffic.
- [x] Verify nginx request timing headers are available when the load-test base URL goes through nginx.
- [x] Verify PR #107 promotion from `release/main-promote` to `main` completes with all Main Runtime CI checks green.
- [x] Verify `origin/main` contains the runtime-only promotion commit for `dev` commit `a23efb9`.
- [x] Verify `Publish GHCR Main` completes for the new main merge commit.
- [x] Generate persistent audit report artifacts for this promotion/diagnostics verification.

### Progress
- PASS: PR #106 is merged to `dev` as `a23efb9` with all CI Dev checks green.
- PASS: dev compose diagnostics endpoint showed `database.commandLatency.sampleCount` and p95 populated; public API traffic increased sample count from 189 to 231.
- PASS: dev nginx path returned `X-Nginx-Request-Time` for `/api/health`, `/api/public/works?page=1&pageSize=12`, and `/api/public/blogs?page=1&pageSize=12`.
- WARN: `X-Nginx-Upstream-Time` and `X-Nginx-Upstream-Response-Time` returned `-` in the current local nginx config, so k6 uses `runner.http_waiting_fallback` for upstream attribution when the upstream header is not parseable.
- PASS: PR #107 `Promote runtime-only tree: dev -> main` merged at `2026-05-06T19:54:25Z` with merge commit `231557b997d321881f29b25deab35a2bbb15a2b5`.
- PASS: PR #107 Main Runtime CI checks all passed: frontend lint/types/unit, backend unit/component/architecture/integration, compose main runtime verification, Pact provider verification, and browser smoke.
- PASS: `origin/main` updated to `231557b`; runtime-only promotion branch commit is `0fa7bf6`, promoting dev source `a23efb94040311b59789c687336ff0a55786619a`.
- PASS: `Publish GHCR Main` run `25458004671` completed successfully for main head `231557b997d321881f29b25deab35a2bbb15a2b5`; frontend and backend image jobs both succeeded.

## Slice: LoadTesting BaseUrl Prod Default Guard

### Rationale
- Local runtime diagnostics confirmed DB command p95 and nginx request_time are collected when traffic goes through nginx.
- The remaining `nginx request_time unavailable` risk is configuration drift: production compose currently falls back to backend-direct `http://127.0.0.1:8080` when `LoadTesting__BaseUrl` is omitted.
- Real Backend Test should default to the public/nginx origin, not backend-direct, because the objective is to attribute client/nginx/app/DB timing.

### TODO Mapping
- [x] RED: prove prod compose with `NEXT_PUBLIC_SITE_URL` but no `LoadTesting__BaseUrl` resolves to backend-direct incorrectly.
 - [x] GREEN: make prod compose default `LoadTesting__BaseUrl` to `NEXT_PUBLIC_SITE_URL` with a safe public-origin fallback.
 - [x] GREEN: update local main bootstrap script to avoid backend-direct default.
 - [x] GREEN: add CI compose smoke guard that fails if main runtime resolves load testing base URL to backend-direct.
 - [x] Verify focused compose config checks.
 - [x] Verify script syntax/static checks.
 - [x] Generate persistent audit report artifacts for this slice.

### Progress
- Backup created under `.agent-backups/loadtesting-baseurl-prod-default-2026-05-07/`.
- RED: prod compose config with `NEXT_PUBLIC_SITE_URL=https://example.test` and no `LoadTesting__BaseUrl` produced `LoadTesting__BaseUrl: http://127.0.0.1:8080`.
- GREEN: `docker-compose.prod.yml` now defaults `LoadTesting__BaseUrl` to `${NEXT_PUBLIC_SITE_URL:-https://woonglab.com}` when an explicit `LoadTesting__BaseUrl` is omitted.
- GREEN: `scripts/main-up.sh` now writes `NEXT_PUBLIC_SITE_URL=http://localhost` and `LoadTesting__BaseUrl=http://localhost`, avoiding backend-direct local main bootstrap defaults.
- GREEN: `scripts/ci-compose-smoke.sh` now injects the main smoke `NEXT_PUBLIC_SITE_URL`, inspects `docker compose config`, and fails if main runtime resolves Real Backend Test to backend-direct `http://127.0.0.1:8080`.
- PASS: focused prod compose config with `NEXT_PUBLIC_SITE_URL=https://example.test` and no `LoadTesting__BaseUrl` produced `LoadTesting__BaseUrl: https://example.test`.
- PASS: focused prod compose fallback config with no `NEXT_PUBLIC_SITE_URL` and no `LoadTesting__BaseUrl` produced `LoadTesting__BaseUrl: https://woonglab.com`.
- PASS: `bash -n scripts/main-up.sh scripts/ci-compose-smoke.sh` passed.
- PASS: `git diff --check -- docker-compose.prod.yml scripts/main-up.sh scripts/ci-compose-smoke.sh todolist-2026-05-07.md` passed.
- PASS: persistent audit report artifacts generated under `backend/reports/loadtesting-baseurl-prod-default-2026-05-07/`.
- PASS: `python3 -m json.tool backend/reports/loadtesting-baseurl-prod-default-2026-05-07/loadtesting-baseurl-prod-default-2026-05-07.json` passed.

## Slice: Public Work Video DTO Split

### Rationale
- Current Work public detail no longer queries `WorkVideos`, but it still exposes public videos through the shared `WorkVideoDto` used by admin/mutation paths.
- Public Work pages use video id, source type/key, playback URL, MIME type, dimensions, duration, timeline preview URLs, and sort order.
- Public pages do not need admin/management fields such as original file name, file size, or created timestamp.
- Splitting public video DTOs should reduce Work read payload and JSON serialization without changing targets, `pageSize`, DB roundtrips, or cache behavior.

### TODO Mapping
- [ ] RED: add public endpoint behavior test proving public Work video payload omits admin-only video fields even when stored snapshot contains them.
- [x] GREEN: introduce a dedicated public Work video DTO and keep admin/mutation `WorkVideoDto` unchanged.
- [x] GREEN: map public Work detail videos to the public DTO only.
- [x] Verify frontend parser remains tolerant of omitted admin-only video fields.
- [ ] Verify focused backend endpoint/contract tests.
- [x] Verify frontend contract tests for public API parsing.
- [ ] Run backend solution tests and a small load smoke as feasible.
- [ ] Generate persistent audit report artifacts for this slice.
- [ ] Push branch, open PR to `dev`, wait for CI, merge if green, then verify promotion path.

### Progress
- Branch created: `perf/public-work-video-dto-split-2026-05-07`.
- Backup created under `.agent-backups/public-work-video-dto-split-2026-05-07/`.
- RED: `dotnet test backend/tests/WoongBlog.Api.IntegrationTests/WoongBlog.Api.IntegrationTests.csproj --filter "FullyQualifiedName~GetWorkBySlug_ReturnsSerializedDetailWithMedia_ForAnonymousClient" --no-restore --logger "console;verbosity=minimal"` failed because seeded public Work video JSON still emitted an admin-only field.
- GREEN: added `PublicWorkVideoDto` for public Work detail and kept admin/mutation `WorkVideoDto` unchanged.
- GREEN: public detail mapping now omits `originalFileName`, `fileSize`, and `createdAt`; `PublicVideosJson` writes/backfills no longer store those admin-only fields.
- PASS: focused RED test passed 1/1 after implementation.
- PASS: focused non-Postgres component tests passed 28/28 for public query handlers and Work video flows.
- PASS: frontend public API parser/contract tests passed 22/22.
- PASS: `dotnet test backend/WoongBlog.sln --no-restore --filter "FullyQualifiedName!~PostgresPersistenceContractTests" --logger "console;verbosity=minimal"` passed: Contract 1 skipped, Component 126, Unit 59, Architecture 35, Integration 214.
- BLOCKED locally: Postgres contract subset could not run because `docker` is not available in this WSL distro; CI must exercise `PostgresPersistenceContractTests`.
- PASS: `npm run typecheck` passed.
- PASS: `git diff --check` passed.
- BLOCKED locally: load smoke could not run because neither `docker` nor `k6` is available in this WSL distro.
- GREEN: added schema patch `20260507_public_work_videos_public_dto` so existing DBs with the earlier public video read-model patch already applied can still strip admin-only video fields from `PublicVideosJson`.
- PASS: repeated `dotnet test backend/WoongBlog.sln --no-restore --filter "FullyQualifiedName!~PostgresPersistenceContractTests" --logger "console;verbosity=minimal"` passed after the schema patch addition: Contract 1 skipped, Component 126, Unit 59, Architecture 35, Integration 214.
- PASS: persistent audit report artifacts generated under `backend/reports/public-work-video-dto-split-2026-05-07/`.
- PASS: `python3 -m json.tool backend/reports/public-work-video-dto-split-2026-05-07/public-work-video-dto-split-2026-05-07.json` passed.
