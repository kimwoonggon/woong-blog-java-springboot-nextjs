# TODO List - 2026-04-19

## Frontend Full Function Test260419 - 2026-04-19

### User Instructions Captured

- Backend changed heavily and frontend changed slightly, so test every homepage/frontend function.
- Do not assume 200+ is the exact count; first identify the total function count, then define TODOs.
- Include admin, public write/read, diagram, H2, upload, and all other frontend functionality.
- Save Playwright videos as `.webm` under `tests/playwright/test260419`.
- Create a TODO list based on the discovered feature inventory.

### Inventory Result

- Final discovered feature TODO count: 447.
- Source basis: existing Playwright behavior tests, route/page files, components, and frontend API clients.
- Inventory artifacts:
  - `tests/playwright/test260419/feature-inventory.json`
  - `tests/playwright/test260419/feature-inventory.md`
  - `tests/playwright/test260419/todolist-test260419.md`
  - `tests/playwright/test260419/test-files.txt`
- Route/page files inspected: 23.
- Component files inspected: 60.
- Frontend API exports inspected: 37.

### Category Counts

| Category | Count |
| --- | ---: |
| ADMIN | 153 |
| AUTH | 16 |
| EDITOR | 87 |
| UPLOAD | 55 |
| DIAGRAM | 15 |
| PUBLIC | 67 |
| RESP | 33 |
| MISC | 21 |

### Plan And TODO Mapping

| ID | TODO | User instruction coverage | Status | Verification |
| --- | --- | --- | --- | --- |
| T260419-SETUP | Confirm plugin-panic prevention, backup current files, and prepare `test260419` artifact directory. | Safe setup before long-running Playwright work. | Completed | `plugins=false`; cache paths resolved; backup created at `../woong-blog-codex-backups-2026-04-19/test260419-inventory-suite/pre-test260419-20260419.tar.gz`. |
| T260419-INV | Identify total frontend function count before writing detailed TODOs. | User correction that 200+ was approximate. | Completed | Generated 447 feature TODO candidates. |
| T260419-TODO | Create detailed TODO inventory for every discovered feature. | Full frontend functionality TODO list. | Completed | Detailed 447-row TODO is in `tests/playwright/test260419/todolist-test260419.md`. |
| T260419-CONFIG | Add dedicated Playwright config for Chromium desktop/tablet/mobile and `test260419` webm output. | Strict browser/video execution. | Completed | Added `playwright.test260419.config.ts`. |
| T260419-RUN | Run feature chunks serially and save webm artifacts. | Full frontend validation with videos. | Pending | Not started. |
| T260419-REPORT | Generate final manifest and update TODO statuses from Playwright results. | Traceable final result. | Pending | Not started. |

### Pre-Progress Alignment Check

- Planned work: inventory first, then TODO count, then strict Playwright execution with webm artifacts under `tests/playwright/test260419`.
- Execution so far: feature inventory and detailed TODO list are generated; test execution has not started yet.
- Alignment: current work matches the corrected instruction to avoid guessing 200+ upfront.

## Backend Content Clean Architecture Refactor - 2026-04-19

### User Instructions Captured

- Analyze backend code and refactor toward the original C# CQRS intent.
- Split service responsibilities by single responsibility principle and distribute use-case logic into handlers appropriately.
- Review and refactor Domain, DTO, and table design where incorrect design can create DB query performance load.
- Preserve code readability.
- Use skills appropriately, including skill discovery before implementation.
- Create a plan and TODO list first, then execute with verification after each TODO.
- User explicitly allowed subagents for this implementation.
- Split test-only work to a subagent when useful; that worker must inspect/update only backend test files and must not edit production code.

### Skill Discovery And Usage

- Local skills used: `tdd`, `dotnet-best-practices`, `aspnet-minimal-api-openapi`, `find-skills`.
- Skill search command: `npx skills find "dotnet cqrs clean architecture ef core"`.
- Search result decision: found `jeffallan/claude-skills@dotnet-core-expert` with 1.7K installs plus low-install EF/CQRS skills, but no clearly higher-trust official skill. Proceeding with existing local .NET/TDD skills to avoid adding an unreviewed third-party skill during a large refactor.
- Continuation skill search command: `npx skills find dotnet cqrs clean architecture ef core performance`.
- Continuation decision: same external `dotnet-core-expert` result appeared; no new skill installed because local repo skills already cover the implementation and no user approval was given for a global third-party skill install.
- Continuation backup before additional edits: `../woong-blog-codex-backups-2026-04-19/backend-content-clean-architecture/pre-continue-backend-src-tests-20260419T050843Z.tar.gz`.

### Content Search Schema Patch Slice

- Latest scoped instruction: implement only schema/backfill support in `DatabaseBootstrapper.cs`, touching `SeedData.cs` only if seed search-field correctness requires it.
- Work ownership alignment: do not edit tests, handlers, Domain entities, DbContext, or module DI; preserve concurrent backend refactor edits already present in the worktree.
- Pre-edit backup: `../woong-blog-codex-backups-2026-04-19/content-search-schema-patch-20260419T050859Z`.
- Pre-progress alignment check: BCA5 schema/backfill support is the active slice; seed edits are unnecessary because `WoongBlogDbContext.SaveChanges*` synchronizes `Blog`/`Work` search fields for added and modified seed entities.
- Post-progress alignment check: `DatabaseBootstrapper.cs` now applies `20260419_content_search_fields` before seeding; no `SeedData.cs` change was needed.
- Verification: `dotnet build backend/src/WoongBlog.Api/WoongBlog.Api.csproj --no-restore` passed with existing NU1901 warning; `dotnet test backend/WoongBlog.sln --filter "FullyQualifiedName~DatabaseBootstrapperTests"` could not run because the test project currently fails to compile in `WorkVideoEndpointsTests.cs` after parallel work-video constant refactor edits.

### Backend Test-Only Clean Architecture Slice

- Latest scoped instruction: inspect and update only backend test files if needed for the Content Clean Architecture refactor, especially `PublicQueryHandlerTests.cs`, `ArchitectureBoundaryTests.cs`, and `PersistenceContractTests.cs`; do not edit production code or revert other edits.
- Skill usage: local `tdd`, local `dotnet-best-practices`, and local `find-skills` guidance were read; `npx skills find dotnet architecture tests` returned only low-install external skills, so no new skill was installed.
- Pre-edit backup: `../woong-blog-codex-backups-2026-04-19/backend-test-clean-architecture-20260419T000000Z`.
- Pre-progress alignment check: work is limited to backend tests plus this required TODO record; production refactor edits already present in the worktree are preserved.
- Completed test updates: `PublicQueryHandlerTests.cs` now constructs public blog/work query handlers through the new query-store application ports, `ArchitectureBoundaryTests.cs` now checks content query-store persistence implementations through intentional query-store interfaces instead of stale service filenames, and `WorkVideoEndpointsTests.cs` imports domain-owned work-video constants after the constants move.
- Verification after TODO progress:
  - `dotnet test backend/tests/WoongBlog.Api.Tests/WoongBlog.Api.Tests.csproj --no-restore --filter FullyQualifiedName~ArchitectureBoundaryTests`: passed, 13 tests.
  - `dotnet test backend/tests/WoongBlog.Api.Tests/WoongBlog.Api.Tests.csproj --filter "FullyQualifiedName~PublicQueryHandlerTests|FullyQualifiedName~ArchitectureBoundaryTests|FullyQualifiedName~PersistenceContractTests" --no-restore`: passed, 26 tests.
  - `dotnet test backend/tests/WoongBlog.Api.Tests/WoongBlog.Api.Tests.csproj --filter "FullyQualifiedName~WorkVideoEndpointsTests" --no-restore`: passed, 6 tests.
- Post-progress alignment check: final changes remain test-only except for this TODO record, and no production files were edited by this slice.

### Plan And TODO Mapping

| ID | TODO | User instruction coverage | Status | Verification |
| --- | --- | --- | --- | --- |
| BCA1 | Prepare plugin-cache preflight, inspect worktree, run skill discovery, and create backup. | Agent workflow, panic prevention, skill usage, backup before edits. | Completed | Plugin cache paths resolved; worktree was clean; skill search completed; backup created at `../woong-blog-codex-backups-2026-04-19/backend-content-clean-architecture/pre-refactor-backend-src-tests-20260419T045911Z.tar.gz`. |
| BCA2 | Add failing architecture and persistence/query tests that lock Clean Architecture boundaries and search schema behavior. | TDD red step for CQRS/service/domain/table concerns. | Completed | `dotnet test backend/WoongBlog.sln --filter "FullyQualifiedName~ArchitectureBoundaryTests|FullyQualifiedName~PersistenceContractTests"` failed as expected because `Blog.SearchTitle`, `Blog.SearchText`, `Work.SearchTitle`, and `Work.SearchText` do not exist yet. |
| BCA3 | Move content use-case orchestration from actor services into command/query handlers and replace broad services with narrow application ports. | CQRS intent, SRP, handler distribution, readability. | Completed | Content handlers now depend on command/query stores instead of actor facade services; `dotnet test backend/WoongBlog.sln` passed 139 tests. |
| BCA4 | Move domain-owned work video status/source constants into Domain and remove Domain-to-Application dependency. | Domain correctness and architecture boundaries. | Completed | `Domain_Source_DoesNot_Reference_Application_Modules_Or_Infrastructure` passed as part of backend suite. |
| BCA5 | Add search columns/indexes/schema patch and update create/update/seed/backfill paths. | DB/query performance and table design. | Completed | `PersistenceContractTests` passed; Docker Postgres has `20260419_content_search_fields`, `SearchTitle`/`SearchText` columns, and four trigram search indexes. |
| BCA6 | Optimize public blog/work list queries to filter in DB instead of loading all published rows for search. | DB query performance. | Completed | `Public_Content_Query_Persistence_DoesNot_Filter_Search_In_Memory` passed; focused Playwright public search/pagination tests passed. |
| BCA7 | Run targeted tests after each slice and final backend/frontend/browser verification. | Verification after TODO progress. | Completed | `dotnet test backend/WoongBlog.sln`: 139 passed; `npm run typecheck`: passed; `npm run lint`: 0 errors, 4 unrelated warnings; `npx vitest run src/test/admin-bulk-table.test.tsx`: 5 passed; focused Docker-backed Playwright subset: 35 passed. |
| BCA8 | Investigate Codex plugin-cache panic, apply recurrence prevention, and re-check external process state before continuing. | Panic prevention and reliable continuation after subagent/plugin refresh failure. | Completed | `codex-tui.log` showed repeated `plugin cache root should be absolute: No such file or directory` during plugin manifest/featured-sync warmup; cache dirs resolve as absolute paths; `scripts/dev-up.sh` now rejects relative `CODEX_HOME_DIR` and creates/verifies both plugin cache dirs; backed up `/home/kimwoonggon/.codex/config.toml` and ran `codex features disable plugins`, making `plugins` feature false to stop plugin store warm/sync during this verification. Docker state was rechecked after each panic. |

### Pre-Progress Alignment Check

- Planned work: Content-first backend refactor in a single project, stricter Clean Architecture boundaries, DB schema patch allowed, HTTP/API DTO improvements allowed when tests and consumers are updated.
- Execution so far: repository structure and current service/query/domain shape were inspected during planning, skill discovery ran, cache preflight passed, and a backup was created before code/config edits.
- Alignment: current execution matches the planned Content CQRS/Clean Architecture refactor.

### Post-Progress Alignment Check

- Planned work completed: Content handlers now own command/query orchestration through narrow stores, broad actor services were removed, Domain no longer references Application for work-video constants, and public search filtering is DB-backed through `SearchTitle`/`SearchText`.
- Panic prevention completed: repeated plugin-cache panic was traced to Codex plugin store warm/sync; plugin feature was disabled in `/home/kimwoonggon/.codex/config.toml` after backing it up, while `scripts/dev-up.sh` now enforces absolute `CODEX_HOME_DIR` and cache path preflight.
- Verification completed: backend xUnit, TypeScript typecheck, ESLint, targeted Vitest, Docker-backed schema smoke, backend/nginx health, and focused Playwright browser tests passed.
- Alignment: final execution matches the backend CQRS/Clean Architecture refactor goal and the later requirement to address panic cause before rigorous Playwright validation.

## Mermaid Explicit Block Independence And Runtime Stability

### User Instructions Captured

- Implement the plan to make Mermaid explicit `/mermaid` only.
- Remove Mermaid automatic inference from generic code blocks, paragraph fences, and paste fences.
- Keep Mermaid independent inside Tiptap/content rendering.
- Investigate and prevent frontend container high CPU and memory leak caused by Mermaid/public rendering interactions.
- Check easy, middle, and extreme cases through tests.
- Verify public slug, admin, other pages, repeated navigation, and returning to Mermaid content.
- Create or update TODO records before implementation and record verification.

### Plan And TODO Mapping

| ID | TODO | User instruction coverage | Status | Verification |
| --- | --- | --- | --- | --- |
| MEX1 | Confirm current runtime state, stop runaway frontend container, and inspect worktree. | High CPU/memory leak investigation before implementation. | Completed | `docker stats` showed frontend at ~100% CPU and ~1.9GiB; frontend container was stopped. |
| MEX2 | Back up Mermaid-related files before edits. | Backup before modifying code. | Completed | Created `../woong-blog-codex-backups-2026-04-19/mermaid-explicit-only/`. |
| MEX3 | Remove automatic Mermaid inference for paste fences, code blocks, and paragraph fences. | `/mermaid` explicit-only policy. | Completed | `TiptapEditor` no longer converts paste fences; `mermaid-content` no longer infers code/paragraph fences. |
| MEX4 | Keep only explicit `<mermaid-block>` public rendering and excerpt stripping. | Mermaid independence from generic renderer/cards. | Completed | `containsMermaidSyntax` now checks only `<mermaid-block`; backend excerpt removes explicit Mermaid blocks. |
| MEX5 | Add/update unit tests for easy/middle/extreme Mermaid cases and non-Mermaid code block safety. | Easy/middle/extreme test coverage. | Completed | Targeted Vitest passed: 5 files / 32 tests; backend targeted xUnit passed: 13 tests. |
| MEX6 | Add/update browser tests for public slug/admin/other pages/repeated navigation. | Public/admin runtime stability and no OOM/504. | Completed | Docker-backed `admin-mermaid-public-independence.spec.ts` passed; frontend stayed `Up`. |
| MEX7 | Create `mermaid-affect-range-0419.md` impact report with code locations and risks. | Requested deep impact report. | Completed | Created report and verified required sections with `rg`. |
| MEX8 | Run final verification and record results. | Complete implementation safely. | Completed | `typecheck`, `lint`, `build`, targeted Vitest/xUnit, Playwright, `docker stats`, and `docker compose ps` completed. |

### Pre-Progress Alignment Check

- Planned work: make Mermaid explicit `/mermaid` only, isolate Mermaid code paths, stop automatic inference, test runtime stability, and produce the requested impact report.
- Execution so far: Codex plugin cache preflight passed, worktree state was checked, and the runaway frontend container was stopped before edits.
- Alignment: current execution matches the requested Mermaid independence and stability fix.

### Post-Progress Alignment Check

- Planned work completed: Mermaid is now explicit block only, generic fence/code/paragraph inference was removed, public/card/navigation paths are decoupled through `prefetch={false}`, and the requested impact report was written.
- Verification completed: `npm run typecheck`, `npm run lint`, `npm run build`, targeted Vitest, targeted xUnit, Docker-backed Playwright, direct frontend container stats, and compose status checks passed.
- Runtime result: after Playwright, `docker stats --no-stream` showed frontend CPU `0.00%` and memory `94.87MiB`; frontend remained `Up`.
- Alignment: final execution matches the explicit Mermaid-only and runtime stability request.

## Mermaid CPU Peak Comparison - 2026-04-19

### User Instructions Captured

- Compare CPU peak against a no-Mermaid baseline instead of judging Mermaid CPU in isolation.
- Test three directions:
  - plain no-Mermaid 20 admin reads and 20 public reads,
  - explicit Mermaid 20 admin reads and 20 public reads,
  - saved Batch AI prompt containing literal ```mermaid text plus Mermaid 20 admin reads and 20 public reads.
- Save/read the Batch AI prompt through the UI.
- Delete Mermaid-containing generated posts after the experiment.
- Record CPU and memory usage while tests run.

### Results

| Scenario | Browser result | Videos | Frontend peak CPU | Frontend peak memory | Final frontend state |
| --- | --- | ---: | ---: | ---: | --- |
| Plain baseline 20/20 | 40 passed | 40 | 40.09% | 108.10 MiB | 0.00%, 102.6 MiB, Up |
| Explicit Mermaid 20/20 | 40 passed | 40 | 47.08% | 108.90 MiB | 0.00%, Up |
| Batch prompt saved + Mermaid 20/20 | 41 passed | 40 read videos | 39.15% | 104.20 MiB | 0.00%, 101.5 MiB |

### Verification Artifacts

- Plain videos and stats: `tests/playwright/0418test/plain-baseline-0419/`
- Explicit Mermaid videos and stats: `tests/playwright/0418test/mermaid-worstcase-0419/`
- Batch prompt Mermaid videos and stats: `tests/playwright/0418test/mermaid-batch-prompt-0419/`
- All three scenarios produced 40 `.webm` read videos each.
- Mermaid cleanup query deleted 20 generated Mermaid blogs after the final batch-prompt run; remaining Mermaid block counts are 0 blogs and 0 works.

### Post-Experiment Alignment Check

- Planned comparison completed: all three scenarios ran with same Docker-backed app and `docker stats` sampling.
- Verification completed: no sustained CPU runaway remained; final `docker stats --no-stream` showed frontend CPU `0.00%`, memory `102.6MiB`, and all containers `Up`.
- Alignment: final experiment matches the user's CPU peak comparison and Mermaid cleanup request.

## Publish And Promotion - 2026-04-19

### User Instructions Captured

- Commit the current version.
- Push the current branch.
- Merge the branch into `dev`.
- Observe CI/CD.
- If CI/CD is clean, reflect the change into `main`.

### Plan And TODO Mapping

| ID | TODO | User instruction coverage | Status | Verification |
| --- | --- | --- | --- | --- |
| P1 | Confirm current branch, remote, auth, and worktree scope before staging. | Commit/push from the correct repo and branch. | Completed | Current branch is `feat/frontend-renovate-0418`; remote is `origin`; `gh auth status` is authenticated as `kimwoonggon`; worktree scope was reviewed. |
| P2 | Run pre-commit sanity checks. | Avoid publishing broken obvious state. | Completed | `git diff --check` passed; prior typecheck/lint/build/unit/backend/Playwright checks were recorded above. |
| P3 | Commit all intended feature, docs, test, and video artifact changes. | Commit current version. | Completed | Created commit `4e8366d408d76b508700917f4d9f710ca21a2f5e` (`Stabilize admin search mermaid and backups`). |
| P4 | Push `feat/frontend-renovate-0418` to origin. | Push this branch. | Completed | Branch pushed to `origin/feat/frontend-renovate-0418`. |
| P5 | Merge into `dev`, push `dev`, and observe CI/CD. | Merge to dev and monitor CI/CD. | In Progress | `dev` fast-forwarded and pushed to `4e8366d408d76b508700917f4d9f710ca21a2f5e`; first `CI Dev` run failed in frontend unit tests because `admin-bulk-table.test.tsx` still expected `router.replace` after URL-stability switched to `window.history.replaceState`. |
| P6 | If dev CI/CD is clean, merge/publish to `main` and observe CI/CD. | Reflect clean dev result into main. | Blocked | Waiting for repaired `dev` CI/CD result. |

### Pre-Publish Alignment Check

- Planned work: publish the current feature branch, then promote through `dev` and `main` only after checks are clean.
- Execution so far: GitHub auth and branch/remote state were confirmed, and pre-commit whitespace check passed.
- Alignment: current execution matches the requested publish and promotion workflow.

### Dev CI Failure And Fix - 2026-04-19

- Observed failing run: `CI Dev` run `24609349690` for commit `4e8366d408d76b508700917f4d9f710ca21a2f5e`.
- Scope of failure: only `Frontend lint, types, and unit tests` failed; lint, typecheck, backend tests, and compose smoke passed.
- Root cause: `src/test/admin-bulk-table.test.tsx` expected `router.replace('/admin/blog?page=2&pageSize=12', { scroll: false })`, but the intended URL-stability fix now updates admin list paging with `window.history.replaceState` to avoid Next router remount/input reset behavior.
- Fix applied: updated the test to assert `replaceMock` is not called, assert `window.location.pathname` and `window.location.search`, and prevent the mocked `next/link` from leaking `prefetch={false}` to the DOM.
- Verification:
  - `npx vitest run src/test/admin-bulk-table.test.tsx`: passed, 5 tests.
  - `npm run test -- --run`: passed, 45 files / 232 tests.
