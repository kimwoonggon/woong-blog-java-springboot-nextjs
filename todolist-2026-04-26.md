# Backend Full Coverage Review TODO - 2026-04-26

## User Instructions

- Inspect `coverage/backend/full` HTML coverage output.
- Use subagents.
- Review carefully what the current test situation is.
- Identify what tests should be strengthened.
- Keep the review focused on backend coverage/test quality.

## Scope Check Before TODO Execution

- This is an audit-only task.
- Production code and test code must not be modified.
- Existing uncommitted coverage-tooling changes from the prior task must be preserved and not reverted.
- Planned write set:
  - `todolist-2026-04-26.md`
  - `backend/reports/backend-full-coverage-review-2026-04-26/*`
- Coverage source of truth:
  - `coverage/backend/full/report/index.html`
  - `coverage/backend/full/report/Summary.json`
  - per-class HTML files under `coverage/backend/full/report/`
  - `coverage/backend/full/report/Cobertura.xml`
- Specialist skill search completed with `npx skills find dotnet coverage test audit`; no external skill installed because returned skills had low install counts.

## TODOs

- [x] Prepare backup before writing audit artifacts.
  - Verification: `backend/reports/backend-full-coverage-review-2026-04-26/prechange-backup/` and `prechange-backup.patch` created.
- [x] Confirm planned work still matches this TODO list before detailed review.
  - Planned verification: focused workspace/status and coverage artifact inspection.
  - Verification: reviewed `coverage/backend/full` artifacts and current dirty status; audit-only write set remains TODO plus `backend/reports/backend-full-coverage-review-2026-04-26/*`.
- [x] Delegate independent coverage review slices to subagents.
  - Planned verification: collect subagent findings for infrastructure/external seams, API/application behavior, and prioritization.
  - Verification: spawned three explorer subagents for infrastructure/external seams, API/Application behavior, and executive prioritization.
- [x] Parse full coverage metrics locally.
  - Planned verification: extract total, assembly, class, and branch-risk summaries from `Summary.json` and coverage HTML/Cobertura artifacts.
  - Verification:
    - Parsed `coverage/backend/full/report/Summary.json`: full line 90.1%, branch 60.9%, method 93.9%, full-method 85.6%.
    - Extracted assembly coverage for `WoongBlog.Api`, `WoongBlog.Application`, `WoongBlog.Domain`, and `WoongBlog.Infrastructure`.
    - Extracted low-line and low-branch classes from `Summary.json`.
    - Inspected key per-class HTML files for red/uncovered lines: `BlogAiFixService`, `R2VideoStorageService`, `WorkVideoPolicy`, `ValidationExceptionFilter`, auth events, `FfmpegVideoTranscoder`, `ForwardedHeadersOptionsFactory`, `ContentSearchText`, `UploadAssetEndpoint`, and `AiHttpResultMapper`.
- [x] Integrate findings into persistent audit artifacts.
  - Planned verification: Markdown, HTML, and JSON reports include current situation, reinforced-test recommendations, risks, and final recommendation.
  - Verification:
    - Created `backend/reports/backend-full-coverage-review-2026-04-26/backend-full-coverage-review-2026-04-26.md`.
    - Created `backend/reports/backend-full-coverage-review-2026-04-26/backend-full-coverage-review-2026-04-26.html`.
    - Created `backend/reports/backend-full-coverage-review-2026-04-26/backend-full-coverage-review-2026-04-26.json`.
    - Reports summarize current backend coverage, misleading signals, low-coverage production areas, high-risk low-coverage areas, and recommended next coverage targets.
- [x] Validate report artifacts and final consistency.
  - Planned verification: JSON parse, `git diff --check`, and final status review.
  - Verification:
    - Parsed the report JSON with Node and verified report slug, full line coverage 90.1%, full branch coverage 60.9%, and risk-area entries.
    - Ran `git diff --check`; no whitespace errors were reported.
    - Reviewed `git status --short`; audit-only files are newly added alongside pre-existing coverage-tooling changes.

---

# Backend AI Provider Failure Coverage TODO - 2026-04-26

## User Instructions

- Implement backend-only AI provider failure and edge-path coverage from the supplied plan.
- Do not use real OpenAI, Azure OpenAI, Codex, or external service calls.
- Add `BlogAiFixServiceProviderComponentTests` with deterministic HTTP fakes and serialized environment variable handling.
- Add `AiHttpResultMapperTests` without exposing new production APIs.
- Do not touch WorkVideo/R2/HLS coverage in this task.
- Generate persistent audit artifacts under `backend/reports/backend-ai-provider-failure-coverage-2026-04-26/`.

## Scope Check Before TODO Execution

- This is a backend test reinforcement task.
- Expected production code change: none; `BlogAiFixService` already accepts `HttpClient`.
- Existing uncommitted user/previous-agent changes must be preserved and not reverted.
- Planned write set:
  - `todolist-2026-04-26.md`
  - `backend/tests/WoongBlog.Api.ComponentTests/BlogAiFixServiceProviderComponentTests.cs`
  - `backend/tests/WoongBlog.Api.UnitTests/AiHttpResultMapperTests.cs`
  - `backend/tests/WoongBlog.Api.UnitTests/WoongBlog.Api.UnitTests.csproj`
  - `backend/reports/backend-ai-provider-failure-coverage-2026-04-26/*`
- Specialist skill search completed with `npx --yes skills find dotnet testing`; returned .NET testing skills had low install counts, so no external skill was installed. Local `tdd` and .NET conventions will be used.
- Plugin cache directories were verified as absolute paths before long-running validation.

## Instruction Mapping

- Backend-only provider tests: TODOs 3, 4, and 7.
- No real external calls: TODO 3 deterministic fake `HttpMessageHandler`.
- Environment variable isolation: TODO 3 scope helper and xUnit collection.
- Unit mapper coverage through internals: TODO 4 and project reference update.
- Required validation: TODO 5 and TODO 6.
- Mandatory audit report: TODO 7.

## TODOs

- [x] Prepare backup before modifying test project files.
  - Verification: created `backend/reports/backend-ai-provider-failure-coverage-2026-04-26/prechange-backup/prechange-backup.patch` and `prechange-status.txt`.
- [x] Confirm actual work still matches this TODO list before implementation.
  - Verification: inspected `BlogAiFixService`, `AiHttpResultMapper`, existing backend test layout, and project references; plan still fits without production-code changes.
- [x] Add `BlogAiFixServiceProviderComponentTests` for OpenAI and Azure success, failure, malformed JSON, empty content, configuration, URL/header, model/deployment, and cleanup behavior.
  - Verification: `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter BlogAiFixServiceProviderComponentTests` passed with 20 tests.
- [x] Add `AiHttpResultMapperTests` and the minimal UnitTests project reference needed for `WoongBlog.Api` internals.
  - Verification: `dotnet test backend/tests/WoongBlog.Api.UnitTests/WoongBlog.Api.UnitTests.csproj --filter AiHttpResultMapperTests` passed with 5 tests.
- [x] Run targeted backend test validations.
  - Planned verification:
    - `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter BlogAiFixServiceProviderComponentTests`
    - `dotnet test backend/tests/WoongBlog.Api.UnitTests/WoongBlog.Api.UnitTests.csproj --filter AiHttpResultMapperTests`
  - Verification:
    - `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter BlogAiFixServiceProviderComponentTests` passed; 20 passed, 0 failed.
    - `dotnet test backend/tests/WoongBlog.Api.UnitTests/WoongBlog.Api.UnitTests.csproj --filter AiHttpResultMapperTests` passed; 5 passed, 0 failed.
    - Existing warning observed: `NU1901` for `AWSSDK.Core` 4.0.0.17 low-severity vulnerability.
- [x] Run required full validation commands and parse coverage where available.
  - Planned verification:
    - `./scripts/run-backend-coverage.sh component -v minimal`
    - `./scripts/run-backend-coverage.sh full -v minimal`
    - `dotnet test backend/WoongBlog.sln`
    - `git diff --check`
  - Verification:
    - `./scripts/run-backend-coverage.sh component -v minimal` passed; 96 passed, 0 failed.
    - `./scripts/run-backend-coverage.sh full -v minimal` passed after removing a direct `Microsoft.AspNetCore.App` UnitTests reference that violated architecture boundaries.
    - `dotnet test backend/WoongBlog.sln` passed; contract verification skipped as configured, and component/unit/architecture/integration tests passed.
    - `git diff --check` passed.
    - Parsed `coverage/backend/component/report/Summary.json`: component line 41.9%, branch 33.6%, `BlogAiFixService` line 79.2%, branch 57.4%.
    - Parsed `coverage/backend/full/report/Summary.json`: full line 91.3%, branch 64.8%, `BlogAiFixService` line 84.1%, branch 62.7%, `AiHttpResultMapper` line 100.0%, branch 100.0%.
    - Existing warning observed: `NU1901` for `AWSSDK.Core` 4.0.0.17 low-severity vulnerability.
- [x] Generate audit artifacts and final consistency check.
  - Planned verification: Markdown, JSON, and HTML reports include changes, non-changes, commands, coverage notes, risks, and final recommendation.
  - Verification:
    - Created `backend/reports/backend-ai-provider-failure-coverage-2026-04-26/backend-ai-provider-failure-coverage-2026-04-26.md`.
    - Created `backend/reports/backend-ai-provider-failure-coverage-2026-04-26/backend-ai-provider-failure-coverage-2026-04-26.json`.
    - Created `backend/reports/backend-ai-provider-failure-coverage-2026-04-26/backend-ai-provider-failure-coverage-2026-04-26.html`.
    - Parsed the report JSON with Node and verified report slug plus full line/branch coverage.
    - Ran `git diff --check` after audit artifact creation; no whitespace errors were reported.

---

# Backend WorkVideo Policy Coverage Reinforcement TODO - 2026-04-26

## User Instructions

- Scope is backend only.
- Reduce the top coverage risk hotspot in `WorkVideoPolicy` before larger WorkVideo R2/HLS failure coverage work.
- Target production file: `backend/src/WoongBlog.Application/Modules/Content/Works/WorkVideos/WorkVideoPolicy.cs`.
- Do not modify production code unless a real bug is found.
- Do not touch R2/HLS/storage implementation.
- Do not add integration tests unless necessary; prefer pure unit tests.
- Add table-driven tests for:
  - `NormalizeYouTubeVideoId`
  - `ValidateVideoFile`
  - `LooksLikeMp4`
  - `SanitizeOriginalFileName`
- Preserve existing test category trait conventions.
- Update backend coverage/audit report with tests added, previous hotspot, `WorkVideoPolicy` coverage result if available, commands run, and remaining WorkVideo/R2/HLS gaps.
- Required commands:
  - `dotnet test backend/tests/WoongBlog.Api.UnitTests/WoongBlog.Api.UnitTests.csproj --filter WorkVideoPolicy`
  - `./scripts/run-backend-coverage.sh unit -v minimal`
  - `./scripts/run-backend-coverage.sh full -v minimal`
  - `dotnet test backend/WoongBlog.sln`
  - `git diff --check`

## Scope Check Before TODO Execution

- This is a backend-only unit coverage reinforcement task.
- Expected production code change: none.
- Planned write set:
  - `todolist-2026-04-26.md`
  - `backend/tests/WoongBlog.Api.UnitTests/WorkVideoPolicyTests.cs`
  - `backend/reports/backend-workvideo-policy-coverage-reinforcement-2026-04-26/*`
- Existing uncommitted changes must be preserved and not reverted.
- Previous full coverage hotspot from `coverage/backend/full/report/Summary.json`: `WorkVideoPolicy` at 52.7% line coverage and 31.6% branch coverage.
- Specialist skill search completed with `npx skills find dotnet testing`; returned low-install .NET testing skills, so no external skill was installed. Local `tdd` and .NET conventions will be used.
- Plugin cache directories were verified as absolute paths before long-running validation.

## Instruction Mapping

- Backend-only and no R2/HLS/storage implementation changes: TODOs 2 through 5.
- Pure unit tests and trait conventions: TODO 2.
- Table-driven policy coverage: TODO 2.
- Required validation commands: TODO 3 and TODO 5.
- Mandatory audit report: TODO 4.

## TODOs

- [x] Prepare backup before modifying test project files.
  - Verification: created `backend/reports/backend-workvideo-policy-coverage-reinforcement-2026-04-26/prechange-backup/prechange-backup.patch`, `prechange-status.txt`, and a pre-change copy of this TODO file.
- [x] Confirm actual work still matches this TODO list before implementation.
  - Verification: inspected `WorkVideoPolicy`, existing UnitTests layout, trait conventions, and prior coverage report; plan remains pure unit tests with no production-code change.
- [x] Add `WorkVideoPolicyTests` with table-driven coverage for YouTube normalization, video file validation, MP4 prefix sniffing, and original filename sanitization.
  - Planned verification: `dotnet test backend/tests/WoongBlog.Api.UnitTests/WoongBlog.Api.UnitTests.csproj --filter WorkVideoPolicy`.
  - Verification: targeted WorkVideoPolicy unit filter passed with 36 tests, 0 failed, 0 skipped. Existing warning observed: `NU1901` for `AWSSDK.Core` 4.0.0.17 low-severity vulnerability.
- [x] Run required backend validations and parse `WorkVideoPolicy` coverage where available.
  - Planned verification:
    - `dotnet test backend/tests/WoongBlog.Api.UnitTests/WoongBlog.Api.UnitTests.csproj --filter WorkVideoPolicy`
    - `./scripts/run-backend-coverage.sh unit -v minimal`
    - `./scripts/run-backend-coverage.sh full -v minimal`
    - `dotnet test backend/WoongBlog.sln`
    - `git diff --check`
  - Verification:
    - `dotnet test backend/tests/WoongBlog.Api.UnitTests/WoongBlog.Api.UnitTests.csproj --filter WorkVideoPolicy` passed; 36 passed, 0 failed.
    - `./scripts/run-backend-coverage.sh unit -v minimal` passed; 55 unit tests passed and unit coverage report regenerated.
    - `./scripts/run-backend-coverage.sh full -v minimal` passed; contract verification skipped as configured, 96 component tests passed, 55 unit tests passed, 35 architecture tests passed, and 194 integration tests passed.
    - `dotnet test backend/WoongBlog.sln` passed; contract verification skipped as configured, 96 component tests passed, 55 unit tests passed, 35 architecture tests passed, and 194 integration tests passed.
    - `git diff --check` passed.
    - Parsed `coverage/backend/unit/report/Summary.json`: `WorkVideoPolicy` line coverage 100.0%, branch coverage 95.0%.
    - Parsed `coverage/backend/full/report/Summary.json`: full backend line coverage 92.2%, branch coverage 70.5%; `WorkVideoPolicy` line coverage 100.0%, branch coverage 95.0%.
    - Existing warning observed: `NU1901` for `AWSSDK.Core` 4.0.0.17 low-severity vulnerability.
- [x] Generate audit artifacts and final consistency check.
  - Planned verification: Markdown, JSON, and HTML reports include changes, non-changes, commands, coverage notes, risks, and final recommendation.
  - Verification:
    - Created `backend/reports/backend-workvideo-policy-coverage-reinforcement-2026-04-26/backend-workvideo-policy-coverage-reinforcement-2026-04-26.md`.
    - Created `backend/reports/backend-workvideo-policy-coverage-reinforcement-2026-04-26/backend-workvideo-policy-coverage-reinforcement-2026-04-26.json`.
    - Created `backend/reports/backend-workvideo-policy-coverage-reinforcement-2026-04-26/backend-workvideo-policy-coverage-reinforcement-2026-04-26.html`.
    - Parsed the report JSON with Node and verified report slug plus current full `WorkVideoPolicy` coverage of 100.0% line and 95.0% branch.
    - Ran `git diff --check` after audit artifact creation; no whitespace errors were reported.
    - Reviewed `git status --short`; changed files match the planned write set.
    - Final scope check: actual work still matches this TODO list, with no production code, R2, HLS, or storage implementation changes.

---

# Backend WorkVideo R2/HLS Failure Coverage Audit TODO - 2026-04-26

## User Instructions

- Scope is backend only.
- Do not implement tests yet.
- Do not modify production code.
- Create a focused audit report for WorkVideo R2/storage/HLS failure coverage.
- Primary targets:
  - `R2VideoStorageService`
  - `LocalVideoStorageService`
  - `WorkVideoStorageSelector`
  - `WorkVideoPlaybackUrlBuilder`
  - `WorkVideoService`
  - `FfmpegVideoTranscoder`
  - `WorkVideoCleanupStore`
  - `WorkVideoCommandStore`
  - `WorkVideoQueryStore`
  - `WorkVideoEndpoints`
  - HLS job planning/starting code
  - Cloudflare R2 options and storage option validators
- Report path:
  - `backend/reports/backend-workvideo-r2-hls-failure-coverage-audit-2026-04-26/backend-workvideo-r2-hls-failure-coverage-audit-2026-04-26.md`
- Report must include:
  - current coverage snapshot
  - production behavior inventory
  - failure path matrix
  - fake/seam assessment
  - prioritized implementation batches
- Be strict and pessimistic.
- Do not recommend real R2 access.
- Prefer deterministic fakes/temp directories.
- Do not chase coverage percentage alone; prioritize failure behavior and side effects.
- Required commands after writing the report:
  - `dotnet test backend/WoongBlog.sln`
  - `git diff --check`

## Scope Check Before TODO Execution

- This is an audit-only backend task.
- Production and test code must not be modified.
- Planned write set:
  - `todolist-2026-04-26.md`
  - `backend/reports/backend-workvideo-r2-hls-failure-coverage-audit-2026-04-26/*`
- Existing uncommitted changes must be preserved and not reverted.
- Specialist skill search completed with `npx skills find dotnet s3 testing`; returned low-install .NET testing skills, so no external skill was installed. Local `tdd` and .NET guidance will inform the test recommendations only.
- Plugin cache directories were verified as absolute paths before the required solution test.

## Instruction Mapping

- Audit-only and no production/test edits: TODOs 1 through 5.
- Coverage snapshot: TODO 2 and TODO 3.
- Production lifecycle inventory: TODO 2 and TODO 3.
- Failure path matrix: TODO 2 and TODO 3.
- Fake/seam assessment: TODO 2 and TODO 3.
- Prioritized batches: TODO 3.
- Required commands: TODO 4 and TODO 5.

## TODOs

- [x] Prepare backup before writing audit artifacts.
  - Verification: created `backend/reports/backend-workvideo-r2-hls-failure-coverage-audit-2026-04-26/prechange-backup/todolist-2026-04-26.md.before-workvideo-r2-hls-audit` and reviewed pre-change status.
- [x] Confirm actual work still matches this TODO list before detailed audit.
  - Verification: scope remains audit-only; planned writes are TODO and report artifacts only.
- [x] Inspect coverage, production WorkVideo storage/HLS behavior, and existing tests.
  - Planned verification: parse `coverage/backend/full/report/Summary.json`, inspect target production files, and inspect existing backend tests that exercise WorkVideo paths.
  - Verification:
    - Parsed current full backend coverage from `coverage/backend/full/report/Summary.json`: 92.2% line coverage, 70.5% branch coverage.
    - Extracted target coverage for `R2VideoStorageService`, `LocalVideoStorageService`, `WorkVideoStorageSelector`, `WorkVideoPlaybackUrlBuilder`, `FfmpegVideoTranscoder`, `WorkVideoService`, WorkVideo stores, endpoints, policy, HLS plan, and Cloudflare R2 options.
    - Inspected WorkVideo endpoint mappings, upload/confirm/local/YouTube/reorder/delete/HLS command handlers, storage services, selector, playback URL builder, HLS workspace/output publisher/transcoder, cleanup service/store, command/query stores, options, and service registration.
    - Inspected existing WorkVideo unit, component, and integration tests.
- [x] Write the focused Markdown audit report.
  - Planned verification: report includes all requested sections, strict failure-path recommendations, and no recommendation for real R2 access.
  - Verification:
    - Created `backend/reports/backend-workvideo-r2-hls-failure-coverage-audit-2026-04-26/backend-workvideo-r2-hls-failure-coverage-audit-2026-04-26.md`.
    - Report includes current coverage snapshot, production behavior inventory, failure path matrix, fake/seam assessment, and four prioritized implementation batches.
    - Report recommends deterministic fakes/temp directories and explicitly does not recommend real R2 access.
- [x] Run required validation and final consistency checks.
  - Planned verification:
    - `dotnet test backend/WoongBlog.sln`
    - `git diff --check`
    - final status review confirms no production or test code changes.
  - Verification:
    - `dotnet test backend/WoongBlog.sln` passed; contract verification skipped as configured, 96 component tests passed, 55 unit tests passed, 35 architecture tests passed, and 194 integration tests passed.
    - `git diff --check` passed.
    - Reviewed `git status --short`; changes are limited to this TODO file and the new audit report directory.
    - Final scope check: no production code and no test code were modified.
    - Existing warning observed: `NU1901` for `AWSSDK.Core` 4.0.0.17 low-severity vulnerability.

---

# Backend WorkVideo R2/HLS Batch 1 Test Implementation TODO - 2026-04-26

## User Instructions

- Scope is backend only.
- Use `backend/reports/backend-workvideo-r2-hls-failure-coverage-audit-2026-04-26/backend-workvideo-r2-hls-failure-coverage-audit-2026-04-26.md`.
- Implement only Batch 1 tests from that audit report.
- Do not modify production code.
- Do not add an S3 client factory seam yet.
- Do not test `R2VideoStorageService` direct object operations in this batch if that requires a seam.
- Do not use real credentials, real R2, real S3, real Cloudflare, or external network services.
- Do not broaden into Auth/Proxy/Security validator tests.
- Use deterministic fakes, temp directories, and existing helpers.
- Prioritize:
  - `WorkVideoStorageSelector` behavior.
  - `WorkVideoPlaybackUrlBuilder` behavior.
  - `LocalVideoStorageService` behavior with temp directories.
  - HLS path/key/job planning behavior that does not require real ffmpeg or real R2.
  - Cleanup/idempotency behavior that can be verified without external services.
- Run:
  - `dotnet test backend/tests/WoongBlog.Api.UnitTests/WoongBlog.Api.UnitTests.csproj`
  - `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj`
  - `./scripts/run-backend-coverage.sh component -v minimal`
  - `./scripts/run-backend-coverage.sh full -v minimal`
  - `dotnet test backend/WoongBlog.sln`
  - `git diff --check`
- Update the audit report with tests added, files changed, behavior covered, command results, coverage changes for `WorkVideoStorageSelector`, `WorkVideoPlaybackUrlBuilder`, `LocalVideoStorageService`, `FfmpegVideoTranscoder` if applicable, and remaining Batch 2 items that require an S3/R2 seam.

## Scope Check Before TODO Execution

- This is a backend-only test reinforcement task.
- Expected production code change: none.
- Planned write set:
  - `todolist-2026-04-26.md`
  - `backend/tests/WoongBlog.Api.ComponentTests/WorkVideoComponentTests.cs`
  - `backend/tests/WoongBlog.Api.UnitTests/WorkVideoHlsJobPlanTests.cs`
  - `backend/reports/backend-workvideo-r2-hls-failure-coverage-audit-2026-04-26/*`
- `WorkVideoStorageSelector` and `WorkVideoPlaybackUrlBuilder` are Infrastructure classes. Architecture tests forbid adding Infrastructure references to the UnitTests project, so their concrete behavior tests will remain in ComponentTests while pure Application HLS planning stays in UnitTests.
- Specialist skill search completed with `npx --yes skills find dotnet testing`; returned low-install .NET testing skills, so no external skill was installed. Local `tdd` and existing .NET test conventions will be used.
- Plugin cache directories were verified as absolute paths before long-running validation.

## Instruction Mapping

- Backend-only/no production changes/no R2: TODOs 1 through 8.
- Storage selector behavior: TODO 3.
- Playback URL builder behavior: TODO 3.
- Local temp-directory storage behavior: TODO 4.
- HLS deterministic path/key planning: TODO 5.
- Cleanup/idempotency behavior: TODO 6.
- Required validation commands: TODO 7.
- Audit report update: TODO 8.

## TODOs

- [x] Prepare backup before modifying test or report files.
  - Verification: created `backend/reports/backend-workvideo-r2-hls-failure-coverage-audit-2026-04-26/batch1-prechange-backup/` with pre-change copies of this TODO file, WorkVideo component tests, HLS job plan unit tests, and the source audit report.
- [x] Confirm actual work still matches this TODO list before implementation.
  - Verification: inspected the Batch 1 audit section, existing WorkVideo unit/component tests, storage selector, playback URL builder, local storage, HLS publisher, cleanup store, and project architecture boundaries; plan remains test-only with no production code or S3/R2 seam.
- [x] Add storage selector and playback URL builder behavior tests using deterministic fake storage.
  - Planned verification: run targeted component test filters for the new selector and playback tests.
  - Verification: `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter WorkVideoComponentTests` passed with 25 tests after fixing a test-only byte array inference issue.
- [x] Add local storage temp-directory behavior tests for missing deletes, overwrite/delete safety, prefix reads, and path safety where current behavior allows it.
  - Planned verification: run targeted component test filters for local storage tests.
  - Verification: `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter WorkVideoComponentTests` passed with 25 tests. Path traversal enforcement was not asserted because current `LocalVideoStorageService` relies on application-generated storage keys and does not enforce a root boundary internally.
- [x] Add HLS deterministic planning/path tests that require no ffmpeg or R2.
  - Planned verification: run targeted unit tests for `WorkVideoHlsJobPlan`.
  - Verification: `dotnet test backend/tests/WoongBlog.Api.UnitTests/WoongBlog.Api.UnitTests.csproj --filter WorkVideoHlsJobPlan` passed with 3 tests.
- [x] Add cleanup/idempotency and HLS publisher behavior tests that require no external services.
  - Planned verification: run targeted component filters for cleanup store and publisher tests.
  - Verification: `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj --filter WorkVideoComponentTests` passed with 25 tests. Cleanup duplicate suppression is verified after the first pending job is persisted, matching current store behavior.
- [x] Run required backend validation commands and capture coverage changes.
  - Planned verification:
    - `dotnet test backend/tests/WoongBlog.Api.UnitTests/WoongBlog.Api.UnitTests.csproj`
    - `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj`
    - `./scripts/run-backend-coverage.sh component -v minimal`
    - `./scripts/run-backend-coverage.sh full -v minimal`
    - `dotnet test backend/WoongBlog.sln`
    - `git diff --check`
  - Verification:
    - `dotnet test backend/tests/WoongBlog.Api.UnitTests/WoongBlog.Api.UnitTests.csproj` passed with 56 tests.
    - `dotnet test backend/tests/WoongBlog.Api.ComponentTests/WoongBlog.Api.ComponentTests.csproj` passed with 113 tests.
    - `./scripts/run-backend-coverage.sh component -v minimal` passed with 113 component tests and regenerated `coverage/backend/component/report/Summary.json`.
    - `./scripts/run-backend-coverage.sh full -v minimal` passed; contract verification skipped as configured, 113 component tests passed, 56 unit tests passed, 35 architecture tests passed, and 194 integration tests passed.
    - `dotnet test backend/WoongBlog.sln` passed; contract verification skipped as configured, 113 component tests passed, 56 unit tests passed, 35 architecture tests passed, and 194 integration tests passed.
    - `git diff --check` passed.
    - Parsed `coverage/backend/component/report/Summary.json`: component line 44.1%, branch 37.8%, `WorkVideoStorageSelector` 100.0% line / 90.0% branch, `WorkVideoPlaybackUrlBuilder` 100.0% line / 100.0% branch, `LocalVideoStorageService` 91.4% line / 90.0% branch, `FfmpegVideoTranscoder` 0.0% line / 0.0% branch in component-only coverage.
    - Parsed `coverage/backend/full/report/Summary.json`: full line 92.5%, branch 72.6%, `WorkVideoStorageSelector` 100.0% line / 90.0% branch, `WorkVideoPlaybackUrlBuilder` 100.0% line / 100.0% branch, `LocalVideoStorageService` 100.0% line / 90.0% branch, `FfmpegVideoTranscoder` unchanged at 61.7% line / 50.0% branch.
    - Existing warning observed: `NU1901` for `AWSSDK.Core` 4.0.0.17 low-severity vulnerability.
- [x] Update the persistent audit report and perform final consistency checks.
  - Planned verification: report includes tests added, files changed, behavior covered, command results, targeted coverage changes, remaining Batch 2 seam work, and final recommendation.
  - Verification:
    - Updated `backend/reports/backend-workvideo-r2-hls-failure-coverage-audit-2026-04-26/backend-workvideo-r2-hls-failure-coverage-audit-2026-04-26.md` with the Batch 1 implementation report.
    - Created `backend/reports/backend-workvideo-r2-hls-failure-coverage-audit-2026-04-26/backend-workvideo-r2-hls-failure-coverage-audit-2026-04-26.json`.
    - Created `backend/reports/backend-workvideo-r2-hls-failure-coverage-audit-2026-04-26/backend-workvideo-r2-hls-failure-coverage-audit-2026-04-26.html`.
    - Parsed the report JSON with Node and verified the report slug, `WorkVideoStorageSelector` full line coverage, and command count.
    - Ran `git diff --check` after report artifact creation; no whitespace errors were reported.
    - Reviewed `git status --short`; changed files match the planned test/TODO/report write set.
    - Final scope check: actual work remains backend-only test/report changes with no production code, no S3/R2 seam, no direct R2 object-operation tests, and no external service usage.

---

# Origin Dev Addenda Preserved During Merge Prep

# Production-Like Latency Measurement TODO (2026-04-26)

## User Request
- production-like environment에서 public navigation/detail latency를 다시 측정한다.
- `next dev` compile 시간이 섞이지 않게 production runtime 기준으로 확인한다.

## Planned Work
- [x] P1 Inspect Docker/production runtime options and current running processes.
- [x] P2 Start or build a production-like frontend connected to the existing backend/db without relying on `next dev`.
- [x] P3 Run updated latency spec against the production-like endpoint and collect `e2e-latency-summary` artifacts.
- [x] P4 Record measurements, risks, and final recommendation in persistent audit artifacts.

## Verification Log
- Backup seed created: `/tmp/woong-blog-backup-2026-04-26-prod-latency`.
- P1 inspected Docker setup: production frontend needs nginx/proxy routing for `/api` and `/media`; direct frontend-only production runtime is not equivalent.
- P2 built frontend with `BACKEND_PUBLISH_PORT=18080 docker compose -f docker-compose.dev.yml build frontend`; Docker build completed and Next production server logged ready in 79ms after `up -d frontend`.
- P2 host port publish for a temporary nginx container failed with Docker Desktop/WSL `/forwards/expose` 500, so production-like measurement used Docker internal networking.
- P2 internal nginx probe returned HTTP 200 with `time_total` about 0.03s.
- P3 initial official Playwright image pull completed; image pull time was not counted in app latency.
- P3 preserved pass-2: 9/9 passed, budget failures 0, warnings 1. Warning: `Public nav click to Study` 1137.2ms over 1000ms soft warning, below 1500ms hard budget.
- P3 preserved pass-3: 9/9 passed, budget failures 0, warnings 0.
- P4 audit artifacts generated:
  - `backend/reports/production-like-latency-2026-04-26/production-like-latency-2026-04-26.md`
  - `backend/reports/production-like-latency-2026-04-26/production-like-latency-2026-04-26.html`
  - `backend/reports/production-like-latency-2026-04-26/production-like-latency-2026-04-26.json`
  - `backend/reports/production-like-latency-2026-04-26/artifacts/pass-2-summary.json`
  - `backend/reports/production-like-latency-2026-04-26/artifacts/pass-3-summary.json`

## Result Summary
- production-like targeted latency is acceptable: repeated runs had 0 hard budget failures.
- Latest preserved pass slowest measured public steps: Works nav 842ms, Study card detail 565ms, Study list load 565ms, Work detail 503ms, Study nav 460ms.
- Local browser slowness previously observed through `next dev` is still best explained by cold route compilation, not slow backend API responses.

---

# Production-Like Full E2E Measurement Addendum (2026-04-26)

## User Request
- production-like 환경에서 전체 Playwright e2e suite를 실행한다.
- 전체 실행 시간과 실패 목록을 뽑는다.

## Planned Work
- [x] F1 Confirm Playwright full-suite configuration, projects, reporters, and artifact paths.
- [x] F2 Recreate production-like nginx route against the current Docker frontend/backend services.
- [x] F3 Run the full Playwright suite against the production-like endpoint with timing captured.
- [x] F4 Extract failures, duration, report paths, and classify environment vs app/test failures.
- [x] F5 Write persistent audit artifacts.

## Verification Log
- Backup created before this addendum: `/tmp/woong-blog-backup-2026-04-26-full-e2e`.

## Full E2E Measurement Result
- [x] F1 Confirmed full suite: 589 tests in 189 files across `chromium-public`, `chromium-authenticated`, and `chromium-runtime-auth`.
- [x] F2 Recreated production-like nginx route against Docker production frontend/backend services; health probe returned HTTP 200.
- [x] F3 Ran full suite with `node scripts/run-e2e-latency.mjs -- --workers=1 --reporter=list,json` inside `mcr.microsoft.com/playwright:v1.58.2-noble`.
- [x] F4 Extracted failure summary from JSON reporter and latency summary.
- [x] F5 Wrote persistent audit artifacts under `backend/reports/production-like-full-e2e-2026-04-26/`.

## Full E2E Verification Log
- Run started: `2026-04-26T00:22:39+09:00`.
- Run ended: `2026-04-26T02:37:44+09:00`.
- Duration: `2h 15m 5s` (`8105s`).
- Exit code: `1`.
- Total: `589` tests.
- Passed: `248`.
- Failed: `209`.
- Skipped or did not run: `132` (terminal output: `14 skipped`, `118 did not run`).
- Failure status split: `98 failed`, `111 timedOut`.
- Project failure split: `chromium-public 15`, `chromium-authenticated 194`, `chromium-runtime-auth 0`.
- Latency summary: `466` latency artifacts, `0` budget failures, `113` warnings.
- Post-run fresh admin smoke succeeded: `/admin/dashboard` loaded with heading `Dashboard`, no admin workspace error.
- Main conclusion: production-like public read paths are comparatively healthy, but full-suite harness/admin-authenticated/editor flows are unstable. Shared authenticated storage/session ordering is a likely root cause for many `/login`, `401`, missing admin row, and editor field timeout failures.
- Key artifact paths:
  - `backend/reports/production-like-full-e2e-2026-04-26/production-like-full-e2e-2026-04-26.md`
  - `backend/reports/production-like-full-e2e-2026-04-26/production-like-full-e2e-2026-04-26.html`
  - `backend/reports/production-like-full-e2e-2026-04-26/production-like-full-e2e-2026-04-26.json`
  - `backend/reports/production-like-full-e2e-2026-04-26/artifacts/full-e2e-failures.md`
  - `backend/reports/production-like-full-e2e-2026-04-26/artifacts/full-e2e-failures.csv`
  - `backend/reports/production-like-full-e2e-2026-04-26/artifacts/full-e2e-report.json`
  - `backend/reports/production-like-full-e2e-2026-04-26/artifacts/full-e2e-run.log`

---

# Full E2E Test Currency Audit Addendum (2026-04-26)

## User Request
- production-like 테스트에서도 authentication은 test login으로 처리한다.
- 현재 코드가 많이 바뀌었으므로 full e2e 테스트 코드가 현재 코드를 제대로 반영하는지 먼저 파악한다.

## Planned Work
- [x] C1 Confirm current Playwright auth/test-login harness behavior.
- [x] C2 Compare full e2e failure clusters with current implementation selectors/routes/copy.
- [x] C3 Classify failures into outdated tests, auth harness/order issues, current app regressions, and environment/live-only tests.
- [x] C4 Produce persistent audit artifacts with a recommended stabilization order.

## Verification Log
- Backup created before audit addendum: `/tmp/woong-blog-backup-2026-04-26-e2e-currency-audit`.
- C1 confirmed `tests/helpers/global-setup.ts` already uses `/api/auth/test-login`, but the authenticated suite relies on one shared storage state.
- C1 found fixture helpers call `/api/admin/blogs` and `/api/admin/works` through the Playwright `request` fixture; prior production-like run showed 401 fixture failures, so an authenticated API context is needed.
- C2 compared representative failures against current detail pages, local QA boundary, WorkVideoPlayer, dashboard collections, and Notion workspace selectors.
- C3 classified known stale/mismatched areas: `main h1` detail selectors after h1 body heading support, localhost-only QA flags on internal Docker hostnames, mutable CMS content assumptions, and recording/batch harness drift.
- C4 wrote audit artifacts under `backend/reports/full-e2e-test-currency-audit-2026-04-26/`.

---

# Full E2E Stabilization Implementation Addendum (2026-04-26)

## User Request
- Implement the previous agent's full E2E stabilization plan in a fresh context.
- Make `npm run test:e2e` the release-blocking core lane and move recording/live/batch/manual-heavy specs to optional lanes.
- Refresh authenticated admin E2E sessions with `test-login` instead of relying only on shared storage state.
- Update outdated public detail, TOC, QA-flag, mutable seed-content, and authenticated fixture assumptions.
- Use TDD, search for relevant skills before implementation, prepare a backup, verify with targeted checks, and generate persistent audit artifacts.

## Planned Work
- [x] S1 Skill/context prep: load TDD/E2E guidance, search for relevant skills, and verify Codex plugin cache paths.
- [x] S2 Backup: snapshot current target files before code/config/test edits.
- [x] S3 Auth harness: add or update `ensureAdminSession`, authenticated API context helpers, and authenticated page fixture session refresh.
- [x] S4 E2E lanes: split Playwright default core specs from optional recording/live/batch/manual-heavy specs and update package scripts.
- [x] S5 Test currency fixes: update public detail title selectors, QA local-host guards, mutable content assertions, and TOC fixture coverage.
- [x] S6 Verification: run type/unit/targeted Playwright checks that are feasible in the current environment and record results.
- [x] S7 Audit: write Markdown, HTML, and JSON audit artifacts under `backend/reports/e2e-auth-and-core-suite-stabilization-2026-04-26/`.

## Instruction Mapping
- Skill discovery and TDD requirement: S1, S3-S6.
- Backup before modification: S2.
- Core vs optional E2E split: S4.
- Authenticated E2E stabilization through test-login: S3.
- Outdated test updates: S5.
- Targeted rerun order and production-like validation: S6.
- Mandatory audit report: S7.

## Verification Log
- S1 plugin cache paths verified: `/home/kimwoonggon/.codex/plugins/cache` and `/home/kimwoonggon/.codex/.tmp/plugins`.
- S1 loaded local `tdd`, `find-skills`, and `e2e-testing-patterns` skill guidance.
- S1 searched skills with `npx --yes skills find playwright e2e`; best public result was `bobmatnyc/claude-mpm-skills@playwright-e2e-testing` at 2.7K installs, but no new skill was installed because a local E2E testing skill is already available.
- S2 backup created at `/tmp/woong-blog-backup-2026-04-26-e2e-auth-core-stabilization` with `target-files.tgz`, `pre-change.diff`, and `pre-change-status.txt`.
- S3 added `ensureAdminSession(page, returnPath?)` and `ensureAdminApiContext(request, returnPath?)`; authenticated pages refresh admin sessions before each `chromium-authenticated` test, and content fixtures now create admin API data through a test-login-authenticated context.
- S3 moved `admin-auth-authorization.spec.ts` out of the authenticated project and into runtime-auth coverage so negative auth tests are not auto-admined.
- S4 added `PLAYWRIGHT_E2E_PROFILE=core|optional|exhaustive` support in Playwright config and updated `test:e2e`, `test:e2e:optional`, and `test:e2e:exhaustive` scripts.
- S5 added public detail title test ids, work-specific TOC test id support, fixture-backed blog/work TOC tests, localhost/127.0.0.1 QA flag guards, and softened mutable seed title assertions.
- S6 `npm run typecheck`: passed.
- S6 `npm test -- src/test/table-of-contents.test.tsx src/test/local-qa-query-boundary.test.tsx`: passed, 2 files / 12 tests.
- S6 Playwright list validation: core profile lists 415 tests in 172 files, optional profile lists 174 tests in 17 files, exhaustive profile preserves 589 tests in 189 files.
- S6 targeted public detail/TOC on stale port 3000 failed because the already-running server did not include new test ids; verification moved to fresh port 3001.
- S6 targeted public detail/TOC on fresh port 3001: grouped run passed 5/6, with one cold navigation miss in `e2e-visitor-content-exploration`; rerunning that spec alone passed.
- S6 targeted admin smoke on fresh port 3001: `admin-dashboard`, `admin-menus`, and `admin-members` passed 8/8.
- S6 targeted Notion group on fresh port 3001: `ui-admin-notion-autosave-info`, `ui-admin-notion-library-sheet`, and `ui-admin-notion-client-switch` passed 11/11.
- S6 targeted works video group on fresh port 3001: 7/8 passed; `admin-work-video-edit-flow` reorder spec hit one transient Next dev `/admin/works/new` `Unexpected end of JSON input` overlay, then passed on direct rerun.
- S6 `admin-auth-authorization` on `chromium-runtime-auth`: passed 1/1, confirming the negative auth test is not auto-refreshed into admin.
- S6 stale font-loading assertion reproduced in dev CSS because Next emits `font-display: swap`; updated the test to assert `font-display:\s*swap` and reran it successfully.
- S6 earlier-failure regression set passed 12/12 across public, authenticated, and runtime-auth projects.
- S6 full local core lane passed: `PLAYWRIGHT_EXTERNAL_SERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3001 npm run test:e2e -- --reporter=list` finished in 22.1m with 410 passed, 5 skipped, 0 failed, 414 latency artifacts, 0 latency budget failures, and 90 warnings.
- S6 final `npm run typecheck`: passed after the last test assertion update.
- S6 Docker production-like containers were present but were built before the final edits; a meaningful production-like rerun needs a rebuild and is recorded as follow-up instead of being treated as the final signal.
- S6 correction after user review: stopped the existing `service-nginx-1` container that exposed host ports 80/443; future Docker E2E validation must use HTTP on `127.0.0.1:3000` only.
- S7 audit artifacts written:
  - `backend/reports/e2e-auth-and-core-suite-stabilization-2026-04-26/e2e-auth-and-core-suite-stabilization-2026-04-26.md`
  - `backend/reports/e2e-auth-and-core-suite-stabilization-2026-04-26/e2e-auth-and-core-suite-stabilization-2026-04-26.html`
  - `backend/reports/e2e-auth-and-core-suite-stabilization-2026-04-26/e2e-auth-and-core-suite-stabilization-2026-04-26.json`

---

# Dev CI Gate and Promotion Addendum (2026-04-26)

## User Request
- Make the `origin/dev` GitHub Actions gate succeed first.
- Only after `origin/dev` succeeds, allow the staging gate to proceed.
- Only after staging succeeds, promote toward `origin/main`.
- Do not push directly to `main`; use the protected/gated path so failures cannot bypass the gate.
- Keep Docker/browser validation on HTTP `127.0.0.1:3000`; do not expose host ports 80 or 443 for this validation.

## Planned Work
- [x] G1 Confirm current branch, PR, failed GitHub Actions check, and local worktree state.
- [x] G2 Reproduce the current failing CI test locally or isolate the smallest deterministic fix.
- [x] G3 Patch the failing CI test or implementation with the narrowest behavior-preserving change.
- [x] G4 Run targeted local verification for the changed test and related prior hotfix test.
- [x] G5 Commit and push the fix branch, then monitor PR checks until green or the next concrete blocker is known.
- [x] G6 Merge to `dev` only after PR checks pass, then monitor the `dev` branch GitHub Actions gate.
- [x] G7 Let the staging workflow/gate run after `dev` succeeds; record the exact run status.
- [ ] G8 Promote toward `main` only through the repo's protected workflow/PR path after staging succeeds.
- [ ] G9 Write persistent audit artifacts for this CI gate/promotion work.
- [x] G10 Repair promotion/staging workflow availability without bypassing branch protection: keep staging/promotion workflow files, promotion scripts, and auth helper in the main runtime allowlist.
- [x] G11 Fix staging GHCR publish target so `dev` tags publish to the linked runtime package family instead of old private unlinked packages.
- [x] G12 Fix promotion workflow execution on GitHub-hosted runners without relying on executable file mode.

## Instruction Mapping
- Dev before staging before main: G5-G8.
- Prohibit broken-gate promotion: G6-G8.
- No direct `main` push: G8.
- HTTP `127.0.0.1:3000` Docker validation only: G4-G8 if browser/Docker validation is needed.
- Mandatory TODO, backup, verification, and audit: G1-G4 and G9.
- Staging workflow absent from default branch and promotion allowlist repair: G10.

## Verification Log
- G1 confirmed PR #14 targeted `dev` from `feature/fix-dev-ci-gate-20260426`, with stale failed push status and green current PR status.
- G2 local `npm test -- src/test/pact/public-api-consumer.pact.test.ts --run`: passed, 1 file / 6 tests.
- G4 local `npm test -- src/test/azure-backup-lib.test.ts --run`: passed, 1 file / 6 tests.
- G5 reran failed CI Dev push job for PR #14; all PR checks passed.
- G6 merged PR #14 to `dev` as `780e0ceb9fc21e6b7e1557052eae4fcec4198510`.
- G6 `CI Dev` run `24950711142` on `origin/dev`: passed all five jobs.
- G7 blocked automatically: `Publish GHCR Dev` exists on `dev` but not on default branch `main`, so GitHub returned 404 for workflow dispatch and did not list the workflow as active.
- G10 backup created at `/tmp/woong-blog-backup-2026-04-26-main-promotion-gates`.
- G10 branch `feature/fix-main-promotion-gates-20260426` preserved `.github/workflows/publish-ghcr-dev.yml`, `.github/workflows/promote-main-runtime.yml`, `scripts/promote-main-runtime.sh`, `scripts/main-runtime-allowlist.txt`, and `tests/helpers/auth.ts` in the main runtime allowlist.
- G10 local verification passed: `bash -n scripts/promote-main-runtime.sh`, `git diff --check`, `npm test -- src/test/azure-backup-lib.test.ts --run`, and a dry promotion into `/tmp/woong-blog-main-runtime-allowlist-verify`.
- G10 PR #15 merged to `dev` as `499dc7d0cbd510e09b08f6de6d32fc07beb6c2dd`; `CI Dev` run `24951110060` passed.
- G8 bootstrap prerequisite completed through protected PR #16 from `release/main-gate-bootstrap-20260426`; `CI Main Runtime` PR run `24951218633` and main push run `24951328807` passed. No direct `main` push was used.
- G7 staging workflow dispatch became available after PR #16, but initial `Publish GHCR Dev` run `24951430092` failed only during GHCR push with HTTP 403 after staging compose policy checks passed.
- G7 after the user-side GHCR access change, reran failed jobs for `Publish GHCR Dev` run `24951430092` at `2026-04-26T07:48:30Z`; result is pending.
- G7 rerun of `Publish GHCR Dev` run `24951430092` completed as failed. Both frontend and backend passed staging compose policy checks and failed only during GHCR push to old packages `woong-blog-aspcore-nextjs-frontend` and `woong-blog-aspcore-nextjs-backend`.
- G7 package inspection found those old staging packages are private and have no linked repository, while `woong-blog-aspcore-nextjs-runtime-frontend` and `woong-blog-aspcore-nextjs-runtime-backend` are public and linked to `kimwoonggon/woong-blog-aspcore-nextjs`.
- G11 backup created at `/tmp/woong-blog-backup-2026-04-26-dev-staging-main-gate`.
- G11 patched `.github/workflows/publish-ghcr-dev.yml` so staging publishes `:dev` and `:dev-sha-*` tags to `woong-blog-aspcore-nextjs-runtime-frontend/backend`, matching the package family that successfully published from main.
- G11 local verification passed: `git diff --check` and `rg` confirmed the staging workflow now targets `-runtime-${{ matrix.image_suffix }}:dev` tags.
- G11 PR #17 merged to `dev` as `73fd9659482163bc49576c7992dcc09bdfa382b4`; `CI Dev` run `24951772357` passed.
- G7 automatic `workflow_run` staging run `24951865781` failed because GitHub used the stale default-branch `main` workflow definition, which still targeted the old private unlinked GHCR packages. This is a workflow-definition propagation issue, not a staging compose/app failure.
- G7 manual `Publish GHCR Dev` dispatch on `ref=dev` run `24951961846` passed. Backend and frontend both passed local staging image build, staging compose policy verification, GHCR login, and push to the runtime GHCR package family.
- G8 confirmation before main promotion: `main` runtime compose sets `NODE_ENV=production`, `ASPNETCORE_ENVIRONMENT=Production`, secure cookies, HTTPS metadata, HTTPS redirection, and HSTS. The full HTTPS nginx config is `nginx/prod.conf`, but `docker-compose.prod.yml`, `.env.prod.example`, and `scripts/main-up.sh` currently default `NGINX_DEFAULT_CONF` to `./nginx/prod-bootstrap.conf`; final HTTPS-by-default behavior should be handled as a separate explicit follow-up if that default is not intended.
- G12 `Promote Main Runtime` run `24952092808` failed before branch creation because GitHub executed `./scripts/promote-main-runtime.sh` directly and the tracked file mode is `100644`, producing `Permission denied`.
- G12 backup created at `/tmp/woong-blog-backup-2026-04-26-promote-workflow-shell`.
- G12 patched `.github/workflows/promote-main-runtime.yml` to run `bash ./scripts/promote-main-runtime.sh`, removing reliance on executable file mode.
- G12 local verification passed: `bash -n scripts/promote-main-runtime.sh` and `git diff --check`.
